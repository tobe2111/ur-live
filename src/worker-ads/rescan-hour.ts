/**
 * 🌙 야간 재보정 시각(UTC) = KST 04시.
 *
 * 시간별 정비 순환이 이 시각을 **양보**한다(같은 lease 경합 — 둘이 다투면 진 쪽이 스냅샷도 안 남기고
 * 사라진다). 두 곳이 같은 값을 봐야 하므로 상수다 — 따로 적으면 한쪽만 옮겨져 다시 겹친다.
 *
 * 📦 엔트리(`index.ts`)가 아니라 **자기 모듈**에 둔다: `maintenance-cron.ts` 가 이 값을 쓰는데
 *   엔트리에서 가져오면 순환 import 가 된다(엔트리가 그 모듈을 import 하므로).
 *   엔트리는 하위호환을 위해 그대로 재수출한다.
 */
export const RESCAN_HOUR_UTC = 19
