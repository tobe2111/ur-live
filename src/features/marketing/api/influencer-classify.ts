/**
 * 🏷️ 2026-07-21 인플루언서 카테고리 콘텐츠 기반 자동 분류 (순수 — 테스트 가능).
 *   기존: 발굴 키워드의 카테고리를 물려받기만 → 자동확장 키워드('자동')·오분류(뷰티 채널이 "서울 맛집"
 *   검색에 걸리면 평생 맛집) 발생. 채널 **이름+소개글 텍스트 신호**로 분류하고, 신호 없을 때만 키워드
 *   카테고리 폴백. 신규 저장 + 기존 풀 재분류(백필) 양쪽에서 사용.
 */

/**
 * 분류 카테고리 — **첫 매치 승**이라 순서가 우선순위. 원칙:
 *   ① 구체·니치 신호를 먼저(네일<뷰티, 반려/육아/운동을 맛집보다 먼저 → "반려간식/이유식/다이어트식단"이 맛집으로 새지 않게).
 *   ② 음식은 창업(B2B)→카페→맛집(매장/리뷰)→푸드(요리/레시피 크리에이터) 순으로 세분.
 *   ③ 넓은 단어(맛있/음식 등)는 캐주얼 언급까지 잡아 오탐 → **강한 신호만** 사용.
 * SEED 수집 카테고리 14종 전부 + 카페 = 15종을 콘텐츠로 분류(과거엔 9종만 있어 운동/반려/리빙/IT/취미/푸드
 * 채널이 규칙에 없어, 소개글의 스치는 음식어에 걸려 맛집으로 오분류됐음). 인바운드 신청 폼과 정합.
 */
const RULES: {
  cat: string
  re: RegExp
  /**
   * 🪪 **이름(핸들)에서만** 인정하는 신호 — 소개글에 있으면 무시한다.
   *
   *   왜 필요한가: 어떤 단어는 **자기를 그렇게 부를 때만** 업(業) 신호다. 소개글에 스치듯 나오면
   *   지나온 경력이나 곁가지 활동일 뿐이다(아래 `마케터` 항목의 실측 근거 참조).
   *   ⚠️ 빈도 분류(`classifyCategoryByHits`)에는 **일부러 안 넘긴다** — 그건 글 본문을 세는 경로라
   *   이름이 없고, 소개글 속 한 번의 언급을 세어 봐야 같은 오탐이 된다.
   */
  nameRe?: RegExp
}[] = [
  // ── 🛒 공동구매/라이브공구 (2026-07-29 대표 지시 — 이 축을 새로 모집) ──
  //   다른 규칙보다 **먼저** 본다: 이건 주제(뷰티/맛집)가 아니라 **행위**(자기 팔로워에게 직접 판다)이고,
  //   유어딜 유어샵 전환에는 그 행위가 주제보다 강한 신호다("뷰티 공구"는 뷰티 리뷰어보다 공구 셀러에 가깝다).
  //   ⚠️ **동음이의어 함정**: 맨 '공구'는 工具(전동공구·공구상가·공구함)와 겹친다. 반드시 공동구매 문맥이
  //   붙은 형태만 잡는다(`공구 오픈/진행/마감/링크/…` · `인스타/블로그/맘카페 공구`). bare '공구' 금지.
  //   ⚠️ bare '라방'도 금지 — 게임/소통 방송에서 더 흔하다(커머스 신호 아님).
  {
    cat: '공동구매',
    // ⚠️ `공구\s*중` 은 **중고**(工具 중고거래)를 삼키므로 부정전방탐색으로 뺀다 — 확장하다 실제로 뚫렸던 자리.
    re: /공동\s*구매|공구\s*(오픈|진행|마감|링크|신청|일정|후기|가격|공지|예약|안내|템)|공구\s*중(?!고)|(인스타|블로그|맘카페|카페|스토어|밴드|카톡|오픈채팅|맘|라방)\s*공구|(최저가|특가|한정|시즌)\s*공구|(뷰티|화장품|식품|반찬|간식|육아용품|리빙|패션)\s*공구|쇼핑\s*라이브|라이브\s*커머스|라이브\s*판매|그립\s*(라이브|방송)|공구\s*(전문|셀러|진행자)/i,
  },
  // ── 뷰티 계열(네일이 뷰티의 부분집합 → 먼저) ──
  { cat: '네일', re: /네일|nail\s*art|젤네일|손톱\s*아트|패디큐어|pedicure/i },
  // ── 사람/생활 니치(맛집보다 먼저 — 반려간식·이유식·다이어트식단이 맛집으로 새는 것 방지) ──
  { cat: '반려동물', re: /반려|강아지|고양이|댕댕|냥이|멍멍|반려견|반려묘|\bpet\b|puppy|kitten|수의사|동물병원/i },
  { cat: '육아', re: /육아|맘스타|아기(?!자기)|신생아|이유식|유아|키즈|워킹맘|엄마표|육아맘|출산|\bkids\b|toddler/i }, // '아기자기' 오탐 제외
  { cat: '뷰티', re: /뷰티|뷰튜버|메이크업|화장품|코스메틱|스킨케어|헤어(?!지|져|질)|미용|피부\s*관리|퍼스널\s*컬러|beauty|makeup|cosmetic|cosmetolog|skincare|\bhair(?!line)|\bsalon\b/i }, // '헤어지다/헤어질'·'hairline' 오탐 제외. \bhair 는 'chair' 를 경계로 배제
  // 🏌️ 골프 — 운동보다 **먼저**(더 구체적). 2026-07-29 대표 지시로 신설.
  //   왜 별도 축인가: 골프연습장·스크린골프는 **인허가 업종**이라 유어딜 이용권 대상이 된다
  //   (운동 축의 홈트/요가 콘텐츠와 결이 다르다). 실측에서 골프장 리뷰 채널(`파쓰리뷰`)이
  //   '맛집'에 앉아 있었다 — 규칙이 없으면 소개글의 스치는 단어로 엉뚱한 축에 눌러앉는다.
  //   ⚠️ 오탐 함정: '드라이버'(운전기사·기기 드라이버) · '아이언'(다리미) · bare '필드'(분야/현장) ·
  //   '골프존'은 브랜드라 OK 지만 bare '라운딩'은 어깨 라운딩(체형)과 겹쳐 골프 문맥을 요구한다.
  { cat: '골프', re: /골프|골린이|스크린\s*골프|파쓰리|파3\s*골프|퍼팅|버디\s*(샷|기록)|\bgolf\b|라운딩\s*(후기|브이로그|영상|기록)|필드\s*(라운딩|레슨)/i },
  { cat: '운동', re: /홈트|헬스|다이어트|요가|필라테스|피트니스|바디\s*프로필|스트레칭|크로스핏|(?<!이)러닝(?!\s*타임|타임)|헬스장|workout|fitness|\bgym\b|pilates/i }, // '러닝타임'·'이러닝' 오탐 제외
  // ── 음식 계열(창업 B2B → 카페 → 맛집(매장/리뷰) → 푸드(요리/레시피 크리에이터)) ──
  /**
   * 📣 **마케팅대행사·마케터** (2026-07-29 대표 지시 — "인플루언서 풀에서 마케팅 대행사를 찾아야겠다").
   *
   *   왜 별 축인가: 대행사는 *한 명이 여러 매장을 물고 온다* — 유어애즈 입장에선 리드 1건이
   *   매장 N건으로 곱해지는 유일한 부류다. 외식창업(=사장 본인)과 결이 완전히 다르므로 섞으면
   *   둘 다 못 고른다. **외식창업보다 먼저** 판정한다("매장 마케팅"이 양쪽에 걸리는데 대행사가 더 구체적).
   *
   *   ⚠️ 오탐 함정(전부 실제로 밟을 수 있는 것들):
   *     · bare `마케팅` — "마케팅 전공/공부/취업" 같은 학생·구직 채널이 대량으로 딸려온다 → 업(業) 신호를 요구.
   *     · `바이럴` 단독 — "바이럴 영상"(콘텐츠 형용사)이라 `바이럴 마케팅`만 받는다.
   *     · `대행` 단독 — 구매대행·배송대행(전혀 다른 업종)이라 **광고/마케팅 문맥을 붙여야** 한다.
   *     · `퍼포먼스` 단독 — 공연(performance)과 겹친다.
   *     · **`마케터` 단독 — 2026-08-03 라이브 실측으로 실제로 밟았다.** 위 함정들엔 "업 신호를 요구"라고
   *       써 놓고 정작 이 토큰만 맨몸으로 남아 있었다. 대행사로 분류된 273명 중 **45명(16%)이 오직 이
   *       단어 하나로** 들어와 있었고, 그 45명은 원래 맛집 10 · 외식창업 8 · IT/재테크 5 · 카페 5 ·
   *       숙소 4 · 여행 2 · 패션 1 · 미분류 10 이었다 — 즉 **이용권 본체 축에서 훔쳐오고 있었다.**
   *       ```
   *         와그잡의 트래블홀릭  "…여행 인플루언서 & 일본 전문 마케터 와그잡"   ← 여행 블로거
   *         럭키뚜래블          "75개국 여행한 디지털 노마드. 15년차 마케터"    ← 여행 블로거
   *         글수저              (제주 흑돼지 맛집 글)                          ← 맛집 블로거
   *       ```
   *       ⇒ 이름에 있을 때만 인정한다(`nameRe`). 실측상 갈림이 깨끗했다 —
   *       **이름 14건**(싱어송마케터·지역전문마케터·로컬마케터즈·QR마케터·디지털마케터 디노)은 전부
   *       자기 정체성 선언이고, **소개글 31건**은 전부 지나온 경력이나 곁가지 언급이었다.
   *       `지망생`은 제외한다 — bare `마케팅`이 학생·구직을 끌고 온다던 바로 그 함정이다.
   */
  {
    cat: '마케팅대행사',
    re: /(마케팅|광고|홍보|바이럴|퍼포먼스|콘텐츠)\s*(대행사|대행|에이전시|회사|기업)|(광고|마케팅)\s*(대행|기획사)|바이럴\s*마케팅|퍼포먼스\s*마케팅|그로스\s*(해킹|마케팅)|온라인\s*마케팅|디지털\s*마케팅|SNS\s*마케팅|블로그\s*마케팅|체험단\s*(모집|대행|운영)|인플루언서\s*(마케팅|섭외|캐스팅)|브랜디드\s*콘텐츠|매체\s*(대행|집행)|광고\s*(운영|세팅|집행)/i,
    nameRe: /마케터(?!\s*지망)/i,
  },
  /**
   * 🎁 **체험단·협찬 참여자** (2026-08-17 대표 지시 — "체험단 키워드로도 인플루언서 db 수집 필요해").
   *
   *   ## 왜 별 축인가 — **행위 신호**라서 주제보다 예측력이 높다
   *   체험단/협찬에 참여한 사람은 **이미 브랜드 협업을 받아 본 층**이다. 제휴 제안의 전환 장벽이
   *   "리뷰만 하던 사람"보다 구조적으로 낮다(공동구매 축을 우선으로 둔 것과 같은 논리 —
   *   그 축 주석의 *"이미 자기 팔로워에게 직접 파는 사람들"* 과 결이 같다).
   *
   *   실측 근거(2026-08-17, 이미 활성인 3개 키워드):
   *   ```
   *     체험단 모집  found 1,041 → 저장 290 · 이메일 62 (21.5%)
   *     공구 협찬    found   547 → 저장 183 · 이메일 60 (33.1%)   ← 전체 최고 수준
   *     체험단 대행  found   925 → 저장 171 · 이메일 20 (11.8%)
   *   ```
   *   축 평균(우선 24.4% · 일반 23.2% · 집중 18.0%)과 견줘 **동급 이상**이다.
   *
   *   ## 🚪 이 룰이 없으면 **영구 차단**이었다 (이것이 대표 요청의 실체)
   *   승격 게이트는 `canAutoPromote(promoCategory(tag))` 이고 분류 불가는 `'자동'` 이 된다. 라이브에서
   *   체험단 후보가 그 상태로 대기하고 있었다 — `체험단`(hits 78) · `블로그체험단`(40) · `제품협찬`(12) ·
   *   `협찬플러스`(21) … **hits 가 아무리 쌓여도 게이트에서 막혀 절대 승격되지 않는다.**
   *   키워드 정원을 늘려도 소용없다(게이트가 정원보다 먼저 막는다).
   *
   *   ⚠️ **대행사 룰 뒤에 둔다** — `체험단 모집|대행|운영` 은 *운영하는 쪽*이고 이미 그 룰이 잡는다
   *     (그게 더 구체적이고 검증된 판정이다). 여기는 **참여자**만 받는다.
   *     🔎 정정(되돌려-검증에서 배운 것): 대행사 축을 실제로 지키는 것은 **순서가 아니라 아래 negative
   *        lookahead** 다 — 룰을 맨 앞으로 옮겨 봐도 `체험단 모집` 은 여전히 대행사로 갔다. 순서가 지키는
   *        것은 *주제 축*(뷰티·운동)이다. 둘을 혼동하면 "순서만 맞으면 안전하다"고 오판한다.
   *   ⚠️ **음식 계열보다 앞·생활 계열보다 뒤**라는 실측 사실(순서를 옮기기 전에 읽을 것):
   *     · `맛집체험단` → **체험단**(맛집 룰보다 앞) — 주제가 맛집이어도 값진 신호는 "참여한다"는 행위다
   *     · `뷰티 체험단` → **뷰티**(뷰티 룰이 더 앞) · `헬스 무료체험` → **운동** — 둘 다 이미 검증된
   *       우선/일반 축이라 그대로 둔다. 시드는 category 를 직접 저장하므로 영향 없고, 해시태그 승격만 해당.
   *   ⚠️ 맨 `무료 체험` 은 **넣지 않았다** — `체험단` 이 이미 `무료 체험단` 을 잡고, 나머지(학원·헬스
   *     무료체험)는 순수 오탐이다. **라이브 후보 목록에 없는 패턴은 넣지 않는다**(추측으로 넓히면 축이 오염된다).
   *   ⚠️ bare `협찬` 을 넣은 이유: 라이브 후보가 실제로 그 형태다(`협찬`·`협찬플러스`·`협찬깡`·`제품협찬`).
   *     이들은 대개 체험단 플랫폼 이름이고, 그 이름으로 검색하면 **그 플랫폼을 쓰는 참여자**가 잡힌다.
   */
  { cat: '체험단', re: /체험단(?!\s*(모집|대행|운영))|서포터즈|협찬|리뷰어/i },
  { cat: '외식창업', re: /외식업|자영업|소상공인|식당\s*(창업|사장|운영)|카페\s*창업|장사\s*노하우|매장\s*마케팅|요식업|음식점\s*사장|가게\s*운영|창업\s*(일기|스토리|브이로그|기)/i },
  { cat: '카페', re: /카페(?!인|트|24)|디저트|베이커리|빵집|브런치|커피|\bcafe\b|dessert|bakery|로스터리/i }, // 카페인·카페트·카페24(쇼핑몰 솔루션) 오분류 제외
  { cat: '맛집', re: /맛집|먹방|맛스타|먹스타|맛기행|노포|미슐랭|맛도리|맛투어|식당(?!\s*(창업|사장|운영))|\bfood\b|mukbang|restaurant/i }, // 맛있·음식·요리·레시피 제거(캐주얼 언급 오탐) → 강한 신호만. '식당'은 창업/사장 맥락을 위 규칙에 양보
  { cat: '푸드', re: /요리|레시피|홈쿠킹|집밥|베이킹|쿠킹|홈카페|자취요리|간편식|밀프렙|에어프라이어|cooking|recipe|baking/i },
  // ── 여행/숙박 ──
  { cat: '숙소', re: /숙소|호텔|펜션|리조트|글램핑|풀빌라|스테이(?!지)|게스트하우스|모텔|hotel|pension|airbnb|resort/i }, // '스테이지' 오탐 제외
  { cat: '여행', re: /여행|트래블|travel|배낭|투어|나들이|해외여행|국내여행|캠핑|백패킹|세계\s*(일주|한바퀴)|\btrip\b/i },
  // ── 스타일/생활/기타(넓음 → 뒤로) ──
  { cat: '패션', re: /패션|스타일링|코디|옷장|데일리룩|하울|fashion|ootd|스트릿|룩북|lookbook/i },
  { cat: '리빙', re: /인테리어|살림|홈스타일링|정리수납|리빙|집꾸미기|홈데코|셀프\s*인테리어|interior/i },
  { cat: 'IT/재테크', re: /가전\s*리뷰|it\s*리뷰|테크|언박싱|앱\s*추천|재테크|주식(?!회사)|부동산|투자|가상화폐|코인|gadget|unboxing/i }, // bare '리뷰' 금지 + '주식회사'(사업자 정보 관용구) 오탐 제외
  { cat: '취미', re: /캘리그라피|드로잉|그림\s*그리기|독서|책\s*(리뷰|과|을|읽)|공예|\bdiy\b|플랜테리어|사진\s*(촬영|기술)|악기\s*연주|낚시|등산\s*브이로그/i }, // '책상' 은 매치 안 됨(뒤 글자 한정)
]

/**
 * 🏷️ 이 분류기가 **만들어낼 수 있는** 카테고리 전체 — RULES 에서 파생(두 벌로 두면 갈라진다).
 *
 *   왜 export 하는가: 서버가 분류하는 축이 어드민 필터 목록에 없으면 **그 축으로 수집된 사람을
 *   화면에서 고를 수 없다**(수집은 되는데 안 보이는 반쪽 상태 — 가장 조용한 실패).
 *   실사고 2건: ① 2026-07-29 '공동구매' 신설 시 UI 목록 미갱신 ② 같은 날 실측에서 **'카페' 4,675명
 *   (풀의 12%)이 필터에 없어 통째로 안 보이고 있었다** — 기존 가드가 *우선 카테고리만* 봐서 놓쳤다.
 *   ⇒ 유닛이 이 목록 ⊆ `POOL_CATEGORIES` 를 강제한다(규칙을 늘리면 화면도 따라오게).
 */
export const CLASSIFIED_CATEGORIES: string[] = Array.from(new Set(RULES.map(r => r.cat)))

/** 실제 카테고리로 인정 안 하는 값(키워드 상속의 부산물) — 재분류 대상 판정에 사용. */
export const NON_CATEGORIES = new Set(['자동', '일반', '', null as unknown as string])

/**
 * 🪦 **은퇴한 카테고리** — 축을 **줄일 때** 여기 한 줄 적으면 나머지가 전부 따라온다 (2026-08-03 대표 지시
 *   *"줄이기도 자연스럽게 만들어줘"*).
 *
 * ## 왜 "규칙에서 지우면 자동" 이 아닌가
 * 그렇게 일반화하면 `shouldClearCategory` 의 경고("모르는 값을 지우면 사람이 손으로 고친 분류까지
 * 날린다")를 정면으로 어긴다. **분류기가 안 만드는 값 ≠ 잘못된 값**이다 — 키워드 폴백(`resolveCategory`)
 * 은 키워드에 적힌 **어떤 문자열이든** 리드에 붙일 수 있고, 그건 정당한 경로다.
 * ⇒ 그래서 추론이 아니라 **선언**으로 받는다. 사람이 "이 축은 끝났다"고 말한 것만 지운다 = "아는 경우".
 *
 * ## 한 줄로 따라오는 것들
 * ```
 *   ① 재분류가 그 값을 가진 리드를 비운다      (shouldClearCategory)
 *   ② 키워드 폴백이 그 값을 더는 안 붙인다      (resolveCategory)  ← 신규 유입 차단
 *   ③ 그 축 키워드가 수집 슬롯을 그만 먹는다    (influencer-auto-collect 의 활성 목록 필터)
 * ```
 * ①만 있으면 ②가 계속 다시 붙여 **영원히 제자리**고, ②만 있으면 옛 리드가 유령 값으로 남는다.
 *
 * ## 줄이는 절차 (이제 두 줄이다)
 * 1. `RULES` 에서 그 규칙 제거  2. 여기에 이름 추가.
 * 끝. 키워드 비활성화·리드 정리·신규 차단은 자동이다.
 * (어드민 필터 `POOL_CATEGORIES` 에서 빼는 건 선택 — 리드가 다 비워지면 그 항목은 빈 필터가 된다.)
 *
 * ⚠️ 규칙과 은퇴를 **동시에** 두지 말 것 — 분류기가 만들면서 지우면 매 회차 붙였다 지웠다 한다.
 *   유닛이 `RETIRED ∩ CLASSIFIED = ∅` 을 강제한다.
 * ⚠️ 되돌리려면 이 목록에서 빼고 규칙을 복원하면 된다. 다만 **이미 비워진 리드는 안 돌아온다**
 *   (재분류가 본문으로 다시 잡아 주긴 하지만 본문 신호가 없던 리드는 미분류로 남는다).
 */
export const RETIRED_CATEGORIES: ReadonlySet<string> = new Set<string>([
  // 예) '골프',  ← 축을 접을 때 여기 한 줄. 같은 커밋에서 위 RULES 의 해당 규칙을 지울 것.
])

/**
 * 🧹 **저장된 카테고리를 지워야 하는가** — 재분류가 `null` 을 받았을 때의 판단(순수).
 *
 * ## 왜 필요한가 (2026-07-29 실측 — 내 앞선 설명이 틀렸던 자리)
 * 재분류(`runReclassifyPool`)는 `classifyCategory` 가 **값을 주면 덮고, `null` 이면 그대로 둔다.**
 * 그래서 "예전엔 맞았지만 지금 규칙은 거부하는" 값이 **영구히 굳는다.**
 * 입주 시공업체 27명에 새 가드를 적용해 봤더니 **6명만 교정되고 21명이 공동구매로 남았다** —
 * 다른 규칙에도 안 걸려 `null` 이 나왔기 때문이다. "측정하면 점진 교정된다"는 낙관은 틀렸다.
 *
 * ⇒ `null` 이어도 **현재 규칙이 그 값을 거부한다는 사실을 아는 경우**엔 지운다.
 *   지우면 키워드 상속/라이브 재보정이 다시 채울 기회를 얻는다(잘못된 값이 그 자리를 막지 않는다).
 *
 * ⚠️ 넓히지 말 것: "현재 규칙이 거부한다"를 **아는** 경우만이다. 모르는 값을 지우면
 *    사람이 손으로 고친 분류까지 날린다.
 */
export function shouldClearCategory(
  stored: string | null | undefined, name: string, description?: string | null,
  /** 🧪 은퇴 목록 주입구 — 기본은 모듈 상수. `pickYtKeywords(…, priorityCats)` 와 같은 테스트 seam 이다.
   *   이게 없으면 목록이 빈 동안 은퇴 동작 검사가 **0건을 돌며 늘 통과**한다(이 레포가 반복해 겪은 헛도는 가드). */
  retired: ReadonlySet<string> = RETIRED_CATEGORIES,
): boolean {
  if (!stored) return false
  if (NON_CATEGORIES.has(stored)) return true
  // 🪦 은퇴 선언된 축 — 사람이 "끝났다"고 말한 값이라 **아는 경우**다(위 RETIRED_CATEGORIES 주석).
  if (retired.has(stored)) return true
  // 공동구매인데 입주 시공업체 신호 → 현재 규칙이 명시적으로 거부하는 조합(위 isFitoutBulkBuy).
  if (stored === '공동구매' && isFitoutBulkBuy(`${name} ${description || ''}`)) return true
  /**
   * 📣 대행사인데 대행사 규칙이 **아예 안 걸리는** 경우 — 2026-08-03 `마케터` 를 이름 전용으로 좁힌
   *   뒤에 생긴 자리다. 실측 45명 중 35명은 다른 규칙(맛집·숙소·외식창업…)이 받아 자동 교정되지만,
   *   **10명은 어느 규칙에도 안 걸려 `null`** 이 나온다 → 위 docblock 이 경고한 그대로 옛 값이 굳는다.
   *   ⚠️ 이 판정은 **아는 경우**다(넓히기 금지 원칙 준수): 저장값이 대행사인데 그 축의 규칙이
   *   이름·소개글 어디에도 안 걸린다는 것을 **직접 확인**한다. 다른 카테고리로 일반화하지 말 것 —
   *   키워드 상속으로 정당하게 붙은 값까지 지우게 된다.
   */
  if (stored === '마케팅대행사') {
    const rule = RULES.find(r => r.cat === '마케팅대행사')
    const text = `${name} ${description || ''}`
    if (rule && !rule.re.test(text) && !(rule.nameRe && rule.nameRe.test(name))) return true
  }
  return false
}

/**
 * 🚪 **해시태그 자동승격 허용 업종** (2026-07-29 대표 승인 — "적합성 게이트 걸자").
 *
 * ## 무엇이 문제였나 (라이브 실측)
 * 자동확장은 *관련성을 안 보고* 서로 다른 채널 5곳이 쓴 태그면 승격했다. 승격 대기 후보 상위에
 * **`주식투자`(hits 247)·`재테크`(227)·`주식초보`(219)·`주식`(216)·`주식공부`(202)·`주식강의`(143)** 이
 * 몰려 있었다 — 주식 태그는 서로 붙어 다녀서, 한 번 물꼬가 트이면 **auto 슬롯 40개를 통째로 먹는다.**
 * 그렇게 들어온 리드는 매장도 이용권 접점도 없어 결국 풀의 '기타'로 남는다(대표 지적).
 *
 * ## 왜 "분류 가능한가"만으론 안 되나
 * `주식투자` 는 분류기가 **`IT/재테크` 로 정확히 분류한다.** 즉 판단 실패가 아니라 **정책 문제**다.
 * 그리고 `IT/재테크` 는 시드에도 있는 정식 축이라, 분류 가능 여부로 거르면 그대로 통과한다.
 *
 * ## 그래서 가른 기준 — **시드 ≠ 자동승격**
 * 시드는 대표가 고른 **소수·고정**(축당 3~11개)이고, 자동승격은 **무한 증식**이다. 같은 축이라도
 * 무한 증식을 허용할 이유는 "그 축에서 실제로 거래가 일어나는가"에 달려 있다. 유어딜의 상품은
 * 매장 이용권과 공동구매다 — 그 접점이 있는 축만 자동으로 넓힌다.
 * ⇒ `IT/재테크`·`취미` 는 **시드로는 유지**(대표가 고른 소수는 그대로 돈다)하되 **자동확장은 막는다.**
 *
 * ⚠️ 이 게이트가 **하지 않는 일**: 이미 활성인 키워드를 끄지 않는다. 실측상 활성 auto 27개는
 * 대부분 성과가 좋고(`맛집` 414명 저장 · `피부관리` 363 · `mukbang` 188), 잡음(`일상`·`리뷰`·`vlog`)은
 * `found/saved` 가 0 이라 **고갈 판정(barren_streak)이 스스로 은퇴시킨다** — #853 이 그 회계를 고쳤다.
 * 손으로 끄면 그 자기치유를 앞질러 잘못 끌 위험만 생긴다.
 * ⚠️ 후보의 `hits` 는 계속 쌓인다 — 나중에 축을 열면 그날 바로 승격된다(정보를 버리지 않는다).
 */
export const AUTO_PROMOTE_CATEGORIES = new Set([
  '공동구매',                                   // 행위 자체가 유어딜 전환 신호
  '체험단',                                     // 이미 브랜드 협업을 받아 본 층(행위 신호 — 위 룰 docblock 실측)
  '맛집', '카페', '푸드', '외식창업', '마케팅대행사', // 매장 = 이용권 본체 · 대행사는 매장을 N건 물어온다
  '숙소', '여행',                               // 숙박 이용권
  '뷰티', '네일', '골프', '운동',                  // 인허가 업종 매장(피부관리·네일샵·골프연습장·헬스장)
  '육아', '반려동물', '리빙', '패션',              // 공동구매 품목이 실제로 도는 갈래
])

/** 이 태그를 자동승격해도 되는가 — 분류 불가('자동')와 정책상 제외 축을 함께 막는다(순수). */
export const canAutoPromote = (category: string | null | undefined): boolean =>
  !!category && AUTO_PROMOTE_CATEGORIES.has(category)

/**
 * 이름+소개글로 카테고리 판정. 신호 없으면 null(호출부가 키워드 카테고리 폴백).
 *   첫 매치 승 — RULES 순서가 우선순위(구체적 신호 먼저).
 */
export function classifyCategory(name: string, description?: string | null): string | null {
  const text = `${name} ${description || ''}`
  for (const r of RULES) {
    // 🪪 이름 전용 신호는 **이름에서만** 본다(위 `nameRe` 주석의 실측 근거).
    if (!r.re.test(text) && !(r.nameRe && r.nameRe.test(name))) continue
    // 🏗️ '공동구매'는 두 가지를 뜻한다 — 아래 가드가 우리가 원하는 쪽만 남긴다.
    if (r.cat === '공동구매' && isFitoutBulkBuy(text)) continue
    return r.cat
  }
  return null
}

/**
 * 🏗️ **입주 공동구매(시공·자재 업체)인가** — 우리가 찾는 공구 셀러가 아니다.
 *
 * ## 라이브 실측 (2026-07-29)
 * 공동구매로 분류된 205명을 열어 보니 상당수가 **아파트 입주 공동구매** 업체 블로그였다:
 * ```
 *   'LX Z:IN 인테리어 광주남구점 …공식대리점'  → LX창호 공동구매 행사 (7/30~8/5)
 *   '팔도방충망님의 블로그'                    → 블랙스텐망 공동구매 시공 후기
 *   '베니시모 휴앤홈 가구'                     → 입주예정자협의회 심사 거쳐 공동구매 제휴업체 선정
 *   '연아건축 055-972-0942'                  → 공동구매기획전 9평 이상 주택
 * ```
 * 이들은 **입주예정자협의회와 단지 단위로 계약하는 시공/자재 업체**다. 문자열은 똑같이
 * "공동구매"지만 우리가 찾는 것 — *자기 팔로워에게 직접 파는 사람* — 과 정반대 축이다.
 * 성격상 **업체 DB(파트너풀)의 대상**이지 인플루언서 풀이 아니다.
 *
 * ⚠️ 왜 지금 막는가: 방금 대기줄을 푼 공동구매 키워드 41개("공동구매"·"최저가 공구"·"특가 공구"…)가
 *    돌기 시작하면 이 부류가 **대량으로** 들어온다. 필터를 나중에 고치면 그때는 이미 섞인 뒤다.
 *
 * ⚠️ 이 가드가 **못 거르는 것**: 소개글이 짧아 신호가 아예 없는 행(실측 205명 중 139명이 판정불가 —
 *    네이버 블로그의 91%가 아직 활동성 측정 전이라 본문이 없다). 그건 필터가 아니라 **측정**의 문제다.
 */
export function isFitoutBulkBuy(text: string): boolean {
  if (!text) return false
  // 인플루언서 공구의 **적극적 신호**가 있으면 업체로 보지 않는다(겸업 블로그 보호 — 오탐이 더 비싸다).
  if (/공구\s*(오픈|진행|마감|링크|신청|일정|후기|공지|예약|템|셀러|전문|진행자)|(인스타|블로그|맘카페|카페|스토어|밴드|카톡|오픈채팅|맘|라방)\s*공구|(뷰티|화장품|식품|반찬|간식|육아용품|패션)\s*공구|쇼핑\s*라이브|라이브\s*커머스|그립\s*(라이브|방송)/i.test(text)) return false
  // ⚠️ 아래 토큰은 전부 **라이브 실측 문구에서 뽑았다**(추측으로 넓히지 말 것 — 넓힐수록 겸업 셀러를 자른다).
  //    '건축'은 bare 로 두면 건축 유튜버를 삼키므로 시공 맥락(후기/사무소/설계/토목/ALC)이 붙을 때만 본다.
  return /입주\s*(예정자|협의회|박람회|공동구매|가구|단지|청소|사전점검)|입예협|창호|방충망|줄눈|탄성코트|중문|붙박이장|시공\s*(후기|사례|문의|전문)|공식\s*지정점|제휴업체|공식\s*대리점|리모델링|\bALC\b|건축\s*(후기|사무소|설계|시공)|ALC\s*건축|토목/i.test(text)
}

// 빈도 계산용 global 사본(모듈 로드 시 1회). 원본 RULES 는 첫-매치-승이라 `g` 를 붙이면 안 된다(lastIndex 오염).
const RULES_G = RULES.map(r => ({ cat: r.cat, re: new RegExp(r.re.source, r.re.flags.replace(/g/g, '') + 'g') }))

/**
 * 🔢 **빈도 기반** 분류 — 글 본문처럼 **긴 텍스트 전용**.
 *
 * ## 왜 별도 함수인가
 * `classifyCategory` 는 **첫 매치 승**이다. 이름·소개글처럼 짧고 밀도 높은 텍스트엔 맞지만,
 * 글 본문 수천 자에 그대로 쓰면 **스치는 한 단어**가 카테고리를 결정한다
 * (여행 후기에 나온 "카페" 한 번이 그 블로거를 카페 블로거로 만든다).
 * 그래서 본문에는 ① 최소 반복 횟수를 요구하고 ② 가장 많이 걸린 규칙을 고른다.
 *
 * ⚠️ 호출부 규약: 이 값으로 **기존 판정을 덮지 말 것.** `classifyCategory` 가 null 일 때
 *    (= 이름·소개글·제목 어디에도 신호가 없을 때)만 채우는 **폴백**으로 쓴다.
 *    정밀도를 지키면서 재현율만 올리는 방향이고, 반대로 쓰면 조용히 오분류가 늘어난다.
 */
export function classifyCategoryByHits(text: string, minHits = 3): string | null {
  if (!text || text.length < 80) return null   // 짧은 텍스트는 빈도 신호가 의미 없다 — 첫-매치 함수 몫
  let best: { cat: string; hits: number } | null = null
  for (const r of RULES_G) {
    r.re.lastIndex = 0
    let n = 0
    for (;;) {
      const prev = r.re.lastIndex
      if (r.re.exec(text) === null) break
      if (r.re.lastIndex === prev) r.re.lastIndex++   // 빈 매치 방어(무한루프 금지)
      if (++n >= 200) break                            // 병적 입력 상한
    }
    if (n >= minHits && (!best || n > best.hits)) best = { cat: r.cat, hits: n }  // 동점은 RULES 순서(우선순위) 유지
  }
  return best?.cat || null
}

/** 저장 시점 최종 카테고리 — 콘텐츠 신호 우선, 없으면 키워드 카테고리(단 '자동'/'일반'은 null 로). */
export function resolveCategory(
  name: string, description: string | null | undefined, keywordCat: string | null | undefined,
  /** 🧪 위와 같은 이유의 주입구(기본 = 모듈 상수). */
  retired: ReadonlySet<string> = RETIRED_CATEGORIES,
): string | null {
  const byContent = classifyCategory(name, description)
  if (byContent) return byContent
  const kc = (keywordCat || '').trim()
  // 🪦 은퇴 축은 폴백으로도 안 붙인다 — 이게 없으면 ①(비우기)과 ②(다시 붙이기)가 매 회차 싸운다.
  return kc && !NON_CATEGORIES.has(kc) && !retired.has(kc) ? kc : null
}

// 🎯 YouTube topicDetails(구글 자체 영상기반 분류)가 구분 가능한 '거친' 카테고리 = topicToCategory 출력 범위.
export const YT_COARSE_CATEGORIES = new Set(['뷰티', '패션', '맛집', '여행', '운동', '반려동물', '취미'])
// 우리 세분 카테고리 → 유튜브가 뭉뚱그리는 상위. 이 관계면 유튜브 상위로 안 덮음(우리가 더 정확: 네일<뷰티, 카페/푸드/외식창업<맛집).
const FINE_PARENT: Record<string, string> = { 네일: '뷰티', 카페: '맛집', 푸드: '맛집', 외식창업: '맛집' }

/**
 * 🧭 최종 카테고리 종합(라이브 재조회/enrichment 용) — 우리 라이브 규칙(liveCat, 15종)을 1차, YouTube 신호(topicCat)를
 *   거친 카테고리 '교정자'로. 과거엔 topicCat 을 빈칸 채움만 했으나, 여기선 적극 교정:
 *   ① 우리 규칙 우선(없으면 저장값). ② 유튜브가 거친 수준에서 불일치하면 교정하되 — 우리가 더 세분(FINE_PARENT)이면 유지,
 *   우리 라이브 규칙이 저장값을 독립 지지하면 유지. ③ 유튜브가 못 판단하는 세분(육아/리빙/IT/숙소 등)은 우리값 유지.
 *   저장값이 있으면 절대 null 로 만들지 않음(정보 소실 방지).
 */
export function reconcileCategory(stored: string | null, liveCat: string | null, topicCat: string | null): string | null {
  const best = liveCat || stored || null
  if (topicCat && topicCat !== best) {
    if (best && FINE_PARENT[best] === topicCat) return best                        // 우리가 더 세분 → 유지
    if (!best) return topicCat                                                     // 미분류 → 유튜브로 채움
    // 🛡️ 2026-07-23 전수조사: '취미'(Hobby)는 유튜브가 매우 광범위하게 붙이는 토픽이라 **교정 권한 없음(채움 전용)** —
    //   About 빈약 채널(liveCat=null)에서 저장된 맛집/뷰티가 취미로 대량 전환되던 파괴적 덮어쓰기 차단.
    if (topicCat === '취미') return best
    if (YT_COARSE_CATEGORIES.has(best)) return liveCat === best ? best : topicCat  // 거친 불일치 → 라이브규칙 지지 없으면 유튜브 교정
  }
  return best
}
