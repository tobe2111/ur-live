/**
 * 📍 인플루언서 활동 지역 토큰 — **서버(수집 저장)와 클라이언트(어드민 필터)가 함께 쓰는 SSOT**.
 *
 *   원래 `features/marketing/api/influencer-region.ts` 안에 있었는데, 어드민 목록에도 같은 목록이
 *   필요해졌다. 화면 쪽에 손으로 복사하면 **두 목록이 갈라지는 순간 필터가 조용히 빈 결과를 낸다**
 *   (저장된 값에 없는 지역을 고르게 되므로 — 에러가 아니라 '0건'으로 보인다). 그래서 값만 여기로 뺐다.
 *   저장 로직(`regionFromKeyword` · `backfillRegions`)은 그대로 워커 쪽에 있고 이 목록을 import 한다.
 *
 *   ⚠️ 의미: 이 값은 **거주지가 아니라 '그 지역을 다루는 콘텐츠를 쓴다'** 는 신호다(수집 키워드 접두에서 유래).
 *   매칭 후보를 좁히는 데만 쓸 것 — 단정 금지.
 */

/** 지역 토큰 — 서울 자치구 + 주요 광역시/도시 + 실제 수집 키워드에 등장하는 생활권 이름. */
export const REGION_TOKENS = [
  // 서울 자치구
  '강남', '강동', '강북', '강서', '관악', '광진', '구로', '금천', '노원', '도봉', '동대문', '동작',
  '마포', '서대문', '서초', '성동', '성북', '송파', '양천', '영등포', '용산', '은평', '종로', '중랑',
  // 광역시·도
  '부산', '대구', '인천', '광주', '대전', '울산', '세종', '제주', '강원', '경기',
  // 경기·생활권(수집 키워드에 실제로 쓰이는 이름)
  '수원', '성남', '분당', '판교', '일산', '고양', '용인', '안양', '부천', '평택', '화성', '남양주',
  // 서울 세부 생활권
  '방배', '사당', '잠실', '건대', '홍대', '신촌', '이태원', '연남', '망원', '성수', '서면', '해운대',
] as const
