// 유어딜 위치기반서비스 이용약관 — 문안 SSOT: src/shared/legal (LegalDocPage 로 렌더).
import LegalDocPage from '@/pages/legal/LegalDocPage'

export default function LocationTermsPage() {
  return <LegalDocPage docKey="location" />
}
