/**
 * 🎨 유어샵 헤드라인 강조색 프리셋.
 *   2026-08-30: `CuratorHeader` 안의 지역 상수였는데 `HeaderMarquee` 분리로 둘이 쓰게 되어
 *   SSOT 로 뺐다 — 값 불변(두 곳이 갈리면 색 팔레트가 조용히 달라진다).
 */
export const ACCENT_PRESETS = ['#6b7280', '#4b5563', '#374151', '#1f2937', '#0E9F6E', '#111827']
