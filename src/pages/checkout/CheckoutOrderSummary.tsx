/**
 * CheckoutOrderSummary — 주문 상품 목록 + 최종 금액 요약.
 *
 * CheckoutPage TD-018 final-pass 분할:
 * OrderSummary 를 CheckoutPage 명세에 맞는 이름으로 re-export.
 * 실제 렌더링은 OrderSummary 에 위임.
 */
export { default } from './OrderSummary'
