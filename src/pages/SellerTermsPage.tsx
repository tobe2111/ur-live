// 유어딜 판매자 이용약관 — 문안 SSOT: src/shared/legal (LegalDocPage 로 렌더).
import LegalDocPage from '@/pages/legal/LegalDocPage'

export default function SellerTermsPage() {
  return <LegalDocPage docKey="seller" />
}
