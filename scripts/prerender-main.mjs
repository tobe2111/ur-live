#!/usr/bin/env node
/**
 * 🛡️ SSR 마이그레이션 Phase 3 placeholder.
 *
 * 가이드: docs/SSR_MIGRATION.md
 *
 * 다음 세션 작업:
 *   1. API 호출 — fetch('https://live.ur-team.com/api/group-buy/products?status=active&category=all')
 *   2. dist/server/entry-server.js dynamic import
 *   3. renderApp('/', initialData) 호출
 *   4. dist/client/index.html 의 <div id="root"></div> 를 <div id="root" data-ssr>{html}</div> 로 교체
 *   5. <script id="__SSR_INITIAL_MAIN__"> 안에 initialData 주입
 *
 * 현재 = placeholder. npm run build 영향 0 (별도 명령으로만 실행).
 */

console.log('[prerender-main] placeholder — Phase 3 에서 진짜 구현. docs/SSR_MIGRATION.md 참조.')
process.exit(0)
