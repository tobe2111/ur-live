/**
 * 🧬 주입 — 업체 정보 한 페이지 (2026-09-16).
 * 각 항목은 "되돌리면 같은 값을 두 곳에서 고치게 되거나, 조용히 틀린 것을 저장한다" 형태다.
 */
const TEST = 'src/tests/unit/store-info-one-page-2026-09-16.test.ts'
/** 소스 문자열이 아니라 **실제 렌더**로 판정하는 짝. */
const RENDER = 'src/tests/unit/store-info-render-2026-09-16.test.tsx'

export default [
  {
    name: 'store-info: 부분 성공을 하나로 뭉갠다',
    file: 'src/pages/seller-store-info/useStoreInfo.ts',
    find: "        failed.store = errText(e, '매장 정보 저장에 실패했습니다')",
    replace: "        failed.shop = errText(e, '저장에 실패했습니다')",
    test: TEST,
    why: '어느 쪽이 실패했는지 안 알리면 이미 저장된 절반까지 사장님이 다시 입력한다.',
  },
  {
    name: 'store-info: 실패한 쪽도 저장된 것으로 친다',
    file: 'src/pages/seller-store-info/useStoreInfo.ts',
    find: '      if (!failed.store) for (const k of STORE_KEYS) next[k] = form[k]',
    replace: '      for (const k of STORE_KEYS) next[k] = form[k]',
    test: TEST,
    why: '기준선이 옮겨지면 실패한 칸이 더 이상 "바뀜" 이 아니라 다시 저장할 수가 없다.',
  },
  {
    name: 'store-info: 안 바뀐 칸까지 통째로 보낸다',
    file: 'src/pages/seller-store-info/useStoreInfo.ts',
    find: '    for (const k of STORE_KEYS) if (form[k] !== initial[k]) storePatch[k] = form[k]',
    replace: '    for (const k of STORE_KEYS) storePatch[k] = form[k]',
    test: TEST,
    why: '서버의 "빈 값 무시" 규칙에 기대게 된다 — 그 규칙이 바뀌는 날 조용히 덮어쓴다.',
  },
  {
    name: 'store-info: 전파 예고를 아무 때나 띄운다',
    file: 'src/pages/seller-store-info/useStoreInfo.ts',
    find: '  const storeDirty = useMemo(() => STORE_KEYS.some((k) => form[k] !== initial[k]), [form, initial])',
    replace: '  const storeDirty = dirty',
    test: TEST,
    why: '소개글만 고쳐도 "이용권 N개 반영" 이 뜬다. 한 번 거짓말한 문구는 다음에도 안 믿는다.',
  },
  {
    name: 'store-info: 미리보기에 매장 확인 PIN 을 띄운다',
    file: 'src/pages/seller-store-info/StorePreviewCard.tsx',
    find: '          <Row icon={Phone} text={form.phone} placeholder="전화번호" />',
    replace: '          <Row icon={Phone} text={form.verify_pin} placeholder="전화번호" />',
    test: TEST,
    why: '비공개 칸이 "손님에게 보이는 모습" 카드에 들어가면 사장님이 공개된 줄 안다 — 개인정보 사고.',
  },
  {
    name: 'store-info: 미리보기가 서버 값을 다시 불러온다',
    file: 'src/pages/seller-store-info/StorePreviewCard.tsx',
    find: "import type { StoreInfoForm } from './useStoreInfo'",
    replace: "import api from '@/lib/api'\nimport type { StoreInfoForm } from './useStoreInfo'",
    test: TEST,
    why: '저장 후에만 맞는 카드가 된다 — 타이핑하는 대로 안 바뀌면 미리보기가 아니다.',
  },
  {
    name: 'store-info: 매장 목록의 [정보] 를 옛 모달로 되돌린다',
    file: 'src/pages/SellerStoresPage.tsx',
    find: '<Link to={`/seller/store?id=${s.seller_id}`}',
    replace: '<Link to="/seller/stores"',
    test: TEST,
    why: '입구가 갈리면 한쪽에서 고친 값이 다른 쪽과 어긋난다 — 같은 모달이 두 화면에서 열리고 있었다.',
  },
  {
    name: 'store-info: 셀러 홈 패널의 [정보] 만 옛 자리로',
    file: 'src/pages/seller-page/MyStoresPanel.tsx',
    find: '                  to={`/seller/store?id=${s.seller_id}`}',
    replace: '                  to="/seller/stores"',
    test: TEST,
    why: '두 입구 중 하나만 고치는 것이 이 레포의 반복 사고다(실제로 여기가 나중에 발견됐다).',
  },
  {
    name: 'store-info: 유어샵 설정 주소를 통째로 없앤다',
    file: 'src/routes/seller.routes.tsx',
    find: '      <Route path="/seller/mini-shop" element={<Navigate to="/seller/store" replace />} />',
    replace: '',
    test: TEST,
    why: '안내 문구·북마크에 퍼져 있는 옛 주소가 404 가 된다.',
  },
  {
    name: 'store-info: 셀러 프로필에 소개 칸을 되살린다',
    file: 'src/pages/SellerProfileEditPage.tsx',
    find: '        <div className="space-y-6">',
    replace: '        <div className="space-y-6">{formData.bio}',
    test: TEST,
    why: '같은 값을 두 화면에서 고칠 수 있으면 반드시 한쪽이 낡는다 — 그게 이 통합의 이유다.',
  },
  {
    name: 'store-info: 네비가 없어진 유어샵 설정을 계속 가리킨다',
    file: 'src/components/seller/seller-tab-groups.ts',
    find: "      { path: '/seller/store', labelKey: 'seller.nav.storeInfo', fallback: '업체 정보' },",
    replace: "      { path: '/seller/mini-shop', labelKey: 'seller.nav.miniShop', fallback: '유어샵 설정' },",
    test: TEST,
    why: '없어진 화면을 가리키는 메뉴는 낡은 지도다 — 누르면 리다이렉트로 튕긴다.',
  },
  {
    name: 'store-info(render): 미리보기를 서버 값으로 고정한다',
    file: 'src/pages/SellerStoreInfoPage.tsx',
    find: '              <StorePreviewCard form={form} category={category} />',
    replace: '              <StorePreviewCard form={{ ...form, name: \'\' }} category={category} />',
    test: RENDER,
    why: '타이핑을 안 따라오면 미리보기가 아니다 — 문자열 검사로는 증명이 안 되는 자리다.',
  },
  {
    name: 'store-info(render): 안 바뀌어도 저장을 켜 둔다',
    file: 'src/pages/SellerStoreInfoPage.tsx',
    find: 'disabled={!dirty || saving}\n                className="ur-btn ur-btn-lg ur-btn-primary hidden w-full disabled:opacity-40 lg:flex"',
    replace: 'disabled={saving}\n                className="ur-btn ur-btn-lg ur-btn-primary hidden w-full disabled:opacity-40 lg:flex"',
    test: RENDER,
    why: '누를 게 없는 버튼은 눌러 보게 만들고, 빈 저장이 서버로 나간다.',
  },
  {
    name: 'store-info(render): 폰 고정 바의 저장을 없앤다',
    file: 'src/pages/SellerStoreInfoPage.tsx',
    find: '        <SellerBottomBar>',
    replace: '        <SellerBottomBar key="x">{null}</SellerBottomBar>\n        <SellerBottomBar>',
    test: RENDER,
    why: '폰에서 저장 버튼이 화면 밖으로 밀리면 긴 폼의 마지막에 도달할 수 없다.',
  },
  {
    name: 'store-info(split): 옮긴 라우트를 등록하지 않는다',
    file: 'src/features/seller/api/seller-registration.routes.ts',
    find: 'mountSellerSessionRoutes(sellerRegistrationRoutes, ensureSellerColumns)',
    replace: '',
    test: TEST,
    why: '빌드는 통과하고 /my-seller-status·/switch-to-seller·/switch-to-user 만 조용히 404 가 된다.',
  },
  {
    name: 'store-info(split): 옮긴 파일이 ALTER TABLE 을 자기가 한다',
    file: 'src/features/seller/api/seller-registration/session-routes.ts',
    find: '  app.get(\'/my-seller-status\', async (c) => {',
    replace: '  app.get(\'/my-seller-status\', async (c) => {\n    await c.env.DB.prepare(`ALTER TABLE sellers ADD COLUMN x TEXT`).run().catch(() => null)',
    test: TEST,
    why: '메모가 두 벌이 되어 요청마다 DDL 이 돈다 — CLAUDE.md 머니 룰이 금지하는 per-request DDL.',
  },
  {
    name: 'store-info(split): 워커 동적 import 를 alias 로 되돌린다',
    file: 'src/features/seller/api/seller-registration/session-routes.ts',
    find: "const { createSessionCookie } = await import('../../../../worker/utils/session')",
    replace: "const { createSessionCookie } = await import('@/worker/utils/session')",
    test: TEST,
    why: '워커에서 alias 동적 import 는 런타임 크래시다(2026-04-22 사고 클래스).',
  },
]
