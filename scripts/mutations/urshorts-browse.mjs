/**
 * 🎬 유어쇼츠 전체 보기(`/urshorts`) 도시·종류 축 (2026-09-21) — 주입 매니페스트.
 * 가드: src/tests/unit/urshorts-facets-2026-09-21.test.ts
 */
const TEST = 'src/tests/unit/urshorts-facets-2026-09-21.test.ts'

export default [
  {
    name: '🎬 분류기가 모르는 것을 찍는다 — 시가 안 나와도 첫 지역으로 때운다',
    file: 'src/shared/urshorts-tags.ts',
    find: '  return { si, area }\n}',
    replace: "  return { si: si ?? '서울', area }\n}",
    test: TEST,
    why: '모르면 null 이 이 파일의 불변식이다. 찍으면 "부산 영상" 칩에 서울 것이 섞이고, 에러가 안 나 아무도 신고하지 않는다.',
  },
  {
    name: '🎬 시가 잡혔는데 다른 시의 상권을 채택한다',
    file: 'src/shared/urshorts-tags.ts',
    find: '    if (si && r.key !== si) continue',
    replace: '    if (false) continue',
    test: TEST,
    why: "'고성'(강원·경남) 같은 이름이 이미 잡힌 시를 뒤엎는다 — 부산 영상이 강원 칩으로 간다.",
  },
  {
    name: '🎬 카테고리 동점을 억지로 가른다',
    file: 'src/shared/urshorts-tags.ts',
    find: '  return tied ? null : best',
    replace: '  return best',
    test: TEST,
    why: '"호텔 브런치" 는 숙소이기도 식사이기도 하다. 찍으면 반은 틀리고 화면에만 조용히 남는다.',
  },
  {
    name: '🎬 연결된 이용권의 카테고리보다 글자 추정이 이긴다',
    file: 'src/shared/urshorts-tags.ts',
    find: '  const category = linked ?? extractCategory(',
    replace: '  const category = extractCategory(',
    test: TEST,
    why: '우리가 실제로 파는 것이 진실이다. 뒤집히면 한 번 잘못 뽑힌 추정이 상품을 계속 이긴다.',
  },
  {
    name: '🎬 값 없는 영상이 칩 개수에 섞인다 — "모름" 이라는 도시가 생긴다',
    file: 'src/pages/urshorts/facets.ts',
    find: "    if (!v) continue",
    replace: "    if (false) continue",
    test: TEST,
    why: '빈 문자열이 키가 되어 이름 없는 칩이 하나 생긴다.',
  },
  {
    name: '🎬 두 축이 AND 가 아니라 OR 이 된다',
    file: 'src/pages/urshorts/facets.ts',
    find: "    if (f.si && (it.region_si ?? '') !== f.si) return false",
    replace: "    if (false) return false",
    test: TEST,
    why: '"부산 + 식사" 를 고른 사람에게 서울 식사가 섞인다 — 칩이 거짓말이 된다.',
  },
  {
    name: '🎬 내려간 도시의 옛 링크가 빈 화면으로 남는다',
    file: 'src/pages/urshorts/facets.ts',
    find: '    si: f.si && city.some((x) => x.key === f.si) ? f.si : null,',
    replace: '    si: f.si,',
    test: TEST,
    why: '공유된 `?si=제주` 로 들어왔는데 그 도시가 비면, 누를 수 없는 칩과 빈 목록만 남는다.',
  },
  {
    name: '🎬 화면이 분류기를 다시 import 해 상권 키워드 수백 개를 소비자 번들에 싣는다',
    file: 'src/pages/urshorts/facets.ts',
    find: "import { shortsRegionLabel } from '@/shared/urshorts-regions'",
    replace: "import { regionLabel as shortsRegionLabel } from '@/shared/urshorts-tags'",
    test: TEST,
    why: '칩 열 개를 그리려고 388줄짜리 지역 표를 내려받게 된다 — 화면은 멀쩡해 보여 아무도 모른다.',
  },
  {
    name: '🎬 작은 지역 표가 진짜 표와 갈린다',
    file: 'src/shared/urshorts-regions.ts',
    find: "  '부산', '울산', '경남', '대구', '경북', '광주', '전남', '전북',",
    replace: "  '부산', '울산', '경남', '대구', '경북', '광주', '전남',",
    test: TEST,
    why: '지역이 하나 빠지면 그 도시 영상은 어드민에서 고를 수 없고 서버 PATCH 도 400 을 낸다.',
  },
  {
    name: '🎬 공개 SQL 이 글자 추정을 상품보다 먼저 본다',
    file: 'src/features/urshorts/api/urshorts.routes.ts',
    find: '         COALESCE(p.category, s.category) AS category,',
    replace: '         COALESCE(s.category, p.category) AS category,',
    test: TEST,
    why: '한 번 잘못 뽑힌 글자 분류가 실제로 파는 상품을 영원히 이긴다 — 에러 없이 칩만 틀린다.',
  },
  {
    name: '🎬 일괄 분류가 서브리퀘스트 한도를 넘겨 뒤쪽을 조용히 흘린다',
    file: 'src/features/urshorts/api/urshorts.routes.ts',
    find: '    const batch = todo.slice(0, 8)',
    replace: '    const batch = todo',
    test: TEST,
    why: '무료 요금제는 인보케이션당 50 서브리퀘스트다 — 넘치면 에러가 아니라 "일부만 채워짐" 으로 끝난다.',
  },
  {
    name: '🎬 어드민이 아무 문자열이나 도시로 저장한다',
    file: 'src/features/urshorts/api/urshorts.routes.ts',
    find: '      if (v && !REGION_SI.includes(v)) {',
    replace: '      if (false) {',
    test: TEST,
    why: '오타 하나가 칩을 갈라 "부산" 과 "부산시" 두 칩이 생긴다 — 둘 다 진짜처럼 보인다.',
  },
  {
    name: '🎬 등록할 때 분류를 안 돌린다',
    file: 'src/features/urshorts/api/urshorts.routes.ts',
    find: "    await classifyAndFill(DB, c.env, Number(r.meta.last_row_id)).catch(() => null)\n    return c.json({ success: true, video_id: parsed.id })\n  } catch (err) {\n    return safeError(c, err, '유어쇼츠를 추가하지 못했습니다', '[urshorts:admin]')",
    replace: "    return c.json({ success: true, video_id: parsed.id })\n  } catch (err) {\n    return safeError(c, err, '유어쇼츠를 추가하지 못했습니다', '[urshorts:admin]')",
    test: TEST,
    why: '새로 넣은 영상이 영원히 미분류로 남아 전체 보기의 칩에 안 잡힌다(어드민이 손으로 채우기 전까지).',
  },
  {
    name: '🎬 「전체 보기」가 다시 세로 뷰어로 간다 — 목록을 볼 방법이 없어진다',
    file: 'src/components/home/UrShortsRail.tsx',
    find: '        <Link\n          to={URSHORTS_BROWSE_PATH}',
    replace: '        <Link\n          to={URSHORTS_VIEWER_PATH}',
    test: TEST,
    why: '대표가 신고한 바로 그 증상이다 — 전체 보기를 눌러도 무엇이 있는지 볼 수 없다.',
  },
  {
    name: '🎬 하드로드 진입점이 청크 병렬화에서 빠진다',
    file: 'src/worker/index.ts',
    find: "      : url.pathname === '/urshorts' ? 'urshorts'\n",
    replace: '',
    test: TEST,
    why: '목록 페이지 청크가 엔트리 뒤에 직렬로 따라와 로더 구간이 길어진다 — 에러는 안 난다.',
  },
]
