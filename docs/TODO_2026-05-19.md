# 📋 운영 + 개발 TODO — 2026-05-19 (KT Alpha 통합 후)

> 이 문서는 진행 중 / 미완 작업의 단일 진실원천 (SSOT).
> 우선순위: 🔴 즉시 / 🟡 다음 단계 / 🟢 시간 날 때

---

## 🔴 즉시 운영자 액션 (15분 — 사용자가 직접)

### 1. Migration 0269 적용 — brand_name 컬럼
```powershell
npx wrangler@3 d1 execute toss-live-commerce-db --remote --file=migrations/0269_products_brand_name.sql
```

### 2. 대량 등록 재실행 (카테고리 + 브랜드 자동 분류)
- `/admin/kt-alpha` → **📦 대량 등록 2,260개** 클릭
- 약 1분 대기 → 카테고리: "voucher" → "편의점/카페/도서/..." 자동 갱신
- 브랜드명 (스타벅스/GS25 등) 자동 채워짐

### 3. 식사권 좌표 cron 수동 trigger
DevTools Console (어드민 로그인 상태):
```javascript
fetch('/api/admin/_run-cron', {
  method: 'POST',
  headers: { Authorization: 'Bearer ' + localStorage.getItem('admin_token'), 'Content-Type': 'application/json' },
  body: JSON.stringify({ name: 'restaurant-geocode' })
}).then(r => r.json()).then(d => console.log(JSON.stringify(d, null, 2)))
```

### 4. KT Alpha 비즈머니 충전 (실제 발송용)
- https://bizgift.giftishow.co.kr/ → 비즈머니 충전
- 최소 10만원 (테스트), 권장 100만원~ (운영)
- 충전 후 `/admin/kt-alpha` → **잔액 갱신** 클릭

### 5. 본인 폰으로 테스트 발송 1건
- 일반 사용자 계정으로 로그인
- 딜 충전 (예: 5천원)
- 메인 페이지 → 검색 → "스타벅스" → 1천원 교환권 → "🎁 딜로 교환"
- 본인 폰에 MMS 도착 확인

---

## 🟡 다음 코드 작업 (필요 시 진행)

### 인프라
- [ ] **R2 Bucket 활성화** — 셀러 사업자등록증 업로드 (Dashboard 에서 bucket 생성 + binding)
- [ ] **KV namespace 등록** — `RATE_LIMIT_KV`, `SESSION_KV` (Dashboard binding)
  - 현재 일부 추가됨 (스크린샷에서 확인) — 추가 확인 필요
- [ ] **ALIGO 알림톡 KISA 승인** — 5종 템플릿 (`docs/OPERATOR_MANUAL_ACTIONS_2026-05-19.md` Phase 10)
- [ ] **D1 migration 0267, 0268, 0269 적용 확인** — production 미반영 가능성

### 신규 기능 — 미구현
- [ ] **인플 ↔ 셀러 매칭 시스템** — 셀러가 인플 검색/연락 (전체 신규)
- [ ] **다객실 한 결제 검증** — 코드는 만들어졌지만 실 데이터 테스트 필요
- [ ] **8.8% 원천징수 자동 계산** — 코드는 hook 됨 / 지급조서 export UI 완성됨

### KT Alpha 후속
- [ ] **브랜드 아이콘 활용** — gift_catalog.brand_icon_url 을 products.brand_icon_url 로 복제
- [ ] **발송 실패 자동 재시도** — 현재는 1회 실패 후 어드민 알림만
- [ ] **사용된 voucher 자동 상태 갱신** — KT Alpha 0201 getCouponInfo 주기 호출
- [ ] **셀러 본인 휴대폰 등록 강제** — `/seller/profile` 진입 시 알림

### UI 개선
- [ ] **/admin/products 좌측 카테고리 트리 사이드바** (현재 칩 필터만)
- [ ] **CSV export/import** — 어드민 상품 일괄 처리
- [ ] **인라인 가격/재고 편집 강화** — 이미 부분 구현
- [ ] **검색 결과 정렬 옵션** — `/browse?sort=price-asc` 등
- [ ] **식사권 등록 폼에 카카오 장소검색 필수화** — 좌표 누락 방지

### 품질 부채
- [ ] **NOT NULL INSERT 5건** — warn-only, 산발적으로 발생 (`scripts/check-sql-not-null-insert.mjs`)
- [ ] **i18n 새 키 [TODO:lang] 외주 번역** — 80+ 키, 6개 언어
- [ ] **SellerLiveAnalyticsPage TS 에러** — `color` property mismatch (사전 이슈)
- [ ] **CHECK 제약 추가 migration** — voucher_orders/stay_bookings status

### 운영 가이드 업데이트
- [ ] **`docs/OPERATOR_MANUAL_ACTIONS_2026-05-19.md` 갱신** — 본 TODO 와 일치시키기
- [ ] **`docs/CURRENT_WORK.md` 마지막 commit 까지 반영**

---

## 🟢 시간 날 때 (장기)

### 모니터링
- [ ] **KT Alpha 비즈머니 잔액 부족 알림 채널** — 현재는 dashboard 알림만 / Slack/이메일 추가
- [ ] **발송 성공률 dashboard 추가** — `/admin/kt-alpha` 에 시계열 그래프
- [ ] **카테고리별 매출 통계** — 어떤 브랜드/카테고리 인기인지

### UX 고도화
- [ ] **메인 홈에 "🎁 선물하기" 입구** — 친구에게 보내기 (현재는 본인만)
- [ ] **검색 자동완성 KT Alpha 상품 포함** — 일부 동작하지만 정렬 보강 필요
- [ ] **카테고리별 사용자 행동 분석** — 어떤 카테고리 클릭하는지 추적

### 보안 / 컴플라이언스
- [ ] **`KT_ALPHA_TOKEN_KEY` Plain text → Secret** 전환 (Cloudflare Dashboard)
- [ ] **개인정보처리방침에 KT Alpha 발송 항목 추가**
- [ ] **법인 사업자등록증 KT Alpha 콘솔에 업로드 확인**

---

## 📊 최근 완료 (참고)

### 2026-05-19 session
- ✅ KT Alpha B2B API 통합 (0101/0111/0102/0112/0201/0202/0203/0204/0301)
- ✅ 어드민 페이지 `/admin/kt-alpha` + 카테고리별 관리 + 전체 삭제
- ✅ 셀러 voucher 받기 + 본인 휴대폰 검증
- ✅ 비사업자 8.8% 원천징수 자동 + 지급조서 CSV export
- ✅ 자동 대량 등록 (chunked 50/batch)
- ✅ 메인 홈 카테고리별 섹션 + 브랜드 칩 2차 분류
- ✅ 교환권 상세페이지 Kakao 스타일 디자인 + 딜 단위 표시
- ✅ Admin/Products Coupang WING 스타일 (검색/필터/정렬/페이지네이션)
- ✅ 상품권 → 교환권 전체 변경 (6개 언어)
- ✅ 셀러 사이드바 "💰 딜 정산" 명확화
- ✅ /restaurant-map 클라이언트 geocoding → 서버 cron (속도 1-3초 개선)
- ✅ 검색창 글씨 흰색 영구 해결 (글로벌 CSS)
- ✅ 수동 cron trigger endpoint

---

**최종 업데이트**: 2026-05-19
**다음 검토**: KT Alpha 비즈머니 충전 + 실제 발송 테스트 완료 후
