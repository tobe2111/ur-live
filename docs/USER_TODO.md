# 👤 사용자(운영자)가 직접 해야 할 일 — TODO

> Claude 가 대신 못 하는, **사용자(어드민 권한/의사결정)만 할 수 있는** 액션 목록.
> "내가 뭐 해야 해?" 라고 물으면 Claude 가 이 파일 기준으로 알려줌.
> 완료한 항목은 `[x]` 로 체크.

## 🔴 지금/곧 (배포·검증)
- [ ] **배포 확인** — feature 브랜치가 main 자동 머지 → Cloudflare Pages 배포됐는지 GitHub Actions 녹색 확인.
- [ ] **스키마 적용** — 배포 후 새 컬럼/테이블 적용. **셋 중 하나** (아무거나):
  - 그냥 둠 → 매일 새벽 3시(18 UTC) cron 이 자동 적용 (아무것도 안 해도 됨)
  - **(쉬움)** 즉시 → `/admin/health` 페이지 → "DB 스키마 복구" 섹션 → **"지금 스키마 복구" 버튼 클릭** ✨ (신규)
  - (고급) curl: `curl -s https://live.ur-team.com/api/_internal/repair-schema -H "Authorization: Bearer <admin_token>"`
- [ ] **시각 검증** (Claude 가 화면을 못 봄 — 큰 UI 변경 직접 확인):
  - 홈 `/` = 교환권 + "딜 모으는 법" 으로 바뀜
  - 하단바 = 홈/동네딜/쇼핑/링크샵/마이
  - 동네딜 `/group-buy` 위치 필터
  - 공급자 `/supplier`(대시보드·발송관리) / 어드민 `/admin/suppliers`(승인·지급)
  - 셀러/공급자 로그인 페이지 상호 이동 링크

## 🟡 운영 액션 (도매몰 가동 시)
- [ ] **첫 공급자 승인** — 도매상이 `/supplier/register` 가입하면 `/admin/suppliers` 에서 승인(pending→approved). 승인 전엔 로그인 불가.
- [ ] **공급상품 승인** — 공급자가 등록한 상품을 `/admin/products` → "공급자 등록 상품" 탭에서 승인(→ 셀러 카탈로그 노출).
- [ ] **요율 확정** — `platform_settings` 값 점검/확정:
  - `influencer_store_intro_pct` (영입자 매장영입 커미션, default 1.5%)
  - 공급자 마진 기준 수수료(D3), 에이전시 2% 등

## 🟠 결정 필요 (P1 — 정하면 Claude 가 구현)
- [ ] **D7 세금 자동화** — 비사업자 영입자 현금 출금 시 주민번호 수집 + 3.3% 원천징수, 팝빌/바로빌 국세청 자동신고. (현재: 사업자 3.3%/비사업자 8.8% payout cron 동작, 세금계산서 자동발행·RRN 은 수동)
- [ ] **D9 정산 주기** — 공급자/영입자 지급 주기(즉시/주간/월간).
- [ ] **출금 방식** — 영입자 커미션: 현재 auto-push(잔고>10만원 자동송금) vs pull(사용자 출금신청) 중 선택.

## 🟢 선택 (원하면 Claude 가 구현)
- [x] ~~어드민 "스키마 복구" 버튼~~ → 완료 (`/admin/health`, commit 46fae25)
- [x] ~~공급자 입점 혜택을 비즈니스 랜딩(`/business`)에 노출~~ → 완료 (commit 46fae25)
- [ ] version-check: 일정 시간 무시 시 자동 새로고침(현재는 배너 수동)
- [ ] B2B 공급사↔셀러 채팅/문의 기능 (검토용 — 아래 메모 참고)

---
_최종 업데이트: 2026-06-01 (도매몰 INC-1~8 완성 + 노출 동선 + 버전배너 세션)_
