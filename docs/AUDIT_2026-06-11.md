# 전 영역 심층 감사 — 2026-06-11 (무료 티어 제약)

> 12+ 전문 에이전트 병렬 감사 + 직접 코드 재검증(오탐 기각). 사용자 제약: **무료 티어 유지** —
> 유료 필요 항목(Cloudflare Queues, Cache Reserve)은 "참고"로만, 무료 방법만 "추천/적용".

## ✅ 이번 감사로 수정 완료 (배포됨)

### 💰 머니 손실 (실제로 돈이 새던 곳)
| 건 | 내용 | 커밋 |
|---|---|---|
| High#1 | affiliate 환불 미회수 (status='granted' 필터 0건 버그) | 356e8c2~ |
| High#2 | 에이전시 매장영입 커미션 환불 미역전 (단일 함수 추출) | 동일 |
| High#3 | 구매자 referral_bonus 무한 파밍 (역전 함수 신설) | 동일 |
| Med-B | signup_bonus 동시 첫주문 이중적립 (store 부분 UNIQUE) | 동일 |
| Med-F | 이중 원천징수/지급조서 중복 (멱등 가드 + UNIQUE) | 동일 |
| Med-A | confirm 크래시 멱등 구멍 (early-return 제거, [UNLOCK]) | 동일 |
| Med-C | webhook 커미션 누락 (공용 멱등 헬퍼, [UNLOCK]) | 동일 |
| invite | 초대보상 이중적립 (claim-before-credit) | 동일 |
| growth_bonus | 월성장보너스 이중적립+매일실행 (1일 게이트) | 동일 |

### 🔐 보안
| 건 | 내용 |
|---|---|
| 🔴 PII | 비인증 셀러 list가 전 셀러 email/phone/사업자번호 노출 → 비민감 컬럼만 |
| 🔴×6 | 금융 작업(공급자 지급/출금 승인/커미션율/세무/정산)에 finance 롤 게이트 (super 무영향) |
| 🟢 | getSellerIdFromToken type 검증 |
| IDOR/권한상승/토큰누출 | **0건** (전 변경성 핸들러 소유권 제한 확인) |

### 🗄️ 정합성/멱등
| 건 | 내용 |
|---|---|
| UNIQUE 5종 | user_coupons/community_gb_refunds/time_deal_claims/seller_follows/invite_rewards repair-schema 보강 |
| 죽은 cron | 0 3(geocode/kt-sync)·0 9(리마인더) wrangler 등록 — 영영 안 돌던 것 복구 |
| yearMonth | 1월 'YYYY-00' 버그 정정 |
| wholesale_orders.updated_at | 환불 보상 CAS 무음실패 컬럼 |

## ✅ 2차 마감 (🔴 + 🟡 일부 — 이번 세션 추가 배포)

### 🔴 머니/정합성 — 전부 수정 완료
1. ✅ **숙소 confirm/cancel race** (stays-public.routes.ts) — confirm: Toss 승인 후 `stay_bookings` pending→confirmed CAS claim(진 thread 멱등 반환), cancel: confirmed/pending→cancelled CAS claim(진 thread 409). 캘린더 이중 차감/이중 복원(오버부킹·팬텀재고) 차단. locked payment.routes 의 reserve-before-charge 패턴과 동일.
2. ✅ **admin/seller 취소 no-refund** — admin 단일: CANCELLED/REFUNDED + 캡처상태면 `refundOrderFully` 자동 라우팅(Toss취소+전 커미션역전+재고복원+감사로그). admin bulk + seller bulk: 결제완료 섞이면 `REFUND_REQUIRED` 차단(주문별 환불 유도).
3. ✅ **payouts-generate 이중 INSERT** — (payee,기간) pre-check + INSERT OR IGNORE + `idx_payouts_period_unique`(repair-schema). cron 이중실행/수동재실행에도 중복 0.

### 🟡 무료 코드 정리 — 일부 완료
- ✅ **funnel KV write 한도/세션 보호** — 고볼륨 view/click 5% 샘플(inc 20 보정), analytics 쓰기를 ANALYTICS_KV 전용 격리(SESSION_KV 잠식 차단 — 세션 끊김 방지). 미바인딩 skip(0원).
- ✅ **initSentry 미호출 silent no-op** — captureException/Message 가 Sentry 미초기화 시 `console.error` 폴백 → Workers Logs(무료)에 결제/웹훅 보안·금전 이벤트 기록.
- ✅ **죽은 코드 삭제** — `src/middleware/rateLimit.ts`(importer 0) 제거. (firebase-admin 의존성은 build external+import 0 이라 번들 무영향 → 제거 보류, 가치 낮음.)
- ✅ **seller 로그인/리프레시 per-request 7 ALTER** → `ensureSellerColumns` WeakSet 메모이즈(isolate 1회).
- ✅ **/api/home/categories N+1**(최대 25쿼리) → 윈도우 함수(ROW_NUMBER PARTITION BY) 3쿼리.

### 💰 2차 감사 중 추가 발견·수정 (머니 race)
- ✅ **예약(appointments) 취소 딜 이중환급** — refund_status 사전체크만(원자성 X) → 동시/더블탭 취소 시 포인트 이중적립. order PAID→CANCELLED CAS claim 한 thread 만 환급(진 thread 멱등 skip) + 환급 3 writes DB.batch 원자화. *(스테이/주문취소/payout 과 동일 claim-before-credit 패턴)*

## ⏳ 남은 백로그 (우선순위순 — 전부 무료)

### 🟢 무료로 "켜기만" 하면 되는 것 (사용자 액션 — Cloudflare 대시보드)
1. **Tiered Cache** (Caching→Tiered Cache, 무료 토글) → 크로스-PoP 캐시 네이티브, KV L2 코드 삭제 가능
2. **Workers Logs** (무료 토글) → 추측 대신 로그 (captureException 폴백이 여기 기록됨)
3. **Web Analytics** (무료) → Core Web Vitals RUM

### 🟡 코드 정리 — ✅ 전부 마감 (3차)
- ✅ per-request DDL hot/warm 경로 전부 메모이즈 — seller 로그인 7 ALTER(`ensureSellerColumns`), seller-public 중복 3곳 제거, seller-analytics 4곳. (잔여는 admin 전용 저빈도 — 영향 미미)
- ✅ N+1 — /api/home/categories(25→3쿼리, 윈도우함수), 마이주문 order_items(N→1, IN 일괄), 숙소 야간 캘린더(아래 [UNLOCK])
- ✅ DB.batch — appointments 환급(+ CAS 이중환급 차단), stays confirm 성공경로, supplier 운송장 3 writes
- ✅ [UNLOCK 승인] payment.routes.ts 야간 캘린더 2N왕복→2 batch + releaseStays 단일 batch / helpers.ts clawback 루프→batch

### 🛡️ 재발 방지 (후 감사 의존 탈피)
- CLAUDE.md '💸 머니/정합성 코드 작성 룰' 신설 — 머니 버그 13건의 공통 4클래스를 작성 시점 룰로
- `scripts/check-money-patterns.sh` pre-commit 추가 (inline DDL / 무환불 CANCELLED 자동 감지)
- DEV_IMPLEMENTATION_PLAYBOOK Step 3 에 머니 룰 매핑 추가

## 종합 판정
- **코드 백로그 0** — 머니/보안/정합성/성능 감사 발견 건 전부 마감 (2026-06-11 3차).
- 남은 것은 사용자 대시보드 액션 3건뿐: Tiered Cache / Workers Logs / Web Analytics (전부 무료 토글).
