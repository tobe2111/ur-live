# 홈 헤더에 유어쇼츠 진입점 — 시안 5안 (2026-09-09)

대표: *"이 부분에 유어쇼츠 페이지로 가는 버튼이 있으면 좋을까? 시안들 받아보고 싶네?"*
(모바일 홈 상단 헤더 캡처와 함께)

시안 이미지: `assets/home-header-shorts-entry-2026-09.png` (다크·라이트 두 열 × 여섯 줄)

## 지금 무엇이 문제인가

유어쇼츠로 가는 문이 **레일 안 「전체 보기」 하나뿐**이다. 그 레일은 인기 이용권 **다음**에 있으므로
(2026-09-07 대표 확정 — 히어로 바로 아래로 올리면 첫 딜 표시가 늦어진다), 스크롤해서 거기까지
내려간 사람만 유어쇼츠가 있다는 걸 안다. 홈 첫 화면에는 단서가 하나도 없다.

## 헤더가 이미 지고 있는 계약 (시안이 지켜야 하는 것)

| 줄 | 지금 하는 일 | 소스가 못박은 것 |
|---|---|---|
| ① 로고 + 유틸 3개 | 검색·알림·장바구니 | 앱 도구 묶음 |
| ② 위치 제목 + [목록\|지도] | 같은 목록의 **다른 보기**로 가는 전환 | *"하단 탭에 지도가 없으므로 이 컨트롤이 유일한 통로 — 지우지 말 것"* |
| ③ 카테고리 칩 | 같은 화면 안의 **필터** | 활성은 *"색이 아니라 무게로"*(잉크 + 밑줄) |

그리고 09-01 결정: **브랜드색은 하단 탭 하나에만.** 그래서 시안 다섯 개 어디에도 파란 면 버튼이 없다.

## 안

| 안 | 무엇 | 강점 | 대가 |
|---|---|---|---|
| **안 1 · 유틸 아이콘 4번째** | 검색·알림·장바구니 옆 쇼츠 아이콘 | 가장 작다. 규칙 무접촉 | 아이콘만으로 "쇼츠"를 못 알린다. 유틸(앱 도구) 묶음에 콘텐츠를 끼우는 것이라 성격이 갈린다 |
| **안 2 · 보기 토글 3분할** | [목록 \| 지도 \| 쇼츠] | 이미 있는 컨트롤을 늘릴 뿐 | 🔴 **그 컨트롤의 계약을 깬다.** 목록·지도는 같은 338곳의 두 보기인데 쇼츠는 다른 콘텐츠다. 누른 사람은 "같은 딜을 영상으로 보는 것"을 기대한다 |
| **안 3 · 카테고리 줄 맨 앞** | 전체·식사 앞에 아이콘 + 쇼츠 | 손가락 동선이 가장 좋다 | 옆 항목은 필터(화면 안)인데 쇼츠만 페이지 이동이다 |
| **안 4 · 위치 줄 글자 링크** | 토글 왼쪽 「유어쇼츠 ›」 | 글자라 무엇인지 분명하다. 세로 공간 0 | 토글과 나란히 서서 한 벌처럼 보일 수 있다 |
| **안 5 · 헤더 아래 얇은 띠** | 썸네일 3장 + 「유어쇼츠 3편 ›」 | 무엇을 보러 가는지 그림으로 먼저 말한다. 가장 안 놓친다 | 세로 +42px. **첫 딜이 그만큼 밀린다** — 09-07 에 레일을 아래로 내린 이유와 정면으로 맞선다 |

## 추천 — 안 4 (위치 줄 글자 링크)

다섯 중 **두 계약을 하나도 안 깨면서 무엇인지 말로 알려 주는 유일한 안**이다.
① 세로 공간을 안 먹어 첫 딜 표시 시각(559ms→304ms로 당겨 둔 값)에 영향 0
② "유어쇼츠"라고 적혀 있어 아이콘 해독이 필요 없다
③ 색을 안 쓴다(잉크 글자 + 꺾쇠) — 브랜드색은 하단 탭에만 남긴다는 결정 유지
④ 롤백이 `<span>` 하나 제거

더 세게 원하시면 **안 5**. 다만 그건 09-07 "유어쇼츠는 인기 이용권 다음" 결정을 부분적으로
되돌리는 것이라 대표 판단이 필요하다.

**안 2 는 추천하지 않는다** — 동작은 되지만 사용자가 기대한 것과 다른 화면이 뜬다.

## ⚠️ 자리보다 먼저 볼 것 두 가지

1. **지금 유어쇼츠는 3편이고 셋 다 이용권이 안 붙어 있다**(`product_id` null, 2026-09-09 실측).
   헤더 자리를 내주면 사람이 더 오는데, 도착한 화면에는 **살 것이 없다.**
   진입점을 늘리기 전에 영상에 이용권을 붙이는 편이 순서상 앞이다.
2. **레일 「전체 보기」가 실제로 안 눌리는지 우리는 모른다.** 그 클릭을 재고 있지 않다.
   "안 보여서 안 온다"인지 "봤는데 안 누른다"인지에 따라 처방이 다르다 — 후자면 진입점을 늘려도 안 는다.

## 신규 부품

`ShortsIcon` — `src/components/icons/urdeal-icons.tsx` 에 없다(현재 10개). 24 그리드·획 1.6·둥근 끝,
세로 프레임(9:16 암시) + 채운 재생 삼각형. 시안 이미지의 아이콘이 그것이다.

## 🔴 자리 확정 — 카테고리 줄 오른쪽 끝 (대표 2차 지시)

대표가 캡처에 빨간 박스로 자리를 찍어 줬다: *"빨간색 부분에 넣는게 좋지 않을까? 아이콘도 만들까?"*
⇒ 위 다섯 안 중 **어느 것도 아니고 새 자리**다. 그리고 이 자리가 위 안들보다 낫다:

- 필터들과 **거리로 분리**된다(같은 줄이지만 반대쪽 끝) — 안 3 의 "필터와 성격이 갈린다"가 해소된다
- 지금 **실제로 비어 있다**(카테고리 5개가 왼쪽 절반만 쓴다)
- 세로 공간 0 — 안 5 의 "첫 딜이 밀린다"가 없다
- [목록\|지도] 토글과 **다른 줄**이라 안 4 의 "한 벌처럼 보인다"가 없다

시안 이미지: `assets/home-header-shorts-catrow-2026-09.png` (아이콘 3안 + 자리 6안 × 두 테마)

### 🔴 구현 함정 — 스크롤 밖에 고정해야 한다

카테고리 줄은 `overflow-x-auto` 다(`MobileHomePage.tsx` 의 `<nav>`). 진입점을 그 안에 넣으면
**카테고리가 하나만 늘어나도 같이 밀려 화면 밖으로 사라진다.** 지금은 5개가 다 들어와 스크롤이
안 생겨서 티가 안 날 뿐이다. ⇒ `<nav>` 를 `[스크롤 영역][고정 진입점]` 두 칸으로 나눈다.

### 자리 안

| 안 | 무엇 | 대가 |
|---|---|---|
| A · 글자만 | 「유어쇼츠 ›」 | 새 부품 0. 눈을 끄는 힘이 가장 약하다 |
| **B · 아이콘 + 글자** | 아이콘이 끌고 글자가 확인시킨다 | 없음 |
| C · 아이콘만 | 가장 좁다 | 처음 보는 사람이 모른다. 유어쇼츠는 아직 이름을 아는 사람이 없다 |
| D · 아이콘 + 글자 + 점 | 로고의 점을 빌린다 | 상시로 켜 두면 곧 아무도 안 본다. **"새 영상 있음"에만** 켜야 뜻이 산다 |
| E · 알약 | 눌리는 것임이 가장 분명 | 카테고리 줄의 유일한 면이라 필터보다 무거워진다(09-01 "색이 아니라 무게로"와 맞선다) |
| F · 썸네일 + 글자 | 무엇을 보러 가는지 그림으로 | 썸네일 요청이 첫 화면에 한 장 는다(지금 레일은 화면에 다가올 때만 받는다) |

### 아이콘 안 (대표 *"아이콘도 만들까?"* → 만든다)

`urdeal-icons.tsx` 에 재생/영상 계열이 없다(현재 10개). 24 그리드·획 1.6·둥근 끝, 하단 탭과 같은 규약.
**판정 기준은 하나: 실제로 쓰이는 16px 에서 뭉개지지 않는가.**

| 아이콘 | 무엇 | 판정 |
|---|---|---|
| **1 · 세로 프레임 + 재생** | 9:16 화면 그 자체 | 16px 에서 가장 안 뭉갠다. 쇼츠의 형태를 말한다 |
| 2 · 겹친 두 장 + 재생 | "여러 편을 넘긴다" | 16px 에서 뒤 장이 앞 장에 먹힌다 |
| 3 · 원 + 재생 | 가장 읽기 쉽다 | 범용 「재생」이라 유어쇼츠라는 신호가 약하다 |

### 추천 — **안 B + 아이콘 1**

아이콘이 눈을 끌고 글자가 무엇인지 확정한다. 새 이름(유어쇼츠)이라 **글자를 빼면 안 된다**(안 C 탈락).
면을 안 쓰므로 카테고리 필터보다 무거워지지 않는다(안 E 탈락). 첫 화면 요청도 안 는다(안 F 탈락).

**안 D 의 점은 나중에 얹는다** — "마지막 방문 이후 새 영상이 있을 때만" 켜지는 조건이 붙을 때.
조건 없이 상시로 켜면 한 주 만에 배경이 된다.

## ⚠️ 자리보다 먼저 볼 것 두 가지 (변함없음)

1. **지금 유어쇼츠는 3편이고 셋 다 이용권이 안 붙어 있다**(`product_id` null, 2026-09-09 실측).
   진입점을 늘리면 사람이 더 오는데 **도착한 화면에 살 것이 없다.**
2. **레일 「전체 보기」가 실제로 안 눌리는지 우리는 모른다.** 그 클릭을 재고 있지 않다.

## ⏳ 구현 대기

대표 선택 후 진행. 선택 전에는 코드 변경 없음.
