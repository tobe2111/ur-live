# 유어샵 역할 모델 (UrShop Role Model)

> 작성: 2026-07-02 · 출처: 대표 논의 ("인플루언서·매장 업주·에이전시·유어딜 운영·일반 유저 5부류가 각각 만족스러운 성과를 내는 이상적 구조")
> 상태: **부분 폐기 — 아래 정정을 먼저 읽을 것.** 레이아웃/모드 설계는 유효, **신분 분류와 경제 표는 폐기**.

---

## ⚠️ 2026-08-27 정정 — 이 문서를 그대로 구현하지 말 것

이 문서는 2026-07-02 자 설계이고, 그 뒤 대표 결정 네 건이 전제를 갈아치웠다. 그런데 문서 상태가
*"방향의 SSOT · 다음 세션도 이 문서를 따라 구현할 것"* 이라 **읽는 세션이 폐기된 모델을 구현하게 된다.**

| 이 문서의 전제 | 현재 사실 |
|---|---|
| 이름 **링크샵** | **유어샵** (2026-08-26 대표). URL `/u/{handle}`·코드 식별자(`linkshopPath`·`nav.linkshop`·이 파일명)는 **불변** |
| **5부류 신분**(인플루언서·매장 업주·에이전시·운영·일반 유저)으로 사람을 나눔 | **폐기.** 신분이 아니라 **행위 2개** — **담기(소개)**=누구나 · **운영(대행)**=`seller_operators` 위임받은 사람만 (2026-08-26 대표) |
| 유어샵을 "소유하는 건 3부류" | **누구나** 가입 시 자동 생성(`KakaoAuthService.upsertUser` handle 발급) |
| 경제 엔진 = 어필리에이트 핀 2% + 초대 + 멀티티어 stack | **셋 다 종료**(2026-08-22/23). 소개비는 **매장이 제안한 딜**에만 붙는다(`seller_influencer_deals`, 제안서의 % 그대로) |
| 매장 영입 1.5% | **2%** · **기간 제한 없음** (2026-08-27 대표) |

**여전히 유효한 것**: §2 역할 적응형 2모드(본인 상품 유무 → 큐레이터↔스토어프론트 레이아웃),
"+" 담기가 항상 어딘가 노출돼야 한다는 원칙(막다른 골목 제거), 소유권 판정 단일화(§5.5 — 가드
`check-linkshop-ownership` 가 강제 중).

현행 구조의 SSOT 는 [urdeal-platform-model.md](./urdeal-platform-model.md) §7 이다.

---

## 0. 왜 이 문서가 필요한가 (문제)

링크샵(`/u/{handle}`)은 **한 개의 URL/표면**인데, 소유자가 누구냐에 따라 **정반대의 콘텐츠 모델**을 요구한다.

- **인플루언서**: 링크샵의 주인공 = **남의 상품 추천(핀)**. 본인 상품이 없음.
- **매장 업주**: 링크샵의 주인공 = **본인 상품**. 남의 상품 추천은 정체성을 흐림.

이 둘을 하나의 고정 레이아웃에 욱여넣으니:
- 모든 상품/공구 상세에 뜨는 **"+" 핀 버튼**("내 링크샵에 담기")이 매장 링크샵에선 **안 보임** → **막다른 골목**.
- 2026-06-26 "추천템 숨김" 결정으로 사업자 링크샵에서 핀을 껐더니, "내가 담은 게 왜 안 뜨지?" 혼란 발생.

**"핀 켜/꺼" 이분법으론 항상 한쪽이 어정쩡하다.** 근본 해법은 **역할이 표현을 정하고, 경제 엔진은 공통으로 두는 것**.

---

## 1. 5부류 매트릭스 (누가 뭘 하고 무엇으로 성과를 내는가)

| 부류 | 링크샵 소유? | Job(핵심 할 일) | 성과 지표 | 경제(현행 SSOT) | 코드 실체 |
|---|---|---|---|---|---|
| **일반 유저** | ✅ 자동생성 | 딜 발견·구매, 가끔 친구에 추천 | 딜 적립·절약, 가벼운 핀 수익 | 핀 어필리에이트 **기본 2%**(`affiliate_commission_rate`) | `users` + handle |
| **인플루언서** | ✅ | 팔로워에 **추천**해 수익화 (+매장 영입) | 핀 클릭→구매 커미션, 영입 매장 매출 | 핀 2% + 매장영입 **1.5%**(`influencer_store_intro_pct`) | `sellers.seller_type='influencer'` |
| **매장 업주** | ✅ | **본인 상품/이용권** 판매 | 판매액·현금 정산 | 판매 시 플랫폼 **−5%**(`commission_rate_default`) | `sellers.seller_type='store_owner'` |
| **에이전시** | ❌ (뒷단) | 여러 매장 관리·영입·성장 | 관리 매장 매출 rollup, 영입 커미션 | 매장영입 커미션(`agencies.store_intro_commission_pct`) | `agencies` |
| **유어딜 운영** | ❌ (플랫폼) | 위 4부류가 **다 거래하게** | 총 GMV × take rate | 판매 5% + 후원 15% + 마진 | 플랫폼 |

> ※ 원천징수: 사업소득 3.3% / 기타소득 8.8% (`tax-withholding.ts`) — 인플루언서·영입 적립 지급 시 적용.
> ※ 도매 공급자(제조사)는 **B2B(유통스타트)** 축이라 소비자 링크샵과 별개 — 이 문서 범위 밖(서비스 분리 룰).

### 핵심 통찰 (프레임 교정)
**링크샵을 "소유"하는 건 위 3부류(유저·인플루언서·매장 업주)뿐이다.**
에이전시·운영업체는 링크샵을 소유하지 않고, **그 링크샵들이 잘 돌 때 성과를 먹는 뒷단**이다.
→ 그래서 질문은 둘로 쪼개야 한다:
1. **링크샵 표면 설계** = 유저·인플루언서·매장 업주 3부류 문제
2. **성과 rollup** = 에이전시·운영업체는 대시보드/집계 트랙 (링크샵 레이아웃과 무관)

---

## 2. 이상적 구조 — "역할이 표현을 정하고, 엔진은 공통"

### 원칙 1 · 링크샵은 1개, 콘텐츠 블록은 2개, 순서는 역할이 결정
- 블록 A = **내 상품/이용권** (본인이 판매 등록한 것)
- 블록 B = **추천(핀)** (남의 상품을 "+"로 담은 것)
- **본인 상품 보유 여부로 모드 자동 전환**:

| 상태 | 모드 | 레이아웃 |
|---|---|---|
| 본인 상품 **있음** (매장 업주) | **스토어프론트 모드** | 내 상품 = hero(상단) · 추천 = 하단 **opt-in "추천" 섹션**(기본 off) |
| 본인 상품 **없음** (유저·인플루언서) | **큐레이터 모드** | 추천(핀) = hero |
| 둘 다 (both) | 스토어프론트 우선 + 추천 opt-in on 권장 | 상동 |

### 원칙 2 · 경제 엔진은 하나, 그리고 **stack(중첩)**
핀 2% + 판매 −5% + 영입 1.5% 는 **동시에 쌓인다.** 한 사람이 매장도 하고 남의 상품도 추천하면 **둘 다 적립.**
→ **운영업체(유어딜)에 최적**: 추천이든 판매든 모든 행위가 거래를 만들고 take rate가 붙는다.

> ⭐ **재원 확정 원칙 (2026-07-08)**: 핀 2%·영입 1.5% **그리고 에이전시 커미션(§4)까지 전부 매장 promo(5% 밖) 재원**(`promo_funding_source=owner`) — 유어딜 5% 를 **어떤 축도** 안 건드린다(8월 flip, 예외 없음). 에이전시는 매장-인플 조율로 promo 마진에서 스스로 먹는 독립 사업자(= 쇼핑 벤더의 오프라인판). 상세: `commission-funding-restructure.md` §확정 원칙.
→ 그래서 운영 관점에선 **"핀을 숨기는 것 = 발견·거래 기회를 끄는 손해"**. 숨기지 말고 *배치*로 해결한다.

### 원칙 3 · "+" 핀 버튼은 모두에게 유지, 목적지만 명확히
- 핀 버튼(`PinButton`)은 모든 상품/공구 상세에 그대로 노출(범용 발견·어트리뷰션 엔진).
- **매장 업주(스토어프론트 모드)가 담으면** → 하단 "추천" 섹션에 담기고 **안내 토스트**: "내 링크샵 하단 '추천'에 추가됐어요".
- 막다른 골목 제거 + 정체성 보존 동시 달성.

---

## 3. 화면 구조 (와이어프레임)

### 큐레이터 모드 (일반 유저 · 인플루언서 — 본인 상품 없음)
```
┌────────────────────────────┐
│  [배너 히어로 + 이름 + bio] │
│  [YouTube][Insta]  공유     │
├────────────────────────────┤
│  🔍 검색                    │
│  추천템 N                   │  ← 핀이 주인공(hero)
│  [카드][카드]               │
│  [카드][카드]               │
│  교환권·동네딜 N            │
│  [카드][카드]               │
├────────────────────────────┤
│  MORE INFO +  (사업자면)    │
└────────────────────────────┘
```

### 스토어프론트 모드 (매장 업주 — 본인 상품 있음)
```
┌────────────────────────────┐
│  [배너 히어로 + 이름 + bio] │
│  [YouTube][Insta]  공유     │
├────────────────────────────┤
│  내 이용권 N                │  ← 본인 상품이 주인공(hero)
│  [카드][카드]               │
│  내 상품 N                  │
│  [카드][카드]               │
│  ─────────────────         │
│  ✨ 정지원님의 추천 (opt-in) │  ← 부가, 명확히 라벨, 하단
│  [카드][카드]               │     (기본 off, 켜야 노출)
├────────────────────────────┤
│  MORE INFO +  ← 판매자 정보 │  ← 맨 밑 쇼핑몰식 푸터(구현 완료)
└────────────────────────────┘
```

---

## 4. 에이전시 · 운영업체 트랙 (링크샵 밖 — rollup)

- **에이전시**: `/agency/*` 대시보드에 **관리 매장 매출/정산 rollup** + 영입 성과. (현재 이 rollup이 가장 비어 있는 영역 — 별도 설계 필요.)
- **운영업체(유어딜)**: `/admin/*` 에 전체 GMV·take rate·부류별 활성도. 링크샵은 **수요 캡처 표면**이므로, 운영 KPI = "죽은 클릭(담았는데 안 보이는 핀) 0" + 부류별 전환.

---

## 5. 단계별 구현안 (phased)

| 단계 | 내용 | 범위 | 리스크 |
|---|---|---|---|
| **1** | **매장 링크샵 하단 "추천" 섹션 opt-in 부활** | `CuratorPage`가 pins 전달 + `SellerPublicPage`가 `CuratorPinsSection`을 하단 렌더(설정 on일 때). 인프라 이미 존재 | 낮음(additive) |
| **1b** | 핀 담기 시 **안내 토스트**("하단 추천에 추가됨") + 매장 대시보드에 "추천 표시 on/off" 토글 | `PinButton` 성공 콜백 + 셀러 설정 1개 | 낮음 |
| **2** | **모드 자동 전환** (본인 상품 유무로 큐레이터↔스토어프론트) | `CuratorPage`/`SellerPublicPage` 렌더 분기 정리 | 중간(로딩 잠금 주의) |
| **3** | **에이전시 매출 rollup 대시보드** | `/agency` 신규 집계 뷰 | 중간(별도 설계) |
| **4** | 부류별 온보딩 분기 (가입 시 "추천할래요 / 팔래요"로 초기 모드 힌트) | 온보딩 모달 | 낮음 |

> 각 단계는 독립 배포 가능. 1단계만으로도 "막다른 골목" 혼란은 해소된다.

---

## 5.5 소유권 모델 (SSOT — CI 가드 강제, 2026-07-07)

> **왜 이 섹션이 생겼나**: 링크샵이 반복적으로 "내 가게인데 방문자로 보임 / 편집 기능이 사라짐"으로
> 깨졌다. 근본 원인은 `/u/{handle}` 이 **두 페이지 컴포넌트**로 렌더되는데(일반 유저=`CuratorPage`,
> 사업자=인라인 `SellerPublicPage`), **소유자 판정 신호가 서로 달랐기** 때문이다. 세션마다 한쪽만
> 패치하니 다른 쪽 seam 이 계속 터졌다. 아래를 **불변식**으로 고정한다.

**단일 불변식**: **`/u/{handle}` 링크샵의 주인 = 로그인한 소비자 유저** (`user_id === curator.id`, 단일 신호).
- `seller_token` 은 **셀러 대시보드(`/seller/*`) 접근용일 뿐**, 링크샵 *뷰/편집* 을 가르지 않는다.
- 프로필·소개·SNS·배너·주소(handle)·핀 편집은 전부 **소비자 API `/api/curator/me/*`** (ur_session 쿠키)로 처리.
- 소유권 계산은 `CuratorPage.isOwner`(소비자 정체성)에서 **한 번** 하고, 인라인 `SellerPublicPage` 에는
  `ownerOverride` prop 으로 **내려준다**. `SellerPublicPage.isOwner = !!ownerOverride || tokenOwner`
  (tokenOwner = seller_token 폴백, **레거시 standalone `/profile`·`/s` 진입 + 소비자 계정 없는 셀러-only** 전용).
- 순수 뷰 자식(`CuratorHeader`/`InfoTab`/`VouchersTab`/`VideosTab`)은 소유/편집을 **prop 으로만** 게이트 —
  `localStorage` 의 `seller_token`/`seller_id` 를 직접 읽지 않는다.
- 셀러 API 를 요구하는 유일한 링크샵 필드(`kakao_chat_link` 인라인 편집)만 `canSellerEdit`(=`!!seller_token`)로
  별도 게이트(토큰 없으면 어포던스 숨김 → 401 방지). 나머지는 토큰 무관.

**강제**: `scripts/check-linkshop-ownership.mjs` (audit-gate + `verify.yml` strict) — 위 3 불변식(①신호전달
②isOwner 가 ownerOverride 포함 ③뷰자식 prop 구동)을 검사. 되돌리면 CI 차단. 예외 `linkshop-ownership-ok`.

> 이 클래스 외 링크샵 반복 버그도 각각 가드 보유: **로더 블링크**=`check-loader-continuity.mjs`,
> **테마**=`check-theme-consistency.mjs`, **OG/메타**=worker SSR CURATOR 슬롯. 새 링크샵 불변식을 발견하면
> 패치가 아니라 **가드부터** 만든다(CLAUDE.md 철학).

---

## 6. 현재 코드 vs 목표 차이

| 항목 | 현재 | 목표 |
|---|---|---|
| 매장 링크샵 핀 표시 | ❌ 완전 숨김 (2026-06-26) | 하단 "추천" opt-in 섹션 |
| 모드 전환 | 없음(linked_seller면 무조건 스토어프론트) | 본인 상품 유무로 자동 |
| "+" 핀 담기 후 피드백 | 없음 → 매장은 어디 갔는지 모름 | 토스트 안내 + 위치 명시 |
| 에이전시 성과 뷰 | 부분(영입 커미션만) | 관리 매장 매출 rollup |
| 경제 엔진 stack | ✅ 이미 stack (핀+판매+영입 동시) | 유지 |

---

## 7. 열린 결정사항 (대표 확인 필요)

1. **매장 "추천" 섹션 기본값**: off(직접 켜야 노출) vs on(기본 노출)? → 정체성 보수적이면 **off 권장**.
2. **인플루언서 본인 상품 판매 허용?**: 인플루언서가 매장 상품도 등록하면 both 모드. 허용 범위?
3. **에이전시 rollup 우선순위**: 지금 착수 vs 링크샵 3부류 먼저 정리 후?
4. 라이브커머스 영구중단(`LIVE_COMMERCE_SUSPENDED`) 유지 전제 — 인플루언서 `canBroadcast`는 휴면. 추천/영입이 인플루언서 주 수익 축.

---

## ✅ 구현 로그
- 2026-07-04 **1단계 + 1b 구현** (대표 "다 해줘 이상적으로" — commit 은 PR 참조):
  ① `users.linkshop_show_recommend`(기본 0=off, sellers 컬럼한도 회피) — curator.routes GET/PATCH/DDL 3곳.
  ② CuratorPage → SellerPublicPage `curatorPins` 전달 재개(2026-06-26 숨김의 opt-in 해소).
  ③ SellerPublicPage: 라이브 섹션 아래·푸터 위에 `CuratorPinsSection`(기존 고아 컴포넌트 재사용) —
  opt-in ON 이면 방문자 노출, 오너뷰에선 OFF 여도 흐린 미리보기 + 안내. 오너 편집 배너에 "추천 ON/OFF"
  토글(유저 세션 인증 PATCH /api/curator/me/profile). ④ 1b: 매장 업주(seller_id 보유)가 핀 담으면
  "내 링크샵 하단 '추천'에 담김 + ON 안내" 토스트(usePinAction). 기본값 off = §7 열린결정 1 보수안 채택.
