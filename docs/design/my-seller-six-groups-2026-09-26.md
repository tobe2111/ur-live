# 🏪 마이에서 끝내기 — 판매를 여섯 묶음으로 (2026-09-26 시안)

> 대표: *"일단 마이에서 대부분 끝내야 해. 시안을 보여줄래?"*
> 앞선 지시: *"모두 마이에서 하도록"* · *"셀러대시보드를 쓸 필요없게끔"*

**시안(렌더본)**: https://claude.ai/artifact/Lq8d3pnYM9xWZALsa1YFDE
**상위 설계 SSOT**: `docs/design/seller-in-consumer-app-2026-09.md` (§14 선별 · §19 재확정 · §20 모두 마이에서)

---

## 0. 먼저 숫자를 바로잡는다 — "65개" 가 아니라 **12개**

전 세션이 대표께 *"셀러 화면이 ~55개"* 라고 보고했다. 라우트 수는 맞지만 **오해를 준다.**
`seller-nav.ts` 를 실제로 세면 **매장 사장님(`store_owner` + `SELLER_STORE_ONLY_MODE`)이
사이드바에서 보는 항목은 12개**다. 나머지는 탭 형제 라우트 · 검색 전용(11개) · 로그인/콜백
통과 화면이다.

⇒ *"대부분 마이에서"* 는 55개를 옮기는 일이 아니라 **12개를 옮기는 일**이고, 그중 절반은
이미 손이 닿아 있다. 규모 판단이 바뀌므로 이 정정이 이 문서의 출발점이다.

## 1. 지금 — 12개 중 **완전히** 마이에서 끝나는 것은 2개

| 메뉴 | 라우트 | 지금 | 제안 |
|---|---|---|---|
| 대시보드 홈 | `/seller` | ✅ 오늘 카드가 대체 | 유지 |
| 유어샵 | `/u/me` | ✅ 소비자 화면 | 유지(나가는 게 맞다) |
| 이용권 등록 | `/seller/meal-voucher/new` | ✅ **마이**(같은 폼을 시트로) | 완료 |
| 이용권 관리·수정 | `/seller/group-buy` | 🔶 중지·재개만 | ✅ 마이 |
| ~~숙소~~ | `/seller/stays` | 🧹 **내림**(셀러 소유 0 · 예약 0) | 플래그로 복귀 가능 |
| 주문 | `/seller/orders` | 🔶 확인 대기만 | ✅ 마이 |
| 정산 | `/seller/settlements` | ✅ **마이**(출금 + 지난 정산) | 완료 |
| ~~쿠폰~~ | `/seller/coupons` | 🧹 **내림**(셀러 생성 0) | 플래그로 복귀 가능 |
| 소개 파트너 | `/seller/influencer-deals` | ✅ **마이**(제안 수락·거절) | 완료 |
| 사업자 정보 | `/seller/business-info` | 🔶 계좌만 | 🔶 서류는 나감 |
| 매장 | `/seller/stores` | 🔶 전환만 | ✅ 마이 |
| 알림톡(브랜드메시지) | `/seller/alimtalk` | ✅ **마이**(잔액·내역 — 발송은 전용 화면) | 완료 |

**완전 2 · 반쪽 5 · 나감 5 → (목표) 완전 9 · 반쪽 1 · 나감 2.**

> ✅ **2026-09-26 2차 — 목표에 도달했다(대표 *"나머지도 다 해줘. 그리고 뺄 것들 빼자"*).**
> 실제 **완전 8 · 반쪽 1 · 나감 2**: 홈 · 유어샵 · 주문 · 이용권(관리+등록) · 정산(출금+**명세**) ·
> 가게 · **소개 파트너** · **브랜드메시지**. 반쪽은 사업자 정보(서류 업로드만 나감),
> 나감은 이용권 등록 위저드의 원본 화면과 운영 가이드.
>
> 🧹 그리고 **목록 자체가 줄었다** — 실측 0인 메뉴 넷을 내렸다(쿠폰·프로모 코드 → `SELLER_COUPONS_HIDDEN`,
> 숙소·체험 캠페인·후기 인증 → `SELLER_DORMANT_HIDDEN`). **12개 → 9개.**
> 분모가 줄었으니 "완전 8/9" 은 분자만 늘린 결과가 아니다.
>
> ⚠️ **숙소는 같은 날 넣었다가 같은 날 접었다.** 넣은 근거는 대표 지시(*"숙소까지 해줘"*)였고
> 접은 근거는 실측(셀러 소유 숙소 상품 0 · 예약 0)이다. **지운 게 아니라 접었다** —
> `StaysSheet`·라우트·API 그대로이고 플래그 한 줄이면 돌아온다.
>
> 🔴 **브랜드메시지에서는 보내지 않는다**(발송 = 등급 C). 잔액·최근 발송만 읽고 발송·충전은
> 전용 화면으로 보낸다. 진척을 말할 때 "알림톡이 마이에서 된다" 로 뭉뚱그리지 말 것.

🔑 지금의 문제는 "손이 안 닿는다" 가 아니라 **"시작만 되고 마무리는 대시보드"** 다.
어제 넣은 환불·분석·출금·PIN·계좌는 실제로 마이에서 끝나지만, **메뉴 단위로 보면 반쪽**이다.

## 2. 왜 낱개 목록이 아니라 묶음인가

지금 마이의 판매 영역은 `ToolRow` **낱개 4줄 + 전체 도구 1줄**이다. 도구가 늘 때마다 이 목록이
길어진다 — 그 끝은 **마이 안에 두 번째 사이드바**이고, 그러면 화면만 옮겼을 뿐 대시보드를
없앤 게 아니다.

⇒ 사장님이 실제로 생각하는 단위로 묶는다. **화면 수를 여섯으로 고정**하고, 도구가 늘면
묶음 *안에서* 자라게 한다.

| 묶음 | 흡수하는 메뉴 | 시트 안에서 끝나는 일 |
|---|---|---|
| **오늘**(카드) | `/seller` · `/seller/realtime` | 매출·주문·대기 + 사용처리 |
| **주문** | `/seller/orders` · `/seller/appointments` | 대기 확인 · 전체 목록(기간·상태) · 주문 열기 · 환불 · 예약 |
| **이용권** | `/seller/group-buy` | 목록 · 중지/재개 · 가격·수량 · 안내 문구 |
| **손님** | `/seller/coupons` · `/seller/alimtalk` · `/seller/influencer-deals` | 쿠폰 발행·현황 · 알림톡 · 파트너 승인·요율 |
| **정산** | `/seller/settlements` · `/seller/ledger` | 받을 돈 · 출금 · 지난 내역 · 명세 · 계좌·PIN |
| **가게** | `/seller/stores` · `/seller/business-info` | 전환 · 영업시간·휴무·연락처 · 승인 상태 설명 |
| **매출 분석** | `/seller/store-dashboard` | 2주 추이 · 이번 달 · 많이 팔린 순 |

## 3. 나가는 셋 — 복제하지 않는 이유

같은 폼이 두 벌이 되면 **반드시 한쪽만 고쳐지고**, 그때부터 두 화면이 다른 값을 저장한다
(이 레포가 반복해 당한 클래스). 아래 셋은 한 손으로 끝낼 수 없어 **기존 화면을 그대로 쓴다** —
그 화면 맨 위에는 `BackToMyBar`(§20)가 이미 "마이로 돌아가기" 를 띄운다.

1. **이용권 등록** — 사진 여러 장 · 옵션 · 사용 조건 · 매장 선택. 시트에 넣으면 스크롤이 두 겹이 되고,
   사진을 고르는 동안 시트가 닫힌다.
2. **숙소 달력** — 한 달 달력은 가로 폭을 요구한다. 시트 폭에서 날짜 칸이 손가락보다 작아진다.
3. **사업자등록증** — 촬영 + 글자 읽기 + 심사. 1년에 한 번 하는 일이라 마이에 상주시킬 이유가 없다.

## 4. 순서 (자주 누르는 순)

1. **주문** — 하루에 가장 많이 연다. 지금은 어제 주문을 찾으려면 반드시 나가야 한다. *(읽기 위주)*
2. **이용권** — 가격·수량 수정이 제일 잦은데 그 버튼이 `/seller/group-buy` 안에만 있다. *(쓰기 · 좌석 확인)*
3. **정산** — 출금·PIN·계좌는 이미 있다. 명세·지난 내역을 붙이면 돈 일이 한 곳에서 끝난다. *(🔴 머니)*
4. **가게** — 영업시간·휴무를 마이에서 아예 못 고친다. 승인 상태를 같은 자리에서 설명하면 문의가 준다.
5. **손님** — 쿠폰·알림톡·파트너. 셋 다 **손님에게 무언가 나가는 일**이라 확인 단계를 같은 모양으로.

각 단계는 독립적이고, 중간 상태가 어색하지 않다 — 묶음이 생기면 기존 낱개 줄이 그 안으로 들어갈 뿐이다.

## 5. 🔴 대표 판단이 필요한 것 (등급 C)

1. **정산 묶음을 언제 켜느냐** — 출금은 마이에 있지만 **라이브에서 돈이 나가 본 적이 없다**(E3).
   명세를 붙이기 전에 staging 실결제 1회가 먼저다 (`STAGING_CHECKLIST.md` S-MYSELL-9·10).
2. **쿠폰 발행 · 알림톡 발송을 마이에서 허용할지** — 둘 다 되돌릴 수 없어(발행·발송) 규칙상 결재 항목.
   기본안: 마이에 두되 **확인 한 번 더**.
3. **가격 수정을 마이에서 할 수 있게 할지** — 손님이 보는 값이 한 손으로 바뀐다. 편한 만큼 실수도
   한 손으로 난다. 확인 단계 여부를 정해 주시면 그대로 만든다.

---

## 구현 로그

- 2026-09-26 시안 작성 (PR #1547). 선행 완료분은 `seller-in-consumer-app-2026-09.md` §20 구현 로그 참조(PR #1546, `55dccae`).
- 2026-09-26 **묶음 셋 구현 — 승인이 필요 없는 범위만**. 낱개 `ToolRow` 목록 → **묶음 다섯**
  (주문 · 이용권 · 정산 · 매출 분석 · 가게). 서버 변경 0 — 세 시트 모두 이미 있는 엔드포인트만 쓴다.
  - 신규 `OrdersSheet`(지난 주문 · 상태 칩 · 더 보기 · 주문 열기 · 확인) — `GET /api/seller/orders`
  - 신규 `VoucherSheet` + `VoucherEditSheet`(목록 · 중지/재개 · 가격 · 정가 · 수량 · 이용 안내)
    — `GET/PUT /api/seller/products/:id`. **가격이 바뀌면 확인 한 번**(결정 ③ 전까지 보수적 기본값).
  - 신규 `StoreSheet`(가게 이름 · 연락처 · 주소 · 소개 · 승인 상태 · 가게 전환) — `PUT /api/seller/profile`.
    🔴 **계좌 필드를 안 보낸다** — 섞이면 서버가 PIN(412)·소유자(403) 게이트를 켠다.
  - ♻️ `SellingList` **삭제** — 이용권 묶음이 그 일을 이어받았다(같은 목록이 두 곳이면 갈린다).
  - 🔴 **머지된 코드의 결함 수리**: `BankSheet` 가 계좌 변경의 **412 `PIN_REQUIRED`** 를 안 받아
    막다른 길이었다(출금이 막혀 계좌를 넣으러 온 사장님이 거기서 또 막혔다). 이제 그 자리에서
    PIN 을 풀고 **계좌로** 돌아온다(`pinReturn` — 출금에서 왔으면 출금으로).
  - 가드: `seller-groups-in-my-2026-09-26.test.ts` 21건 + 주입 15건 **되돌려-검증 전부 빨간불 확인**.
  - 번들: `UserProfilePage` 92 → 108 KB(전부 lazy) · critical-chunks **17 불변**.
- 2026-09-26 **후속 — 이용권 등록 · 숙소도 마이 안으로, 쿠폰은 내림** (대표 *"이용권 등록, 숙소까지
  해줘 쿠폰은 앞으로 필요없을 것 같은데? 확인해줘"*).
  - 🎟️ **등록**: 폼을 복제하지 않는다 — `SellerMealVoucherNewPage` 를 **`embedded` 로 그대로** 시트에
    띄운다(`VoucherNewSheet`). `SellerLayout` 에 `bare` 추가(껍데기만 벗고 훅은 그대로, 도매
    리다이렉트보다 **먼저** 반환 — 시트 안에서 튕기면 마이가 사라진다). 완료·취소는 라우팅 대신
    콜백. **폼·검증·제출 payload·임시저장 한 글자도 안 바뀐다**(시험이 고정).
    🏝️ 그 페이지들은 `dark:` 가 금지된 라이트 고정이라 **`light-island` 클래스**로 감싼다(주석 아님).
    ⏳ `lazy` 로 갈라 **누를 때** 받는다 — 위저드 45 KB 는 별도 청크, 마이 청크는 +4 KB 만.
  - 🏨 **숙소**: `StaysSheet`(목록·상태·객실/예약 수, **읽기 전용**) — 이용권 묶음 안에서 연다.
    **달력·객실 편집은 전체화면 유지**(한 달 달력은 가로 폭을 요구한다).
  - 🎫 **쿠폰**: 메뉴에서 내림(`SELLER_COUPONS_HIDDEN`, 가역). **라우트·페이지·API 보존.**

### 📏 그때 같이 잰 것 — 숙소도 쿠폰과 **같은 상태**다 (2026-09-26 라이브 실측)

| | 값 |
|---|---|
| 셀러 수 | 11곳 |
| **셀러가 만든 상품** | **3개 — 전부 `meal_voucher`** |
| 숙소 상품 | 77개인데 **셀러 소유 0개**(전부 `seller_id IS NULL` = 플랫폼·데모) |
| 셀러가 만든 쿠폰 | **0건** (`coupons` 유일 1행 `WELCOME2026` 은 `seller_id IS NULL`) |
| `promo_codes` · `district_coupons` | 각 0행 |

> ⚠️ **쿠폰을 빼는 근거(사용 0)가 숙소에도 똑같이 적용된다.** 그래도 숙소를 만든 것은 대표가
> 명시로 요청했기 때문이고, 쿠폰과 달리 **앞으로 팔 물건**이라는 판단은 대표 몫이다.
> 숙소가 실제로 안 쓰일 것이라면 같은 플래그 방식으로 내리면 된다(라우트 보존).

- ⏳ **아직 안 한 것**: 손님 묶음에서 쿠폰이 빠져 **알림톡 · 소개 파트너** 둘만 남았다 —
  발송·승인이라 **등급 C**(대표 승인 대기).
  정산 묶음의 명세·지난 내역 — 출금이 아직 E3 이라 staging(S-MYSELL-9·10) 뒤.
