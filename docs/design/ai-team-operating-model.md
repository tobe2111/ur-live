# AI 팀 운영 모델 — 역할 에이전트 6 + 결재함 1 + 완료 판정 기준 (2026-09-07 대표 확정)

> 대표(2026-09-07): *"유어딜 이 서비스 구현 운영을 직접 이 클로드를 통해서 각 세계 최고의 전문가 담당자
> (경영, 기획, 마케팅, 디자인, 개발, 정산) 팀을 빌딩해서 최대한 자동화 운영을 하고 싶은데 … 지금은 내가
> 수기로 하나하나 다 컨트롤을 하고 있어."* → *"응 그렇게 하고, 지금껏 구현을 해오면서 미완성, 에러,
> 문제들이 곳곳에 숨어있었는데도 완성되었다고 판단을 하는 경우가 많았어. … 진작에 문제없는 완벽히
> 운영할 수 있는 형태로 진행하자."*

이 문서는 **모든 세션이 따르는 운영 SSOT** 다. 역할 정의는 `.claude/agents/*.md`, 결재함은 `docs/decisions/`,
강제는 `src/tests/unit/ai-team-operating-model.test.ts` + 주입 매니페스트가 한다. 문서만 고치고 그 셋을
안 고치면 가드가 빨간불을 낸다(이 레포의 규칙: 규율은 문서가 아니라 테스트로).

---

## 0. 왜 이 모델인가 — 병목의 실체 (2026-09-07 실측)

대표가 "수기로 하나하나 컨트롤"하게 된 원인은 실행력 부족이 아니다. 실측:

| 항목 | 실측 | 의미 |
|---|---|---|
| 워커 cron | 62개 파일 | 기계 실행은 충분하다 |
| 결정론 가드 | `scripts/check-*` 126개 · 불변식 100 | 기계가 지키는 것도 충분하다 |
| 역할 정의 | `.claude/agents/` **0개** | 모든 세션이 같은 범용 세션. 매번 대표가 레일·범위를 지정한다 |
| "대표 판단" 대기 | handoff 문서 **98건**에 산재 | 결정할 것이 문서·PR·Notion 에 흩어져 있어 대표가 찾아다닌다 |
| 반복 루틴 | 9개 중 반복 1개, 그 1개(System health check)는 **매일 실패** | 자동 운영 루틴이 사실상 0 |
| 완료 보고 | "완료" 뒤에 미완성·에러가 남아 있던 사례 다수(대표 지적) | 완료의 정의가 사람마다 달랐다 |

⇒ 고칠 것은 셋이다. **① 역할을 파일로 고정 ② 결정을 한 곳으로 모음 ③ "완료"를 증거 등급으로 정의.**

---

## 1. 원칙 5개 (위반 시 가드가 막는다)

1. **완료는 주장이 아니라 증거다.** "완료" 라는 말은 §4 의 **증거 등급 4(라이브 판정) 이상**에서만 쓴다.
   등급 3 이하는 "구현됨 · 검증됨 · 배포됨" 으로만 말한다. 보고 첫 줄에 `[E1~E5]` 등급을 반드시 적는다.
2. **결정은 한 곳에서 한다.** 대표가 판단할 것은 `docs/decisions/<날짜>-<슬러그>.md` 로만 올린다.
   handoff 본문·PR 코멘트·채팅에 "대표 판단 필요" 라고만 적고 끝내는 것을 금지한다.
3. **역할은 파일이다.** 세션이 어떤 역할로 일하는지는 `.claude/agents/<역할>.md` 의 프론트매터가 정한다.
   허용 도구·금지 레일·읽을 SSOT·보고 형식이 거기 있다. 채팅에서 "너는 CFO 야" 라고 부여하지 않는다.
4. **머니 경로와 대외 발송은 사람 스위치다.** 에이전트는 숫자를 재고 이상을 판정해 올린다.
   결제·정산·요율·환불·게이트 ON·이메일/알림톡/소셜 발송은 **대표가 켠다**(§2 등급 C).
5. **룰은 가드로 환원한다.** 이 문서의 규칙 중 레포에서 관측 가능한 것은 전부 테스트가 지킨다.
   못 지키는 범위는 테스트 주석에 적는다(과신 방지).

---

## 2. 결정권 매트릭스

세 등급뿐이다. 애매하면 **한 등급 위**로 올린다.

| 등급 | 뜻 | 대표 개입 |
|---|---|---|
| **A 자율** | 하고 나서 handoff 에 기록 | 없음 |
| **B 보고 후 진행** | 세 줄(레일·머니 접촉·롤백) 보고하고 **기다리지 않고** 진행 | 사후 열람 |
| **C 결재** | `docs/decisions/` 항목 올리고 **대표 승인 전 실행 금지** | 승인/반려 |

| 행위 | 등급 | 비고 |
|---|---|---|
| 버그 수정 · 가드 추가 · 테스트 · 문서 · 리팩토링(동작 불변) | A | 잠금표(Toss·로딩) 파일은 예외 → C |
| 소비자 UI 변경(마크업·문구·색) | B | 대표 확정 시안이 있는 범위 안에서만. 시안 밖 방향 전환은 C |
| 서버 로직 변경(비머니) · cron 주기/게이트 조정(비머니) | B | |
| 서비스 경계 교차 변경(도매↔유어딜↔공구 서비스↔유어애즈) | B | CLAUDE.md 세 줄 보고 룰 그대로 |
| 결제·정산·요율·환불·원장·커미션 코드 | **C** | + 단독 세션 + staging 실결제 |
| 게이트/플래그 ON(`platform_settings`·env·feature-flags) | **C** | OFF 로 되돌리는 것은 B |
| 이메일·알림톡·소셜·블로그 **발행** | **C** | 초안 생성은 A(draft-first 룰) |
| 외부 서비스 유료 전환 · 한도 상향 · 결제수단 | **C** | |
| 삭제·purge·데이터 일괄 수정(D1 UPDATE) | **C** | 읽기는 A |
| 잠금표(Toss V2 · 로딩) 파일 수정 | **C** | 기존 `[UNLOCK]` 절차 그대로 |
| 명칭 SSOT · 브랜드 색 · 아이콘 컨셉 변경 | **C** | |
| Routine 신설/삭제 · 에이전트 정의 변경 | B | 결정권 등급을 낮추는 변경은 C |
| PR 머지 | B | CI 초록 + 이 문서 §4 E3 증거 첨부 시. 머니 경로 PR 은 C |

**기한과 기본안**: C 항목은 반드시 `기한` 과 `기본안(답 없을 때)` 을 적는다. 기한이 지나도 **C 는 자동
실행되지 않는다**. 기본안은 대표가 "기본안대로" 한 마디로 승인할 수 있게 하기 위한 것이지, 무응답을
승인으로 읽으라는 뜻이 아니다.

---

## 3. 역할 7개 (6 판정 + 1 운영 — 2026-09-07 대표 "모두 진행")

각 역할의 정본은 `.claude/agents/<name>.md` 다. 여기서는 지도만 둔다.

| 역할 | 파일 | 한 줄 사명 | 핵심 지표 | 주기 |
|---|---|---|---|---|
| 경영 사무국 | `ceo-office` | 결재함을 비우고, 다른 다섯 역할이 낸 것을 대표가 5분에 읽게 만든다 | 결재함 체류일 · 미기록 결정 0 | 매일 |
| 기획 | `planning` | "코드에 있다 ≠ 살아 있다" 를 매주 재확인하고 로드맵과 게이트를 정합시킨다 | FEATURE_STATUS 정확도 · 인계 누락 0 | 주 1 |
| 마케팅 | `marketing` | 유어딜 자체 홍보 초안과 SEO 계약을 지킨다. 발송은 안 한다 | 색인 URL 생존 · 초안 검토 대기 수 | 주 1 |
| 디자인 | `design` | 코레일톡 디자인 시스템과 anti-slop 규칙이 화면에서 지켜지는지 **렌더해서** 본다 | 다크 대비 결함 0 · slop 래칫 | 주 1 |
| 개발 | `dev` | CI·가드·배포·에러가 초록인지 매일 판정하고, 빨강은 그날 고친다 | 가드 GREEN 수 · 배포 후 라이브 판정 | 매일 |
| 정산 | `finance` | 원장·payout·수수료가 코드 SSOT 와 일치하는지 매일 판정한다. 스위치는 안 켠다 | 원장 무결성 · 과지급 0 | 매일 |
| **운영** | `ops` | 어드민 수동 큐(승인·정산·환불·분쟁·신고·발행 대기 — 실측 58개 엔드포인트)를 규칙 안에서 비우고 예외만 결재로 올린다. 송금·발송은 안 한다 | 큐 체류 48h 초과 0 · 예외 비율 | 매일 |

**공통 규칙(전 역할)**: 자기 레일 밖 파일을 만지지 않는다 · 보고 첫 줄에 `[E등급]` · 시각은 KST ·
Notion 개발 로그는 머지 시 · 결정은 `docs/decisions/` · "할 것이 없으면 없다고 짧게" (억지로 채우지 않는다).

---

## 4. 완료 판정 기준 — 증거 등급 E1~E5

대표가 지적한 "완성됐다고 했는데 문제가 숨어 있던" 사고의 공통점은 **등급 2~3 에서 '완료' 라고 말한 것**이다.

| 등급 | 이름 | 증거(보고에 첨부할 것) | 이 단어를 써도 됨 |
|---|---|---|---|
| **E1** | 작성됨 | diff 존재 | "작성했다" |
| **E2** | 검증됨 | `tsc 0` · 해당 vitest pass · 관련 가드 GREEN · **되돌려-검증(주입) 빨강 확인** | "검증됐다" |
| **E3** | 머지·배포됨 | PR 머지 해시 + `main.yml` 성공 + 배포 산출물 도달(청크 해시 변화 등) | "배포됐다" |
| **E4** | 라이브 판정됨 | 라이브에서 **그 변경이 의도한 효과**를 실측(curl 응답·D1 행·브라우저 하네스·하트비트 `rr`) | **"완료"** |
| **E5** | 사용자 판정됨 | 대표 또는 실제 사용자가 화면/결과를 확인 | "확정" |

규칙:
- **E2 없이 E3 로 가지 않는다**(CI 가 대신 해 주지 않는다 — 가드 주입 검증이 40분 걸려도 기다린다).
- **E3 에서 멈추면 "완료" 가 아니다.** 배포 후 라이브 판정(E4)을 같은 세션이 하거나, 못 하면 `send_later`
  로 자기 체크인을 걸어 두고 handoff 에 "E4 대기 · 판정 명령" 을 적는다.
- **머니 경로는 E4 가 staging 실결제다.** 실결제 없이 "완료" 금지.
- **E4 판정은 "에러가 안 났다" 가 아니라 "의도한 효과가 났다"** 다. 이 레포의 사고 대부분은 에러 없이
  조용히 틀려 있었다(soft-404 · 캐시 컬럼 드리프트 · 헛도는 가드).
- 보고 형식(고정): 첫 줄 `[E4] 홈 카드 다음 사진 미리받기 — 라이브 최악 5.0s → 1.8s (하네스 실측)`.
  등급 미기재 보고는 ceo-office 가 반려한다.

---

## 5. 결재함 프로토콜 — `docs/decisions/`

- 파일 하나 = 결정 하나. `docs/decisions/<YYYY-MM-DD>-<slug>.md`. 템플릿 `docs/decisions/_template.md`.
- 필수 필드: `상태(open/approved/rejected/expired)` · `등급(C)` · `역할` · `질문(한 문장)` · `근거(실측 수치)` ·
  `선택지` · `기본안` · `기한` · `롤백` · `결정(대표가 한 말 그대로)` · `반영 커밋`.
- `ceo-office` 루틴이 매일 open 항목을 KST 로 요약해 대표에게 올린다(푸시 + Notion 미러가 연결된 세션이면 Notion).
- 대표가 답하면 **그 답을 한 말 그대로** `결정` 필드에 옮기고 상태를 바꾼다(요약·의역 금지 — 이 레포는 의역된
  결정이 다음 세션을 반대로 이끈 사례가 있다).
- 승인된 항목은 실행 세션이 `반영 커밋` 을 채운다. 채워지지 않은 approved 는 ceo-office 가 매일 다시 올린다.
- `CURRENT_WORK.md` 처럼 **공유 파일에 append 하지 않는다**(충돌 원인). 파일 단위라 세션 간 충돌 0.
- **대표가 보고 답하는 정본 창은 어드민 `/admin/decisions` 다** (2026-09-08 대표 *"매번 깃헙에서 보기에는 불편해"* →
  *"어드민에 나오는게 낫지 않나?"* → *"어드민으로 해"*). 결재 파일을 빌드 때 인라인해 배포마다 자동 최신(`AdminDecisionsPage`),
  카드에서 번호 클릭/한 줄 입력이 D1 우편함 `decision_answers`(`/api/admin/decisions/*`)에 쌓인다. 브리핑·알림·Notion 의
  결재 링크는 **`https://urdeal.kr/admin/decisions`** 로 낸다(GitHub 파일 링크는 기술 참조로만).
- **Notion 📥 결재함은 보조 입력창 + 거울이다** (같은 날 먼저 만든 것).
  데이터소스 `collection://540a578f-a234-4d4c-a38c-bf69ba3c18ee`(서비스 개발 현황 허브 아래). **SSOT 는 여전히 이 폴더의
  파일**이고 Notion 은 거울 + 입력창이다. 커넥터 대리인 루틴(`[connector-proxy]`, 4시간)이 동기화한다 —
  **어드민 우편함(우선) → 파일** · Notion `대표 답`(보조) → 파일 · GitHub → Notion 거울. 어느 경로든 원문을 한 글자도
  바꾸지 않고 `결정` 필드로 옮겨 PR(문서만)로 머지하고, 머지 뒤 우편함에 `synced` 를 찍는다. 채팅으로 답하면 그 세션이 즉시 옮긴다(더 빠르다). 대표 답이 파일에
  들어가 **머지된 뒤**에야 실행기가 집는다 — Notion 에 적은 것만으로는 구현이 시작되지 않는다(최대 4시간 지연).

---

## 6. 루틴 (Claude Code Routine · 판정=fresh-session / 실행=세션 바인딩 · KST)

| 루틴 | 역할 | 주기(KST) | 하는 일 | 산출 |
|---|---|---|---|---|
| 개발 일일 판정 | dev | 매일 08:00 | 최근 24h 머지 PR 의 E3/E4 판정 · `audit-gate` · 열린 PR CI 빨강 · 죽은 루틴/cron 하트비트 | handoff + 빨강이면 수리 PR |
| 운영 일일 큐 | ops | 매일 08:15 | 셀러·사업자정보·정산·환불·분쟁·신고·대리등록·영수증·블로그·문의 큐 집계 → 규칙 안/밖/정보부족 분류 · SLA 48h · 초안 | handoff + 예외는 결재 항목 |
| 정산 일일 판정 | finance | 매일 08:30 | 원장 무결성(`ledger-integrity-check` 결과) · payout 과지급 0 · 수수료 SSOT 대조 · 게이트 상태 | handoff + 이상이면 결재 항목 |
| 결재함 브리핑 | ceo-office | 매일 09:00 | open/approved-미반영 항목 요약 · 등급 미기재 보고 반려 · Notion 미러 · **월요일: 역할별 한 줄**(한 것·최고 E등급·열린 것 — 과정 없음, 대표 2026-09-07 "역할별 한 줄 넣어줘") | 대표 푸시 1건 |
| 기획 주간 | planning | 월 09:30 | FEATURE_STATUS ↔ 코드 정합 · 결재 기한 경과 · 인계 누락(`check-current-work-sync`) | handoff |
| 마케팅 주간 | marketing | 화 09:30 | `live-contracts` 결과 · sitemap/robots 생존 · 블로그/소셜 초안 검토 대기 · SEO 메타 실측 | handoff + 초안(draft) |
| 디자인 주간 | design | 수 09:30 | `dark-contrast`·`render-smoke` 워크플로 결과 · slop/모양 래칫 · 시안 대기 목록 | handoff + 결함이면 수리 PR |
| **승인 실행기** | (승인 항목의 역할) | 4시간마다 | `docs/decisions/` 의 `approved` 이고 `반영 커밋` 빈 항목 **하나**를 집어 그 역할로 구현 → draft PR → `반영 커밋: PR #N (draft, 머지 대기)`. **WIP 1** · 승인 0건이면 무동작 · 머니/게이트/잠금표는 PR 까지만(켜는 건 사람). ⚠️ **fresh-session 이 아니라 세션 바인딩**(2026-09-07 실측 2회: fresh-session 은 브랜치를 못 민다 — 아래 주의) | draft PR + handoff `*-exec-*` |

> 🧭 **1인 운영의 원칙 (2026-09-07 대표 *"1인 운영이 가능하게끔 하려면? 지금 형태의 팀원 구성으로 두면 돼?"*)**: 여섯 역할은 전부 *판정*이고 *큐를 비우는* 역할이 없었다 — 그래서 `ops` 를 더했다. 원칙은 **"큐는 기계가 비우고, 예외만 사람에게"** 다. 단, 큐가 0 인 지금 큐 자동화 코드를 크게 짓지 않는다 — 규칙은 **게이트 OFF 로** 넣어 두고 볼륨이 생기면 결재로 켠다. 지금 대표 시간을 먹는 것은 운영이 아니라 **매장 확보**(승인 매장 1곳)라, 영입 파이프라인(후보 추출·초안·추적, 발송은 대표)이 먼저다. 결재 항목 3건: `2026-09-07-store-acquisition-pipeline` · `-seller-auto-approval` · `-payout-auto-approve-threshold`.

> 🔒 **fresh-session 루틴은 푸시를 못 한다 (2026-09-07 21:16 KST 실측 판정)**: 실행기를 fresh-session 으로 두 번 돌렸다 — 1회차 2분·2회차 20분(46만 토큰, 세션 상태 "review ready")인데 **원격 브랜치가 하나도 안 생겼다**(진단용 `claude/exec-retry-*` 푸시까지 지시했는데도). 루틴이 만드는 세션의 config 가 `outcomes: []`·`allowed_push_branches: []` 라 산출이 컨테이너 안에 갇힌다. ⇒ **산출을 레포에 남겨야 하는 루틴(실행기·커넥터 대리인)은 커넥터와 push 를 가진 사람 세션에 `persistent_session_id` 로 바인딩**한다. 판정만 하는 역할 루틴 7개는 보고가 산출이라 fresh-session 그대로 둔다(보고는 세션 UI 와 ceo-office 브리핑이 담는다). 바인딩 세션이 아카이브되면 실행기도 멈춘다 — 그때는 다른 사람 세션에 재바인딩하거나 Routines UI 에서 push 브랜치를 허용한다.

> 🔁 **"계속 일하는 팀"의 실체 (2026-09-07 대표 *"실시간으로 이 팀원들이 계속 일을 하게끔 하는 건 어때?"* → "항상 켜 두기"가 아니라 "일이 생기면 즉시 깨어나기")**: 일은 시간이 아니라 세 신호에서 온다 — ① 사건(PR 구독·CI 빨강·하트비트 침묵) ② **결재 승인**(진짜 백로그 — 승인 실행기가 집는다) ③ 주기 판정(위 표). 항상 켜 두면 에이전트가 일을 *만들고*(그게 "완료라고 했는데 문제가 숨어 있던" 코드가 된다), 여섯이 같은 레포를 동시에 만져 충돌이 상시가 되며, 대기 자체가 토큰을 태운다. **대표가 결정하는 속도가 곧 팀의 속도다.**

규칙: 루틴은 **판정과 초안**까지다. §2 의 C 는 절대 루틴이 실행하지 않는다. "변화 없음" 이면 대표에게
보고하지 않고 handoff 한 줄만 남긴다(소음 0). 루틴 프롬프트는 이 문서와 역할 파일을 **먼저 읽으라**로 시작한다.

기존 "System health check"(2026-04 생성, MCP 미연결, 매일 실패)는 위 표로 **대체**한다. ⚠️ 그 루틴은 `http_api` 로 만들어져
에이전트가 끌 수 없다(실측 거부) — **대표가 Routines UI 에서 끈다.** 루틴 6개는 커넥터 0 으로 생성됐으므로(세션이 넘길 권한 없음)
PR 생성·Notion 미러는 대표가 Routines UI 에서 GitHub·Notion 을 붙인 뒤부터 자동이다. 붙이기 전에도 판정(git·curl·D1·어드민 API)은 돈다.

---

### 대표가 보는 것은 세 층뿐 (2026-09-07 대표 질문 *"팀원들이 작업하는 과정도 보는 것까지 구현하는 건 낭비인가?"* → 낭비다)
| 층 | 무엇 | 대표 시간 |
|---|---|---|
| 매일 | 결재함 브리핑 한 줄(푸시·이메일) | 1분 |
| 답할 때 | 어드민 `/admin/decisions` 카드에서 번호 클릭 또는 한 줄(폰 가능) · 보조: Notion 📥 결재함 · 급하면 채팅 | 항목당 30초 |
| 매주(월) | 브리핑 끝에 **역할별 한 줄**: 한 것 · 최고 E등급 · 열린 것 | 3분 |
| 필요할 때 | 의심 항목의 handoff 파일 또는 세션 기록을 열어 E4 근거를 찔러봄 | 그때만 |

과정 감시 화면은 만들지 않는다 — 루틴 세션 기록(claude.ai/code)과 handoff 가 이미 과정 전체를 남기고, 실시간 감시는
"수기 컨트롤"의 다른 형태다. 2~3주 뒤에도 "누가 뭘 했는지 파일을 뒤진다"는 느낌이 들면 그때 어드민 탭(§7-5)을 붙인다.

## 7. 롤아웃

| 단계 | 내용 | 상태 |
|---|---|---|
| 1 | 이 문서 · `.claude/agents/` 6개 · `docs/decisions/` · 가드(테스트+주입) · CLAUDE.md 배선 | ✅ 이번 세션 |
| 2 | 루틴 6개 생성 + 죽은 health-check 비활성 | ✅ 이번 세션(생성 결과는 handoff 에 ID 기록) |
| 3 | 첫 주 산출물을 대표가 읽고 **주기·소음 조정**(너무 잦으면 줄이고, 놓치면 늘린다) | 대표 |
| 4 | 기존 handoff 98건의 "대표 판단" 을 `docs/decisions/` 로 이관(기획 루틴이 주당 10건씩) | 자동 |
| 5 | 역할별 지표를 어드민 `/admin/system-monitoring` 에 탭으로 노출(선택) | 결재 후 |

---

## 8. 이 모델이 스스로 못 막는 것 (정직하게)

- 에이전트가 **E4 라고 적고 실제로는 E2 인 경우**. 가드는 등급이 적혔는지만 본다. 대표가 handoff 의
  E4 근거(수치·명령)를 한 번씩 찔러 보는 것이 유일한 방어다 → ceo-office 가 브리핑에 근거 줄을 그대로 싣는다.
- 루틴이 **실패하고 조용히 넘어가는 경우**. `list_triggers` 의 `last_run` 을 dev 루틴이 매일 본다(루틴이 루틴을 본다).
- 결정을 **채팅에서 구두로** 받고 `docs/decisions/` 에 안 옮기는 경우. 세션 종료 가드
  (`check-current-work-sync`)가 handoff 만 보고 decisions 는 못 본다 → 후속 가드 후보.
