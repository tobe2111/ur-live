# 🚦 오픈 차단 vs 오픈 후 처리 — 릴리즈 체크리스트 재분류

> **대표 지시 2026-07-29**: *"릴리즈 체크리스트를 '오픈 차단(blocker) / 오픈 후 처리 가능' 두 열로 재분류해라.
> 각 항목에 근거 한 줄. `INTERNAL_API_TOKEN` 무방비 엔드포인트 조사와 `0 18` 결측 원인은 어느 쪽인지 판정 포함."*
>
> 원본: `operator-mall-release-checklist.md`(#856) · 근거: `execution-evidence-audit-2026-07-29.md`

---

## 0. 분류 기준 (이걸 먼저 고정한다)

**오픈 차단**은 "중요한 것"이 아니라 **아래 셋 중 하나에 해당하는 것**이다:

| | 기준 | 왜 |
|---|---|---|
| **A** | 없으면 **돈이 잘못 움직인다** | 되돌리는 비용이 파일럿 이득보다 크다 |
| **B** | 없으면 **되돌릴 수 없는 것이 남는다** | 배포로 못 지운다(색인·법적 고지·평문 저장) |
| **C** | 없으면 **경로를 완주할 수 없다** | 파일럿이 검증하려던 것 자체가 성립 안 함 |

**오픈 후 처리 가능**은 *"덜 중요하다"* 가 아니라 **"그 사이 손실이 축적되지 않거나 자가치유된다"** 는 뜻이다.
⇒ 각 항목의 근거 한 줄은 **A/B/C 중 무엇에 걸리는지 또는 왜 안 걸리는지**를 적는다.

> ⚠️ 파일럿 규모(매장 1~3, 주문 소량)를 전제로 한다. **규모가 커지면 아래 여러 항목이 오른쪽 →
> 왼쪽으로 이동한다**(특히 알림·문의·반품 입구). 규모 전제가 바뀌면 이 표를 다시 그릴 것.

---

## 1. 🔴 오픈 차단 (blocker)

| # | 항목 | 근거 한 줄 | 기준 |
|---|---|---|---|
| **B1** | **결제 레일 생존 실증** — 소액 실결제 1건 | 마지막 성공 결제가 **2026-03-31**(4개월 전). 결제가 지금도 되는지 **아무도 모른다** — 파일럿 첫 주문이 그 확인이 되면 안 된다 | C |
| **B2** | **#844 공구 특가 결제 배선 머지 + 실결제 검증** | 안 붙으면 운영자가 약속한 특가로 안 팔린다. 절차서는 #872 | C |
| ~~**B3**~~ | ~~sitemap 몰 격리(#859) 배포~~ | ✅ **해소(2026-07-29 라이브 확인 — §5.1)** | B |
| **B4** | **미수령 고지 문구(X4c) — 임시 표기라도 확정** | 픽업 공구는 청약철회 고지가 **법적 의무**. 문구 없이 파는 것은 되돌릴 수 없다 | B |
| ~~**B5**~~ | ~~**`DATA_ENCRYPTION_KEY` 등록**~~ ✅ **해소(2026-08-02 실측 — §8)** | 파일럿이 만드는 **실사용자 카카오 토큰이 평문으로 쌓인다.** 나중에 켜도 그 기간분은 이미 저장됨. 등록은 5분 작업 | B |
| ~~**B6**~~ ✅ | ~~**장애 인지 경로 1개 이상**~~ **해소(2026-08-03 01:31 실측 — §8.2)** — `DISCORD_WEBHOOK_URL` 또는 `SENTRY_DSN`, **Pages·Workers 양쪽** | 둘 다 없으면 **런타임 에러가 사람에게 도달하는 경로가 0**. ⚠️ 2026-07-29 확인: **cron 은 Workers 에서 도는데 그쪽 시크릿이 0개** — Pages 에만 넣으면 `daily-self-diagnostic` 경보는 여전히 갈 곳이 없다(§5.3) | C |
| **B7** | **몰 브랜딩 기본값(X5a)** | 정하지 않으면 몰을 **열 수 없다**(세션 ③-a 가 이걸 기다림) | C |
| ~~**B8**~~ | ~~**cron 캐리어에 `TOSS_SECRET_KEY` 등록**~~ ✅ **해소(2026-08-02 실측 — §8)** | **지금 도는 세 블록의 머니 작업이 결제 API 를 못 부른다** — 아래 §6. 만료 환불이 안 나가고 막힌 주문이 영원히 stuck 인데 **하트비트는 `ok:true`**. 파일럿 첫 환불이 그 확인이 되면 안 된다 | A |

> 🟢 **B1·B2 는 같은 결제로 동시에 해소된다** — #872 절차서의 소액 실결제가 둘 다 증명한다.
> 🟢 **B5 는 대표 인계 ④번에 이미 포함**돼 있어 순서대로 진행하면 자연 해소된다.

---

## 2. 🟡 오픈 후 처리 가능

| # | 항목 | 근거 한 줄 | 언제까지 |
|---|---|---|---|
| ~~**L1**~~ ✅ | ~~`INTERNAL_API_TOKEN` 미설정~~ **해소(2026-08-03 실측)** | **무방비가 아니라 잠김이다** — 아래 §3 판정 | 여유 |
| **L2** | ~~**`0 18` 결측일(07-25·27)**~~ ✅ | **지연이지 손실이 아니다** — 아래 §4 판정. **07-29·30 연속 정상 발화로 격상 사유 소멸**(원인은 미규명 유지) | 관측만 |
| **L3** | **cron 트리거 등록(0~5단계)** | `*/5`·`0 18`·`0 19` 는 이미 돈다. 안 도는 것 중 파일럿에 필요한 건 **주간 지급뿐**이고 첫 정산은 D+7. **0단계(선언↔등록 일치·PUT 초록불) 완료** | **첫 지급 전** |
| **L4** | **`seller:null` 1,710원 규명** | 오픈 자체를 막지 않으나, 규명 전 지급을 켜면 **안 나가거나 잘못 나간다**(대표 확정 — 지급 단계 필수) | **첫 지급 전** |
| **L5** | **결제 실패 소비자 알림 신설** | `PaymentFailPage` 가 실패 사유를 즉시 보여준다. 알림 부재가 **돈을 잃게 하지 않는다** | 규모 확대 전 |
| ~~**L6**~~ ✅ | **알림톡** — **해소(2026-08-03 01:10 실측 — §8.2)**. ~~원인 규명 완료, 미해소~~: 기능 결함이 아니라 키 문제다. 대표가 3종을 등록했으나 Workers 쪽 이름이 **`' ALIGO_USER_ID'`(앞 공백)** 라 런타임엔 undefined → 3종 AND 가드에 걸려 **여전히 0건**(§8.1). 대표가 공백 제거 재등록해야 함 | 픽업 확인은 **QR 기반이라 알림 없이 완주**된다. 파일럿 규모면 운영자가 직접 안내 | 규모 확대 전 |
| **L7** | **픽업 주문 반품 입구 부재** | 소비자 신청 게이트가 `DELIVERED`+7일이라 픽업은 못 올린다. 단 **운영자 셀프 환불이 이미 동작**해 수동 처리 가능 | 규모 확대 전 |
| **L8** | **소비자 주문 부분환불 UI** | C7(실온 3일 후 부분환불)이 발생해야 필요. **파일럿 첫 주에 발생하지 않는다** | C7 시나리오 전 |
| ~~**L9**~~ ✅ | **`BACKUP_BUCKET` 바인딩** — **해소(2026-08-03 03:15 실측: `BACKUP_BUCKET → ur-live-backups` 바인딩 확인).** 선언이 `wrangler.toml` 에 들어가 배포가 복원했다. ~~격상~~: #971 이 주간 백업 cron 을 점화했는데 cron 캐리어에 **R2 바인딩이 0** 이다(실측). `handleD1Backup` 은 버킷 없으면 **throw** 하므로 조용하진 않지만, **일요일 20:00 UTC(월 05:00 KST)에 디스코드 경보가 뜬다.** GH Actions 백업은 계속 도니 데이터 손실은 아니다 | ~~일요일 전~~ ✅ |
| **L10** | **게이트 현황판(`ops-status`) 복구** | #862 에 수정 포함. 배포되면 해소 | 배포 시 |
| **L11** | **`ANTHROPIC_API_KEY`·`ANALYTICS_KV`·`RATE_LIMITER`** | 전부 fail-soft(블로그 초안·분석 write skip·in-memory 폴백). `RATE_LIMIT_KV` 는 **정상 동작 실측** | 여유 |
| **L12** | **X2 예치금 숫자 · 도매 철거 · X3 도매 cron 부착** | **도매 레일** 항목이라 방배 소비자 경로와 교차하지 않는다 | 철거 PR 시 |
| **L13** | **X6 CF 토큰 재발급** | 세션의 진단 자립도 문제. 파일럿 동작과 무관 | 여유 |
| **L14** | **운영자 대시보드 몰 스코프(세션 ⑤)** | 파일럿 매장이 1~3 이면 스코프 없이도 자기 주문을 식별할 수 있다 | 매장 4곳부터 |
| **L15** | **운영자 문의 경로** | X8 확정(대표 연락처) — **이미 해소** | ✅ |
| **L16** | ~~cron Worker 에 시크릿 0개~~ → **부분 해소**(2026-08-02: TOSS·ALIGO·CACHE_KV 확인). R2/일부 KV 는 잔존(§5.3) | 파일럿 규모(매장 1~3)면 리마인더·재시도는 대표가 수동 대체 가능. **DB 만 쓰는 정산·원장은 정상 동작** | 규모 확대 전 (단 B6 몫은 **차단**) |

---

## 3. 판정 ① — `INTERNAL_API_TOKEN`: **오픈 후 처리 가능** (조사 완료)

**결론: 무방비 엔드포인트는 0건이다. 반대로 잠겨 있다.**

전수 조사한 소비처 4곳이 **전부 동일한 fail-closed 패턴**이다:

```ts
if (!opsToken || opsToken !== reqToken) return c.json({ success:false, error:'Forbidden' }, 403)
//  ^^^^^^^^^ env 미설정이면 여기서 곧바로 403 — 열리는 게 아니라 닫힌다
```

| 엔드포인트 | 파일 | 미설정 시 |
|---|---|---|
| `POST /api/_internal/clear-rate-limit` | `internal-admin-tools.routes.ts:70` | 403 |
| `POST /api/_internal/reset-admin-password` | `internal-admin-tools.routes.ts:87` | 403 |
| `GET /api/moderation/stats` | `moderation.routes.ts:64` | 403(+ 상위 인증 401) |
| `POST /api/referral-tree/calculate-commission` | `referral-tree.routes.ts:613` | 403 |

**라이브 실측(2026-07-29 10:48 UTC)** — 토큰 없이 호출(가드가 DB 접근 앞이라 부수효과 0):
```
POST /api/_internal/clear-rate-limit  →  403 {"success":false,"error":"Forbidden"}
GET  /api/moderation/stats            →  401 (상위 인증에서 먼저 차단)
```

> ⚠️ **`env-readiness` 의 분류가 오해를 만든다.** 이 키는 `security` 그룹에 있고 그 그룹 설명이
> *"보안 — fail-open(동작하나 약화)"* 다. **이 키만은 fail-closed 다.** 제가 처음 감사에서
> *"미설정이면 fail-open"* 이라고 적은 것은 그 그룹 라벨을 그대로 읽은 것이고 **틀렸다.**
> ⇒ 후속 작업: `health.routes.ts` 의 이 항목 note 에 *"미설정 시 fail-closed(복구 경로 잠김)"* 로 정정.

**그래서 실제 리스크는 보안이 아니라 가용성이다**: 어드민 비밀번호 초기화·rate-limit 해제 같은
**복구 레버가 잠겨 있다.** 파일럿 중 대표가 어드민에서 잠기면 이 경로를 못 쓴다.
다만 ① 지금도 어드민 로그인은 정상 동작하고 ② 별도 `BOOTSTRAP_TOKEN` 경로가 있으며
③ 키 등록은 언제든 5분이면 된다 ⇒ **오픈을 막을 이유가 없다.**

> 💡 `DATA_ENCRYPTION_KEY`(B5) 와 **같은 작업**에서 함께 등록하면 비용이 사실상 0 이다.

---

## 4. 판정 ② — `0 18` 결측: **오픈 후 처리 가능** (단 관측 유지)

**결론: 결측은 지연이지 손실이 아니다.** 그 블록의 머니 작업이 전부 **누적·멱등** 술어를 쓴다.

| 작업 | 대상 선정 | 하루 걸러도 |
|---|---|---|
| `supplier-settlement-mature` | `status='pending' AND available_at <= now` | 다음 회차에 그대로 잡힘 |
| `auto-settlement` | `used_at < cutoff AND settlement_id IS NULL` | 같음 |
| `affiliate-mature` / `referral-mature` | hold 기간 경과분 전체 | 같음 |
| `expired-voucher-refund` | `status='unused' AND expires_at < now` | 같음(+CAS 로 이중환불 차단) |

**"어제 하루치만" 처리하는 작업이 없다.** 그래서 결측일의 대상은 다음 발화 때 함께 처리된다 — **누락이 아니라 24h 지연.**

**원인은 아직 미규명이고, 여기서 추측하지 않는다.** 확인된 사실만:
- 등록은 돼 있다(5일치 실행 흔적).
- 두 결측일(07-25·27)에 **다른 신호가 없다** — 그날 실패 기록도, 다른 블록 이상도 없다.
- 하트비트 저장은 07-29 배포라 **그 시점 관측 데이터가 존재하지 않는다.**

> ⇒ **관측이 먼저다.** 지금 원인을 파는 것은 데이터 없이 추측하는 일이고, 이 레포가 반복해 실패한 방식이다.
> 하트비트가 이제 매 실행을 기록하므로 **다음 결측이 나면 그때는 증거가 남는다.**

### ✅ 관측 결과 (2026-07-31) — 격상 사유 없음

하트비트 시행 후 **2일 연속 정상 발화**. 부수효과 행(`frontend_errors.ledger_mismatch`)이 같은 시각을 독립 확인한다:

| 날짜 | 하트비트 | 부수효과 증거 |
|---|---|---|
| 07-29 18:00 | (기록 전) | `ledger_mismatch` @ `2026-07-29 18:00:44` |
| 07-30 18:00 | 15개 작업 전부 `ok` | `ledger_mismatch` @ `2026-07-30 18:00:45` |

⇒ 07-25·27 결측은 **연속 패턴이 아니었다.** 위 격상 기준(주 2회)에 걸리지 않으므로 **L2 는 관측 유지로 종결**.
같은 회차에 `0 19`(reconciliation·influencer-payout)도 07-30 19:00 정상 발화 확인.

> ⚠️ 다만 **결측의 원인은 여전히 미규명**이다. "2일 정상"은 원인 규명이 아니라 재발 없음일 뿐이다.
> Observability Logs 가 켜지기 전까지는 다음 결측 때도 *언제 안 뛰었는지*만 알고 *왜*는 모른다.

**단 두 가지는 오픈 후에도 계속 본다**:
1. **주 2회 이상 결측이면 격상** — 지연이 누적되면 정산 주기 약속(주간)이 깨진다.
2. **`payouts-generate`(주 1회)는 결측 비용이 다르다** — 하루가 아니라 **일주일** 밀린다.
   L3·L4 와 함께 **첫 지급 사이클 전**에 트리거 상태를 확정할 것.

---

## 5. 진행 기록

### 5.1 ✅ B3 sitemap 몰 격리 — **라이브 반영 확인** (2026-07-29)

| 확인 | 결과 |
|---|---|
| #859 머지 | `b7fc891` (2026-07-29 08:53 UTC) |
| 배포 반영 | `b7fc891` ⊂ `58ecd86` — 그 SHA 의 `main.yml` 배포가 **success**(10:38 UTC) |
| 라이브 sitemap | `https://urdeal.kr/sitemap.xml` → **HTTP 200 · 872 URL** |
| 과필터 안 났나 | `/products` **500** · `/s`(셀러) **8** — fail-closed(`AND 0=1`) 로 빠졌다면 0 이었을 자리 |

⚠️ **출력만으로는 가드의 작동을 증명할 수 없다** — 현재 몰이 0개라 조건이 있으나 없으나 결과가 같다.
그래서 판정 근거를 **① 배포 SHA 포함 ② 과필터 부재** 둘로 나눠 세웠다.
진짜 판정은 **첫 몰 상품이 생겼을 때** 그 URL 이 본진 sitemap 에 **안 들어오는지**로 한다.
⇒ **첫 몰 개설 직후 이 확인을 1회 반복할 것**(오픈 시점 리마인드 항목에 포함).

**덤으로 확인된 것** — `#843` 의 죽은 URL 정리도 라이브에 반영돼 있다:
`/group-buy`(리다이렉트) **0건** · `/live/{id}`(폐기 기능) **0건** · `/vouchers?category=` 는 **실재 한글 카테고리 3종**.

> 🔎 **환경 정정**: 이 컨테이너에서 `urdeal.kr` 이 **직접 조회된다**(과거 인계의 *"CONNECT 403 으로 막힘"* 은
> 현재 유효하지 않음). 단 `live.ur-team.com/sitemap.xml` 은 `urdeal.kr` 로 **301** 이라 정본에서 재도록 할 것.

### 5.3 🔴 cron Trigger 실측 + Worker/Pages 분리 (2026-07-29 대표 화면 확인)

**등록된 cron 은 정확히 3개**다 — Workers `ur-live` → Settings → Trigger events:

```
0 19 * * *      (Next: 2026-07-29 19:00)
*/5 * * * *     (Next: 2026-07-29 13:30)
0 18 * * *      (Next: 2026-07-29 18:00)
```

| cron | 감사 판정 | 실측 대조 |
|---|---|---|
| `*/5` · `0 18` | 🟢 등록 | ✅ 일치 |
| `0 19` | ⚪ 판정 불가 | → 🟢 **등록으로 확정**(`reconciliation`·`influencer-payout` 정상) |
| `*/2` · `0 * * * *` · `0 9` · `0 0 * * 1` · `0 20 * * 0` | 🔴 미등록 | ✅ 일치(5/5) |
| `0 3` | ⚪ 판정 불가 | → 🔴 **미등록으로 확정** |

⇒ **cron 작업 69개 중 26개만 트리거 아래 있고 42개는 트리거 자체가 없다.**
⇒ **0단계의 기준선이 확정됐다**: `wrangler.toml` 의 `crons` 는 **이 3개를 반드시 포함**해야 한다(PUT 은 전체 교체).

**같은 화면에서 드러난 두 번째 사실 — 시크릿이 Pages 에만 있다.**
대표 확인: *"worker 에서는 없는데 pages 에 다 넣어놨어."* 상세·영향은
`execution-evidence-audit-2026-07-29.md` §2.2-b. 요지는 **cron 이 DB 만 쓰는 일은 정상이고
바깥과 말하는 일(알림톡·Toss·Discord·KV 워밍)은 전부 무음 skip** 이며, **하트비트가 이걸 못 잡는다**(전부 `ok=true`).

**세 번째 — Observability 전부 Disabled**(Logs·Traces·Exports·Sampling, Tail Worker 없음).
`0 18` 결측일 원인을 못 찾은 이유가 이것이다. **Logs 만 켜면 다음 결측 때 근거가 남는다.**

### 5.2 ⏰ 오픈 시점 리마인드 — **대표에게 다시 알릴 2건** (대표 지시)

> 🟡 항목이지만 **기한이 있다.** 오픈 시점에 세션이 대표에게 **반드시 재고지**한다.

| # | 항목 | 왜 이때 | 기한 |
|---|---|---|---|
| **L4** | `seller:null` 1,710원 규명 | 규명 전 지급을 켜면 **안 나가거나 잘못 나간다** | **첫 지급 전(D+7)** |
| **L3** | cron 트리거 등록(0~5단계) | 주간 지급(`0 0 * * 1`)이 안 돌면 운영자가 **정산을 못 받는다** | **첫 지급 전(D+7)** |

**추가로 오픈 직후 1회**: §5.1 의 몰 sitemap 재확인(첫 몰 상품이 본진에 안 들어오는지).

---

## 6. 🆕 B8 — 지금 도는 머니 cron 이 결제 API 를 못 부른다 (2026-08-01)

0단계 배포 로그가 **cron 캐리어의 바인딩을 확정**했다. 지금까지는 대표 화면 한 장뿐이었는데,
`wrangler deploy` 출력이 같은 것을 독립 확인했다:

```
Your worker has access to the following bindings:
- Durable Objects: LIVE_STREAM, RATE_LIMITER
- D1 Databases: DB
- Vars: FRONTEND_URL, SCRAPER_URL
```

**KV 도 R2 도 시크릿도 없다.** 그리고 **지금 등록돼 도는** 세 블록 안에 이런 것들이 있다:

| cron | 작업 | `TOSS_SECRET_KEY` 가 없으면 (에러 없이) |
|---|---|---|
| `*/5` | `scheduled-cleanup` | 만료 선물 자동 환불이 `if (tossSecretKey)` 로 **통째 스킵** — 5분마다 |
| `0 18` | `auto-settlement` | 만료 바우처 환불의 **결제취소 호출 실패** — 고객 돈이 안 돌아간다 |
| `0 19` | `reconciliation` | 막힌 주문이 `stillStuck++; continue` — **영원히 stuck** |

**셋 다 하트비트에 `ok: true` 로 남는다.** 이게 이 감사가 계속 말해 온 것의 가장 구체적인 사례다 —
**하트비트는 "돌았다"를 증명하지 "했다"를 증명하지 않는다.**

### 왜 지금까지 안 보였나

`env-readiness` 는 **HTTP 엔드포인트라 Pages 를 잰다.** Pages 에는 Toss 키가 있어서 초록불이 뜬다.
그 초록불이 *cron 이 그 키를 쓸 수 있다* 는 뜻이 아닌데, 같은 이름이라 그렇게 읽힌다.

### ✅ 판정 완료 — **확정** (2026-08-01 13:10:59Z 실측)

배포(13:06:25Z) 이후 첫 `*/5` 발화에서 **`cron-env-missing` 하트비트가 실제로 기록됐다.**

```
name: cron-env-missing   at: 2026-08-01T13:10:59.765Z   cron: */5 * * * *
```

⇒ **런타임이 스스로 "요구하는 키가 없다"고 답했다.** 추정이 아니라 실행 증거다.
근거는 셋이 됐다: 대표 화면(시크릿 0) · 배포 로그 바인딩 목록 · **런타임 하트비트**.

> ⚠️ **판정 직전에 오판할 뻔했다.** 13:07 조회에서는 이 행이 없었는데, 그때의 마지막 `*/5` 발화가
> **13:06:08 — 배포 완료 17초 전**이었다. 옛 코드에는 이 관측 자체가 없으니 안 뜨는 게 당연했다.
> **"신호가 없다"와 "신호를 낼 코드가 아직 없다"는 다르다.** 발화 시각과 배포 시각을 대조하지
> 않았으면 "내 판정이 틀렸다"는 반대 결론을 냈을 것이다.

### 🔁 그런데 그 관측이 정작 내용을 잃었다 (같은 커밋에서 수리)

첫 기록의 `result` 가 **null** 이었다 — *어떤 키가 없는지*가 안 담겼고, 그게 이 관측의 존재 이유였다.
원인은 `summarizeResult` 가 **객체가 아닌 반환값을 통째로 버리는 것**. 내 두 하트비트
(`cron-env-missing`·`cron-unmatched`)가 문자열을 반환해 그대로 증발했다.

같은 파일이 2026-07-29 에 이미 한 번 당한 클래스다(24자 초과 문자열 드롭 — *"가장 알고 싶은 값이
정확히 그 이유로 사라졌다"*). **관측을 만들면서 그 관측의 값이 사라지는 경로를 밟았다.**
원시값(문자열·숫자·불리언·배열)도 남기도록 고치고 유닛으로 고정했다. 다음 발화부터 키 이름이 보인다.

### 🔑 빠진 키 확정 (2026-08-01 14:35:59Z — 배포 14:32:52Z 이후 첫 발화)

```
cron-env-missing  cron=*/5 * * * *
result: TOSS_SECRET_KEY(scheduled-cleanup)
        ALIGO_API_KEY(scheduled-cleanup,retry-alimtalk)
        CACHE_KV(cache-prewarm)
```

**요구하는 셋이 전부 없다.** 그리고 이 한 줄이 **그동안 따로 열려 있던 세 항목을 동시에 설명한다**:

| 키 | 그동안의 미스터리 | 이제 알게 된 것 |
|---|---|---|
| `TOSS_SECRET_KEY` | B8 — 만료 환불·정합이 왜 안 되나 | cron 캐리어에 키가 없다 |
| `ALIGO_API_KEY` | **L6 — 알림톡 발송 0건** | cron 쪽 발송·재시도(`retry-alimtalk` dead-letter 포함)가 통째로 skip 된다 |
| `CACHE_KV` | **SSR 전역 워밍이 한 번도 안 돌았다**(2026-07-12 기능) | `cache-prewarm` 이 KV 없이 돈다 — 기능이 죽어 있던 게 아니라 **바인딩이 없었다** |

> ⚠️ **정밀하게**: 이건 **cron 캐리어(Workers)** 의 이야기다. 요청 경로(Pages)에서 나가는 알림톡은
> 별개이고 여기서 판정되지 않는다. 측정된 것은 *cron 이 그 키를 못 본다* 까지다.

`0 18`·`0 19` 블록은 아직 오늘 발화 전이라, 18:00Z·19:00Z 에 `DISCORD_WEBHOOK_URL` 부재도 같은 방식으로 찍힐 것이다.

### 조치

Workers `ur-live` → Settings → Variables and Secrets 에 **`TOSS_SECRET_KEY` 를 Secret 으로 추가**.
Pages 것과 **같은 값**(복사이지 이동이 아니다 — Pages 에서 지우면 결제가 죽는다).
`DISCORD_WEBHOOK_URL`·`DATA_ENCRYPTION_KEY` 도 어차피 양쪽이라 **같은 세션에서 함께** 처리하면 된다.

---

## 8. 키 등록 실측 판정 (2026-08-02~03) — ⚠️ **초판에 오진 2건, 아래 §8.1 이 정정판**

대표가 키를 등록한 뒤 **두 런타임을 각각** 측정했다. 한쪽만 보면 틀린다 —
`env-readiness` 는 HTTP 엔드포인트라 **Pages 만** 재고, cron 캐리어(Workers)는
하트비트 `cron-env-missing` 이 유일한 창이다(§5.3 의 분리가 바로 이것).

### 무엇으로 판정했나 — **부재 목록의 여집합**

`cron-env-missing` 마지막 행(22:50 KST)에 적힌 부재 키는 **`CACHE_KV` 하나뿐**이었다.
그 명부는 `*/5` 에 대해 **TOSS·ALIGO·CACHE_KV 세 개를 전수로** 검사하므로,
**목록에서 빠진 이름은 곧 "있다"는 양성 증거**다.

| 항목 | 판정 | 근거 |
|---|---|---|
| **B8** `TOSS_SECRET_KEY`(Workers) | ✅ 존재 | 부재 목록에 없음 |
| **L6** `ALIGO_*`(Workers) | ✅ 존재 | 부재 목록에 없음 → **알림톡 0건의 원인이 키 부재였음이 확정** |
| **B5** `DATA_ENCRYPTION_KEY` | ✅ 존재(Pages) | `env-readiness` 직접 확인. **cron 경로엔 참조 0** 이라 Pages 만으로 충족 |
| `CACHE_KV` | ✅ 양쪽 | Pages 직접 확인 + Workers 는 22:50 이후 **침묵**(조건부 write 라 부재 0) |

> ⚠️ 마지막 줄만 **추론**이다. 침묵은 '정상'과 '관측 정지'를 구분 못 하므로, 같은 `*/5` 의
> `cache-prewarm` 이 23:25 에 정상 비트를 남긴 것을 대조해 **디스패치는 살아 있음**을 먼저 확인했다.
> 확증은 #973(매 회차 기록) 배포 후 `ok — 요구 키 전부 존재` 로 얻는다.

### 아직 열린 것 (⚠️ 아래 §8.1 에서 정정됨)

- ~~**B6** `DISCORD_WEBHOOK_URL` — Pages·Workers 양쪽 모두 부재~~ → **틀렸다. §8.1 참조.**
- `INTERNAL_API_TOKEN`(L1) · `BACKUP_BUCKET`(L9) · `ANALYTICS_KV`·`RATE_LIMITER`(L11) — 전부 오른쪽.
- 토스 키의 **유효성**(존재 ≠ 유효)은 `daily-self-diagnostic` 프로브가 03:00 KST 에 판정한다.

### 8.1 🔁 정정 (2026-08-03 00:35 KST — CF 바인딩 **직접 조회**)

CF API 토큰이 다시 살아 있어 추론 대신 바인딩 이름을 직접 읽었다. **두 군데가 틀렸다.**

**① B6 — 디스코드는 들어가 있었다.** `env-readiness` 는 **Pages 만** 재는데 판정을 "양쪽 없음"
으로 말했다. 실제로는 **Workers(cron 캐리어)에 바인딩 존재**(= 일일 진단 경보가 갈 곳이 생겼다),
Pages 는 **프로젝트 설정엔 등록됐으나 실행 중 배포(08-02 22:45 KST)가 그보다 앞선다.**

> 🔑 **Pages 환경변수는 그 뒤에 생성된 배포부터 적용된다.** 설정에 있는데 런타임에 없는 상태는
> 정상이며 **둘 다 사실**이다. ⇒ 다음 배포(#973 머지)로 자동 해소. 대표 추가 조치 없음.

**② L6 — 알림톡은 여전히 0건이다(해소 아님).** Workers 바인딩 이름에 **공백**이 있다:

| 실제 이름 | 영향 |
|---|---|
| `' ALIGO_USER_ID'` (앞 공백) | `env.ALIGO_USER_ID` = undefined → 발송 가드 `API_KEY && USER_ID && SENDER_KEY` 가 거짓 → **통째로 skip(무에러)** |
| `'DATA_ENCRYPTION_KEY '` (뒤 공백) | cron 경로 참조 0 이라 **동작 피해 없음**(B5 는 Pages 로 충족). 같은 붙여넣기 습관의 증거 |

**대표 조치**: Workers `ur-live` → Variables 에서 그 둘을 지우고 **공백 없이** 재등록.

**③ 계기도 같이 틀렸다** — 명부에 `ALIGO_API_KEY` 하나만 있어, 3종 중 둘만 살아 있어도
`ok — 요구 키 전부 존재` 로 답했다. 3종 전부를 명부에 넣어 수리했다.
**규칙: 가드가 보는 조건 = 코드가 보는 조건.** 부분집합만 보면 나머지가 사각지대다.

> 🔎 이 클래스는 **바인딩 이름을 `repr` 로 봐야만** 잡힌다 — 눈으로는 앞뒤 공백이 안 보인다.

### 8.2 ✅ 최종 실측 (2026-08-03 01:31 KST — #973 머지·배포 후)

대표가 **전부 Secret 으로** 재등록했고(이름 공백 0 확인), #973 배포가 `CACHE_KV` 를 복원했다.

| 런타임 | 상태 |
|---|---|
| **Workers**(cron) | 바인딩 12개 — `TOSS_SECRET_KEY`·`ALIGO_*`3·`DISCORD_WEBHOOK_URL`·`DATA_ENCRYPTION_KEY` **전부 Secret**, `CACHE_KV` 복원 |
| **Pages**(HTTP) | `blocking_missing` 0 · `security_missing` **0** · 잔여 `BACKUP_BUCKET`·`RATE_LIMITER` 뿐 |

⇒ **B6 해소**(양쪽 다 존재) · **L1 해소**(`INTERNAL_API_TOKEN`) · **L6 해소**(01:10:35 회차에서
부재 목록이 `CACHE_KV` 하나로 줄어 알림톡 3종 전부 런타임 도달 확인).

> 🔑 **판정 방식**: CI 초록불이 아니라 **효과를 직접 측정**했다. GitHub API 직접 호출이 이 세션에선
> 403 이라 감시기가 **조용히 죽어 있었고**(그래서 Verify 완료 알림이 안 왔다), 그 침묵이
> "아직 안 끝남"과 구분되지 않았다 — 오늘 하루 쫓던 바로 그 형태다.
> ⇒ 배포 확인은 **머지 커밋 해시 대조 + 바인딩 재조회** 두 갈래로 했다.

### 8.3 ⏫ 새로 생긴 것 — 백업 cron 은 켜졌는데 **버킷이 없다**

#971 이 `0 20 * * SUN` 을 점화했지만 cron 캐리어의 R2 바인딩은 **0개**다.
`handleD1Backup` 은 2026-06-12 수리로 **throw** 하므로 조용한 0건은 아니고, 디스코드가 이제
살아 있어 경보도 간다 — 다만 그건 **일요일에 알게 된다**는 뜻이다.
⇒ `CRON_REQUIRED_ENV` 에 `BACKUP_BUCKET` 을 넣어 **지금** 보이게 했다(별칭 `SUN`↔`0` 흡수 포함).

**대표 조치(일요일 전)**: R2 버킷 `ur-live-backups` 생성 → Workers `ur-live` 에 `BACKUP_BUCKET` 바인딩.
⚠️ **절대 공개 버킷으로 만들지 말 것** — D1 전량 덤프(주문·정산·개인정보)다.

---

## 7. 한 줄 요약

**오픈을 막는 것은 이제 3건이다**(§8.2 실측으로 B5·B6·B8 닫힘) — B1·B2(같은 실결제 1건) · B4 미수령 고지 문구 · B7 몰 브랜딩.
> 셋 다 **키가 아니라 판단·행위**가 필요하다. 키로 닫히는 항목은 남아 있지 않다. — B1·B2(같은 실결제 1건) · B4 고지 문구 · B6 장애 인지 · B7 브랜딩.
> 그중 **B6 만 키 등록으로 닫힌다**(Pages·Workers 양쪽 `DISCORD_WEBHOOK_URL`). 나머지는 판단·행위가 필요하다.
> (이전 서술: ~~B3 해소 후 6건, B8 추가로 7건~~) 그중 5건은 대표가
이미 진행 중인 순서 안에 있다(B5·B8 키 등록 · B7 브랜딩 · B1·B2 실결제).
**새로 확인해야 할 것은 B4 고지 문구 · B6 장애 인지 2건**이다.

`INTERNAL_API_TOKEN` 과 `0 18` 은 **둘 다 오른쪽**이다 — 전자는 조사 결과 **무방비가 아니었고**,
후자는 **자가치유되는 지연**이다(07-29·30 연속 정상 발화로 격상 사유도 소멸). 둘 다 오픈을 막을 근거가 없다.

---

## 구현 로그

- 2026-07-29 신설 — 대표 지시 *"두 열로 재분류 · 근거 한 줄 · 두 판정 포함"*. 코드 변경 0.
  `INTERNAL_API_TOKEN` 은 소비처 4곳 전수 + 라이브 실측으로 fail-closed 확정.
  `0 18` 은 블록 내 머니 작업 4종의 대상 선정 술어를 읽어 누적·멱등 확인.
