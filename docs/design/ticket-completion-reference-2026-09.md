# 결제 완료 화면 참고 시안 (코레일톡) - 2026-09-02

대표: *"이거봐 이 색상과 느낌. 디자인 조화. 난 이게 좋아. 물론 화이트 다크 테마 나뉘겠지만."*

시안: [`assets/korailtalk-payment-complete-ref.jpg`](./assets/korailtalk-payment-complete-ref.jpg)
(코레일톡 기차 승차권 결제 완료 화면, 다크. 동탄 16:09 → 부산 18:31.)

## 1. 이 화면이 조화로운 이유 (분해)

색이 예쁜 게 아니라 **규칙이 적어서** 조화롭다. 여섯 개로 다 설명된다.

| # | 규칙 | 시안에서 |
|---|---|---|
| 1 | **표면은 두 톤, 테두리와 그림자는 0** | 바탕 진남색(순검정 아님) / 카드 한 톤 위. 카드 경계는 색 차이로만 |
| 2 | **강조색 하나, 자리 셋** | 블루가 사는 곳: 티켓 상단 밴드 · 주 행동 글자("승차권 확인") · 강조 단어("오는 열차"). 그 외 어디에도 없다 |
| 3 | **숫자가 주인공** | 16:09 / 18:31 이 화면에서 제일 크다. 역 이름은 그 위에 작게. 날짜는 밴드에 |
| 4 | **티켓 은유** | 색 밴드(날짜 + "19일 전") + 본문 = 실물 승차권 한 장. 카드 안에 outline 버튼 하나 |
| 5 | **나머지는 전부 회색** | 안내 3줄은 작은 네모 불릿 + 회색. 크로스셀("오는 열차도 찾아볼까요?")도 같은 카드 표면, 새 장치 없음 |
| 6 | **색이 있는 것은 격리** | 서비스 아이콘 5개만 컬러이고 맨 아래 한 줄에 가둬 둠 |

없는 것도 중요하다: 그라디언트 0 · 이모지 0 · 섹션 라벨(eyebrow) 0 · 체크 원 아이콘 0 · 정보 상자 색깔 0.
"결제가 완료되었어요"는 **제목 한 줄이 전부**이고 초록 체크가 없다.

⇒ 이건 8-30 이후 우리가 정한 규칙과 **같은 문법**이다 (로즈가 사는 자리 셋 · 모양 잠금 · 가운뎃점 사슬 금지). 다만 우리는 규칙을 *지우는 데* 썼고, 이 화면은 규칙으로 *한 장을 지었다*. 차이는 §2.

## 2. 우리 현재 (2026-09-02 실측)

| 화면 | 지금 | 시안과의 거리 |
|---|---|---|
| **이용권 결제 완료** | **화면이 없다.** `GroupBuyConfirmPaymentPage` 가 승인 확인 뒤 **1.5초 만에 `/my-vouchers` 로 자동 이동**. 사용자는 "완료되었어요"를 본 적이 없다 | 가장 큼. 시안이 정확히 이 자리다 |
| 쇼핑·딜충전 결제 완료 `PaymentSuccessPage` | 초록 체크 원 + `shadow-lg` + `border` 카드 + amber/red/yellow/gray 정보 상자 = **표면 5종**. 시안의 정반대 | 큼. ⚠️ Toss 잠금 파일(시각만 변경은 승인 선례 있음) |
| 이용권 지갑 카드 `MyVouchersPage` | 08-31 대표 승인 시안 4. 밴드 없음, 만료일은 본문 텍스트 | 중간. 밴드 하나면 티켓이 된다 |
| 다크 팔레트 | 바탕 `#0D0F12` · 카드 `#1C1C1E`/`#1A1C21` | **이미 같은 계열**(순검정 아님). 팔레트는 안 바꿔도 된다 |
| 라이트 팔레트 | 바탕 `#FAF7F5` 크림 · 카드 흰색 | 시안엔 없지만 규칙 1 그대로 적용 가능(크림/흰 두 톤, 테두리 0) |
| 강조색 | 로즈 `#E0526B` | 블루 대신 로즈. **하나만** 쓴다는 점이 같다 |

## 3. 매핑 - 시안 요소 → 유어딜 이용권

| 시안 | 유어딜 이용권 결제 완료 |
|---|---|
| 밴드: `2026.09.21 (월)` · `19일 전` | 밴드(로즈): **사용 기한** `2026.09.21 (월)까지` · `D-19` |
| `기차 승차권 · 1매` | `식사 이용권 · 1매` (카테고리 이용권 라벨 SSOT) |
| `동탄 16:09 → 부산 18:31` | **매장명 크게** + 상품명. 숫자 주인공은 `17,900원` 한 개 (시간 개념이 없으니 가격이 큰 숫자) |
| outline `승차권 확인` | outline `이용권 확인` → `/my-vouchers` |
| 안내 3줄(네모 불릿) | 매장에서 QR/코드로 사용 · 미사용 시 100% 자동환불 · 기한 지나면 자동 환불(규정 SSOT 문구) |
| 크로스셀 `오는 열차도 찾아볼까요?` | `이 매장 다른 이용권도 볼까요?` 또는 `근처 이용권` (같은 매장 `otherDeals` 가 상세에 이미 있다) |
| 서비스 아이콘 줄 | 넣지 않는다. 우리 하단 네비가 그 역할이고, 컬러 아이콘은 지금 체계에 없다 |

## 4. 제안

**A. 이용권 결제 완료 화면 신설** (자동 이동 폐기). 시안을 1:1 로 옮긴다. 잠금 파일 아님(`GroupBuyConfirmPaymentPage` 105줄 확장 또는 새 페이지).
   화이트: 크림 바탕 + 흰 카드 + 로즈 밴드. 다크: `#0D0F12` + `#1A1C21` 카드 + 로즈 밴드. 테두리·그림자 0 양쪽 다.
**B. 지갑 카드에 밴드** - 08-31 시안 4 위에 로즈 밴드(기한 + D-day)만 얹는다. 시안 4 는 대표 승인분이라 **변경 승인 필요**.
**C. `PaymentSuccessPage` 시각만 시안 문법으로** - 체크 원 제거, 표면 5종 → 2종, 정보 상자 색 제거. Toss 잠금이라 `AskUserQuestion`.
**D. 색 규칙 확정** - 강조는 로즈 하나. 블루는 쓰지 않는다(시안의 블루는 코레일 브랜드색이지 문법이 아니다).

권장 순서: **A → B → C**. A 는 지금 화면이 아예 없어 회귀 위험 0 이고, 시안을 가장 그대로 보여 줄 수 있다.

## 5. 구현 todo

- [ ] A. 이용권 결제 완료 화면 (밴드 · 매장/상품 · 가격 · outline 버튼 · 안내 3줄 · 크로스셀) 라이트/다크
- [ ] A. 하네스 `--route=/group-buy/confirm-payment` 시드(승인 응답) 추가해 전·후 렌더
- [ ] B. 지갑 밴드 (대표 승인 후)
- [ ] C. `PaymentSuccessPage` 시각 정리 (Toss 잠금 승인 후)
- [ ] 가드: 결제 완료 표면에 `shadow-` · `border-` · 컬러 정보상자 0 (기존 `check-shape-lock` 계열 래칫으로)
