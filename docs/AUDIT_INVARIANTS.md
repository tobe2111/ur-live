# 🛡️ 감사 불변식 레지스트리 (AUDIT INVARIANTS)

> **대표 지시 (2026-06-26)**: "이상적인 상태라면 더는 이후 감사에선 보지 않고 넘어갈 수 있도록 환경 설정을 해줘."
>
> 한 번 깨끗하다고 확인된 영역의 불변식을 **결정론적 가드로 박아 기계가 지키게** 한다.
> 미래 세션은 수동 전수감사 대신 **`bash scripts/audit-gate.sh`** 를 돌려, GREEN 도메인은 가드를
> 신뢰하고 **재감사를 건너뛴다**. RED 만 보면 된다.

## 🚦 한 줄 점검

```bash
bash scripts/audit-gate.sh           # 전체 (105개 불변식)
bash scripts/audit-gate.sh money     # 특정 도메인만 (separation|auth|money|schema|classify|ui|structure|deploy)
```

- **ALL GREEN** → 아래 표의 "감사 스킵" ✅ 도메인은 수동 재감사 불필요. 그 영역의 *새 코드*만 가드가 통과하는지 보면 됨.
- **RED** → 그 가드가 가리키는 사이트만 재감사/수정. 전수조사 불필요.
- 가드가 **없는** 영역(아래 "🔍 가드 미보유")만 수동 감사 대상.

## ✅ 감사 스킵 가능 도메인 (가드 GREEN = 신뢰)

| 도메인 | 불변식 (무엇을 보장) | 지키는 가드 | 마지막 수동 전수감사 |
|---|---|---|---|
| **서비스 분리** | 도매몰↔유어딜↔유어애즈 3-서비스가 안 샘: 교차역할 API 0(유어애즈 `/api/ads` 포함), 도매 어드민 스코프 내, 소비자 상품조회가 도매 원본 격리, 유어애즈가 도매 폴더에 비의존(공용 인프라는 `@/worker/utils/seller-auth`·`@/services/naver-commerce-core`), **대시보드 라우팅이 다중역할/겸업 계정을 lock-out 안 함(대시보드 레이아웃/페이지가 가산 권한 플래그 `is_*` 단독 게이트로 서비스간 redirect/return null 0 — 셀러↔도매=서버권위 `wholesale_only`, 또는 다중역할 보호 동반조건 `!loggedIn`/단일역할 `role !==`)** | `check-dashboard-api-crossrole`(5그룹) · `check-wholesale-admin-api-scope` · `check-wholesale-admin-nav-reachability` · `check-consumer-product-supply-isolation` · `check-seller-wholesale-redirect` | 2026-06-30 (셀러 대시보드 겸업 lock-out fix + 신규 가드: is_distributor=capability ≠ exclusivity, computeWholesaleOnly SSOT) |
| **인증·세션·RBAC** | 역할-한정 403 0, 유저↔대시보드 상호 로그아웃 0, OAuth iOS 영속, dead-link 0(**객체 리터럴 `to: '/x'` 포함** — 2026-08-12 까지 JSX `to=` 만 봐서 배열+map 링크 36건이 무검사였다), 권한 누락 0, **로그인 유도는 토큰으로만(가격 null/0 을 로그아웃으로 오판 0)**, **도매 로그아웃 유지(자동 재로그인 probe 가 명시 로그아웃 무력화 0)**, **도매 엣지캐시 인증 누수 0(비로그인 public CDN 캐시가 로그인 판매사에 서빙 → '공급가 미설정' 0)** | `check-dual-login-guard` · `check-dashboard-login-session-coexist` · `check-auth-cookie-pattern` · `check-internal-links` · `check-api-auth` · `check-light-input-guard` · `check-login-gate-by-price` · `check-wholesale-autologin-guarded` · `check-wholesale-login-spa-navigate` · `check-wholesale-cache-auth-leak` | 2026-06-29 (도매 엣지캐시 인증 누수 — 인증별 응답 public 캐시는 캐시키 분리(cache-auth-ok) 필수, 클라 v=in 부착) |
| **머니·정합성(패턴)** | CAS-선점/무환불 CANCELLED/빈화면-위장/CSV 인젝션/폐기 가격모델 드리프트/잔액 절대값write/**잔액↑ 원장기록 삼킴** 안티패턴 0 | `check-money-patterns` · `check-status-constraints` · `check-query-iserror` · `check-csv-injection` · `check-deprecated-pricing` · `check-balance-absolute-write` · `check-balance-without-ledger` | 2026-08-01 (원장 불일치 4명 추적 — 잔액만 늘고 거래기록 0 인 유저를 만든 빈 catch 4곳) |
| **커미션 예산 [INV-CB]** | 플랫폼 부담 성장 커미션(어필리에이트·멀티티어·영입자·에이전시)의 적립은 오케스트레이터(`creditOrderCommissions`) 경유만 — 3P 주문당 예산(수수료−PG준비금) 캡 우회 호출/신규 적립 INSERT 0. 수학(비례배분·합≤예산)은 `commission-budget.test.ts` 유닛이 보장. ⚠️ 이 불변식은 오늘의 **"platform net ≥ 0"**(커미션이 5% 를 PG준비금 바닥까지 잠식 허용)이다 — 2026-07-08 확정 원칙의 더 강한 **"platform net == 5%"** 는 아래 #44(8월 flip 시 신설) | `check-commission-budget` | 2026-07-04 (재원 구조 개편 — `commission-funding-restructure.md`) |
| **커미션 재원=promo·5%불가침 [#44 — flip 시 신설, 미구현]** | (2026-07-08 확정 원칙) **모든** 성장 커미션(어필리에이트·멀티티어·인플 영입·인플 share·**에이전시 1%·30% share 포함**)은 매장 promo(5% 밖, `promo_funding_source=owner`) 재원 → **원장 `platform:revenue` = 결제액 5% 전액 불변(성장 커미션 debit 0, 예외 없음)**. 에이전시도 조율 마진(promo)에서 먹으므로 5% 무관(= 쇼핑 벤더 모델). **PG 는 이 5% 안에서 흡수(원장 밖 비용)** — 실현마진 5%−PG 와 구분. 오늘 owner 스위치는 어필리에이트만 커버 → flip 이 나머지 전 축 확장. 가드: `commission-budget.ts` flip 플래그로 플랫폼 예산 0 강제 + `commission-budget.test.ts` `platformNet==fee` 단언 + `check-commission-budget` R4(어떤 성장 커미션도 `platform:revenue` debit 0, 예외 없음) | `check-commission-budget`(R4 예정) | **미구현** — 8월 promo flip. 설계: `commission-funding-restructure.md` §확정 원칙 |
| **수집 예산 학습 레인격리** | 서브리퀘스트 학습 상한(`ads_subreq_cap_*`)은 **레인마다 다른 키** — 건당 비용이 다른 레인(전화 스윕·인플루언서 = fetch 1 / 보강 = fetch 4~6 + D1)이 한 키를 공유하면 서로의 관측을 덮어써 **어느 레인도 자기 한도를 학습 못 함**(보강 레인이 남의 백오프로 env 300 → 실제 37 로 굶음). 유어애즈는 `subreqCapKey(lane)` 경유 + 키 값 파일간 중복 0 | `check-subreq-cap-lane` | 2026-07-28 (파트너풀 보강 "무증거 종료" 진단 — 공유 캡 키 분리 후 신설) |
| **서브리퀘스트 플랫폼 천장** | 학습 상한은 **플랫폼 한도를 넘을 수 없다**(무료 인보케이션당 50 → 기본 45, `ADS_SUBREQ_PLATFORM_CAP` 로 조정). 자기교정 루프는 *실패를 관측할 수 있을 때만* 작동하는데, 건당 4~6 fetch 인 보강 레인은 라운드가 **잡을 예외 없이** 죽어(`limit_hit:false`·`crash` 없음) 하향이 한 번도 안 걸렸다 → 회복(×1.25)만 반복돼 `company_enrich=172`(천장의 3.4배)까지 한 방향 드리프트, 매 라운드 4~9번째 리드에서 무증거 사망. 같은 조사에서 `runCompanyAutoCollect`·MX스윕·상가정보·바이어발굴은 **천장도 학습도 없이** env 숫자(110/80/60/60)를 그대로 썼다. R1 `resolveSubreqBudget` 3인자 · R2 `nextSubreqCap` 5인자 · R3 예산 **초기화 식**이 천장 경유(파일 어딘가에 심볼만 있으면 통과하던 공허성은 2026-07-29 실측으로 제거). 예외 `platform-cap-ok` | `check-subreq-platform-cap` | 2026-07-29 (신설 — 라이브 학습값 172 vs 55/65 대비로 발견) |
| **크롤 재시도 쿨다운** | `ad_company_leads` 에서 **크롤 대상을 고르는 SELECT**(website 보유 + email 미보유)는 `enrich_checked_at` 쿨다운 조건 필수 — 없으면 실패한 리드가 `email IS NULL` 이라 다음 회차에도 또 선두 → **매시간 같은 앞줄만 재크롤**하고 백로그는 영영 미도달(에러·로그 0 이라 조용히 방치된다). 보강 레인(2026-07-27)·수집 레인(2026-07-28)에서 **같은 사고가 두 번** 발생 | `check-crawl-cooldown` | 2026-07-28 (수집 레인 이메일 보충 블록에 쿨다운 누락 발견 — 세 번째 재발 차단) |
| **접힌 리드(중복병합) 누수** | 중복 병합은 **삭제하지 않고** 패자를 `active=0 + merged_into=<승자>` 로 표시만 한다(되돌리기 가능) — 그래서 **리드를 고르는 모든 쿼리가 `merged_into IS NULL` 을 봐야 한다**. 실사고(2026-07-28 첫 병합 1,523행 직후): ① 정비 스윕이 `active=0 AND 전화있음` 만 보고 접힌 행을 **전원 부활**시킬 뻔했고(병합 무효화) ② 보강 레인이 접힌 행을 다시 크롤 대기열에 넣었으며 ③ 상태줄 `held_no_contact` 가 그만큼 부풀어 대표가 읽는 KPI 가 거짓이 됐다. 예외는 `merged-filter-ok` 주석 | `check-merged-lead-filter` | 2026-07-28 (신설 — 첫 실전 병합 직후 발견) |
| **수집 러너 스케줄 누락** | 수집·스윕 진입점(`run*Collect` · `sweep*` · `match*Emails` 등)은 ur-ads `scheduled()` 블록에 물려 있어야 한다 — 코드가 정상이고 테스트도 통과하는데 **'언제 도는가'만 빠져** 어드민이 버튼을 누른 날에만 돌던 사고가 2026-07-28 하루에 **두 번**(`runMakerCollect` 제조사 풀이 수동 85건에 고착 · `matchRegistryEmails` 는 D1 전용이라 매시간 공짜인데 스케줄 부재). 자동수집은 **영구적으로 도는 것**이 요구사항(대표 지시). 수동 전용이 의도면 `no-cron-ok` 주석 + 이유 | `check-collector-cron` | 2026-07-28 (신설 — 같은 사고 두 번째에) |
| **세션 인계** | 브랜치가 `src/` 를 바꿨으면 `docs/CURRENT_WORK.md` 가 **한 번은** 갱신돼 있음 — 다음 세션이 옛 상태로 오판해 이미 고친 것을 또 파는 사고 방지. 문서/테스트만 바꾼 브랜치는 대상 아님(소음 억제). 남길 4가지: ①다음 세션 첫 액션 ②완료분+해시 ③**이번에 틀렸던 판단** ④대기 결정 | `check-current-work-sync` | 2026-07-28 (실사고 후 신설 — 보강 레인 5건 머지 중 인계 0회, 룰만 있고 강제가 없어 놓침) |
| **DB·스키마** | 컬럼/bind/NOT NULL/SELECT* /컬럼예산/복구가능성 정합 | `check-schema-refs` · `check-sql-*` · `check-no-select-star-products` · `check-products-column-budget` · `check-product-detail-fields-repairable` | (상시 가드) |
| **런타임 크래시(pagination NaN)** | request page/limit/offset/days 등이 비숫자('abc')일 때 `parseInt/Number → NaN → SQL .bind(NaN) → 500` 금지(전 서비스 목록 엔드포인트) — 정수 파싱은 `intParam(raw, def)`(`@/shared/pagination`) 경유 강제(0/음수 클램프 보존). ID 해석 parseInt(isNaN 가드 보유)는 무관 | `check-pagination-nan` | 2026-07-01 (도매몰 라이브 전수조사 — `/api/wholesale/catalog?page=abc` 500 발견 → 전 서비스 100+ 라인 intParam 전환 후 가드) |
| **상품 종류·라우팅** | group_buy_status 로 종류판별·라우팅 금지(쇼핑↔교환권 오분류) | `check-groupbuy-status-classify` | (상시 가드) |
| **동네딜↔쇼핑 완전분리** | 동네딜 표면/도구(리스트 API·데모 시드·alias·수기 폼)에 배송형(general) 유입 금지 — 동네딜=로컬 이용권 전용 | `check-dongnedeal-separation` | 2026-07-02 (대표 확정 — 유령 general 데모 사고 후 신설) |
| **도매주문 상태머신** | wholesale_orders.status 가 canonical 집합만(정의 밖 오타/고아 상태 write 0) — 전이는 transitionWholesaleOrder | `check-wholesale-order-status` | 2026-06-27 (B2B 플로우 상태머신 신설: 수락/거절/취소/구매확정 + 발송 전 정산보류) |
| **UI·테마·첫페인트** | dark variant 일관성, RQ initialData 신선도, 모바일 하단잘림 | `check-theme-consistency` · `check-query-initialdata` · `check-mobile-viewport` | 2026-06-26 (크래시/빈상태 clean) |
| **시각 표기(KST 정합)** | DB 타임스탬프(`CURRENT_TIMESTAMP`/`datetime('now')` = UTC·`Z` 없음)를 `new Date()` 로 순진하게 파싱 금지 — 브라우저는 로컬(KST)로 오해석하고 워커는 `.toLocaleString('ko-KR')` 이 UTC 시각을 한국어로 찍어 **9시간 어긋남**. SSOT = `src/utils/date.ts`(`parseUTCDate`/`formatKST*`/`kstDayStartMs`·`kstDayEndMs`). 규칙 A(어디서든 `new Date(x.created_at).toLocale*` 금지) + 규칙 B(pages/components/hooks 는 비교·정렬까지 금지). 래칫(`utc-date-baseline.json`) — 신규/증가만 차단, 잔여분은 점진 정리 | `check-utc-date-parse` (래칫, `--rebaseline`) | 2026-07-27 (어드민 최근 활동 전수조사 — 연속 주문 감지 영구 미발동·알림톡 주문일시 9h 이름·주문 날짜필터 경계 누락·교환권 만료일 하루 이름 fix 후 신설. **서버/워커측 잔여 0**, 클라 87건 동결) |
| **레인 전진(조용한 전진 0)** | 커서를 `platform_settings` 에 저장하는 지점 **앞** 창에 루프가 있는데 그 루프에 **시간 상한**(`Date.now() - t0`·`deadline`·`shouldStop`)이 없으면 차단. 루프가 CPU/시간 한도로 죽으면 뒤의 커서 저장에 **도달하지 못해** 다음 회차가 같은 지점을 또 훑고 또 죽는다 ⇒ **영원히 전진 0**(에러는 '느린가 보다'로 읽힌다). 마감선 중단은 `done` 을 **false** 로 남길 것 — true 면 커서가 0 으로 리셋돼 풀 뒷부분이 영영 처리되지 않는다. ⚠️ 함수 경계를 모르는 120줄 창 휴리스틱이고 CPU 실사용량은 판단하지 않는다 → **래칫**(신규만 차단) | `check-cursor-after-loop` (래칫, `--rebaseline`) | 2026-08-03 (같은 실패가 **두 번** 났다: commerce 08-02 · quality 08-03. `ads-cpu-deadline` 이 문서로 확정해 뒀는데도 다음 레인에서 재발 — 문서로는 못 막는다는 증거) |
| **코드 구조(god 파일 방지)** | 신규 파일 600줄 초과 차단 + 기존 대형 파일이 `file-size-baseline.json`(82개 동결)보다 성장 시 차단(줄이는 건 OK) → god 파일 재발 0 | `check-file-size` (래칫, `--rebaseline` 로 동결값 갱신) | 2026-06-29 (대표 "리팩토링 반복 말고 애초에 막아라" — MyVouchersPage 1296→386·GroupBuyListPage 1309→827 분해 후 동결). 2026-07-11: PR CI(verify.yml)는 `--changed-only`(merge-base vs origin/main 변경 파일만 판정 — main 드리프트가 무관한 PR 을 실패시키던 문제 차단, 당일 3회 재발); 게이트/rebaseline 은 전수 `-a` 유지 |
| **빌드·배포 안전** | vite 단독빌드/405 라우터/SW등록/하드코딩 시크릿 금지 | `check-build-command` · `check-router-patterns` · `check-no-sw-register` · `check-no-secrets` | (상시 가드) |
| **KV delete 무료한도** | ① `cacheInvalidate`(worker/utils/cache.ts) KV.delete 는 `L2_KV_ENABLED` 게이트 뒤(쓰기 OFF 면 삭제 skip — 대칭) ② fan-out KV.delete(`arr.map/forEach(...=><kv>.delete)`) 무방비 추가 0. Cache API(`caches.default`)·단발 KV.delete(저빈도)는 무관 | `check-kv-delete-budget` | 2026-07-21 (KV delete 한도 초과 사고 — `cacheGet` L2 OFF 인데 삭제만 살아 28/호출 낭비) |
| **게이트 도달성(끄고 켤 화면)** | `OPS_GATES` 의 `kind:'setting'` 게이트가 어드민 편집 화면에 실재할 것 — 게이트만 만들고 화면을 안 만들면 그 게이트는 **코드에만 존재하고 영영 OFF** 로 남는다. `turn_on_when` 에 "켜지 않는다" 가 명시된 것만 면제 | `ops-gate-reachable.test.ts` (vitest, verify.yml) | 2026-08-12 — 같은 사고가 세 번. ① 08-03 미수령 정책 비율(대표가 확정했는데 넣을 화면이 없어 값이 비어 있었다) ② 08-12 `pickup_unclaimed_policy_enabled`(설정 화면엔 *"시스템 모니터링에서 켜라"* 안내문만 있는데 그 화면은 **조회 전용**)·`partial_refund_enabled`(**어디에도 없었다**) ③ 08-12 `gb_pricing_enabled` — 🔴 **과소청구 긴급 킬스위치인데 당길 손잡이가 없었다**(돈이 새는 중에 멈출 방법 0). ⇒ **"게이트가 OFF" 를 "안 켰다" 로 읽지 말 것 — "못 켰다" 일 수 있다.** ⚠️ 못 잡는 것: `kind:'env'`(Cloudflare 소관) · 화면엔 있는데 저장 검증에 걸려 거부되는 경우(실제로 `flip_pilot_seller_ids` 를 숫자 검증 배열에 넣어 `Number('5,12')=NaN` 으로 저장이 막힐 뻔했다) |
| **cron 하트비트 커버리지** | 모든 스케줄 작업이 `safeCron` 경유 + safeCron 이 성공·실패 무관 실행기록을 남길 것 | `check-cron-heartbeat` (strict: verify.yml + audit-gate) | 2026-07-28 — `safeCron` 은 **예외를 던질 때만** 기록했다(cron_failures/벨/Discord). 실제로 아픈 정지는 예외가 없다: ① cron 미발화 ② 게이트 OFF 조기 return ③ 내부 `.catch(() => null)` 로 삼켜 **성공으로 집계**. 유어애즈 자동 정비가 ③ 으로 07-26 부터 멈췄는데 아무도 몰랐다(#793). 당시 cron **70개 중 실행기록을 남기는 건 3개**뿐. → safeCron(68개 작업의 유일 진입점)에 하트비트 추가 + 어드민 `GET /api/admin/cron-heartbeats`(오래된 순 = 멈춤 의심 순). 예외는 `cron-heartbeat-ok` 주석 |
| **cron 슬롯이 실제로 등록돼 있을 것** | `src/worker/scheduled.ts` 의 `if (cron === 'X')` 블록에 든 `safeCron` 이름은, X 가 `wrangler.toml` crons 에 **있을 때만** 산 것이다. 없는 슬롯의 cron 은 죽은 것으로 `scripts/cron-dead-slot-baseline.json` 에 동결 — **새 이름이 죽은 슬롯에 들어오면 위반**(래칫, 빼는 건 자유) | `check-cron-slot-registered` (strict: verify.yml + audit-gate) | 2026-08-03 — 숙박 데모 72개가 **추첨 배지 없이** 89,000원짜리 진짜 숙박권으로 보이고 있어 자가치유 백필을 `demo-fcfs-renew` 에 넣고 배포했는데 **라이브가 그대로**였다. 그 cron 이 `if (cron === '0 * * * *')` 안에 있고 그 표현식이 wrangler crons 에 **없다**(3단계 보류 — 도매 예치금 자동 환불 규모 미측정). 즉 **한 번도 발화한 적이 없다**: 시간당 슬롯 하트비트 32건이 **전부 `ads:*`**(별도 워커)이고 메인 워커 것은 **0건**이었다. 같은 블록에 `kt-alpha-voucher-retry`·`toss-refund-retry`·`webhook-failed-drain` 등 20개가 함께 잠들어 있다(총 32개 동결). **에러가 없어 배포는 초록불**이고 리뷰에도 안 걸린다. 짝인 `check-cron-heartbeat` 는 *"safeCron 으로 감쌌는가"* 를 보므로 **감싼 것이 안 불리는** 이 경우를 못 본다 — 관측 배선과 발화 여부는 다른 층이다. ⚠️ 못 보는 것: **ur-ads 워커**(자기 crons 로 실제 발화) · **CF 대시보드에서 직접 건 cron**(레포가 못 봄 — `wrangler.toml` SSOT 전제) · 슬롯은 살아 있는데 **핸들러가 조기 return** 하는 경우(그건 하트비트 몫). 갱신 `--rebaseline` |
| **하트비트 이름을 조용히 없애지 않을 것** | 디스패치 파일에서 **리터럴 하트비트 이름이 사라지면** `BEAT_RENAMED_TO`(개명 지도)에 후임을 적거나 `beat-retire-ok` 예외를 남길 것 | `check-beat-name-retirement` (strict: verify.yml + audit-gate) | 2026-08-25 — 하트비트 행(`cron_hb:{name}`)은 **코드보다 오래 산다.** 이름을 그만 쓰면 그 행은 영원히 `stale` 이고 `ok` 를 영원히 문다. 그리고 이 레포에선 **영원한 빨간불 하나가 경보 채널 전체를 침묵시킨다** — `uptime.yml` 이 `down && open` 이면 아무것도 안 했기 때문이다. 실측: `d1-backup`(08-02 OOM 사망, 후임 `d1-backup-chunked` 가 인수)이 `/api/_healthcheck/cron` 을 21일째 503 으로 붙잡아, 이슈 #1056 이 08-04 부터 무갱신이었고 **그 사이 08-24 일간 16개(정산 성숙·원장 정합·만료 이용권 환불)가 통째로 빠졌는데 새 신호가 0** 이었다. 같은 날 `__tick` 을 트리거별로 쪼개며 **같은 함정을 다시 팠다**(배포 40분 뒤 새 빨간불). 둘 다 배포 후 사람이 라이브를 봐서 알았다 — 커밋 시점에 알 수 있는 사실인데도. ⚠️ 못 보는 것: **리터럴만** 본다(`` `__tick:${cron}` `` 같은 템플릿 이름은 열거 불가) · `origin/main` 대비 *변화*만 본다(이미 쌓인 옛 고아는 라이브 조회로만 판정 — 08-25 전수 조회 결과 1건) · 유어애즈 레인은 대상 아님(런타임 `knownBaseNames` 가 판정). 예외 `beat-retire-ok` 주석 |
| **머지 충돌 마커 0** | 추적 파일에 `<<<<<<< ` / `>>>>>>> ` (및 그 사이의 `=======`)가 남지 않을 것 | `check-conflict-markers` (strict: verify.yml + audit-gate + pre-commit) | 2026-08-02 — 이 레포에서 **두 번** 났다: 07-11 `CLAUDE.md` 에 마커 3줄이 커밋된 채 남아 다음 세션이 하이진으로 제거 · 08-02 세션이 머지 충돌 중 `git add -A && git commit` 을 이어 붙여 `576525a` 로 마커째 푸시. **`git add -A` 는 충돌 파일도 '해결됨'으로 표시하므로 git 이 막아 주지 않는다.** 여러 세션이 같은 파일(특히 handoff)을 동시에 파는 구조라 충돌이 상시 → 재발 확률이 낮지 않다. ⚠️ 마크다운 구분선(`=======`) 오탐을 피하려고 **혼자서는 위반이 아니고** 같은 파일에 open/close 가 있을 때만 센다. ⚠️ 못 막는 것: 마커 없이 한쪽만 남긴 **잘못된 해소**(내용 손실)는 문자열로 판정 불가 — 사람이 diff 를 봐야 한다. 예외 `conflict-marker-ok` |
| **가드 주입본 혼입 0** | `check-guard-mutations` 가 **의도적으로 심어 둔 결함**이 브랜치 커밋에 섞여 있지 않을 것 — origin/main 대비 바뀐 **파일 전부**에서 주입 지문 6종을 찾는다(`if (false)` · `=> p` 항등 대체 · `// (… 제거)` 주석 · `__probe-*` import · 가짜 `mk(` · `VALUES (?, 'x',`) | `check-branch-scope` (strict: verify.yml + audit-gate) | 2026-08-26 — 실사고. audit-gate 가 타임아웃돼 **세 번 띄운 것이 셋 다 살아** 같은 워킹트리에 동시에 결함을 주입했고, 그 사이 `git add -A` 가 6건을 집어 커밋했다. 그중 하나는 **존재하지 않는 `__probe-lane` import** 라 배포됐으면 유어애즈 워커가 런타임에 깨졌고, 다른 하나는 2026-08-03 CPU 사망(6시간 20분)의 원인이던 `!laneAlarmOn` 게이트 제거였다. 🩸 **수습도 틀렸다** — 첫 정리에서 `git diff -- <경로 목록>` 의 경로를 **손으로 골라** 여섯 번째를 놓쳤고(CI 가 대신 잡았다), 그 빨간불을 보고도 '내 변경 탓'부터 의심했다. ⇒ 사람이 경로를 고르지 않게 전수로 본다. ⚠️ **커밋된 상태만 본다(워킹트리 X)** — pre-commit 짝 `check-no-injection-in-progress.sh` 와 **둘이 한 쌍**이고 한쪽만으론 반쪽이다(검증도 워킹트리에 심으면 초록이 뜬다 — 반드시 커밋해서 확인). ⚠️ 못 막는 것: 주입 지문이 아닌 오염(다른 브랜치 코드 혼입 등) — 그건 리뷰가 볼 일이다. 예외 `branch-scope-ok` |
| **cron 표현식 = CF 문법 + 계정 한도** | `wrangler*.toml` 의 모든 cron 이 CF 문법일 것(**day-of-week 는 1-7 또는 MON-SUN — 0 없음**) + 배열 비어 있지 않음 + 중복 없음 + **계정 전체 트리거 합 ≤ 5**(Workers Free) | `check-cron-syntax` (strict: verify.yml + audit-gate) · `cron-schedule.test`(8) | 2026-08-02 — 벽이 **두 개**였고 두 번째는 첫 번째를 고쳐야 보였다. ① 문법(code 10100): 주간 D1 백업이 `0 20 * * 0` 인데 CF 는 0 을 거부한다. 스케줄 등록이 **원자적 전체 교체**라 이 한 줄이 배열 전체를 무효화해 **백업 cron 이 등록된 적이 없었다**(하트비트 0 · R2 객체 0 · 재해복구 0). ② **계정 한도(code 10072)**: `SUN` 으로 고쳐 배포했더니 *"Workers Free limit of 5 cron triggers per account"* — 이 계정은 이미 정확히 5(ur-live 3 + cleanup-cron 1 + ads 1). 6번째를 넣자 PUT 이 거부되고 **그 뒤 모든 worker-deploy 가 이 단계에서 실패**해 cron 코드 배포가 전면 정지했다(실측). ⇒ 한 파일만 보면 못 잡는다 — `wrangler*.toml` 전부를 합산한다. ⚠️ 못 하는 것: 문법·개수가 맞아도 **CF 에 실제 등록됐는지**는 모른다 (유일한 답은 `worker-deploy` 로그의 `schedule:` 목록) · 배열에서 항목을 **빼는**(=삭제) 실수도 못 막는다. 예외 `cron-syntax-ok` |
| **레인이 예산을 우회하지 않는다**(유어애즈) | `scheduled()` 의 생 `ctx.waitUntil(await import(…))` 레인이 동결값보다 늘지 않을 것 | `check-ads-dispatch-bypass` (strict: verify.yml + audit-gate, 래칫 7건 동결) | 2026-08-02 — 레인을 띄우는 길이 둘인데 **성격이 정반대**다: `kick`=`SELF.fetch`=**자식 인보케이션**(자기 CPU 예산, 예산 분산 대상) ↔ 생 `waitUntil`=**부모가 직접 CPU 를 태움**(안 세고 못 미룬다). 즉 예산을 아무리 정교하게 나눠도 우회분이 조용히 새어 나간다. 이미 두 번 아팠다: `sheets-sync` 가 생 waitUntil 이라 **관측 밖**이었고(#882 이전, 3시간 멈춰도 침묵 경보 미발동) 바로 그 레인이 라이브에서 `Worker exceeded CPU time limit` 원문을 처음 보여줬다 · `daily-batch` 는 18:00 UTC(=`collect-commerce` 짝수시와 겹치는 무거운 회차)에 예산 밖에서 돈다. ⚠️ **인플루언서 전용이 아니다** — 레인 34개 중 **29개가 B2B**(업체/파트너풀/공공데이터)이고 08-02 01:00 KST 에 CPU 한도로 죽은 3개(`collect-commerce`·`collect-neis`·`collect-nps`)는 **전부 B2B** 였다. 두 도메인이 같은 부모 예산을 나눠 쓴다. ⚠️ 못 막는 것: 동결된 레인이 *무거워지는* 것(문자열로 판정 불가 — 하트비트의 최장성공↔최소실패 경계로만 보인다). 예외 `dispatch-bypass-ok` 주석(코드 위 줄 가능) |
| **유어애즈가 유어딜에 피해를 못 준다** | ① 유어애즈 코드가 유어딜 업무 테이블(orders·products·sellers·users·payments…)에 **쓰기 0** ② 유어딜 DB 에 남은 유어애즈 테이블 **36개 동결**(새로 늘리면 빨간불) ③ 유어딜 워커(ur-live) cron 에서 도는 유어애즈 작업 **1건 동결** | `check-ads-urdeal-isolation` (strict: verify.yml + audit-gate) | 2026-08-27 대표 지시 *"유어애즈 때문에 유어딜이 부정적인 영향을 받는 일이 없도록 해"* — **이 경계는 이미 한 번 무너졌다**: 유어애즈가 긁어 담은 리드가 유어딜의 주문·결제와 같은 D1 파일에 쌓여 494 MB / 한도 500 MB(**99%**)까지 갔고, 그 상태의 의미는 *"한도에 닿으면 유어애즈만 멈추는 게 아니라 **주문·결제 쓰기가 같이 죽는다** — 유어딜은 원인이 아니라 인질이었다"* 였다(`leads-db.ts` 헤더). 2026-08-19 에 리드 테이블 7개를 별도 DB로 옮겨 그 사고 자체는 막았지만 **경계를 지키는 장치는 없었다** — 다음 세션이 무심코 유어딜 DB 에 유어애즈 테이블을 하나 더 만들거나 유어애즈 작업을 유어딜 cron 에 얹어도 아무도 모른다. 실측 기준선(08-27): 업무테이블 쓰기 **0곳** · 유어딜 DB 안 유어애즈 테이블 34개/**565행**(무시 가능) · 유어딜 cron 의 유어애즈 작업 1건(`outreach-email-drain`, 실측 **0ms**·큐 0건). ⚠️ **못 막는 것**: 동결된 것이 *무거워지는* 것(테이블 행이 늘거나 cron 작업이 느려지는 것) — 문자열로는 판정 불가이고 D1 `rowsWritten` 과 `cron_hb:` 하트비트로만 보인다. 런타임 라우팅은 `ads-leads-db.test.ts` R1~R4 가 보는 짝이다. 예외 `ads-isolation-ok` 주석 |
| **가드가 실제로 실패할 수 있다** | 등록된 각 주입(알려진 결함)을 심으면 지정 테스트가 **반드시 빨간불**일 것 | `check-guard-mutations` (strict: verify.yml) | 2026-08-02 — 이 레포의 반복 사고는 "검사가 실패한다"가 아니라 **"검사가 실패할 수 없다"** 이고, 하루에 **세 번** 났다: ① `배열을 reverse 해도 같은가` — 15개 배열의 reverse 는 i→14−i 라 **홀짝이 보존**돼 2조 분할에 영향 0(정렬을 통째로 지워도 초록) ② `phase 이름이 바뀌면 조가 흔들린다` — 이웃이 `?` 앞에서 이미 갈려 **실제로는 안 일어나는 일**이었다 ③ `한 회차가 예산을 넘지 않는다` — 픽스처에 **일 1회 레인이 하나도 없어** `always` 가 늘 빈 배열(라이브에선 예산 8 에 12개가 떠 3개가 CPU 한도로 잘리는데도 초록). 셋 다 **손으로 주입해 보고서야** 알았고, 손으로 하면 다음 세션은 안 한다. → 주입 매니페스트를 두고 CI 가 대신 깨뜨린다. 두 실패 모드를 잡는다: **헛도는 가드**(결함을 심었는데 통과) · **낡은 지도**(`find` 문자열이 소스에 없음 = 코드가 옮겨갔다). ⚠️ 소스를 실제로 고쳤다 되돌리므로 병렬 스텝 금지. **새 가드를 만들면 여기에 한 줄 추가할 것** — 항목 없는 가드는 조용히 헛돌 수 있다 |
| **유어애즈 레인 격리** | ads cron 의 수집·스윕 레인은 `kick()`(SELF = 새 인보케이션 = 새 서브리퀘스트 예산) 경유일 것. 인라인 `ctx.waitUntil` 금지 | `check-ads-lane-isolation` (strict: verify.yml + audit-gate) | 2026-07-29 — `localdata`(인허가)·`nara`(조달업체)가 **6~9회 실행 내내 `total_saved: 0`**. 원인은 레인 코드가 아니라 **호출 방식**이었다: 정상 레인(storeinfo 12,549·commerce 105,733·company 10,506)은 전부 kick 경유, 0건 레인은 전부 인라인 — 한 인보케이션에 여러 레인이 얹혀 Cloudflare 서브리퀘스트 한도(무료 실효 ~50)를 다퉜다. 백필은 혼자 최대 192 fetch(2일×16업종×6페이지)를 **매시간** 쏟아부어 같은 인보케이션의 시트 미러까지 굶겼다. ⚠️ 라우트(`/__ads/collect-localdata` 등)는 **이미 있었고 cron 만 안 썼다** — 실패 양식은 "레인 추가 시 kick 을 빠뜨림". kick 경로 오타(→404 무음 실패)도 함께 검사. 예외는 `ads-lane-ok` 주석 |
| **예산 루프 부기 몫** | 서브리퀘스트 예산 루프는 `budget.left > 0` 까지 소진하지 않고 **루프 뒤 D1 쓰기 몫**(커서·학습상한·자기 스탬프)을 남길 것 | `check-budget-bookkeeping` (strict: verify.yml + audit-gate, 래칫) | 2026-07-30 — 하루에 **세 번** 만난 실패 양식. D1 도 서브리퀘스트라, 예산을 다 쓰면 루프 뒤 쓰기가 던지고 호출부의 `.catch(() => null)` 이 조용히 삼킨다. 하필 마지막이 **자기 스탬프**여서 *레인이 돌았는데 "안 돈 것"처럼 보인다*. 실측: 카카오 전화 스윕이 `left <= 2` 만 남기는데 뒤에 5회 → `last_run` 이 17시간 고착 · 통신판매는 `> 0` 까지 소진 → **하트비트는 남았는데(완주) 스탬프는 전날 것** = 우리가 기다리던 `diag.error` 원문이 매 회차 유실(원인 규명이 하루 늦어짐). ⚠️ **못 잡는 것**: 형태만 본다 — 예약 숫자가 실제 뒤따르는 쓰기 수보다 작아도 통과한다(그 정합은 레인별 단위 시험이 맡는다). 플랫폼 한도를 실제로 친 회차도 예약으로는 못 살린다. 예외 `zero-reserve-ok` 주석 |
| **시드 버전 단조증가** | 가이드/블로그 시드 버전 상수(`GUIDE_SEED_VERSION`·`BLOG_SEED_VERSION`)는 main 히스토리가 쓴 적 없는 더 큰 번호일 것 | `check-seed-version-monotonic` (strict: verify.yml + audit-gate + pre-commit) | 2026-07-29 — 시드 동기화는 "코드 버전 > DB 저장 버전"일 때만 돈다. **이미 쓴 번호를 다시 쓰면 조건이 거짓이라 에러 없이 아무 일도 안 일어난다** — 배포는 성공하고 라이브 문서만 옛날 것으로 남는다. 가정이 아니라 이 레포에서 **이미 두 번** 났다: `GUIDE_SEED_VERSION = 8` 이 2026-07-20 서로 다른 두 커밋에서 쓰였고(v11 주석이 그 수습을 기록 — "병행 배포 양쪽(각자 v8)이 모두 재시드되도록 9 로 합침"), `= 4` 도 두 번. 2026-07-29 에도 PR #451·#425 가 동시에 12 를 잡아 머지 직전에 손으로 잡았다. 세션이 여러 개 동시에 도는 한 계속 난다. ⚠️ 한계: 아직 머지 안 된 *다른 브랜치*의 번호는 볼 수 없다 — 나중에 머지하는 쪽이 CI 에서 걸려 +1 하면 된다. 예외 `seed-version-ok` |
| **규칙 버전 bump** | 규칙 파일을 의미 있게 고쳤으면 그 파일의 버전 상수도 올릴 것 | `check-rules-version-bump` (strict: verify.yml + audit-gate, classify 만 차단) | 2026-07-29 — 재처리 선택이 `COALESCE(v,0) < 상수` 라 상수를 안 올리면 이미 처리된 행에 새 규칙이 안 닿는다. classify 는 시간 폴백이 없어 **영구 누락**(2026-07-27 사고의 수습 메커니즘이 이것) · enrich 2종은 7일 폴백이 있어 warn. |
| **라우트 경로 중복 금지** | 같은 파일 안에서 동일한 `<Route path="...">` 가 2회 이상 등록되지 않을 것 | `check-duplicate-routes` (strict: verify.yml + audit-gate) | 2026-07-29 — `/influencer` 가 두 번 등록돼(`InfluencerDashboardPage` / `InfluencerLandingPage`) 먼저 선언된 대시보드가 항상 이겼고, **2026-05-15 에 만든 B2B 영입 랜딩이 두 달 넘게 한 번도 렌더되지 않았다.** 에러·경고·빌드실패 어느 것도 없다 — 라우터는 첫 번째를 고를 뿐이다. 대표 확정으로 랜딩이 `/influencer`, 대시보드는 `/influencer/dashboard` 로 분리. ⚠️ **못 잡는 것**: 경로가 *다른데* 한쪽이 다른 쪽을 그림자처럼 가리는 경우(`/a/:id` vs `/a/new`) — 문자열 비교로는 판정 불가이고, React Router v6 는 정적 세그먼트를 우선 랭킹하므로 대개 안전하다. 예외 `duplicate-route-ok` |
| **서버 라우트 중복 금지** | 같은 파일 안에서 같은 라우터·같은 메서드·같은 경로가 2회 이상 등록되지 않을 것 | `check-duplicate-hono-routes` (strict: verify.yml + audit-gate) | 2026-09-05 — `sellerOrdersRoutes.get('/products/:id')` 가 **두 번** 정의돼 있었다. Hono 는 먼저 등록된 쪽이 이기므로 뒤엣것은 등록된 날부터 한 번도 실행된 적이 없다 — 에러·경고·빌드실패 어느 것도 없다. 하필 그 죽은 쪽이 `productDetailColsHealed` 로 전 컬럼을 주는 '좋은' 구현이라, 다음 사람이 거기를 고치면 **아무 일도 안 일어난다**. 화면(App.tsx) 중복은 `check-duplicate-routes` 가, 순서 그림자는 `check-route-shadowing` 이 보는데 **정확히 겹치는 서버 라우트만 아무도 안 보고 있었다**(실측: 중복을 주입해도 shadowing 가드는 초록). ⚠️ 첫 구현이 `c.get('user')`·`formData.get('file')` 까지 잡아 38개 파일을 오탐했다 — 줄 맨 앞 앵커 + 라우터꼴 수신자 이름으로 교정. ⚠️ **못 잡는 것**: 파일이 갈린 중복 · 경로 문자열이 다른 그림자. 예외 `duplicate-hono-route-ok` |
| **라우트 그림자 금지 (API)** | Hono 라우터에서 정적 경로가 같은 모양의 `/:param` 경로보다 **먼저** 등록될 것 | `check-route-shadowing` (strict: verify.yml + audit-gate) | 2026-09-02 — Hono 는 **등록 순서**로 매칭한다. `/:id` 가 먼저면 그 뒤의 `/support-contact` 요청이 `id="support-contact"` 로 잡혀 핸들러의 숫자 검증에 걸려 **영원히 400/404** 가 난다. 에러가 나긴 나는데 **엉뚱한 에러**라 원인 추적이 어렵고, 라우트는 코드에 멀쩡히 있어 아무도 죽었다고 생각하지 않는다. 위의 중복 가드는 **같은 경로 문자열**만 보므로 이 그림자를 못 잡는다(그 행의 "못 잡는 것"이 정확히 이것이고, 이 가드가 그 자리를 메운다). 라이브 실측으로 **3곳이 죽어 있었다**: `GET /api/group-buy/stays/my-bookings` → 400 "Invalid productId" (**내 숙소 예약 화면이 로그인과 무관하게 항상 "불러오지 못했어요"** — 대표 스크린샷) · `GET /api/curator/recommendations` → 404 · `GET /api/seller/gb/support-contact` → 400(대표 콘솔 신고). ✅ **정규식 제약 파라미터는 그림자가 아니다** — `/sellers/:id{[0-9]+}` 는 `/sellers/unlinked` 를 안 가린다(라이브 200 실측). 오탐을 내면 가드가 꺼지므로 그 예외가 가드의 일부다. ⚠️ **못 잡는 것**: `app.route(prefix, sub)` 조립·문자열 연결 경로 — 같은 파일·같은 라우터 변수·리터럴 경로만 본다. 예외 `route-shadow-ok` |
| **도달 불가 라우트 금지(래칫)** | 정적 `<Route path="...">` 경로가 소스 어딘가에 문자열로 등장할 것 — 즉 **누를 데가 있을 것** | `check-orphan-routes` (strict: verify.yml + audit-gate, baseline `scripts/orphan-routes-baseline.json`) | 2026-08-01 — *"실패가 아니라 조용한 부재"* 의 세 번째 재발이라 클래스로 막았다. 라우트를 달면 빌드·타입·테스트가 전부 초록이라 **다 된 것처럼 보이는데**, 링크가 없으면 아무도 못 간다. 실측: 정적 라우트 **361개 중 21개**가 소스 어디에도 문자열로 없다 — `/account/settings`(사용자가 자기 테마·언어를 바꾸는 화면)가 **주석에만** 있었다. 판정은 **일부러 느슨**하다(`notifyUser(..., '/my-returns')` 같은 인자도 도달 경로다) — 엄격하게 하면 오탐이 62건까지 늘고, **오탐 많은 가드는 결국 아무도 안 켠다**(`check-input-text-color` 가 두 달간 미등록으로 방치된 이유). ⚠️ **앱 밖 진입은 고아가 맞다** — OAuth 콜백·PG 리턴 URL 은 외부 콘솔에 등록돼 소스에 링크가 없다. 그래서 삭제가 아니라 **래칫**이고 동결이 정답이다. ⚠️ **못 잡는 것**: 주석에만 적힌 경로도 "등장"으로 센다(위 `/account/settings` 가 그 경우라 가드가 아니라 사람이 찾았다). 예외 `orphan-route-ok` |
| **번들 gzip 예산 실측** | 총 gzip JS 를 **zlib 로 직접 계산**하고(디스크 `.gz` 사이드카 의존 금지), 측정값 0 은 통과가 아니라 위반 | `check-bundle-size --budget` (verify.yml) | 2026-07-29 — `.gz` 사이드카만 읽는데 vite 는 그 파일을 만들지 않아 **totalGzip 이 항상 0** → `0 > 1.5` 가 영원히 거짓 → 예산이 몇 달간 통과만 했다. 그동안 raw 예산 상향 5번의 근거로 "gzip 은 여유 있다"가 인용됐는데 **그 값이 죽어 있었다.** CI 실측 **2.707 MB** 로 교정(임계 2.9, 헤드룸 ~7%). ⚠️ 교정 전 추정치는 2.2~2.5MB 로 **틀렸다** — 임계값은 지어내지 말고 CI "Bundle size report" 로그에서 읽을 것 |
| **잠금표 심볼 실재** | `CLAUDE.md` 잠금표가 지목한 파일의 심볼이 실제로 그 파일에 존재할 것 | `check-lock-table-symbols` (strict: verify.yml + audit-gate) | 2026-07-29 — 잠금표는 "이 심볼을 건드리지 마라" 인데 코드가 리네임·삭제돼도 표는 남는다. 실측 2건: `linkUserExtraRoles`(→`issueLinkedRoleTokens` 리네임, 동작은 유지) · `MainHomePage`(홈이 `HomeRoute` 로 바뀌며 **참조 0인 죽은 파일**이 됐는데 표는 "eager import 유지" 를 요구 — 그대로 따르면 **죽은 컴포넌트를 되살린다**). CLAUDE.md 스스로 경고하는 "문서가 낡아 다음 세션이 옛 구조로 오판" 클래스. ⚠️ **한계**: 문자열 존재만 본다 — 심볼이 *주석에만* 남아도 통과한다(MainHomePage 가 정확히 그 경우라 이 가드는 못 잡았고 사람이 찾았다). 리네임·삭제는 잡고, "언급은 있는데 더는 안 살아 있음"은 못 잡는다 |
| **비공개 라우트 크롤 차단** | `ProtectedRoute` 경로는 robots.txt 에서 Disallow 될 것(의도적 예외는 스크립트의 `ALLOWED_GAPS` 에 사유와 함께) | `check-robots-private-routes` (strict: verify.yml + audit-gate) | 2026-07-29 — 실측 71건 미차단(셀러 대시보드 ~40개 포함). ⚠️ 한계: robots 매칭을 prefix + `$` + 최장일치 Allow 로만 근사(중간 `*` 미지원, 현재 미사용). "공개인데 막힘" 방향은 `check-sitemap-routes` 담당 |
| **sitemap URL 실재** | sitemap 의 정적 `loc` 이 ① 실재 라우트와 매칭 ② 리다이렉트 전용 아님 ③ robots.txt Disallow 와 무모순 | `check-sitemap-routes` (strict: verify.yml + audit-gate) | 2026-07-29 — 네 번째 재발(`/search` 07-28 + `/group-buy`·voucher 카테고리 6개·`/live/{id}`×100). ⚠️ 한계: 동적 URL 은 **경로 모양만** 검사(그 id 실재 여부는 런타임). catch-all(`*`)은 매칭 제외 — 포함 시 전건 통과로 무의미해짐 |
| **tsconfig 타입체크 무력화 차단** | tsconfig 에 `baseUrl`·`ignoreDeprecations` 없음 + `paths` 상대경로 + (CI) 검사 대상 파일 수 ≥ 500 | `check-tsconfig-resolution` (strict) + verify.yml 검사대상 수 스텝 | 2026-07-29 — `baseUrl` 이 TS 6 에서 에러로 승격돼 tsc 가 **파일 0개 검사 후 종료**했다. 짧은 출력이 성공과 구분되지 않아 "로컬 tsc 는 죽어 있다"는 인계가 여러 세션 이어졌다. ⚠️ `ignoreDeprecations` 는 고정 TS 5.5 가 거부 → CI 파손(버전 결합) |
| **AI 디자인 흔적(래칫)** | ① **평면 그라디언트 0** — `bg-gradient-to-* from-X to-X` 처럼 from/to 가 같은 색이면 마크업은 "그라디언트" 라고 선언하는데 **단색이 렌더된다** ② **UI 자리 이모지 증가 금지** — 아이콘 필드·칩/탭 라벨·제목/버튼 텍스트 | `check-design-slop` (strict: verify.yml + audit-gate · 래칫 `scripts/design-slop-baseline.json`) | 2026-08-30 — 대표 *"AI로 디자인을 한 티가 절대 없어야 해"*. 실측 **평면 그라디언트 85곳 / 48파일**(2026-06-19 흑백 리매핑에서 `from-pink-500 to-purple-500` 류가 통째로 붕괴한 잔재 — 브라우저는 그라디언트 계산을 계속 하고 코드는 거짓말을 한다) · **UI 이모지 101곳**(OS 마다 다른 그림이 나오고, 같은 줄의 lucide 선 아이콘과 언어가 갈리고, "임시로 채워 둔 것" 으로 읽힌다). 둘 다 **에러를 내지 않는다** — 화면만 조용히 밋밋해지고 배포는 초록불이라 문서로는 못 막는다. ⚠️ **일부러 안 막는 것**: 사람에게 보내는 메시지 본문(카톡 공유문·토스트)의 이모지 — 한국어 메신저 문화에서 자연스럽다 · 소스 주석 · 진짜 그라디언트(from/to 다름). ⚠️ **못 하는 것**: "이 화면이 세련됐나" 는 기계가 판단 못 한다 — 이건 바닥선일 뿐이다. 측정 대상 0건이면 통과가 아니라 실패(스캔 경로 노후 차단). 예외 `design-slop-ok` |
| **할인율은 사진 위에 없다** | 어떤 카드 구현이든 할인율(%)을 **절대배치 요소 안에** 렌더하지 않는다 — Tailwind `absolute` 와 인라인 `position:'absolute'` 를 **둘 다** 본다 | `check-discount-not-on-photo` (strict: verify.yml + audit-gate) | 2026-08-31 대표 *"할인율이 사진 안으로 들어가면 안돼. 할인율을 강조하기도 해야하고."* — 그 뒤 **같은 규칙을 세 번** 어겼다: `GroupBuyFeedCard`(08-31) · `vouchers/shared.tsx` 카드+행(09-01) · `group-buy/OtherDealsRow`(09-01, 상세의 "이 셀러의 다른 공구"). 기존 가드 둘은 **파일 이름을 박아 둔 계약 테스트**라 넷째 구현이 생기면 또 비껴간다. 게다가 `OtherDealsRow` 는 Tailwind 가 아니라 **인라인 스타일**이라 className 을 보는 검사로는 애초에 못 잡았다 — 그래서 규칙을 파일 무관·표기 무관으로 옮겼다. ⚠️ **못 잡는 것**: 배지를 별도 컴포넌트로 빼고 부모가 absolute 로 감싸는 경우(줄이 갈린다) · CSS 클래스/`::before` 로 위치를 잡는 경우 · "사진을 실제로 가리는가" 자체(렌더해서 눈으로). 합성 대조 3종(Tailwind 위반 · 인라인 위반 · 정상)을 매 실행마다 통과시킨다. 측정 대상 0건이면 통과가 아니라 실패. 예외 `discount-on-photo-ok` |
| **AI 디자인 흔적 — 문구·라벨(래칫)** | 대외·마케팅 표면에서 ① **em-dash `—` 0** ② **섹션번호 뱃지(`01`/`02`…) 0** ③ **한글 라벨에 `uppercase` 0** | `check-anti-slop-copy` (strict: verify.yml + audit-gate · 래칫 `scripts/anti-slop-baseline.json`) | 2026-09-01 — 대표 *"AI 스럽지 않은 디자인으로 모두 마무리"*. CLAUDE.md 의 anti-slop 절이 *"가장 자주 걸리는 것 다섯 개"* 를 적어 두고도 **강제하는 검사가 하나도 없었고**, 재 보니 대외 랜딩 `/about` 이 그 다섯 개를 **전부** 위반하고 있었다(em-dash 4 · `01~04` 섹션번호 · `ABOUT URDEAL` eyebrow · 가운뎃점 과밀 · **다섯 섹션이 전부 같은 카드 계열**). ③ 은 특히 조용한 결함이다 — `uppercase` 는 한글에 **아무 효과가 없어**(=죽은 스타일) 눈에 안 띄고, 남는 `tracking-wider` 만 자간을 벌려 가독성을 떨어뜨린다. 영문 편집 디자인의 small-caps eyebrow 를 그대로 옮겨 붙인 자국이며 실측 **24곳**이었다. ⚠️ **범위를 좁힌 이유**: em-dash 를 전 범위로 재면 401건(대부분 대시보드 토스트·aria-label)이고, 그 값을 래칫으로 삼으면 **랜딩에 새로 생긴 한 개가 401건 사이에 숨는다.** 스킬 자신이 dashboard 를 제외하므로 대외 표면으로 한정해 0 을 지킨다 — 새 대외 페이지를 만들면 `MARKETING` 목록에 추가할 것(안 하면 그 페이지는 무검사다). ⚠️ **`<input>` 의 `uppercase` 는 제외** — 입력 글자를 대문자로 바꾸는 **기능**이라 지우면 동작이 바뀐다(코드 `AG-A8K3F1` 입력창 2곳). ⚠️ **못 잡는 것**: eyebrow 예산 · 같은 레이아웃 계열 반복 · 가운뎃점 밀도 — 셋 다 "무엇이 한 섹션인가" 를 정적으로 못 정한다(렌더해서 눈으로). 기준 파일이 없으면 **통과가 아니라 실패**(없으면 첫 실행이 무조건 초록이라 검사가 무의미해진다). 합성 픽스처로 매 실행마다 자기검사. 예외 `anti-slop-ok` |
| **배송비 판정 SSOT (장바구니 ↔ 결제)** | 비배송(이용권·교환권) 판정이 `isNoShippingProduct`(`shared/product-flow`) **한 곳**을 거친다 + 구매 흐름 파일에서 `shipping_fee || <숫자>` 금지 | `check-no-shipping-ssot` (strict: verify.yml + audit-gate) | 2026-09-01 대표 *"이용권은 배송비도 없는데?"* — 프리뷰 하네스로 실제 재현했다: 같은 장바구니가 `/cart` 에선 **상품 64,900 + 배송비 6,000 = 70,900**, `/checkout` 에선 **64,900**(합계는 맞는데 상품 목록엔 3,000원 두 줄이 남아 화면이 스스로와 어긋났다). 원인 둘 — ① 같은 판정이 두 파일에 따로 적혀 갈라졌다(CheckoutPage 는 2026-06-22 에 "이용권 카테고리도 비배송" 으로 넓혔는데 CartPage 는 `deal_only===1` 에 머물렀다) ② `item.shipping_fee || 3000` 이 셀러가 명시한 **0** 을 3,000 으로 되돌렸다(`\|\|` 는 0 을 삼킨다). 게다가 서버(`cart.routes`)가 `p.category` 를 아예 안 보내고 있어 CheckoutPage 의 2026-06-22 수리도 절반은 죽어 있었다. 합계가 결제 직전에 줄어드는 화면은 깎아 준 것도 틀린 것도 아닌 **못 믿을 화면**이다. ⚠️ **못 잡는 것**: 서버 견적(`/api/orders/shipping-quote`)이 틀리는 경우(클라만 본다) · 배송비 **줄이 화면에 보이는지**(렌더해서 눈으로 — 하네스 `--cart`). 합성 대조 4종(직접 비교 · `\|\|` 기본값 · 정상 · **대상 파일 소실**)을 매 실행마다 통과시킨다. 예외 `no-shipping-ssot-ok` |
| **띄어 쓴 가운뎃점 사슬(래칫)** | 소비자 tsx(비주석)와 ko 로케일 소비자 키에서 한 줄에 ` · ` 가 둘 이상인 사슬(+`join(' · ')`)이 동결값을 넘지 않는다 | `check-middle-dot-chain` (strict: verify.yml + audit-gate) | 2026-09-01 디자인 방향 PR A. **붙여 쓴 가운뎃점(`맛집·카페·뷰티`)은 한국어 표준 표기라 막지 않는다** — 흔적은 영어 랜딩 습관인 **띄어 쓴 구·절 사슬**("즉시 발급 · 전 지점 사용 · 결제 즉시 사용")이다. 처음 세었을 땐 붙여 쓴 것까지 200줄이 잡혀 규칙 자체가 틀렸음을 알았다. 합성 대조 4종(사슬·join·붙여 쓴 점 오탐 없음·로케일 소비자 키만). 동결 파일 없으면 실패. 예외 `middle-dot-ok` |
| **모양 잠금(체계 밖 모서리 래칫)** | 소비자 tsx 에서 `rounded-3xl`·`rounded-t-3xl`·`rounded-sm`·임의 px 모서리가 동결값을 넘지 않는다 | `check-shape-lock` (strict: verify.yml + audit-gate) | 2026-09-01 디자인 방향 PR A. 마이페이지 한 화면에 반지름이 넷(24·12·pill·원형) 공존 — 컴포넌트를 각자 다른 데서 가져온 것처럼 보이는 흔적. 체계 = 카드 xl(12) · 컨트롤 full · 시트 t-2xl · 뱃지 md · 입력 lg. `rounded-2xl`(769건)은 이번엔 체계로 인정(12 로 모으는 건 페이지 단위 작업). 인라인 `borderRadius` 는 못 본다. 예외 `shape-lock-ok` |
| **대시보드 버튼 체계(래칫)** | 셀러/대시보드 표면의 버튼이 **자기 색·모서리·높이·굵기를 스스로 정하지 못한다** — `ur-btn ur-btn-{sm\|md\|lg} ur-btn-{primary\|secondary\|danger\|ghost}` 로만. 원시 주 버튼(`bg-gray-900/black/brand` + `text-white`) 수를 동결 | `check-dashboard-button-system` (strict: verify.yml + audit-gate · 래칫 `scripts/dashboard-button-baseline.json`) | 2026-08-31 — 대표 *"버튼 배치가 중구난방이고 체계적이지 않다"*. 실측: 주 버튼 **92개 · 77파일 · 모양 조합 48가지**(최다 패턴도 5번뿐 = **지배적 패턴이 아예 없다**), 주 행동 색 3갈래(`bg-gray-900` 127 · `bg-black` 19 · `bg-brand` 20), 모서리 5종. 즉 페이지마다 각자 정하고 있었다. 코드모드로 67개를 체계로 이행하고 나머지를 동결. ⚠️ **못 잡는 것**: 템플릿 리터럴(`${}`) 안의 조건부 className(문자열 리터럴만 본다) · `ur-btn` 위에 `!h-14` 같은 유틸로 체계를 덮어쓰는 경우 · *배치*(그건 `DashboardActions` 의 `primary` **단수 prop** 이 구조로 막는다 — 둘 이상을 주 행동으로 못 만든다). 측정 대상 0건이면 통과가 아니라 실패. 예외 `dashboard-button-ok` |
| **다크 대비(실제 렌더)** | 다크 모드에서 **밝은 표면 위 밝은 글자**(그리고 반대 방향)가 없을 것 — 17개 소비자 경로를 실제로 렌더해 글자색 vs 실제 뒤 배경색의 WCAG 대비를 재고 3.0 미만이면 신고 | `check-dark-contrast` (`dark-contrast.yml` 브라우저 워크플로 + audit-gate · 예외 `scripts/dark-contrast-baseline.json`) | 2026-09-03 — 대표 *"글자가 또 하얘 · 이런 경우 지금 많은 것 같은데 전수조사 필요해"*. 지도 검색창(`/map?q=부산`)의 입력 글자가 **흰 배경 위 흰색**이었다(실측 대비 **1.1:1**). 원인은 오타가 아니라 구조다 — 전역 `.dark input:not(...)`(특이도 **0,5,1**)이 `text-gray-900`(**0,1,0**)을 **언제나** 이긴다. 게다가 지도에 달린 `light-fixed` 는 **주석**이라 테마 가드를 면제할 뿐 런타임 효력이 0 이었다 ⇒ 가드는 초록인데 화면은 안 보이는 "조용한 부재". 그리고 이 사고는 **늘어날 수밖에 없는 구조**였다: 2026-09-02 에 지도·홈 패널을 "테마 무관 흰 면"으로 바꿨는데 전역 다크 규칙은 여전히 "앱이 다크면 표면도 어둡다"를 전제한다. 🔴 **grep 으로는 못 잡는다** — 같은 className 에 밝은/어두운 분기가 함께 있어 (`panel ? A : B`) 정적 검사가 실제로 **0건**을 냈다. 특이도 싸움의 승자는 브라우저만 아니까 렌더해서 잰다. 입력은 **값을 채워** 재고(빈 칸이면 placeholder 만 잰다), 시트/모달은 **열어서** 잰다. 양방향을 봐서 `SiteFooter` 저작권(`dark:text-gray-600`, 2.4:1)도 함께 잡았다. ⚠️ **측정 텍스트 200개 미만이면 통과가 아니라 실패**(렌더가 깨져도 findings 0 이면 초록 뜨는 헛도는 가드 방지). ⚠️ **못 보는 것**: 사진/그라디언트 위 글자(배경색 계산 불가) · 호버/포커스 상태 · 경로 목록 밖 화면(경로 목록이 곧 범위다). 장식(빈 별점·구분자)만 baseline `allow` 에 등록하고 **읽는 글자는 등록 금지** |
| **딜 카드 통일(래칫)** | 소비자 표면에서 딜/상품 카드는 **공유 카드 한 벌**(`GroupBuyFeedCard` 등)로만 그린다 — "사진 + 가격 + 이동 + 반복" 을 자기 마크업으로 그리는 파일을 신규 차단 | `check-deal-card-unify` (strict: verify.yml + audit-gate · 래칫 `scripts/deal-card-baseline.json`) | 2026-09-03 — 대표 *"이런 이용권들 디자인이 왜 통합적으로 관리가 안되는거지?"*. 통일을 **화면 단위**로 해 왔고, 그걸 지키던 검사(`urshop-card-unify.test.ts`)가 **"이미 고친 화면들의 목록"** 이라 아무도 안 본 화면은 영원히 안 걸렸다 — 홈 섹션↔피드(08-19) · 유어샵 이용권·핀(08-27) · 유어샵 내 상품(09-03) · 숙소 목록(09-03), **네 번 다 대표가 눈으로 찾았고** 검사는 그 뒤를 따라갔다. "검사가 실패할 수 없음" 의 변종 — 실패는 하는데 **볼 수 없는 곳**이 있었다. ⇒ 목록을 나열하지 말고 소비자 표면 **전체**를 훑는다. ⚠️ 두 번 좁게 짜서 두 번 놓쳤다(`.map` 블록 정규식 → JSX 중첩에 실패 / `.map` 뒤 창 → 카드를 지역 컴포넌트로 뺀 경우를 놓침) → **파일 단위**로 본다. ⚠️ **못 하는 것**: 카드가 예쁜지(이건 "한 벌인가"만 본다) · 공유 카드 위에 껍데기를 덧씌우는 경우. 운영자 몰(공구 서비스)은 별개 서비스라 allowlist. 측정 대상 200개 미만이면 통과가 아니라 실패. 예외 `deal-card-ok` |
| **상태 색(래칫)** | 상태 라벨 표(`{label|t, cls|c|color|bg}`)에서 상태를 **중화되는 색조**(`rose/emerald/amber/blue/…`)로 구분하지 않는다 — `tone-*`(index.css `--tone-*`) 또는 `red`(중화 제외)만 | `check-status-tone` (strict: verify.yml + audit-gate · 래칫 `scripts/status-tone-baseline.json`) | 2026-09-03 — 대시보드 상태 배지가 **전부 같은 회색**이었다. `tailwind.config.js` 가 2026-06-19 대표 지시("아예 흑백, 기능 빨강만 유지")로 장식 색조를 잉크 스케일로 중화하는데, 상태 배지가 하필 그 색조로 상태를 구분하고 있었다. 라이브 CSS 실측: `.bg-rose-50` == `.bg-emerald-50` == `rgb(248 247 252)` · `.text-rose-700` == `.text-emerald-700` == `rgb(61 60 58)` ⇒ **반려와 승인이 픽셀 단위로 같았다.** 에러도 안 나고 "색이 있는 것처럼" 보여서 몇 달간 아무도 몰랐다 — 이 레포가 반복해 겪은 **조용한 부재**. 대표 원칙(기능 신호는 색 유지)은 지키되 그 신호를 중화되는 색조로 표현하지 않는다. ⚠️ **못 하는 것**: 상태표가 아닌 곳의 장식색(흑백이 의도다) · tone 을 **잘못** 고른 경우(반려에 ok) · 표 형태가 아니라 인라인으로 흩어진 배지. 양성/음성 대조 내장(매칭이 죽으면 0건 초록 대신 실패). 예외 `status-tone-ok` |
| **이미지 폴백(래칫)** | `cfImage()` 로 감싼 `<img>` 에 `onError` 가 있을 것 — SSOT `cfImageOnError(el, 원본URL)` 가 [원본 1회 재시도 → 그래도 죽으면 숨김(부모 배경 노출)] 로 처리한다. 무방비 `<img>` 수를 동결 | `check-image-fallback` (strict: verify.yml + audit-gate · 래칫 `scripts/image-fallback-baseline.json`) | 2026-08-31 — 대표 *"유어샵 나머지"*. 실측: cfImage 를 쓰는 `<img>` **92개 중 47개가 무방비**였다. 리사이저가 원본을 못 가져오거나(핫링크 차단·외부 CDN 5xx) 원본이 404 면 그 자리에 **깨진 이미지 아이콘**이 그대로 뜬다 — 에러 로그도 배포 실패도 없어서 아무도 모른다. 절반이 빠진 건 취향이 아니라 **배선 누락**이고, 배선은 눈에 안 보여 계속 새로 빠진다. 소비자·셀러 29곳을 먼저 배선하고(#1275), 이어서 도매·어드민·몰 18곳까지 배선해 **baseline 0** — 지금부터 무방비 <img> 는 한 개도 못 들어온다. ⚠️ 0 을 기대하는 검사는 매칭이 죽어도 0 이라 초록불이므로 **합성 대조**(일부러 위반인 조각 + 정상 조각)를 매 실행마다 통과시킨다. ⚠️ **못 잡는 것**: CSS `background-image`(onError 개념이 없다) · 아무것도 안 하는 빈 핸들러 · cfImage 를 안 거치는 raw `<img>`(그건 `check-consumer-img-cfimage` 의 몫). 측정 대상 0건이면 통과가 아니라 실패. 예외 `image-fallback-ok` |
| **가드 레지스트리(안 도는 가드 차단)** | ① `scripts/check-*.{mjs,sh}` 는 전부 어딘가에서 **실제로 실행**될 것(audit-gate/워크플로/훅, 또는 러너가 호출하는 npm 스크립트) ② 가드가 코드에서 지목한 **고정 파일 경로는 존재**할 것 | `check-guard-registry` (strict: verify.yml + audit-gate) | 2026-07-29 — 이 레포의 반복 사고는 "검사 실패"가 아니라 **"검사가 안 돎"**이다. 같은 날 실측 3건: ⓐ 번들 gzip 예산이 `.gz` 사이드카를 읽는데 vite 는 안 만들어 **측정값 항상 0 → 영원히 통과** ⓑ `check-input-text-color` 가 `dark:text-white` 를 오탐해 켤 수 없어 **어디에도 미등록**(파일은 있으니 보호받는 것처럼 보임) ⓒ `check-linkshop-ownership` 이 대상 파일 부재를 조용히 `continue`. ⚠️ **못 막는 것**: 등록됐는데 판정 로직이 틀려 늘 통과하는 경우(ⓐ) — 그건 각 가드가 "측정 0건이면 실패"를 스스로 선언해야 한다. 예외 `guard-registry-ok` |
| **input 라이트 가시성(흰 글자)** | input/textarea 의 **base 토큰**에 `text-white` 와 `bg-white` 가 동시에 있으면 안 됨(라이트 모드에서 글자 안 보임) | `check-input-text-color` (strict: verify.yml + audit-gate) | 2026-05-19 작성 → **2026-07-29 수리 후 최초 등록**. `\btext-white\b` 가 `dark:text-white` 안에서도 매치돼 CLAUDE.md 가 요구하는 정상 패턴(`bg-white … text-gray-900 dark:text-white`)을 위반으로 신고했다(실측 6건 전부 오탐). 켜면 정상 코드가 빨간불이라 아무도 못 켰다 → variant-aware 로 교정. `check-light-input-guard`(전역 `.dark input` 규칙 대응)와 **다른 층**: 이쪽은 element 자기 className |
| **Firebase 토큰 인증 수용 금지** | `requireAuth`/`optionalAuth`/토큰발급 엔드포인트가 Firebase ID token 을 인증 수단으로 받지 않을 것 + `googleRoutes` 미마운트 | `check-no-firebase-auth` (strict: verify.yml + audit-gate) | 2026-07-28 — Firebase 서비스계정 개인키가 `archive/` 문서에 3개월 public 노출(#798). 그 키로 [커스텀 토큰 발급 → Firebase 공개 REST 로 ID 토큰 교환 → Bearer 제출] 하면 **임의 uid 로 로그인**이 됐다(서명은 Google 공개키로 정상 검증되므로 통과). **키를 폐기해도 수용 경로가 남으면 새 키 유출 시 재발**하므로 문 자체를 닫음. KR=카카오 세션 전용·셀러/어드민=JWT·GLOBAL 미런칭(#804)이라 실사용자 0(대표 확인). 되살리려면 `firebase-auth-ok` 주석 + 새 키 발급 + 본 표 갱신 |
| **시크릿 자재 유입 차단** | 추적 파일 어디에도 실제 키 본문(PEM 개인키·Toss live·Stripe·AWS·PAT 등)이 없을 것 — 확장자/경로 무관 | `check-secret-material` (strict: verify.yml + audit-gate + pre-commit) | 2026-07-28 — `archive/` 19개 `.md`/`.txt` 에 Firebase 서비스계정 개인키·Toss live·Stripe 시크릿이 **추적된 채** 3월부터 public 노출. 기존 두 가드가 모두 통과시킴(`verify.yml` 의 검사는 `src/**` 의 `.ts/.tsx` 만, `check-no-secrets.sh` 는 키 이름 패턴 위주). ⚠️ 이 가드는 **작업트리만** 본다 — history 유출은 스캔이 아니라 **회전**으로만 해결된다 |
| **유어애즈 레인 관측·격리** | ① 시각 게이트 레인은 `makeHourGates` 경유(발화 조건과 **주기 신고**가 한 자리 — 매시간으로 잘못 신고해 정상 레인이 매일 `stale` 로 경보되는 것 0) ② 부모 cron 인보케이션에서 `for(…) await env.SELF.fetch(…)` 라운드 루프 0 ③ 드라이버 핸들러가 라운드를 다 돌고 응답하지 않을 것(부모의 `kick` 이 await 하므로 **부모 수명이 라운드에 묶인다** — 느린 레인이 하트비트도 없이 굶는다) ④ 레인 등록(`laneReg`)이 `recordKnownLanes` 보다 먼저 끝날 것 ⑤ `deploy-ads.yml` `paths` 가 worker-ads 의 직접 import 최상위 경로를 전부 덮을 것(안 덮으면 **배포가 안 돌아 ur-ads 가 조용히 낡은 코드로 계속 돈다**) | `ads-lane-cadence.test.ts`(유닛 29) · `check-ads-lane-isolation` | 2026-07-29 — 라이브 실측에서 전부 **현재형 결함**이었다: `maintenance?phase=quality` 가 정상인데 age 167분·stale / 07:00 틱이 8개 레인에서 **조용히 절단**(느린 레인만 전멸, 실패기록 0) / #835 의 드라이버 격리가 *서브리퀘스트만* 옮기고 수명은 안 옮겨 효과 0 / `src/worker/**`·`social-media`·`supply` 가 배포 트리거에서 누락. ⚠️ 못 막는 것: 시각/N **값의 정합**, **단발** 느린 레인(드라이버 아님), **전이** 의존 경로 |
| **문서 SSOT 동기화** | 서비스 **구조**가 바뀌면 플랫폼 모델 문서가 같은 커밋에서 갱신될 것 · 소비자 블로그 시드에 폐기 명칭/중단 기능/도매 내용 재유입 0 · 블로그의 수치 사실(수수료 5%·원천징수 3.3% 등)이 코드 SSOT 와 어긋나지 않을 것 | `check-platform-model-sync` · `check-blog-seed-currency` · `check-blog-fact-sync` | (상시 가드) — 문서가 낡으면 다음 세션이 옛 구조로 오판한다. 상세는 CLAUDE.md 해당 절 |
| **i18n 6개 언어 동기화** | 번역 키가 ko/en/ja/zh/es/fr 6개 로케일에 모두 존재할 것 | `check-i18n-sync` | 2026-07-29 등록 — 파일은 예전부터 있었으나 `package.json` 에 **정의만** 돼 있고 아무도 호출하지 않았다(`check-guard-registry` 가 그 오통과를 잡아 등록) |
| **소비자 이미지 cfImage 경유** | 소비자 표면의 이미지가 raw `<img src>` 로 외부 원본을 직접 물지 않을 것(엣지 리사이즈 우회 → LCP·트래픽 회귀) | `check-consumer-img-cfimage` | (상시 가드) — cf-image 잠금(CLAUDE.md 로딩 잠금)의 런타임 짝 |
| **per-request DDL 방지** | `ensureXxx(DB)` 메모이즈가 **WeakSet + 객체 키**일 것(primitive 키는 WeakSet 에 못 들어가 매 요청 DDL 이 돈다) | `check-weakset-primitive` | (상시 가드) |
| **부기 비용 예산 차감** | 유어애즈 스키마/부기 쿼리도 서브리퀘스트 예산에서 차감될 것(공짜로 세면 실제 천장을 넘긴다) | `check-schema-cost-counted` | 2026-07-29 (수집량 계측 정정 #851) |
| **구 도메인 사용자 노출** | 정본은 `urdeal.kr`(2026-07-20 이전 완료) — 사용자에게 보이는 표면(`src/pages`·`components`·`features`·`shared`·`public/locales`)에 `live.ur-team.com` 0. 이전을 *구현하는* 코드(워커 호스트 집합·전환기 CORS)와 테스트는 면제 | `check-legacy-domain` (strict: verify.yml + audit-gate) | 2026-07-29 — 구 도메인은 **전 경로 301** 이라 링크가 도착하고 **에러가 안 난다**. 그래서 결제 완료 넛지가 소비자에게 *"이미 `live.ur-team.com/u/{handle}` 링크샵이 준비돼 있어요"* 를 9일간 보여주고 있었다(2026-07-03 배선 당시 문자열). ⚠️ 검사 대상 파일이 0개면 **통과가 아니라 실패** |
| **감사 레지스트리 동기화** | 이 문서가 실제 `audit-gate.sh` 와 어긋나지 않을 것 — ① 게이트가 돌리는 모든 가드가 표에 이름으로 존재 ② 한 줄 점검의 "전체 (N개)" 가 실제 개수와 일치 | `check-audit-registry-sync` (strict: verify.yml + audit-gate) | 2026-07-29 — 실측에서 문서는 **49개**라 적고 게이트는 **72개**를 돌리고 있었고, 표에서 아예 빠진 가드가 **8개**였다. 이 문서는 장식이 아니라 *"무엇을 손으로 재감사하지 않아도 되는가"* 의 목록이라, 빠진 가드는 **이미 기계가 지키는 영역을 사람이 또 파게** 만든다. ⚠️ **못 보는 것**: 설명이 맞는지는 안 본다(이름만 있고 내용이 옛것이어도 통과) · verify.yml 전용 검사는 대상 아님 |
| **크리티컬 청크 구성 동결** | `index.html` 의 entry `<script>`+`modulepreload` 에 등장하는 **청크 이름 집합**을 `scripts/critical-chunks-baseline.json`(현재 17개)에 동결 — **새 이름 진입 = 위반**(줄어드는 건 개선이라 통과, `--rebaseline` 안내). 측정 0건(=index.html 파싱 실패)도 위반 | `check-critical-chunks` (strict: verify.yml build 직후 + audit-gate) | 2026-07-29 — 총합 예산(`check-bundle-size`)은 **후행** 감지기다: 임계값을 넘어야 울리므로 "청크 하나가 통째로 크리티컬에 새로 들어왔다" 는 **구조 변화**를 여유가 있는 동안 놓치고, 나중에 **무관한 PR 이 총합에 걸려 막힌다.** 이 레포에서 세 번 재발했다(2026-05-24 −248KB · 05-27 −305KB 로 두 번 덜어냈는데도 07-29 에 app-components 가 다시 20% — 76 모듈 중 엔트리 eager 는 14 개뿐, 62 개 280KB 가 얹혀 감). 총합이 아니라 **구성**을 봐서 무엇이 새로 들어왔는지 바이트가 아니라 **이름으로** 즉시 알려준다. ⚠️ **못 잡는 것**: 이미 크리티컬인 청크가 이름 그대로 **내부에서 커지는 것** — 그건 총합 예산의 몫이다(둘은 짝). ⚠️ 빌드 산출물이 없으면 명시적 SKIP 후 exit 0(상주 지점은 verify.yml 의 build 직후) |
| **소비자 표면 역할청크 누수** | 워커가 표면별로 주입하는 `route-chunk-map`(2026-07-12 하드로드 청크 병렬화)의 소비자 7표면 preload 목록에 **역할 전용 청크**(`app-seller-components`/`admin`/`agency`/`wholesale`/`dashboard`/`marketing`/`live`/`streaming`)가 등장하면 위반. 검사 대상 0건(표면 키가 바뀌어 아무것도 안 봄)·맵 파싱 실패·빌드했는데 맵이 빈 것도 전부 위반 | `check-surface-role-leak` (strict: verify.yml build 직후 + audit-gate) | 2026-08-27 — 위 `check-critical-chunks` 는 **`index.html` 만** 본다. 그런데 홈의 실제 첫 페인트 바이트는 index.html **＋ 워커 주입 맵**이고, 후자는 **어느 가드도 안 보고 있었다.** 그 사각지대로 실측 3건이 조용히 나가고 있었다: 홈 → `app-seller-components` **65KB**(원인: 공용 `RoleGate.tsx` → `shared/seller-roles.ts` 가 셀러 봉투에 묶임) · 홈 → `app-wholesale-hooks`+admin/wholesale/marketing 컴포넌트 · `/browse` → 또 65KB(원인: `lib/seller-tracking.ts` — **이름만 셀러**이고 실제론 소비자 4개 페이지가 쓰는 70줄 유틸). 전부 에러 없음·화면 정상·바이트만 샘. ⚠️ **못 잡는 것**: 역할 전용이 아닌 청크가 이름 그대로 커지는 것(총합 예산의 몫) · `MAX_LINKS` 캡에 잘려 맵에 안 실린 청크(브라우저는 결국 받는다) |
| **가드 자기검증(측정 0 = 실패)** | 가드 스크립트는 **자기가 몇 개를 쟀는지 확인하고 0이면 실패**할 것 — 대상이 비면 위반도 0이라 초록이 뜨는데 그 초록은 아무것도 보장하지 않는다. 래칫(`guard-selfcheck-baseline.json`, 현재 44건 동결): **새 가드와 목록에서 빠진 가드만** 요구, 기존 가드에 선언을 넣었으면 `--rebaseline` 로 동결을 줄인다 | `check-guard-selfcheck` (래칫, strict: verify.yml + audit-gate) | 2026-07-29 — 하루에 실측 3건: ⓐ `check-bundle-size` 가 vite 가 만들지 않는 `.gz` 사이드카를 읽어 **측정값 항상 0** → 예산이 몇 달간 통과만 했고 그 죽은 값이 raw 상향 5번의 근거로 인용됐다 ⓑⓒ 도매 어드민 nav/api 가드가 네비 정의 이사(2026-07-22) 후 **허용 경로 0개 · 스캔 화면 0개**로 한 달 넘게 초록. `check-guard-registry`(등록 여부)의 **짝** — 저건 "안 도는 가드", 이건 "돌지만 아무것도 안 재는 가드". ⚠️ **못 보는 것**: 선언이 *맞는지*는 안 본다(`x === 0` 의 `x` 가 엉뚱한 값이어도 통과) · 셸 가드(`.sh`)는 형태가 제각각이라 제외. 예외 `zero-scan-ok` 주석 + 이유 |
| **공공데이터 자리표시자 판정** | 공공데이터 API 가 값 대신 돌려주는 자리표시자(`N/A` 등)를 실값으로 저장하지 않을 것 | `check-public-data-sentinel` (strict: verify.yml + audit-gate) | 2026-07-29 (세션② #860 — 수집 4대 구멍). 본 표에는 2026-07-29 레지스트리 동기화 가드가 **누락을 잡아** 등재됨 |
| **동시 세션 겹침 조기경보** | 브랜치가 고친 파일을 그 사이 `origin/main` 이 바꿨으면 경고 — 단순 '뒤처짐'이 아니라 **실제 겹침(파일 교집합)** 만 본다(매번 우는 경고는 우회를 습관으로 만든다). 인계 문서는 세션별 파일이라 제외 | `check-branch-overlap` | 2026-07-29 — 하루에 **3건 중복 개발**(#840 드라이버 격리 · #857 보강 처리량 · CLASSIFY bump)과 한 브랜치의 **3회 재병합**(`src/worker-ads/index.ts`). main 인계는 *"착수 전 origin/main 을 보라"* 를 **룰로만** 적어 뒀다. 히스토리 재현으로 검증 — 실제 충돌했던 두 파일(`enrich.routes.ts`·`index.ts`)을 정확히 지목. ⚠️ 못 막는 것: **아직 머지 안 된** 다른 브랜치의 작업(main 에 없으므로 안 보인다) · 같은 문제를 *다른 파일*에서 고치는 중복 |

> "마지막 수동 전수감사 = 2026-06-26" 인 도메인은 그날 5개 병렬 에이전트 + 코드 재검증으로 전수조사 완료.
> 그 결과를 위 가드로 박았으므로, **다음 세션은 가드 GREEN 이면 그 도메인을 다시 전수조사하지 말 것.**

## 🔍 가드 미보유 (= 아직 수동 감사 대상)

가드로 박기 어려운(런타임·문맥 의존) 잔여 영역. 새 작업이 이걸 건드리면 수동 확인.

| 영역 | 왜 가드 어려움 | 대안 |
|---|---|---|
| 결제 환불/취소 **금액 정확성** (셀프취소 ↔ refundOrderFully 대칭) | 머니 *흐름*은 정적패턴으로 안 잡힘 | `check-money-patterns`(패턴만) + staging 실결제 1회 (CLAUDE.md 룰) |
| 신규/빈/로그아웃 **런타임 크래시** | 훅순서/null deref 는 런타임 | 2026-06-26 AST 분석으로 clean 확인. 새 페이지는 `?? []`·`formatNumber`·early-return前 훅 규칙 준수. (pagination NaN 크래시 클래스는 이제 `check-pagination-nan` 로 가드 — 위 표) |
| 외부 PG/카카오/Toss **실응답** | 외부 의존 | staging smoke + 진단 엔드포인트 |

## 📋 2026-06-26 전수감사 결과 (5도메인 병렬)

| 도메인 | 결과 | 조치 |
|---|---|---|
| 인증·RBAC·리다이렉트·IDOR | ✅ clean | 가드 GREEN → 스킵 등록 |
| 신규/빈/로그아웃 크래시 | ✅ clean (AST 검증) | 구조적 방어 확인 → 스킵 등록 |
| 서비스 분리 | 🔴 단건 ID 누수 1건 | **fix + 신규 가드** `check-consumer-product-supply-isolation` |
| 정산·지급 | 🟡 low 2건 | 인플 원천징수 하드코딩 → SSOT fix / 제조사 출금가능 표시 → TECHNICAL_DEBT |
| 결제 셀프취소 | 🔴 3건 (쇼핑 숨김 gated, latent) | TECHNICAL_DEBT 등록 + 쇼핑 재오픈 전 fix (staging 검증 필수) |

## 규칙 (이 레지스트리 유지보수)

1. 새 불변식 가드를 만들면 → `audit-gate.sh` 에 추가 + 이 표에 등록.
2. 수동 전수감사를 한 도메인은 → "마지막 수동 전수감사" 날짜 갱신.
3. 가드가 못 잡는 새 사고가 나면 → 가드부터 만들고(애초에 없도록) 이 표 + `audit-gate.sh` 갱신.
4. 미래 세션 감사 요청 시: **먼저 `audit-gate.sh` 실행 → GREEN 도메인 스킵, RED·미보유 영역만 작업.**

### 🎛️ 처리량 노브 요금제 커버리지 (`check-plan-knob-coverage.mjs`)

**왜**: 2026-08-02 하루에 **같은 결함을 세 번** 만났다 — 플랫폼 천장 → 보강 벽시계 → DO 알람·레인 예산.
전부 *"이 상수가 요금제를 모른다"* 였고 전부 사람이 발견해서 고쳤다. 발견에 의존하면 다음 노브도 놓치고,
놓치면 **유료로 바꿔도 그 축은 안 오른다**(에러가 없어 아무도 모른다).

**강제 2가지**
- **R1** `src/worker-ads`·`src/features/marketing/api` 의 숫자 `ADS_*` 노브는 전부 `plan-knobs.ts` 등기부에 분류돼 있어야 한다.
- **R2** `cf` 로 분류한 노브는 요금제 인지 리졸버를 거쳐야 한다(raw `parseInt(env.X…) || N` 금지).

**🔴 전부 `cf` 로 만들면 안 된다** — `external`(YouTube 유닛·카카오/네이버 일 쿼터)을 요금제에 묶으면
**유료 전환이 곧 장애**가 된다: Workers 예산은 늘었는데 그쪽이 403 을 주고 그 레인은 그날 내내 죽는다.

⚠️ **못 잡는 것**: 분류가 *틀린* 경우(외부 쿼터를 `cf` 로 적음) — 문자열로 판정 불가. 등기부의 `why` 를 사람이 읽어야 한다.
⚠️ 첫 판은 **R1 이 헛돌았다**: raw `parseInt` 만 봐서 리졸버로 배선하는 순간 스캐너 눈에서 사라졌다
(등기부에서 지워도 통과). 리졸버 경유 형태도 함께 스캔하도록 고쳐 red 확인.

---

## 💳 결제수단 판정 SSOT (`check-payment-flow-ssot.mjs`) — 2026-08-03

**불변식**: 소비자 표면이 결제수단을 **카테고리로 정하지 않는다.** 판정은
`src/shared/product-flow.ts` `getProductFlow()` — `deal_only=1` → 딜(**교환권**),
`group_buy_status='active'` → 카드(**이용권**·공구), 그 외 → 카드.

**왜**: 세션이 낡은 주석(*교환권을 voucher 카테고리와 등치*)을 믿고 대표에게
**"이용권은 카드로 못 산다"** 고 보고했다. `meal_voucher` 인 김밥천국 할인권은 **카드 결제**이고
SSOT 주석이 바로 그 예를 든다. 그 오판으로 *"카드로 살 수 있는 상품이 없으니 새로 만들어야 한다"* 는
잘못된 절차까지 갈 뻔했다(실제로는 라이브에 3개 있었다).

⚠️ **못 잡는 것**: 서버가 런타임에 실제로 무엇을 태우는지. 그건 실결제로만 안다.
⚠️ **첫 판의 R1 이 헛돌았다**: `payment_method: 'deal'` **리터럴**만 봐서, 정작 잡아야 할
`payment_method: isVoucherCategory(c) ? 'deal' : 'toss'` 를 통과시켰다(주입 검증에서 초록이 떠 발견).
지금은 `payment_method` 대입 지점에서 위로 훑어 **먼저 만나는 근거**(SSOT vs 카테고리)로 판정한다.

## 🚦 기능 현황판 동기 (`generate-feature-status.mjs --check`) — 2026-08-03

**불변식**: `docs/FEATURE_STATUS.md` 가 `src/shared/feature-flags.ts` 와 일치한다(자동 생성).

**왜**: 이 레포엔 **종료됐는데 코드가 온전히 남은 기능**이 9개다. 라우트·페이지·API 가 다 살아 있고
꺼진 사실은 플래그 한 줄로만 표현된다 ⇒ **파일을 열어보는 것으로는 판단할 수 없다.**
세션이 이미 종료된 **딜 충전**(2026-07-18)을 대표에게 실결제 절차로 제안했다.

⚠️ **못 잡는 것**: 서버측 게이트(`platform_settings`·env)와 데이터로만 꺼진 것(공구 세션·추첨 설정).
표는 **표면 노출**만 말한다 — 🟢 라고 경로가 완주된다는 뜻이 아니다.

---

## 🧪 가드 주입 검증 (헛도는 가드) — `check-guard-mutations.mjs -s`

**보장**: 매니페스트의 각 결함 주입이 실제로 **빨간불을 낸다**. 두 모드를 잡는다 —
**헛도는 가드**(결함을 심었는데 통과) · **낡은 지도**(`find` 가 소스에 없음 = 코드가 이사했는데 항목이 안 따라옴).

**왜 여기 들어왔나 (2026-08-04)**: 이 게이트에 **그 검사가 통째로 빠져 있었다.**
같은 날 이 세션이 티스토리 코드를 옮기고 `audit-gate.sh` 로 *"ALL GREEN 87"* 을 확인한 뒤 커밋했는데,
CI 는 **낡은 지도 2건**으로 빨간불이었다. 게이트가 **안 보는 검사**를 근거로 "전부 통과"라고 보고한 셈이다.
⚠️ 그리고 `-s` 없이 부르면 실패해도 **exit 0(warn)** 이라, 스크립트를 직접 돌려도 놓치기 쉽다 —
이 줄에 `-s` 가 붙어 있는 이유다.

⚠️ **느리다**(주입마다 vitest 1회 = 수 분). 그래도 게이트의 값은 *"안 보는 곳이 없다"* 는 것이므로 감수한다.
🔑 **새 가드를 만들면 매니페스트에 한 줄 추가할 것.** 코드를 옮겼으면 `find` 앵커도 같이 옮긴다.
