# 한 달 넘게 막혀 있던 PR 셋(#1149 · #1269 · #1295)을 어떻게 할까?

상태: open
등급: C
역할: dev
올린 날: 2026-09-15
기한: 2026-09-22

## 질문

세 PR 은 전부 `mergeable_state: dirty`(main 과 충돌)라 **CI 가 한 번도 돌지 않은 채** 열려 있습니다.
각각 **되살릴지 / 닫을지**를 정해 주세요. (셋 다 전제는 현재 main 에서 여전히 성립합니다 — 아래 실측.)

## 근거 (실측 — 2026-09-15 현재 main)

> 🩸 **첫 판에서 "9건 전부 판정" 이라고 썼는데 틀렸습니다.** 열린 PR 은 **10건**이었고
> **#1001 · #1108 · #1109 를 빠뜨렸습니다**(목록을 다시 안 뽑고 대화 기록에서 셌습니다).
> 셋 다 실측으로 판정해 닫았습니다 — 아래 §추가 3건.

### 되살린 것 두 건은 이미 처리했습니다
- **#1455**(검사기 구멍) · **#1123**(몰 운영자 콘솔) → **#1458** 로 옮겨 CI 에 올렸습니다.
  옮기는 과정에서 #1123 의 IDOR 가드가 **주석 덕에 통과**하고 있던 것을 잡았습니다.
- **#1160 · #1297 · #1259** → 전제가 깨졌거나 main 에 이미 반영돼 있어 **닫았습니다**.
- **#1444**(대외 소개서) → 머지 완료(`64ef0fff`).

### 남은 셋

| PR | 규모 | 머니 | 전제 재확인 | 충돌 |
|---|---|---|---|---|
| **#1149** 공구 서비스 분리 | 97파일 · +3,541 · 19커밋 | 없음 | ✅ 전부 성립 | 24파일 |
| **#1269** 현금 정산 수수료 | 16파일 | 💰 **있음** | — | dirty |
| **#1295** 후기 보너스 매장 부담 | 11파일 | 💰 **있음**(게이트 기본 OFF) | — | dirty |

**#1149 전제 재확인**(전부 `0건` = 아직 안 고쳐짐):
`MallOriginBanner` · `ContinueShoppingLink` · `stampOrdersMall` · `MyMallAddress` ·
`/mall/apply` · `/api/mall/of-order` · `path="/pass"` 전부 부재.
`CartPage`·`CheckoutPage`·`PaymentSuccessPage` 의 `mall` 언급 **각 0건**.
⇒ *"손님이 담는 순간부터 전부 유어딜"* 이라는 그 PR 의 진단은 **오늘도 사실**입니다.

**⚠️ 다만 한 달 사이 표적이 두 배가 됐습니다.** #1149 의 ⑫는 `/group-buy/:id` → `/pass/:id`
경로 이전인데:
```
2026-08-12 그 PR 이 다룬 링크   49곳 / 34파일
2026-09-15 현재 main 의 링크    101곳 / 60파일   ← 2배
```
그대로 머지하면 **절반이 옛 주소로 남습니다.** 옛 라우트를 살려 두는 설계라 화면이 깨지진 않지만,
"이전했다"고 말할 수 없는 상태가 되고 다음 세션이 그 표를 믿습니다.

### 추가 3건 (빠뜨렸다가 마저 판정 — 전부 **닫음**)

셋 다 브랜치가 5~6주 뒤처져 **머지가 곧 삭제**가 됩니다:
```
#1001  1,830 files  −117,498    #1108  2,772 files  −267,103    #1109  2,691 files  −251,356
```
그 위에 각자 고유한 사유가 있습니다:

- **#1001 몰 파일럿 시안 5화면** — 디자인 문서(`operator-mall-pilot.md`)는 **이미 main 에 있고**,
  색 판단(로즈 `#E0526B` 대비 몰 그린)은 **2026-09-02 코레일톡 시안으로 대체**됐습니다.
  ⚠️ 다만 **화면은 아직 안 됐습니다** — `SellerOrdersPage` 의 `md:hidden` **0건**.
  원하시면 오늘 코드 위에서 새로 하는 편이 1,830파일 충돌 해소보다 쌉니다.
- **#1108 유어애즈 인계 문서** — 그 문서의 유일한 액션(`6/16`)이 끝났습니다
  (`COLLECT_KEYWORDS_PER_ROUND` 6 → 9 → **14**, `influencer-round-width.ts` SSOT 분리).
- **#1109 알람 5차 이관(보강 2레인)** — 🔴 **근거가 라이브 실측으로 사라졌습니다**(아래).

### 🔴 함께 드러난 라이브 사실 — 유어애즈 수집이 서 있습니다 (사고 아님)

#1109 의 근거를 그 PR 자신의 지시대로(`cpu_deaths` 로 반복 판정) 쟀더니:
```
하트비트 143건 전체에서 cpu_deaths > 0 인 레인:  0건
ads:enrich-company / enrich-prospects            cpu_deaths=0, last_cpu_death_at=null
```
대신 **전 레인이 서 있습니다**:
```
ads 레인 67 중 59가 stale · 알람 레인은 2026-09-15T00:00Z 이후 무실행(16시간)
ads:lanes-paused  → paused=true
ads:read-budget   → written=34,396  wbudget=30,000  wover=true
업체 수집 일별:  09-11 1,001 · 09-12 626 · 09-13 860 · 09-14 524 · 09-15 **7** · 09-16 **0**
```
원인은 **설계된 동작**입니다 — `read-budget.ts:221` `SEPT_2026_WRITE_THROTTLE = 30_000`,
`SEPT_2026_THROTTLE_UNTIL_MS = 2026-10-01`(2026-09-02 한도 사고의 처방). 하루 쓰기 예산을
다 쓰면 `budgetBlocked` 가 **전 레인**을 세웁니다.
⇒ **고칠 것이 아니라 아셔야 할 것**입니다. 10-01 에 자동 해제되고, 그전에 수집을 돌리려면
env `ADS_DAILY_WRITE_BUDGET` 조정이 필요한데 그건 **대표가 대시보드에서** 하실 일입니다
(세션은 플랫폼 쓰기를 하지 않습니다). 단일 관측으로 손대지 말라는 것도 그 레인의 규칙입니다.

## 선택지

1. **#1149 를 전용 세션에서 되살린다** — 97파일·충돌 24개 + 경로 이전 101곳 재수행.
   장점: 공구 서비스 분리가 실제로 닫힌다(대표가 *"페이지 구분을 잘 해야겠어"* 라고 지시한 그 건).
   단점: 곁다리로 할 수 없는 크기다. 머니 접촉은 없다.
2. **#1149 를 조각내서 순서대로** — ①~④(결제 동선 배너·링크) → ⑤⑥(운영자 온보딩) → ⑧⑩⑪(어드민 분리)
   → ⑫(경로 이전)로 나눠 각각 작은 PR 로. 장점: 각 조각이 독립 revert·리뷰 가능, 경로 이전은
   가장 나중에 최신 링크 수로 한 번에. 단점: 여러 세션이 걸린다.
3. **#1149 를 닫는다** — 진단은 문서로 남기고 코드는 버린다. 장점: 정리. 단점: 3,541줄과
   되돌려-검증 44건이 사라지고, 분리는 여전히 안 된 채 남는다.

**#1269 · #1295 는 선택지가 다릅니다** — 둘 다 **머니 경로**라 대표 결재 + 단독 세션 +
staging 실결제가 규칙입니다. 게다가 #1269 는 **세금 기준 질문이 미해결**입니다:
원천징수 기준에서 수수료를 빼느냐에 따라 100만원 정산이 **870,300원 ↔ 867,000원** 으로 갈립니다.
이건 코드가 아니라 사실 확인(세무)이 필요합니다.

## 기본안 (답이 없을 때 권하는 것 — 자동 실행되지 않는다)

**#1149 는 2번(조각내서 순서대로).** 이유: 한 달 드리프트로 ⑫(경로 이전)의 표적이 두 배가 됐고,
그 조각만 따로 떼면 나머지 ①~⑪은 오늘 바로 되살릴 수 있는 크기다.
**#1269 는 세무 답이 나오기 전까지 보류**(추측으로 원천징수 기준을 정하면 되돌리기 어렵다).
**#1295 는 게이트가 기본 OFF 라 코드만 먼저 머지하고 ON 은 별건**으로 — 다만 대표 결재가 먼저다.

## 롤백

되살린 것은 전부 조각별 독립 revert(#1458 도 두 변경이 서로 독립). 닫는 쪽은 브랜치가 남으므로
언제든 다시 열 수 있다.

## 결정 (대표가 한 말 그대로)

**#1149 만 정해졌다 — 그래서 상태는 여전히 `open` 이다**(#1269 · #1295 는 대표 판단 대기).
> 🩸 2026-09-16 에 이 칸을 `partial` 로 적었는데 **정의 밖 값**이라
> `ai-team-operating-model.test.ts` 가 빨간불을 냈다. 상태는 네 개뿐이다:
> `open` · `approved` · `rejected` · `expired`. 반만 정해진 건 `open` 이다.

**2026-09-16 대표: "너가 할 수 있는 거 순서대로 진행"**

세션이 직전 메시지에서 제시한 셋 — (a) #1149 첫 조각 되살리기 · (b) 오늘 수리의 미측정 입력
20곳 측정 마무리 · (c) 낡은 주석 1줄 정리 — 을 그 순서로 진행하라는 뜻으로 받았다.
⇒ **#1149 는 선택지 2번(조각내서 순서대로)** 으로 확정. 첫 조각은 ①~④(+⑦) 결제 동선.

⚠️ **이 결정이 덮지 않는 것**(여전히 대표 판단 대기):
- **#1269** — 세무 확인(원천징수 기준에서 수수료 차감 여부)이 선행. 코드 문제가 아니다.
- **#1295** — 💰 머니 경로. 게이트 기본 OFF 라 코드 머지는 가능하나 결재가 먼저.
- ~~**#1149 의 ⑫(경로 이전)** — 표적이 101곳으로 늘어 마지막 조각으로 미룬다.~~
  ✅ **2026-09-16 진행** — 마지막 조각으로 실제 착수. 실측 표적은 67곳/51파일이었다(101 은
  API·import 를 포함한 수). 인계: `docs/handoff/2026-09-16-pass-route-migration.md`.

## 조각별 진행 (2026-09-16)

| 조각 | 범위 | PR | 상태 |
|---|---|---|---|
| ① ~ ④(+⑦) | 결제 동선 배너·링크 | #1471 | 머지 `1f2dbb9f4` |
| ⑤⑥ | 운영자 몰 온보딩(신청→승인) | #1480 | 머지 `cbadfcc91` |
| ⑧⑩⑪ | 어드민 서비스 분리(nav 섹션·몰 스코프) | #1483 | 검증 통과, 머지 대기 |
| ⑫ | 이용권 상세 주소 이전 `/pass/:id` | — | 작성·검증 완료 |

⚠️ **E4(라이브 판정)는 대표 몫이 둘 남았다** — ① staging 실결제 1회(몰 흔적 없는 본진 결제에서
배너가 **안 떠야** 정상) ② 운영자 신청→승인 한 바퀴(가게가 열리는가 · 이미 올린 상품이 따라오는가 ·
예약어가 거부되는가). 세션은 거기까지 못 간다.

## 반영 커밋
