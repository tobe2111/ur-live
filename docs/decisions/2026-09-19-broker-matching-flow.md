# 대행사(중개사) → 매장 → 인플루언서 매칭 플로우 — 대표 문장 11단계의 원문 기록

상태: approved
등급: C
역할: planning
올린 날: 2026-09-19
기한: 2026-09-19

> 🔁 **같은 날 같은 결정이 두 세션에 도착했다.** 결재(선택지·기본안·롤백)는 `2026-09-19-agency-flow-final.md` 가 정본이고
> 구현은 PR #1499(`e3e182c2`)로 **이미 main 에 있다**. 이 파일은 대표가 소개서 세션에 보낸 **11단계 문장 원문**과,
> 그 문장이 코드의 어디에 닿는지를 적는 자리다. 두 파일이 다르게 읽히면 대표 원문(아래)이 이긴다.

## 질문

09-16 결재(`2026-09-16-broker-payout-model.md` — 유어딜이 각자에게 직접 송금)의 **절차**를 어떻게 잇는가.
누가 코드를 만들고, 누가 입력하고, 언제 매칭되고, 어디서 성과를 보는가.

## 결정 (대표가 한 말 그대로 — 2026-09-19)

> 1. 대행사가 셀러 대시보드 가입. 그대로.
> 2. 대행사가 매장 등록. 이때 매장 코드가 자동 생성되고, 중개사 몫 %와 인플루언서 커미션 %도 같이 정한다.
> 3. 어드민 매장 승인. 승인 전에도 이용권 등록은 되지만 메인 노출은 승인 후. 이미 라이브 규칙이라 플로우에만 넣는 것.
> 4. 대행사가 사장님에게 코드를 준다. 사장님이 가입하며 그 코드를 입력하면 그 매장의 주인이 된다. 원안의 "대행사가 매장 코드 입력"을 뒤집은 것. 권한을 내주는 쪽이 입력해야 동의가 되고, 코드가 새도 남의 매장을 못 가져갑니다. 사업자등록증 확인은 그대로 붙습니다.
> 5. 매칭 완료. 사장님 = owner, 대행사 = operator. 사장님 가입은 첫 정산 전까지만 끝나면 된다. 이용권 판매를 막지 않습니다.
> 6. 대행사가 이용권 생성. 그대로.
> 7. 대행사가 인플루언서에게 코드가 담긴 링크를 보낸다. 탭하면 가입, 가입 즉시 매칭. 원안의 "가입 유도"와 "코드 입력" 두 단계가 한 탭이 됩니다.
> 8. 인플루언서가 마이페이지에서 코드 입력. 직접 온 사람용 입구로 유지.
> 9. 매장 단위 고유 링크가 마이페이지에 생긴다. 복사·카톡 공유 버튼. 이용권이 바뀌어도 링크는 그대로. "매장당 이용권 1개"는 코드로 막지 않고 운영 정책으로 둡니다.
> 10. 인플루언서가 링크로 홍보. 귀속·적립·환불 역전은 지금 레일 그대로.
> 11. 성과 확인. 대행사는 셀러 대시보드에서 매장별·인플루언서별 성과와 자기 몫, 인플루언서는 마이페이지에서 자기 적립. 대행사 몫은 9-16 결재대로 유어딜이 직접 송금.
>
> 이 플로우로 결정났어

같은 날 다른 세션에 준 부속 확정(정본 파일 인용): *"중개사 몫과 인플루언서들도 각각 협의를 해야하니 커미션 % 를 매번 케이스마다
조정 가능하긴 해야해"* · *"중개사가 끼지 않는 매장 직접 운영도 유사한 형태로 하자."*

## 원안과 달라진 두 곳

| 원안 | 확정 | 왜 |
|---|---|---|
| 대행사가 사장님 매장의 코드를 **입력**해 연결 | 대행사가 만든 매장의 코드를 **사장님이 가입하며 입력** | 권한을 내주는 쪽이 입력해야 동의가 된다. 코드가 새도 남의 매장을 못 가져간다 |
| 인플루언서 "가입 유도" + "코드 입력" 두 단계 | 코드가 담긴 링크 **한 탭** = 가입 + 매칭 | 마이페이지 코드 입력은 직접 온 사람용 입구로만 유지 |

## 11단계가 코드의 어디에 닿는가 (PR #1499 · 2026-09-19 실측)

| # | 대표 문장 | 구현 | 상태 |
|---|---|---|---|
| 1 | 대행사 셀러 대시보드 가입 | 셀러 계정(별도 실체 없음) | 종전 |
| 2 | 매장 등록 → 코드 자동 생성 + 두 요율 | `POST /api/seller/stores` → `seller-broker-terms.routes` · `store_codes(kind='owner_claim')` · `seller_meta.broker_share_pct` / `influencer_pct_cap` | ✅ |
| 3 | 어드민 승인, 승인 전 등록 가능·메인 미노출 | `sellers.status` · `approvedSellerProductSql`(09-16) | 종전 |
| 4 | 사장님이 가입하며 코드 입력 → 주인 (등록증 확인 그대로) | `/store/find?code=` → `GET /store-claims/lookup-by-code` → `submitStoreClaim` → 어드민 승인 → `transferStoreOwnership` | ✅ (코드는 "찾기"만 대신, 승인은 사람) |
| 5 | owner / operator, 첫 정산 전까지만, 판매 안 막음 | `seller_operators` · 주인 없는 매장은 계좌 없음(정산만 막힘) | 종전 |
| 6 | 대행사 이용권 생성 | 매장 전환 토큰 · 운영자 게이트 | 종전 |
| 7 | 코드 링크 한 탭 = 가입 + 매칭 | 협업 코드 `store_codes(kind='influencer')`(기본 % · 승인 필요 옵션 · 상한 사용 수) · `/i/join/:code` · `influencer-code-redeem.ts` → `seller_influencer_deals` | ✅ (`requires_approval=1` 이면 `proposed`) |
| 8 | 마이페이지 코드 입력 | `POST /api/influencer-settlement/codes/redeem` · `DealsAndCodesSection` | ✅ |
| 9 | 매장 단위 고유 링크, 복사·공유, 이용권 바뀌어도 그대로 | `/s/{id}?ref=`(7일 귀속) + 대표 이용권 링크 · `GET /api/influencer-settlement/my-stores` | ✅ |
| 10 | 귀속·적립·환불 역전은 현행 레일 | `findActiveDealPct` · `voucher-clawback` | 종전 |
| 11 | 대행사 성과+자기 몫 / 인플루언서 적립 · 대행사 몫 직접 송금 | `/seller/operating`(매장별·인플루언서별·내 몫) · `/influencer/settlement` · `creditBrokerShare`(`broker-share.ts`) | ✅ 화면 / 🔒 송금은 게이트 `broker_share_enabled` **OFF** — S-BROKER 5건 뒤 대표가 켠다 |

부속 확정 "케이스별 조정": 코드의 % 는 기본값, `PATCH /api/seller-marketing/deals/:id` 로 딜마다 조정(이후 판매분부터). 상한 = 매장 `influencer_pct_cap` ∧ 90.

## 이 결정이 고정하는 불변식 (가드 `agency-flow-codes-2026-09-19.test.ts` 23건 + 주입 7건)

1. 매장 코드는 매장당 하나, 매장 등록 때 자동 생성. 사람이 정하지 않는다.
2. 코드를 입력하는 사람이 주인이 된다. 승계는 `transferStoreOwnership` 하나만 부른다(자물쇠 · 강등 · 영입 보존).
3. 등록증 확인은 그대로. 코드는 "어느 매장인가"를 고르는 손잡이지 증명이 아니다.
4. 사장님 가입은 이용권 판매를 막지 않는다. 막는 것은 첫 정산이다.
5. 인플루언서 매칭은 (매장, 인플루언서) 한 쌍에 한 행. 링크 탭도 코드 입력도 같은 행(UNIQUE). 같은 코드를 두 번 넣어도 활성 딜을 되돌리지 않는다.
6. 매장 단위 링크는 이용권과 독립. 매장당 이용권 수는 코드가 막지 않는다.
7. 두 요율(중개사 몫 % · 인플루언서 **상한** %)은 매장 등록 때 정한다. 개별 소개비는 협업 코드의 기본값 + 딜별 조정, 상한을 못 넘는다.
8. 귀속 · 적립 · 환불 역전은 현행 레일. 새 정산 레일을 만들지 않는다.
9. 대행사 몫은 유어딜이 직접 송금(09-16). 재원은 매장 몫 95% 안이고 유어딜 5% 는 불변. 켜는 것은 대표 판단(S-BROKER 뒤).

## 롤백

정본 파일 그대로 — 코드 레일은 라우트 3파일 + 화면 4곳 환원, 중개사 몫은 게이트 OFF 가 곧 롤백.

## 반영 커밋

- 코드: PR #1499 `e3e182c2` (브랜치 `claude/agency-flow-review-cgfwtl`) — 다른 세션.
- 문서·소개서: 이 파일 · `store-operator-model.md` §9 · 인플루언서 덱 v4 · 대행사 덱 v8 (PR #1497).
