# 2026-09-15 — 셀러 대시보드 3차: D3 밀도·데이터 + A2 매장이 제목 + B2 전 매장 합계 + C 정산 안 소개 수익

**서비스**: 유어딜(사업자 유저 셀러 대시보드 `/seller/*`). 머니 경로 접촉 **없음**(읽기 집계 API 1개 신설, 결제·정산·원장 무접촉).

## 배경

#1429(Rinda 껍데기 + 모바일 우선)가 라이브에 오른 뒤 대표가 셋을 지적했다:
*"매장이 여러개일 수 있잖아"* · *"각 페이지마다 제목을 저렇게 나타내니 아쉬운 것 같기도"* ·
*"소개 수익 페이지는 왜 셀러대시보드에서 안 보지?"* → 시안 캔버스(https://claude.ai/artifact/Q7qjYsAa92VVpGr632Z3Qq,
`docs/design/seller-dashboard-2nd-directions-2026-09.md`) → 대표 확정 **D3 + A2 + B2 + C** + *"다른 세부페이지들도 정리가 필요해."*

## 완료분 (이 PR)

| | 무엇 | 파일 |
|---|---|---|
| D3 | 셀러 스코프 CSS 변수(`--dash-radius 8px` · `--dash-h1 17/18` · `--dash-stat 22` · 여백 16/12 · 간격 14) + `dash-num` 모노 tabular. 공용 대시보드 부품은 변수를 읽고 **어드민은 폴백** | `src/index.css`, `components/dashboard/{DashboardCard,DashboardPageHeader,DashboardStatCard,DashboardEmptyState}.tsx` |
| A2 | 폰 헤더 제목 자리 = `<StoreSwitcher variant="title">`(1곳이어도 이름, 2곳+면 탭해서 전환). PC = `매장 / 페이지` 브레드크럼. 사이드바 224px | `components/SellerLayout.tsx`, `components/seller/StoreSwitcher.tsx`(`switchStore` export) |
| B2 | `GET /api/seller/my-stores/summary` — `resolveActorUserId` → `listOperableStores` 중 **active/approved** 만, 오늘(`DATE(created_at,'+9 hours')`) 매출·주문 + 30일 처리 대기, 그룹 쿼리 2개. 홈 `TodayTicket` 이 **2곳 이상일 때만** 합계 타일 + 매장별 표(행 = 좌석 전환) | `features/seller/api/seller-operators.routes.ts`, `pages/seller-page/{useSellerHome,TodayTicket,MyVouchersRail,WeekSummary}.tsx`, `SellerPage.tsx` |
| C | 정산 탭 `ReferralEarningsCard`(확정/보류 T+7/전환율, `/api/curator/me/dashboard`). 세션 없으면 **null**(빈 카드 금지) | `pages/seller-settlements/ReferralEarningsCard.tsx`, `SellerSettlementsPage.tsx` |
| 정리 | 코드모드 98파일 — 그림자 카드 → `rounded-[var(--dash-radius,16px)] border border-rule bg-white` · `bg-gray-900/800 text-white` → `bg-brand-tint text-brand-text` | `pages/Seller*.tsx`, `pages/seller-*/**`, `components/seller/*` |
| 로케일 | `seller.home.*` 12키 · `seller.vouchers.selling` · `seller.referral.*` 6키 — 6개 언어 | `public/locales/*/translation.json` |

삭제: `pages/seller-page/LazyChart.tsx`(이번 주가 PC 표로 바뀌어 참조 0).

## 검증 (E2)

- tsc 0 · 전체 vitest **680 파일 8,531건 pass**(처음 1건 빨강 = `seller-operators-invariants` 의 낡은 정규식 — 토큰 POST 가 `switchStore` 로 옮겨져 재앵커).
- 가드 GREEN: dashboard-button-system · design-slop · theme-consistency · dashboard-theme · file-size · surface-role-leak 0 · critical-chunks 17 · 번들 예산.
- `npm run build` exit 0(산출물 `route-chunk-map.ts` 는 되돌림).
- 신규 `seller-d3-2026-09-15.test.ts` 10건 + `scripts/mutations/seller-d3.mjs` 주입 6건 **전부 되돌려-검증 빨간불**.
- 브라우저 하네스(`scratchpad/render/harness.mjs`, API mock) — 폰/PC × 다점포/단일 × 홈·이용권·주문·정산 렌더 눈 확인.

## 📱 2차 푸시 — 모바일 특화 (대표 *"모바일로도 특화가 되어야 해"* · *"버튼이랑 글자 깨지고"*)

| 자리 | 폰 | PC |
|---|---|---|
| 정산 지급 현황 | 한 줄 타일 셋(미지급·지급 예정·지급 완료) | 스탯 카드 셋(종전) |
| 소개 수익 카드 | 라벨 왼쪽·숫자 오른쪽 행 셋 | 세 칸 |
| 정산 새로고침 | 아이콘 하나(44px) | 라벨 버튼 |
| 사업자등록 배너 · 딜 잔액 · 안내 | 이모지 0 · 색깔 상자 0(🎫 규칙 ⑥) · 버튼 한 줄 전체 | 같은 카드, 버튼 우측 |
| 기간 칩 | 가로 스크롤(줄바꿈 금지) | 한 줄 |
| 홈 매장별 행 | 터치 높이 py-3 | py-2.5 |
| 이번 주 | 7일 미니 막대(div 7개, 라이브러리 0) | 일별 표 |
| **내 매장 패널**(PC 홈 우측 340px 열) | (폰엔 없음) | 2열 카드 그리드 → **행 목록** + `whitespace-nowrap` 버튼 — 대표 스크린샷의 "이용권 등록" 두 줄 꺾임이 이것 |

가드: `seller-d3-2026-09-15.test.ts` +6건(16) · 주입 +2건(📱 소개 수익 폰 행 소실 · 매장 패널 그리드 환원) 되돌려-검증 red.

## 🩸 이번에 틀렸던 판단

1. **브레드크럼 `<h1>` 이 전역 h1 스타일을 받아 거대하게 렌더**됐다 — 클래스에 `text-[13px]` 를 명시해야 했다. 부품 안의 h1 은 늘 크기를 스스로 정할 것.
2. 정산 하네스가 `toLocaleString of undefined` 로 깨졌는데 앱 버그가 아니라 **mock 이 `deal-balance.total` 과 `tax-summary` 키를 빠뜨린 것**이었다. 하네스 mock 은 실제 응답 모양(`docs`·라우트)에서 옮길 것.
3. (이 세션 앞부분) 룰셋을 이미 초록인 #1429 머지 **전에** 만들게 해서 Verify 재실행 ~35분을 더 태웠다. 룰셋은 다음 PR 부터 적용되게 **머지 뒤에** 걸었어야 했다.

## 다음 세션의 첫 액션

1. 이 PR 이 머지됐으면 배포 판정: `curl -sS "https://urdeal.kr/api/version?cb=$RANDOM"` 의 entry 해시가 `index-bJMmLeJx.js` 에서 바뀌었는지 + `curl -sS https://urdeal.kr/locales/ko/translation.json | grep -c '"allStores"'` 가 1.
2. 라이브 E4: 대표 계정(매장 2곳 이상)으로 `/seller` 홈에 **전 매장 합계 타일 + 매장별 표**가 뜨는지, 폰 헤더가 매장 이름인지, 정산 탭에 소개 수익 카드가 있는지(소비자 세션 없이 셀러만 로그인하면 안 뜨는 게 정상).
3. Notion 개발 업데이트 로그 행(유어딜 · UI) — 이 세션이 MCP 연결돼 있으면 머지 직후 남긴다.

## 남은 결정/대기

- 셀러 페이지 색깔 정보상자 175곳(`bg-blue-50/amber-50…`, 🎫 절 규칙 ⑥) — 문구별 판단 필요라 별건.
- `MyStoresPanel`(설정 > 내 매장) PC 카드는 손 안 댔다.
- #1239 배포 판정은 이 세션의 12:55 KST 트리거가 본다(제안서 자산 200/~453KB + entry 해시).
