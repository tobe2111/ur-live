# 2026-09-24 — 대표 문서 「유어딜_추가제안」 6항목 이행

> 대표: *"순서대로 다 해줘. 두툼한 삼겹살... 이건 왜 터무니없이 저 자리에 있는거지??"*
> 착수 순서(제안 → 승인): **③ 가짜 숫자 → ① 가게소개 → ④⑥ 숙소 → ② 블로그 → ⑤ 공개예정**

## 📌 문서를 읽기 전에 확인한 것 — 여섯 중 다섯은 "없는 기능"이 아니었다

| 항목 | 실측한 현재 상태 |
|---|---|
| ① 가게소개 | 서버가 `long_description`·`seller_bio`·`seller_avatar` 를 **이미 보내는데** 상세 페이지 사용 횟수 **0회** |
| ② 블로그 사진 | 카드에 이미지 자리(`CoverImg`)는 있는데 **글 5개 전부 `thumbnail_url` null** → 이모지 폴백 |
| ③ 숫자 | **이미 박혀 있었다. 그런데 거짓이었다**(아래) |
| ④ 숙소 탭 | 큐레이션 섹션 0개, 목록이 홈과 같은 `GroupBuyFeedCard` 라 사진이 작다 |
| ⑤ 공개예정 | `prelaunch` 플래그·상세 배지("오픈 예정 · 사전 응모 받는 중")가 **이미 있다**. 없는 건 모아 보는 화면 |
| ⑥ 비슷한 스테이 | 숙소 상세엔 없다. 다만 **이용권 상세엔 같은 장치(`OtherDealsRow`)가 이미 있다** |

## ✅ ③ 대외 랜딩의 지어낸 실적 수치 제거

`/introduce` 가 `240만+ 누적 사용자 · 38만+ 누적 거래 · 4,200+ 입점 셀러 · 4.8★ App Store 평점`
을 하드코딩하고 있었다. **같은 날 어드민 실측: 유저 23 · 셀러 11(활성 9) · 주문 최근 id 89.**

🔑 **"더 정확한 숫자로 바꾸면 되지 않나" 도 함정이다** — 공개 피드 `total` 은 358이지만
**표본 100건 중 94건이 데모**(`demo-deal-*`)이고 셀러가 붙은 건 1건이라 358 도 거짓이 된다.
⇒ 실적 수치를 쓰지 않고 **코드가 보증하는 약속**(자동환불·0원·간편결제)으로 대체.

⚠️ **미확인**: App Store·Play 배지 링크(`id6745051422` / `com.urdeal.app`)의 실재 여부.
이 컨테이너에서 두 스토어 도메인이 **프록시에 막혀**(EGRESS_BLOCKED) 확인 못 했다 — 대표 확인 대기.
앱이 없다면 배지도 같이 내려야 한다.

## ✅ ① 상품 설명이 엉뚱한 자리 + 가게 소개란 신설

**대표 질문의 답**: 그 문장은 상품 설명(`description`)인데 `gb-sec-info` 안에서 `<UsageGuide>`
**바로 뒤**에 제목도 구분선도 없이 붙어 있었다 → 위 '유의사항' 불릿의 꼬리처럼 읽힌다.
원인은 디자인이 아니라 **이사 자국**이다 — 2026-09-15 `faed018ff`(#1432, 안 B 마무리)가
`UsageGuide` 를 이 앵커로 끌어올리면서 설명 문단이 뒤로 밀렸다(`git log -S` 로 두 변경이 같은 커밋 확인).

수정: 신규 `StoreIntro.tsx` = `상품 구성`(description) + `가게 소개`(long_description + 사장님 말풍선).
상단 탭에 `가게 소개` 추가. ⚠️ `detail_images` 는 **상단 갤러리에 이미 병합**돼 있어 여기서 다시 안 그린다.

파일크기 래칫이 **906/906 으로 여유 0** 이라 셀러 카드를 `SellerCard.tsx` 로 추출(마크업 불변) → 879줄.

## ✅ ④⑥ 숙소 — 카드는 한 벌로 두고 폭만 키운다

**두 지시가 부딪히는 자리였다.** 2026-09-03 대표 지시 *"여기 UI도 통일화 해야지"* 로 숙소 목록을
홈과 같은 `GroupBuyFeedCard` 로 통일했는데(당시 **네 번째** 카드 세대를 합친 것), 이번 "사진 크게"를
새 카드로 풀면 다섯 번째가 생긴다. ⇒ **폭만** 키웠다(캐러셀 한 장 = 화면 68%, 그리드 25%).

④ 큐레이션 4종(10만원 아래 / 강원 / 서울·부산 / 별점 높은 곳). **제목은 데이터에서 참인 것만** —
"서울에서 한 시간" 류는 거리 보장을 못 해 안 썼다(③과 같은 규칙). 새 API 요청 0.
⑥ 같은 시도 추천, 자기 자신 제외, 화면에 들어올 때만 요청. 자리는 후기 다음(소개 바로 아래면
객실 선택이 밀린다 — 옮기려면 렌더 한 줄).

## ✅ ② 블로그 — 사진이 아니라 폴백이 문제였다

카드에 이미지 자리는 있는데 **글 5개 전부 `thumbnail_url` null** 이라 목록 전체가 폴백으로
떨어졌고, 그 폴백이 **이모지 + 그라디언트**였다(SSOT 금지 둘). 우리 아이콘 + 단색 면으로 교체.
🔑 **진짜 사진은 `/admin/blog` 썸네일 업로더로 넣는다 — 이미 있다.** 채울 내용이지 만들 기능이 아니다.
가독성: 본문 16.5px + 폭 `max-w-[46rem]`(3열에서 본문 칸 760px → 15px 한글 한 줄 95자였다).

## ✅ ⑤ 오픈 예정(`/soon`) — 엔진은 있었고 화면만 없었다

D1 실측 **활성 8건**(2498~2505)이 일반 피드에 흩어져 있었다.
🩸 **내가 처음 한 말이 부정확했다**: *"목록 API 가 prelaunch 를 안 내려준다"* → 실은 내려주는데
**`fcfs_enabled='1'` 인 상품에만** 붙는다(`if (!enabledSet.has(pid)) return p` 안쪽). 8건은 fcfs 가
아니라 플래그가 없었다. 결론(별도 경로 필요)은 같지만 이유가 다르다 — 가드가 이 오류를 잡았다.

⇒ **잠긴 피드를 안 건드리고** 새 엔드포인트 `GET /api/group-buy/prelaunch` + `/soon` 페이지 + 푸터 링크.

## ⏭️ 다음 세션의 첫 액션

1. **배포 후 판정**: `/introduce` 실적 수치 0 · `/pass/2645` 에 `가게 소개` 탭·섹션 ·
   `/stays` 큐레이션 줄 · 숙소 상세 하단 추천 · `/blog` 커버에 이모지 0 · `/soon` 에 8건.
2. **대표 확인 대기**: 앱스토어·플레이스토어 배지의 앱이 실재하는지(이 환경에서 두 도메인이
   프록시에 막혀 확인 불가). 없으면 배지도 내려야 한다.
3. **대표 판단 대기**: 숙소 '비슷한 스테이' 자리를 소개 바로 아래로 옮길지.

## 🩸 이번에 틀렸던 판단

1. **저해상도 스크린샷으로 추측하려 했다** — 처음 받은 캡처는 본문이 안 읽혔는데 섹션 제목만 보고
   진행할 뻔했다. 링크/확대 캡처를 요청한 뒤에야 정확히 읽었다. **안 읽히면 읽힐 때까지 요청할 것.**
2. **가드 앵커를 이름으로 잡았다** — `toContain('handleExpiredVoucherRefunds')` 는 **import 줄** 때문에
   호출을 지워도 초록이었다(주입이 잡았다). 호출 형태로 재앵커.
3. **`StoreIntro` 주석에 "종전 조건 그대로"라고 써 놓고 구현을 빠뜨렸다**(제목과 같은 설명 생략).
   주석을 먼저 쓰면 구현했다고 착각한다 — 시험으로 고정했다.
