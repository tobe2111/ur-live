## ✅ 2026-07-29 (9차-e) — **cron 경보가 매일 오탐을 울리고 있었다 (ur-ads 레인 주기 신고)**

9차-d(#835 머지 + 인계 분리) 후속. 대표 "더 개선 및 고도화 요소들 찾아서 진행해줘".
**실측에서 이미 발생 중이던 결함**을 하나 잡았다 — 가설이 아니라 라이브 응답에 찍혀 있던 것이다.

### 무엇이 잘못돼 있었나

ur-ads 의 `adsBeat` 은 모든 레인에 **워커의 cron 식(`event.cron` = `0 * * * *`, 매시간)** 을 붙여
하트비트를 기록했다. 그런데 하나의 cron 이 **주기가 제각각인 레인들을 디스패치**한다:

| 레인 | 실제 주기 | 매시간으로 신고했을 때 |
|---|---|---|
| 일 1회 9개(`hourUTC === 15/16/17/19/20/21/22/23`) — NPS·고용24·나라장터·MX 스윕 … | 24시간 | 150분 지나면 stale → **하루 21.5시간을 매일 '멈춤'** |
| 짝/홀수시 4개(`hourUTC % 2`) — 상가정보·통신판매·기업수집 … | 2시간 | 여유 30분뿐 |
| 유지보수 5단계 순환(`PHASES[hourUTC % 5]`) | **최대 9시간** | 항상 stale |

`expectedMaxAgeMinutes('0 * * * *')` = 150분이고, 그 판정은
`/api/_healthcheck/cron` → `uptime.yml`(10분) → **이슈 + 메일**로 나간다.

**라이브 증거(2026-07-29 05:47)**: `ads:maintenance?phase=quality` 가 `age 167분 · stale: true`.
5단계 순환이라 정상 동작인데도 경보 대상이었다.

`cron-heartbeat.ts` 의 주석은 **"확실히 이상한 것만 울리는 게 목적"** 이라고 못박고 있다 —
매일 울리는 오탐은 그 설계 의도를 정면으로 깨고, 결국 경보 전체를 무력화한다.

### 고친 방식 — 조건과 주기를 **한 자리에서** 나오게

주기를 따로 적게 두면 언젠가 어긋나고, 어긋나도 조용하다(경보만 늘 뿐). 그래서 분리하지 않았다.

- 신규 `src/worker-ads/lane-cadence.ts`(순수) — `makeHourGates(hourUTC, kick)` 이
  **발화 조건과 주기 신고를 함께** 만든다. `gates.dailyAt(16, …)` / `gates.everyNHours(2, 0, …)`.
  시각 게이트 **12곳 전부** 전환(raw `kick` + `hourUTC` 동거 0건).
- `maxPhaseGapHours(phaseCount)` — 단계 순환의 최대 간격을 계산한다. **단순히 단계 수가 아니다**:
  24가 나누어떨어지지 않으면 자정에서 간격이 벌어진다(5단계 → 단계 4 는 19시 다음이 다음날 4시 = **9시간**).
  단계를 6개로 늘리면 24 % 6 === 0 이라 자동으로 6이 된다.
- `recordCronBeat(..., maxGapMin?)` — 작업이 자기 주기를 **직접 신고**하면 cron 식보다 우선.
  없으면 종전대로 cron 식에서 유도(기존 메인 워커 cron 은 **동작 불변**).
- `listCronHeartbeats` / `getCronHealth` 가 신고값 우선 + `max_gap_min` 을 응답에 노출
  (어드민이 "왜 stale 로 봤나"를 화면에서 알 수 있게 — 판정 근거가 안 보이면 또 오진한다).

### 검증

- 신규 유닛 **35개** — 공식 동치성(`staleGapMinutes(60) === expectedMaxAgeMinutes('0 * * * *')`)을
  고정해 두 곳이 갈라지면 실패한다 · 자정 불연속 · 게이트 발화 횟수.
- **가드를 깨뜨려 확인**: NPS 사이트를 원래 버그 모양(`if (hourUTC === 16 …) kick(…)`)으로 되돌리자
  회귀 테스트가 정확히 그 줄을 잡고 실패 → 복원 후 통과.
- 전체: `tsc 0` · 유닛 **3,073 pass(241 파일)** · `npm run build` exit 0 · **audit-gate 70 ALL GREEN**.
- 머니 경로 무접촉.

### ② 이어서 — **"한 번도 발화하지 않은 레인"도 보이게** (같은 PR)

위 수정의 한계로 남겨뒀던 것을 바로 이었다. 하트비트는 **기록된 행**만 보므로,
게이트는 켜져 있는데 한 번도 안 돈 레인은 목록에 아예 없어 `stale` 판정 대상조차 아니다 —
**"안 도는 건지 원래 없는 건지" 화면에서 구분이 안 된다.** `ads:collect-nps` 가 지금 그 상태이고,
그 때문에 세션 여러 개가 같은 질문을 반복했다.

**손복제 없이 목록을 얻는 방법**: 시각 게이트가 `makeHourGates` 안으로 들어간 덕분에
**발화하지 않는 시각에도 헬퍼는 호출된다** → 그 순간 레인 이름을 알 수 있다.
즉 이 목록은 손으로 관리하는 표가 아니라 **디스패치 코드가 그 자리에서 뱉는 사실**이다
(레인을 추가/삭제하면 자동으로 따라온다 — 이 레포가 반복해 데인 "목록이 사본" 클래스를 피한다).

- `createLaneRegistry()` 가 게이트 통과 레인을 모으고, 스케줄 실행당 **D1 쓰기 1회**로
  `platform_settings.ads_known_lanes` 에 남긴다(부모 예산 +1, fail-soft).
- `GET /api/admin/cron-heartbeats` 응답에 **`never_fired`** + `known_lanes_at` 추가.
- 쿼리는 떼고 비교한다 — 안 그러면 `maintenance?phase=*` 5단계 중 4개가 매번 오탐된다.
- **어드민 화면 배선까지 같은 커밋에서** — `/admin/system-monitoring` 에 '🔭 한 번도 안 돈 레인'
  카드 + 각 행에 판정 근거(`기준 N시간`) 표시. ⚠️ 안 그러면 **내가 고치려던 그 클래스를 내가 만든다**
  ("서버는 고쳤는데 볼 곳이 없다"). 실제로 UI 는 `stale` 만 읽고 있어서 새 필드가 안 보일 뻔했다.

### ③ 관측이 켜지자마자 잡힌 실제 장애 — **부모 cron 틱이 조용히 잘리고 있었다**

위 관측을 켜 놓고 라이브를 보다가 발견했다. 이 세션의 최대 수확이다.

| 틱 | 발화 레인 | 상태 |
|---|---|---|
| 05:00 | 15개+ | 전부 ok |
| 06:00 | — | **5개 레인이 `Worker exceeded CPU time limit.`** 로 실패 |
| 07:00 | **8개에서 절단** | 실패 기록조차 **0** (조용한 절단) |

07:00 에 실패 기록이 없는 이유: `kick` 의 하트비트는 **SELF fetch 가 끝난 뒤** 찍힌다 →
부모가 먼저 회수되면 그 레인들은 **기록도 안 남기고 사라진다**.

**원인**: 파트너풀 이메일 보강만 아직 라운드 루프를 **부모에서** 돌리고 있었다 —
`for (i < 8) await env.SELF.fetch('/__ads/enrich-company')`. 8회 **순차** 왕복이고 한 라운드가
실제로 외부 사이트를 크롤한다. 그동안 부모가 살아 있어야 하니 뒤쪽 레인이 굶는다.
당시 코드 주석은 *"SELF fetch 는 서브요청 1개씩이라 20라운드도 안전"* 이라고 적혀 있었는데,
**서브리퀘스트만 세고 부모의 수명(CPU/시간)을 안 셌다.** 그게 오답이었다.

**처방**: #835 가 인플루언서 레인에 한 것과 동일 — `/__ads/enrich-company-driver` 신설,
부모는 `kick` 1개만. 라운드 수(8)·순차성·기본값 전부 그대로, **도는 장소만** 바뀐다.
이로써 `check-ads-lane-isolation` 의 "수집·스윕 공유 waitUntil 블록"이 **1개 → 0개**가 됐다
(#830 커밋이 "남은 공유 블록은 같은 방식으로 떼면 된다"고 인계에 남긴 마지막 하나).

⚠️ **06:00 의 CPU 한도 실패는 #835 배포(06:17) *이전*이다** — 그 변경 탓이 아니다.
07:00 은 #835 배포 후 첫 틱이지만, #835 는 오히려 부모 부하를 줄이는 방향이었다.

### ④ 예약 체크인이 밝힌 것 — **#835 의 효과가 0이었고, #835 자체가 미완이었다**

07:13 체크인 실측(#835 배포 06:17 이후 두 틱 경과):

| 확인 | 결과 |
|---|---|
| `ads:enrich-influencer-driver` 하트비트 | ❌ **없음** |
| `enrich_lane` | `last_run 05:02` · spent 38/45 — **06·07시 두 틱 연속 미실행** |
| `nb_unmeasured` | 26,277 (기준선 26,191 → **악화**) |

07:00 에 발화한 8개는 전부 **빠른 레인(D1 전용)** 이고, 빠진 것은 전부 **느린 레인(외부 크롤)** 이었다.
여기서 원인이 확정된다 — **부모의 `kick` 은 `await env.SELF.fetch(...)` 다.**
드라이버가 12라운드를 다 돌고 나서 응답하면 **부모는 그 시간 내내 묶인다.**

⇒ **#835 는 라운드를 부모의 *서브리퀘스트 예산*에서만 뺐고 *수명*에서는 못 뺐다.**
내가 ③에서 파트너풀 레인을 두고 지적한 바로 그 오류("서브리퀘스트만 세고 수명을 안 셌다")가
**#835 자체에도 있었다.** 내가 머지한 변경이라 더 분명히 적어 둔다.

**처방**: 두 드라이버 모두 **즉시 응답**하고 라운드는 자기 인보케이션의 `waitUntil` 에서 돈다
(`runRoundsDetached`). 부모의 kick 은 곧바로 풀려 다음 레인을 디스패치한다.
⚠️ 관측을 잃지 않는다 — 라운드 결과를 **드라이버 자신의 하트비트**로 남긴다(#840 의 `driverBeat` 재사용 — ⑦ 참조)
(즉시 응답만 하고 결과를 안 남기면 "돌긴 했나"를 다시 알 수 없게 된다 — 고치려던 그 병).

### ⑤ 배포 트리거가 ur-ads 의존 코드를 안 덮고 있었다 (조용한 no-op)

④를 커밋하다 확인한 것. `deploy-ads.yml` 의 `paths` 는 **손으로 적은 목록**이라 드리프트해 있었다:

| 경로 | ur-ads 가 쓰는가 | 트리거에 있었나 |
|---|---|---|
| `src/worker-ads/**` | ✅ | ✅ |
| `src/features/marketing/**` | ✅ | ✅ |
| **`src/worker/**`** | ✅ (`cron-heartbeat.ts` = ur-ads 의 하트비트 기록기) | ❌ |
| **`src/features/social-media/**`** | ✅ (매시간 소셜 유지보수) | ❌ |
| **`src/features/supply/**`** | ✅ (동적 import) | ❌ |

즉 **그 파일들만 바꾸면 ur-ads 배포가 아예 안 돈다** — 실패도 안 나므로 ur-ads 가 조용히 낡은
코드로 계속 돈다. 이번 세션의 `maxGapMin` 신고도 `src/worker-ads/**` 를 같이 바꿔서 우연히
배포되는 것이지, `cron-heartbeat.ts` 만 고쳤으면 **라이브에 반영되지 않았을 것이다.**

**처방**: 빠진 3개 경로 추가 + **유닛이 worker-ads 의 import prefix 를 스캔해 트리거에 빠진 게
있으면 실패**시킨다(목록이 다시 드리프트하면 CI 가 잡는다). 회귀 주입(원래 빠져 있던 `src/worker/**`
를 다시 제거)으로 실제로 잡히는 것 확인.
⚠️ 못 막는 것: **전이 의존**(worker-ads → marketing → 제3의 폴더)은 안 본다. 직접 import 만.

### ⚠️ 이 수정이 **못** 하는 것 (과신 금지)

- `gates.*` 를 쓰되 **잘못된 시각/N** 을 넣는 경우는 못 잡는다(값의 정합은 검사 밖).
- **게이트가 OFF 인 레인은 여전히 안 보인다** — 의도적 정지와 구분할 수 없어 일부러 제외했다.
  게이트 상태는 `/__ads/health` 가 따로 노출한다(파트너풀 stats 가 이미 읽는다).
- `never_fired` 는 ur-ads 스케줄러가 **한 번 이상 돈 뒤**에야 채워진다(첫 배포 직후엔 빈 배열).

### ⑥ 병합에서 배운 것 — **분리한 인계가 한 번 되살아났다**

main 을 병합하자 `docs/CURRENT_WORK.md` 가 **5,443줄로 복귀**했다. 원인: 아직 **옛 형식으로 이 파일을
편집하던 브랜치들**(#841·#852 등)이 있었고, `.gitattributes` 의 `merge=union` 이 **양쪽을 다 남긴다**.
union 은 로컬 충돌을 없애려고 넣은 것인데, 분리 과도기에는 **되살리는 방향**으로 작동한다.

**처방(되돌리기가 아니라 재분리)**: 마커 앞 `## ` 섹션 308개를 훑어 `docs/handoff/` 에 **없는 것만**
골라내고(내용 정규화 비교), 신규 4건을 파일로 떼어낸 뒤 목차만 남기고 재생성했다 → 102줄.
잘라내기 **전에** "잘릴 구간의 모든 줄이 handoff 어딘가에 있는가"를 확인했다 —
못 찾은 7줄은 전부 내가 쓴 preamble 문구였다(본문 손실 0).
이 절차를 `CURRENT_WORK.md` preamble 에 적어 뒀다. 모든 브랜치가 새 형식이 되면 자연히 사라진다.

⚠️ 같은 병합에서 `src/worker-ads/index.ts` 도 충돌했다(#837 이 localdata 를 **체인 진입점**으로 바꿈).
**양쪽 의도를 다 살렸다** — 경로는 main 의 체인, 발화 조건+주기 신고는 `gates.dailyAt`
(raw kick 으로 두면 일 1회 레인이 매시간으로 신고돼 ①의 오탐이 그대로 재발한다).

### ⑦ 같은 문제를 다른 세션이 **절반** 고쳤다 — 병합에서 둘을 합쳤다

두 번째 병합에서 `#840`(다른 세션)이 **같은 진단**에 도달해 있었다: 파트너풀 라운드 루프를
`/__ads/enrich-company-driver` 로 격리 + `driverBeat`(드라이버가 **자기** 하트비트를 남김) + `kick` 에
`beatName` 추가. 07:00 실측 근거까지 같다.

**하지만 남은 절반이 달랐다.** #840 은 드라이버에서 `await runRoundChain(...)` **후에** 응답한다 →
부모의 `kick` 은 여전히 그 시간 내내 묶인다(서브리퀘스트는 지켰지만 **수명**은 못 지켰다).

**병합 결과 = 둘 다**:
- main 의 `runRoundChain` / `driverBeat` / `driverJson` / `beatName` 구조를 **베이스로 채택**(더 깔끔하다).
- 그 위에 이 브랜치의 `dispatchRoundChain` — **즉시 응답 + 체인은 드라이버의 `waitUntil`**.
  #840 의 `driverBeat` 이 여기서 진가를 낸다: 부모가 이미 떠나도 그 기록은 성사된다.
- `kick` 3번째 인자는 `{ gap, beat }` 옵션으로 통합(main=beat, 이 브랜치=gap — 둘 다 필요).
- 회귀 테스트도 갱신: "드라이버가 체인을 `await` 한 채 응답하지 않는다"를 새로 고정.

⚠️ **교훈**: 동시 세션이 같은 라이브 증상을 보면 **같은 곳을 판다.** 착수 전 `origin/main` 확인은
(main 인계의 '중복 개발' 항목대로) 필수지만, 이미 벌어졌으면 **어느 쪽이 더 멀리 갔는지**를 보고
합쳐야 한다 — 한쪽을 통째로 버리면 그 세션이 알아낸 절반이 사라진다.

### ⑧ 실측이 확정한 것 + 마지막 발견(고아 하트비트)

**08:00 틱 실측 — 절단이 해소됐다.** #840 이 배포된 뒤다:

| 틱 | 발화 | enrich_lane |
|---|---|---|
| 07:00 | 8개 (절단) | last_run 05:02 (두 틱 미실행) |
| **08:00** | **15개 · 실패 0** | **last_run 08:00:22 — 부활** |

`ads:enrich-influencer-driver` 하트비트도 **최초로** 찍혔다. 즉 ③④의 진단은 맞았고,
그 절반(#840)이 배포되자마자 증상이 사라졌다. 남은 절반(즉시 응답)은 이 PR 에 있다 —
08:00 에도 드라이버 비트가 **33초 뒤**에 찍혔다(= 부모가 33초를 기다렸다). 이번엔 15개가 들어갔지만
레인이 하나만 더 늘어도 다시 천장이다.

**그리고 ①의 가치가 라이브에서 그대로 보였다** — 지금 `stale` 로 경보 중인 ads 레인 4개 중
최소 3개가 오탐이다: `maintenance?phase=handle`(256분)·`phase=merge`(197분)은 5단계 순환이라 정상,
`collect-company`(196분)는 홀수시 레인이라 정상. (`max_gap_min` 은 아직 `None` — 이 PR 미배포.)

**🪦 마지막 발견 — 고아 하트비트.** 네 번째 `sweep-kakao-phone`(196분·stale)은 오탐도 정상도 아니었다:
그 레인은 **이름이 `sweep-kakao-chain` 으로 바뀌었다.** 옛 이름의 행은 아무도 갱신하지 않으니
**영원히 stale** 이다 — 고칠 수 없는 경보는 곧 전체 경보를 무시하게 만든다.
`never_fired`(알려졌는데 기록 없음)의 **정확한 반대**라, 방금 만든 known-lanes 목록으로 바로 잡힌다.
→ `orphanLaneBeats()` + API `orphan_lanes` + 어드민 카드. 둘을 같이 보면 화면에서
**"안 도는 것" ↔ "이제 없는 것"** 이 갈린다.
⚠️ 고아 사유는 둘(이름변경/삭제 · **게이트 OFF**)이라 지우기 전에 확인할 것 — 카드에 그렇게 적었다.

### 🎯 병목이 또 옮겨갔다 — 이제 **예산이 아니라 시간**이다 (다음 레버, 코드 0)

레인이 부활한 뒤 08:00 라운드를 뜯어보니 제약이 바뀌어 있었다:

```
bio 0 · yt 14 · naver { selected: 12, tried: 3 }
spent 25 / budget_total 45      ← 예산은 남았다
deadline_hit: true · elapsed_ms 20,296   ← 시간에 잘렸다
```

**`selected: 12` 인데 `tried: 3`** — 고를 건 골랐는데 3건만 처리하고 20초 마감(`ADS_ENRICH_DEADLINE_MS`,
기본 20,000ms)에 끊겼다. 즉 **#835 의 예산 흡수(`naverRoomFromRemaining`)는 지금 효과를 낼 수 없다** —
예산이 아니라 시간이 먼저 끝난다. 네이버 블로거 1건 = fetch 2회(RSS + 모바일홈, 타임아웃 8초)라 비싸다.

**구조적 편향**: 실행 순서가 `bio(199) → yt(203) → naver(210)` 라 **가장 중요한 네이버가 맨 뒤**다
(백로그 26,437 = 블로그의 91%). 마감이 걸릴 때마다 **손해는 항상 네이버가 전담**한다.
반면 유튜브는 `yt_units 36/2000` 으로 여유가 크고 건당 비용도 싸다.

⚠️ **그래서 순서를 바꾸지 않았다 — 표본이 2건뿐이다.**
`05:02` 라운드는 `naver 10 · elapsed 10.5s · deadline_hit: false` 로 멀쩡했다. 마감이 *얼마나 자주*
바인딩되는지 모른 채 순서를 뒤집으면 이번엔 yt 가 같은 방식으로 굶는다. 몇 라운드 더 관측하고 결정할 것.

**먼저 쓸 안전한 레버(코드 0)**: `ADS_ENRICH_DEADLINE_MS` 상향(5s~120s 허용, 기본 20s).
⚠️ **단, 이 PR 의 즉시-응답이 배포된 뒤에.** 지금은 부모의 `kick` 이 드라이버 응답을 기다리므로
마감을 늘리면 **부모가 그만큼 더 묶여** 다른 레인이 다시 굶는다(08:00 에도 드라이버 비트가 33초 뒤에 찍혔다).
순서: ① 이 PR 배포 → ② 마감 상향 → ③ 그래도 `tried ≪ selected` 면 그때 순서 재검토.

### 다음 세션 첫 액션

> 이 세션이 08:00 틱까지 실측했다. **틱 절단(8개→15개)과 보강 레인 부활은 이미 확인됐다** —
> 다시 검증할 필요 없다. 아래는 *이 PR 배포 후* 에만 판정 가능한 것들이다.

1. **배포 확인 후 `/api/admin/cron-heartbeats`** (한 번의 조회로 4가지가 한꺼번에 판정된다):
   - `max_gap_min` 이 일 1회 레인에서 **2910**(48.5시간), 유지보수 단계에서 **1110**(18.5시간)인가.
     → 지금 오탐으로 울고 있는 `phase=handle`·`phase=merge`·`collect-company` 의 `stale` 이 **false** 로 바뀐다.
     ⚠️ 신고값은 각 레인이 **다음 발화 때** 기록한다 — 일 1회 레인은 최대 하루가 걸린다.
   - **`never_fired`** 에 `collect-nps` 가 잡히는가 → 잡히면 그 자체가 몇 세션째 미결이던 질문의 답이다
     (게이트 ON 인데 한 번도 안 돎). 스케줄러가 한 번 돌면 바로 채워진다.
   - **`orphan_lanes`** 에 `sweep-kakao-phone` 이 잡히는가 → 잡히면 그 행은 지워도 되는 고아다
     (레인이 `sweep-kakao-chain` 으로 개명됨). ⚠️ 게이트 OFF 도 고아로 보이니 지우기 전에 확인.
   - 드라이버 비트(`ads:enrich-influencer-driver`)가 **kick 직후 몇 초 안에** 찍히는가
     → 08:00 엔 **33초** 뒤였다(부모가 그만큼 묶였다는 뜻). 즉시-응답이 들으면 이 간격이 사라진다.

2. **그 다음 레버 = `ADS_ENRICH_DEADLINE_MS` 상향** (코드 0, env 만). **반드시 1번 확인 후에.**
   근거는 위 "🎯 병목이 또 옮겨갔다" 절 — `selected 12 / tried 3 · deadline_hit: true` 였다.
   ⚠️ 1번이 안 됐는데 마감만 늘리면 부모가 더 묶여 **다른 레인이 다시 굶는다**. 순서를 지킬 것.
   상향 후에도 `tried ≪ selected` 면 그때 실행 순서(`bio→yt→naver` 에서 naver 를 앞으로)를 재검토.
   ⚠️ 순서 뒤집기는 표본 2건으론 부족하다 — 마감이 얼마나 자주 바인딩되는지 먼저 셀 것.

3. **아직 부모를 붙잡는 것이 남아 있다**(이번 PR 범위 밖):
   **단발 느린 레인**(`collect`·`collect-hira`·`collect-neis` — 드라이버가 아니라 한 방에 오래 걸린다)과
   매시간 무조건 도는 `handleSocialMaintenance`(부모에서 직접 await).
   처방은 같다 — 각 라우트가 **즉시 응답하고 자기 waitUntil 에서 일하며 자기 하트비트를 남긴다**
   (이번에 드라이버 두 개에 한 것과 동일). 1번의 드라이버 비트 지연이 여전하면 여기부터.

4. 9차-d 의 첫 액션 중 **미결분**: NPS 는 **16:00 UTC 이후**에만 판정 가능(하트비트 배포가 07-29 02:00
   이라 그 전엔 발화 시각이 온 적 없다) · 공동구매 축 유입(`?category=공동구매`).
   → `docs/handoff/2026-07-29-835-merge-handoff-split.md`

### 남은 결정 / 대기 (대표 판단) — 9차-d 에서 이월

- ✅ **`CLASSIFY_RULES_VERSION` 3 → 4 — 해결됨(다른 세션).** 2026-07-29 08:3x 실측 확인:
  코드 상수 **4**(`company-classify.ts:56`), 라이브 `reclassify.remaining_unclassified` = **159,383**
  (bump 전엔 0 이었다 = 전량 재검사가 실제로 돌기 시작). 우려했던 **대량 삭제는 없었다** —
  `total_removed` 누적 **129건**(171,307 중). ⚠️ 이 항목을 '미결'로 다시 적지 말 것.
- **접촉 0명** — 발송 가능 22,533명, 큐·초안 인프라 준비됨. 동의/PIPA 절차 포함 대표 판단.
