# 2차 해동 — 길이로 거르다 놓친 14개 (2026-08-31)

## 1차(#1280) 배포 후 라이브를 다시 재고 나온 것

해동 13개는 **전부 반영됐다**(라이브 = 시드, 어드민 정산 476자 → 2195자). 뺀 2개도 보존됐다.
그런데 **전체를 다시 대조하니 14개가 여전히 갈려 있었다.** 1차에서 내가 길이로 걸러 낸 것들이다.

## 🩸 내 판정이 두 군데서 틀렸다

| 내가 세운 규칙 | 왜 틀렸나 |
|---|---|
| "길이가 같으면 공백 차이 = 노이즈" | **폐기어 치환이 같은 글자 수다** — 유통사→판매사, 식사권→이용권 (전부 3글자) |
| "라이브가 더 길면 누가 더한 것" | **옛 도메인이 더 길어서**였다 — `live.ur-team.com`(16) vs `urdeal.kr`(9) |

⇒ **드리프트는 길이가 아니라 내용으로 판정해야 한다.**

## 실제로 라이브에 남아 있던 것

- **도매 가이드 15개 중 12개가 아직 '유통사'** — 2026-06-22 대표 확정으로 폐기된 말이다
  (*"유통사가 아니라 판매사"*). 명칭 SSOT 위반이 어드민 화면에 그대로 있었다.
- **admin `deploy` 가 `npx vite build` 를 가르치고 있었다** — CLAUDE.md 가 2026-05-12 사고의
  원인으로 지목하고 **금지**한 명령이다(`_worker.js` 가 안 갱신돼 워커 변경이 배포에 안 실린다).
  옛 도메인 `live.ur-team.com` 도 함께.
- seller `account-kakao` 에 '식사권'(두 번 은퇴한 말).

## 수리

`UNFREEZE_ONCE_2` 14키 + 별도 마커 `guide_unfreeze_2026_08_31_b`. 1차와 같은 방식 —
**키를 명시한 것만**, 1회만. GUIDE_SEED_VERSION 21 → 22.

## 다음 세션의 첫 액션 — 배포 후 라이브 판정

```
npm run build:seed
어드민 토큰 → GET /api/guides/{seller,admin,agency,wholesale}
dist/client/seed/guides.json 의 content vs 라이브 content_md 를 **내용으로** 대조
```
**남은 드리프트가 0 이어야 한다**(auto-reference 제외 — 기계 생성이라 배포 시점마다 다르다).
0 이 아니면 그 키를 3차 해동에 넣기 전에 **먼저 내용을 봐라** — 진짜 관리자 편집일 수 있다.

## 이번에 또 만든 헛도는 가드

테스트에서 `routes.slice(start, routes.indexOf(']', start))` 로 배열을 잘랐는데,
**타입 표기 `Array<[GuideType, string]>` 의 대괄호에서 잘려** 빈 조각을 검사하고 있었다.
`.not.toContain(...)` 류는 그러면 늘 통과한다. 1차 테스트가 정확히 그 상태였다 —
그래서 그 단언은 버리고 `= [` 부터 잘라 실제 배열을 보게 고쳤다.
