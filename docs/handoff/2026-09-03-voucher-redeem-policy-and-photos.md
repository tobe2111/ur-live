# 2026-09-03 — 이용권 사용 방식 · 리뷰 버튼 · 갤러리 중복 (대표 신고 3건)

**레일**: 🎟️ 유어딜(소비자) · **머니 경로**: 결제·정산·원장 무접촉이나 **이용권 소각(정산 근거) 조건이 바뀐다** ·
**롤백**: 아래 3건이 서로 독립 — 각각 되돌릴 수 있다.

대표 실측 신고: *"바로 사용은 어떻게 하는건데? 그 방식은 위험한거 아니야? 우리는 QR 아니면 매장 확인코드
4자리~6자리야"* · *"리뷰 등록 버튼이 아예 안눌러지게 되어있네?"* · *"사진이 1개인 이용권인데 여러 장이
있는 것처럼 나와서 좌우 버튼을 눌러도 똑같은 것만 나온다"*.

---

## ① 셀프 사용이 무방비였다 — 구멍 세 겹

`self-redeem`(손님이 앱에서 직접 사용 처리)의 게이트가 이랬다:

| 구멍 | 실측 |
|---|---|
| 기본값이 `self_free` | 사장님이 설정을 안 한 매장 전부 |
| `pre.seller_id != null` 조건 | **판매자 없는 상품은 게이트 자체를 건너뜀** — 활성 데모 이용권 100개 전량 |
| 조회 실패 시 fail-open | DB 한 번 삐끗하면 아무나 소각 |

즉 **집에서도 이용권을 태울 수 있었다.**

🩸 **이 세션이 그 구멍을 검증 절차로 안내했다** — 09-02 에 P13 실증 방법으로 *"매장 안 가셔도 됩니다,
'바로 사용' 누르세요"* 라고 적어 대표에게 보냈다. 대표가 정책(QR / 확인코드)으로 바로잡았다.

### 🔑 두 방식은 양자택일이 아니다 (이 오해가 선택지를 잘못 만들게 했다)

**직원 QR 스캔(`/:code/use-by-seller`)은 모드와 무관하게 항상 열려 있다** — 그 라우트에는 모드 검사가
한 줄도 없다(실측). 모드는 **손님 셀프 사용**만 가른다.

| 모드 | 직원 QR | 손님 셀프 |
|---|---|---|
| `scan_only` | ✅ | ❌ |
| **`store_code`** ← 기본 | ✅ | ✅ 확인코드 일치 필수 |
| ~~`self_free`~~ | — | ⛔ **폐기** |

### 수정
- `redemption-settings.ts` — 기본값 `store_code`, `REDEMPTION_MODES` 에서 `self_free` 제거(옛 행은
  **읽는 순간** 기본값이 되므로 데이터 이관 불필요), catch 도 fail-**closed**, 테이블 DEFAULT 변경.
- `group-buy-public.routes.ts` self-redeem — `seller_id` 조건 제거(항상 게이트), 매장 없는 상품은
  `NO_STORE` 403("직원 확인으로만 사용할 수 있어요"), 설정 조회 실패도 403.
- `voucher-redeem-guard.ts` — 죽은 `self_free` 분기 제거(카운터 코드입력 경로).
- `SellerVoucherScanPage.tsx` — '자유 셀프 사용' 선택지 삭제, 기본 `store_code`.

### ⚠️ 결과로 생기는 제약
**데모 이용권으로는 P13(사용→리뷰) 검증을 못 한다** — 확인코드를 발급할 매장이 없다. 대표님 실제 매장
셀러 계정이나 어드민 경로로 바꿔야 한다.

---

## ② 리뷰 등록 버튼이 "고장난 것처럼" 보였다

`disabled={content.length < 10}` 인데 **왜인지 화면 어디에도 안 썼다.** 두 글자 쓴 사람은 버튼이 죽은
줄 알고 떠나므로, 그 뒤에 붙는 서버 판정 문구(구매/사용 여부 403)를 **볼 기회조차 없다** — 09-02 에
넣은 "이용권을 사용한 뒤에" 안내가 이것 때문에 한 번도 보이지 않았다.

수정: `MIN_REVIEW_LEN` 상수 하나로 버튼 조건과 안내를 묶고, 모자랄 때 `N자 더 쓰면 등록할 수 있어요`.

---

## ③ 사진 1장이 2장으로 보였다 — 추측이 아니라 구조

`products.images` 는 만들 때부터 **첫 칸이 커버**다(recondition 이 `[cover, ...imgs]` 로 저장). 그런데
R2 이관(bulk)은 **`image_url` 만** 새 주소로 바꾸고 `images[0]` 은 옛 외부 주소로 뒀다. 같은 사진이
주소 둘을 갖고, 상세는 `[image_url, ...images]` 를 문자열로만 중복제거하므로 **두 칸**이 남는다.

**실측(라이브)**: 활성 이용권 **100개 중 99개**가 이 상태. 그중 **78개는 정확히 [이관본 + 원본] 두 장**
— 대표가 본 화면. id 2841 확인: 커버 1000×750 / 원본 1000×750, 같은 사진.

⚠️ **표시 쪽에서는 원리상 못 고친다** — R2 키가 랜덤 UUID 라 "이 주소가 저 주소의 사본" 이라는 정보가
어디에도 없다(`rehost-image.ts`). 그래서 두 값이 갈리는 **쓰기 시점**에서 고친다.

### 수정 (신규 SSOT `worker/utils/gallery-cover-sync.ts`)
- `replaceGalleryUrl` — 이관 시 갤러리 안의 **같은 주소**도 함께 교체(정확 일치만, 없으면 무동작).
- `repairGalleryCover` — 이미 갈린 행의 첫 칸을 현재 커버로 맞춤 → 중복제거가 걸려 한 장으로 접힘.
  **지우는 게 아니라 주소를 맞추는 것**이라 다른 사진이 사라지지 않는다.
- `repairGalleryCoverDrift` — 라운드당 40건 정리. **R2 바인딩 조기반환보다 앞**에 둔다(주소만 맞추는
  DB 작업이라 버킷이 필요 없고, 뒤에 두면 바인딩 없는 배포에서 영영 안 돈다 — 같은 자리에서 이관이
  넉 달간 죽어 있던 전례가 그 파일 주석에 있다). 진행도는 하트비트 `galleryFixed` 로 보인다.

---

## 검증

`tsc 0` · 신규 `voucher-redeem-and-photos.test.ts` **16건**(순수함수 동작 검증 6 + 배선 계약 10) ·
주입 5건 등록, **각각 되돌려-검증 빨간불 확인** · guard-registry 123 · file-size OK ·
sql bind/column/table 0 · theme OK · 주입 잔재 0.

🩸 **되돌려-검증이 헛도는 검사를 하나 잡았다**: "이관이 두 컬럼을 같이 쓴다" 를 파일 전체에서 찾게 했더니
**recondition 블록에 같은 SQL 문자열이 있어서**, bulk 이관의 UPDATE 를 통째로 지워도 초록이었다.
호출 지점 주변 500자만 보도록 좁혀 red 확인.

## 다음 세션 첫 액션

1. 배포 후 `/api/group-buy/products?status=active&limit=200` 을 받아 **커버가 `/api/media` 인데
   갤러리 첫 칸이 `http` 인 상품 수**를 센다. 99 → 0 으로 수렴해야 한다(시간당 40건).
2. `/group-buy/2841` 에서 좌우 버튼·도트가 사라졌는지(사진 1장) 눈으로 확인.
3. 셀프 사용: 데모 이용권은 `NO_STORE` 403, 실제 매장은 확인코드 없이는 `STORE_CODE_REQUIRED` 403.

## 남은 결정

- **P13 검증 경로** — 데모로 못 하게 됐으니 대표 실매장 계정/어드민 중 무엇으로 할지.
- 기존에 `self_free` 를 **명시로 고른** 셀러가 있으면 이번 배포로 `store_code` 가 된다(읽기 시점 매핑).
  라이브 `seller_redemption_settings` 행 수는 미확인 — 배포 후 확인 권장.

**Notion**: 머지 후 기록.
