## 🧨 2026-07-29 — **도매몰 전면 종료 결정** (유통스타트 id=1 + 메디스타트 id=2 둘 다)

대표 확정: *"도매몰을 안 할 거다. 구현한 거 덮어써도 된다."* → 몰 인프라는 **운영자 SaaS 로 전용**.
계획: `docs/design/wholesale-teardown-plan.md` (코드 변경 0).

### 🔴 다음 세션이 가장 하기 쉬운 치명적 실수
**`src/features/supply/**` 를 통째로 지우면 소비자 결제가 깨진다.** 이름이 supply 라 도매 전용으로
보이지만 아니다 — 소비자가 직접 import 한다:
`order-commissions.ts:128·297`(주문확정) · `order-refund.ts:77`(환불) · `scheduled.ts:233·238·299`(cron) ·
`cache-prewarm.ts:27`. 게다가 `buyer-*`(해외 바이어 풀 7파일)·`maker-*`(제조사 후보 풀 4파일)는
**도매와 무관한 별개 사업**이고 `worker/index.ts:1645·1647` 이 소비자 번들에서도 마운트한다.
⇒ **삭제 대상은 "도매 라우트·화면"이지 디렉터리가 아니다.**

### 🔴 착수 전 머니 게이트 (유일한 선행)
예치금 잔액 · 미확인 충전요청(pending) · 공급자 미지급 정산금 · plus 활성 구독 — **4개 전부 0 확인 후**에만
철거. 대표 동결 순서가 *"기능 OFF 를 먼저 하면 잔액이 갇힌다"* 였는데 **철거는 그보다 강한 조치**다.
0 이 아니면 환급·소진이 먼저. 확인: 대표가 `/admin/wholesale-overview` 직접.

### ⚠️ 도매 화면을 소비자 몰로 포크하지 말 것
`WholesaleCatalogPage`(813줄)는 등급 공급가·`seller_token` 인증분기·예치금 체크아웃이 뼈대에 박혀 있다.
포크하면 매입가 인접 개념이 소비자 노출면으로 넘어와 **불변식 ④** 와 부딪히고 *"신규 몰 예치금 무접촉"* 도 깨진다.
소비자 몰은 `GbMarketplacePage`·`GroupBuyDetailPage` 에 몰 스코프+브랜딩을 얹어 만든다.

### ✅ 선제 가드 구현됨 — `supply-teardown-safety.test.ts`
머니 게이트와 무관하고 코드 삭제도 아니라 **미리 박았다**(삭제 시점엔 이미 늦다).
소비자 호출부↔supply 파일 쌍 6건 + 별개 사업(buyer/maker) 3건 + 소비자 번들 마운트 2건 + cron 1건.
되돌려-검증: 정산 엔진 삭제 → 빨강 / buyer-pool 삭제 → 빨강 / 마운트 제거 → 빨강.
⚠️ 못 막는 것: 파일 남기고 export 만 삭제(tsc 담당) · 라이브 데이터·배포 상태.

### 📌 대표 확정 추가 (2026-07-29)
- **삭제는 기능 구현과 섞지 말고 별도 PR 로 고립** — 섞이면 revert 가 기능까지 되돌리고 리뷰에서 안 보인다
- **정산 엔진이 소비자 worker 에 잔류하는 한 cron no-op 게이트도 잔류** (제거하지 않음)
- **메디스타트(id=2)도 폐기**
- **P0 프론트 최소 세트 = 소비자 몰 화면 + 3분 등록 폼.** 운영자 신청·대시보드는 파일럿 동안 어드민 수동
- 세션 재분할 = 갭 문서 §8-D (③-a 몰 화면 / ③-b 3분 등록 / ③-c 예치금 격리 / ④ 픽업 / ⑤ 셀프개설은 P0 이후)
- 화면 재사용 판정 = 갭 문서 §8-A (**B2B 판매 화면 포크 금지** 명문화)

### ✅ 덤
§8-C(`mall_id=1` 이 본진과 유통스타트를 겸함)가 **자동 해소**된다 — A/B/C 마이그레이션 결정 불필요.
단 판별자와 **신규 몰 id ≥ 3** 전제는 유지(비활성이어도 id=1·2 행은 남는다).

### 📌 프론트엔드 갭 (대표 질문 — "실제 페이지도 만들어야 하지?")
맞다. 세션 ①~④ 계획이 전부 백엔드였다. 현재 화면 실측:
운영자 신청·몰개설 🔴없음 / 3분 등록 🟡풀폼만 있음(`SellerProductNewPage`) /
**소비자가 보는 몰 화면 🔴없음**(도매 화면은 전부 B2B) / 운영자 대시보드 🔴없음 /
공구 목록·상세 🟢재사용(`GbMarketplacePage`·`GroupBuyDetailPage`).
⇒ 세션 ③④ 범위를 **프론트 포함으로 재산정** 필요. P0(파일럿 1~3명)는 운영자 신청·대시보드를
어드민이 대행하면 **소비자 몰 화면 + 3분 등록 폼** 둘만으로 "엑셀을 버렸는가" 판정 가능.
