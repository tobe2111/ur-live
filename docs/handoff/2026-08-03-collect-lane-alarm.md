# 수집 레인을 알람으로 — 대표의 1순위 축이 34개 중 가장 굶고 있었다

> 대표: *"어떻게 하는게 가장 이상적이야?"* → *"응 해줘"*

## 무엇이 문제였나 (전부 실측)

`ads:collect` 는 **마지막 성공이 KST 03:01, 그 뒤 6시간 30분 정지**였다. 그동안 리드 0건, 커서 0전진.
원인이 **둘이 겹쳐 있었다**.

### ① 순번을 거의 못 받는다

```
KST 09:00 회차 (per_tick 3 · 무료 플랜)
  influencer  budget 1 → run:[inbound-onboarding]
                         deferred:[collect, consented-reminder, social-maintenance]
  company     budget 1 → run:[3개]  always:2
  prospect    budget 1 → run:[collect-hira]  deferred:[4개]
  wholesale   budget 0 → run:[collect-maker]
```

인플루언서 도메인은 **레인 4개가 시간당 예산 1칸**을 나눠 쓴다 → collect 는 잘해야 4시간에 한 번.

### ② 그 한 번마저 죽는다

```
22:00:35  디스패치 (run:['collect'])
22:00:38  ads:collect            Worker exceeded CPU time limit.   ← 3초 뒤
22:00:38  ads:enrich-company     Worker exceeded CPU time limit.
22:00:38  ads:collect-storeinfo  Worker exceeded CPU time limit.
22:00:37  ads:sheets-sync        Worker exceeded CPU time limit.
```

자식 인보케이션의 CPU 는 부모에게 청구되므로 B2B 29개와 **같은 벽**에 부딪힌다.
24시간 CPU 사망 29건의 최다 소비자는 `sheets-sync` 6 · `enrich-company` 6 — **둘 다 인플루언서 축이 아니다.**

⇒ **예산 재분배로는 못 푼다.** 누가 굶느냐만 바뀌고 벽은 그대로라, 순번을 받아도 또 죽는다.

## ✅ 처방 — `collect` 를 DO 알람 레인으로 (`ALARM_LANES` 등록부)

자기 인보케이션·자기 CPU 를 갖는다 → B2B 29개와의 경쟁이 **끝난다**.
어젯밤 같은 패턴을 두 번 검증했다(`enrich-influencer`·`maintenance` 둘 다 fail_streak 0, 시간당 12회차 안정).
부수 효과로 인플루언서 도메인 예산 1칸이 비어 **나머지 3개 레인 순번도 빨라진다.**

### ⚠️ `runsPerHour: 1` — 증설이 아니라 복원이다

cron 이 원래 `0 * * * *`(시간당 1회)다. 기본값 12를 그대로 받으면 **설계 의도를 12배 넘는 증설**이고,
그건 네이버로 나가는 요청을 늘리는 일이라 **대표 판단 사항**이다. 여기서는 의도한 값으로 되돌리기만 한다.

### 🩸 내가 어제 틀렸던 판단 (이게 이 문서에서 제일 중요하다)

어제 나는 이 처방을 **"외부 검색량 4배"라며 보류**했다. 세 가지를 잘못 봤다:

1. **YT 검색량은 안 늘어난다** — `ytBudgetTotal`(하루 90~100)이 하드캡이라 회차 수와 무관하게 총량이 같다.
2. **네이버는 하루 25,000 쿼터에 실사용 ~2%** — 4배여도 8%.
3. 무엇보다 **"4배"의 기준이 설계 의도가 아니라 *고장 난 현재*(6시간에 한 번)였다.**

그리고 대표가 "밀지 마라"고 한 건 **측정 레인을 4조각으로 쪼개는 건**이었다. 그건 네이버에 **동시 요청**을
4배로 늘리는 일이라 성격이 완전히 다르다. **나는 그 판단을 엉뚱한 건에 가져다 붙였다.**
⇒ 교훈: *"과거 지시를 새 건에 적용하기 전에, 그 지시가 무엇을 걱정한 것인지부터 맞춰볼 것."*

## 🛡️ 가드 3건 (전부 일부러 깨뜨려 빨간불 확인 후 복원 · 매니페스트 등재)

| 불변식 | 안 지키면 |
|---|---|
| 알람이 몰면 부모는 수집을 안 던진다 | 리스가 이중 *실행* 은 막지만 **던지는 것 자체가 부모 CPU 를 먹는다** — 원인 재발 |
| `laneAlarmOn` 선언이 첫 사용보다 앞 | `const` TDZ → **런타임 ReferenceError**, 그런데 **타입체크는 통과한다**(작성 중 실제로 밟았다) |
| 수집 `runsPerHour: 1` | 빼면 기본 12 = 조용한 12배 증설 = 대표 판단 없이 외부 요청량 변경 |

## ▶️ 다음 세션의 첫 액션 — 배포 후 판정

배포 뒤 **다음 정각**에:

```bash
bash scratchpad/watch_collect.sh
```
- `collect_last_run` 이 **매시간 갱신**되면 성공(지금은 `2026-08-02 18:01:21` 에 고착).
- `focus_cursor` 가 생기고 값이 늘면 **#990 도 같이 판정**된다(그동안 collect 가 안 돌아 미판정이었다).
- `never_run` **16 → 감소** 하면 잠들어 있던 대행사 키워드가 깨어난 것이다.
- `ads_lane_alarm_last:collect` 의 `fail_streak` 이 0 이 아니면 알람 안에서 실패하는 것 — 그건 다른 문제다.

## 📌 남은 것

- **(b) 비-인플루언서 CPU 정리는 후속이다.** `sheets-sync` 는 24시간에 6번 죽는 raw-waitUntil 우회 레인이라
  부모 CPU 를 직접 태운다. 모두를 해치는 순수 낭비지만 collect 살리기와는 별개 건이다.
- 이 세션의 도구 결함 수리: `d1.sh`/`d1b.sh` 가 호출마다 **고유 임시파일**을 쓴다. 예전엔 공유
  `q_res.json` 이라 **백그라운드 감시가 내 쿼리 결과를 덮어썼다**(실제로 오진 직전까지 갔다).

---

## 🪦 축 줄이기를 자연스럽게 (대표 *"줄이기도 자연스럽게 만들어줘"*)

### 왜 필요했나 — 늘리기와 줄이기가 비대칭이었다

**늘리기는 이미 자연스러웠다.** 규칙 하나 추가하면 `CLASSIFIED_CATEGORIES` 가 자동 파생되고,
어드민 필터 누락은 유닛이 막고, 기존 리드도 재분류가 ~2시간 안에 끌어온다(골프 축 222명이 증거).

**줄이기는 반대였다.** 규칙만 지우면 세 군데에 잔재가 남는다:

```
① 리드의 카테고리 값 — 아무 규칙도 안 만드는 유령 값으로 영구 잔존
② 키워드의 카테고리 — 폴백(resolveCategory)이 새 리드에 계속 그 값을 붙임
③ 수집 슬롯        — 죽은 축 키워드가 희소한 회차(시간당 1회·16픽)를 계속 먹음
```

라이브는 아직 **저장 카테고리 18개 = 분류기 18개**로 일치한다 —
**아직 아무것도 지운 적이 없어서** 문제가 안 드러난 상태였다.

### ⚠️ 왜 "규칙에 없으면 지운다"로 일반화하지 않았나

`shouldClearCategory` 의 docblock 이 정면으로 경고한다 —
*"모르는 값을 지우면 사람이 손으로 고친 분류까지 날린다."*

**분류기가 안 만드는 값 ≠ 잘못된 값**이다. 키워드 폴백은 키워드에 적힌 **어떤 문자열이든** 붙일 수 있고
그건 정당한 경로다(실측: 활성 키워드 카테고리 18종 중 `자동` 은 분류기가 안 만든다).
⇒ 추론이 아니라 **선언**(`RETIRED_CATEGORIES`)으로 받는다. 사람이 "끝났다"고 말한 것만 지운다.

### 줄이는 절차 — 이제 두 줄이다

1. `RULES` 에서 그 규칙 제거   2. `RETIRED_CATEGORIES` 에 이름 추가.
끝. 리드 정리·신규 유입 차단·슬롯 회수는 자동이다.

### 🧪 헛도는 가드를 만들 뻔했다 (기록해 둔다)

첫 구현의 동작 검사가 `for (const c of RETIRED_CATEGORIES)` 였는데 **그 목록이 비어 있다**
(접은 축이 아직 없다) → **0건을 돌며 영원히 통과**. 이 레포가 반복해 겪은 바로 그 클래스다.
⇒ `shouldClearCategory` / `resolveCategory` 에 **주입구**(`retired = RETIRED_CATEGORIES`)를 냈다.
`pickYtKeywords(…, priorityCats)` 와 같은 기존 seam 패턴이라 이질적이지 않다.
테스트는 가짜 은퇴 축(`접은축`)으로 **진짜 동작을 검사**한다.

가드 4건 전부 주입해 빨간불 확인(비우기 제거 · 폴백차단 제거 · 슬롯필터 제거 · 규칙과 은퇴 동시선언).

### 📌 아직 못 하는 것

- **어드민 필터(`POOL_CATEGORIES`) 정리는 수동**이다. 리드가 다 비워지면 빈 필터 항목이 남는다(무해).
- `저장된 카테고리 ⊆ 분류기 ∪ 키워드 ∪ 은퇴유예` 는 **DB 를 봐야 알 수 있어** CI 가 못 본다.
  줄인 뒤 한 바퀴(~2시간) 지나서 `SELECT category, COUNT(*) … GROUP BY category` 로 확인할 것.

---

## 📏 측정 레인 전수조사 — 결함 4건 (대표 *"측정으로 전수조사"* → *"모두 구현"*)

audit-gate **84 GREEN** 이라 가드 보유 영역은 건너뛰고, 가드 없는 곳만 라이브로 팠다.

### 발견 요약

```
유튜브 쿼터 10,000/일
  검색  배정 9,000(90회) · 실사용 2,200(22회)   ← 6,800 유휴
  성과  배정 2,000       · 실사용 2,003         ← 소진 → 그날 남은 시간 측정 0
  총 사용 4,200 (42%)

수율   유튜브 45.2%(1,437/3,181)  vs  네이버 블로그 28.6%(3,811/13,337)
       ⇒ **멎은 쪽이 더 좋은 축이었다.** 미측정 YT 3,161 × 45% ≈ 이메일 1,420명
효율   PT 하루 2,003 units 로 106행 = **19콜/행** (코드상 건당 2~3콜)
```

### ①-a 재선택 churn — 19콜/행의 정체 (**가장 중요**)

선택 순서가 `(pub_checked_at IS NULL) DESC, subscriber_count DESC` 인데, 예산으로 건너뛴 행의
UPDATE 가 `pub_checked_at` 을 **안 찍었다**. 그래서 그 행은 **다음 회차에도 맨 앞**이고,
회차마다 `channels.list` 를 다시 태운다. 예산 14로는 20행 중 앞 ~11행만 완주 → 나머지는 영구 churn.

🔑 여기까지 왔다는 건 `if (!chJson) return 0` 을 통과했다는 뜻 = **channels.list 는 성공**했고
About/개설일/카테고리를 지금 쓰고 있다. 즉 "pub 은 확인됨"이 **사실인데 스탬프만 없었다.**
⇒ 스탬프를 찍는다. `perf_checked_at` 은 **여전히 안 찍는다**(0 각인 금지 — 기존 사고 그대로).

### ①-b 성과 상한이 고정 2,000

상수 주석은 *"검색 예산을 크게 **올릴** 때는 이 값을 낮출 것"* 이라고 **한 방향만** 대비했다.
반대(검색이 배정을 안 쓰는 경우)가 실제로 일어났다 — 키워드 쿨다운·고갈 억제가 검색을 22회로 누른다.
⇒ `resolveYtPerfCap(검색실사용)` — 검색 바닥(3,000)은 지키고 나머지를 성과가 받는다.
   실측 대입: 2,200 → **상한 7,000**(3.5배). 기본값 2,000 밑으로는 절대 안 내려간다.

### ③ 선두 교대가 알람에서 반쪽 — ①-b 와 **한 몸**

`depth % 2 === 1 || starvedLastRound(prev)` 인데 **알람은 `depth` 가 항상 0**(`chain.rounds: 1`).
`0 % 2 === 1` 은 영원히 거짓 ⇒ 결정적 교대가 **한 번도 발화한 적 없다.**
지금은 앞 레인이 0을 내서 무해했지만, **①-b 로 YT 가 살아나면 블로거가 굶는다.**
⇒ 깊이 대신 **직전 선두**(`led`)로 교대.

🪦 **기존 유닛이 이 죽은 식을 못 박고 있었다** — *"두 신호를 OR 로 묶는다 — 어느 하나가 죽어도
교대가 산다"* 라고 적어 놓고 실제로는 반쪽을 고정. 가드가 **사실이 아닌 것을 지키는** 형태였다.
그 테스트를 교체하고 왜 바꿨는지 그 자리에 남겼다(되돌리지 말라는 경고 포함).

### ④ cron 시절 창(7초)이 알람에 그대로

7초의 근거는 *"부모 인보케이션이 10.5초에 회수되고 자식이 함께 죽는다"*. **알람엔 부모가 없다.**
증거: 같은 알람의 `collect` 가 **28,643ms 완주**(2회차 연속, fail_streak 0). ⇒ 알람 경로 20초.
⚠️ 지금 병목은 시간이 아니라 예산(`spent 44/45`, 4.7초 종료)이라 **이것만으론 아무것도 안 변한다** —
①-b 와 짝이어야 의미가 있다.

### ✅ 결함 아님 (고치지 않았다)

- **링크인바이오 0** — 큐가 실제로 0(전부 처리 완료). 배정 6은 `naverRoomFromRemaining` 이 실제
  잔여로 재계산해 낭비 없음(실측: 배정 10인데 블로거가 22 처리).
- **건당 2 fetch** — `wantHome` 이 연락처 필요할 때만 켜진다. 이미 최적.
- **큐 순서** — `ORDER BY perf_checked_at ASC` 로 미측정(NULL) 우선. 정상.

### ▶️ 다음 세션의 판정 (라이브 한 사이클 뒤)

```bash
bash scratchpad/d1.sh "SELECT substr(value,1,320) FROM platform_settings WHERE key='ads_influencer_enrich_last'"
```
- `yt_units.total` 이 **2,000 → 7,000** 이면 ①-b 적용됨
- `yt_rows` (이번에 신설한 계측) 와 `yt_units.used` 로 **콜/행**을 계산 — **19에서 내려가야** ①-a 성공
- `led` 가 회차마다 `naver` ↔ `front` 로 번갈면 ③ 성공
- `elapsed_ms` 가 7,000 을 넘는 회차가 보이면 ④ 적용됨(넘을 일이 생겼다는 뜻)

⚠️ **못 하는 것**: 19콜/행이 정확히 왜였는지는 정적으로 확정 못 했다(코드상 2~3콜). churn 이 가장
유력한 설명이고 그래서 계측을 같이 넣었다 — **한 사이클 뒤 숫자가 답한다.**

---

## 🧾 CI 가 하나 더 잡았다 — 순수 이동이 드러낸 선재 위반 (#1015 2차)

`d89c67c`(600줄 래칫 대응 분리) 뒤 Verify 가 **`check-budget-bookkeeping`** 으로 빨간불.

```
⚠️ 예산 루프가 자기 기록을 쓸 몫을 안 남긴다:
   - src/features/marketing/api/influencer-yt-performance.ts:95
```

**원인은 내 이동이 아니라, 이동이 *드러낸* 선재 위반이다.** 래칫 베이스라인이
`influencer-performance.ts:157` 로 **경로에 동결**돼 있었고, 같은 루프가 새 파일로 옮겨가자
동결이 안 맞아 신규 위반으로 잡혔다. 이 레포가 이름 붙인 **"낡은 지도"** 클래스이고,
같은 커밋에서 이미 세 번(테스트 2 + 가드 1) 겪은 것의 네 번째다.

### 미루지 않고 고쳤다 (rebaseline 이 아니라)

```diff
- for (let i = 0; i < allIds.length && budget.left > 0 && !outOfTime(budget); i += 50)
+ for (let i = 0; i < allIds.length && budget.left > 1 && !outOfTime(budget); i += 50)
```

루프 **뒤**에 `DB.batch(stmts)` 가 온다 — **이 회차 측정 결과 전량의 유일한 쓰기**다. D1 도
서브리퀘스트라 루프가 마지막 칸까지 쓰면 batch 가 던지고 `.catch(() => null)` 이 삼킨다.
그러면 **채널콜·영상콜 쿼터를 다 태우고 저장 0** 이고, 스탬프(`perf_checked_at`)도 안 찍히니
그 행들이 다음 회차에도 맨 앞이다 — **이 PR 이 잡으려던 재선택 churn 을 되레 만드는** 경로다.
①-b 로 YT 상한이 2,000 → 7,000 이 되면 이 루프가 예산 끝까지 도는 회차가 실제로 생긴다.

베이스라인에서 그 항목을 지웠다(2건 → 1건, `franchise-collect.ts:91` 만 남음). 남겨 두면
*미래에* 그 좌표에서 생기는 새 위반이 조용히 통과한다.

### 불변식으로 못 박음 + 주입 검증

`ads-enrich-throughput.test.ts` 에 배선 검사 + `check-guard-mutations` 매니페스트 137번째 항목.
**둘 다 손으로 깨뜨려 빨간불 확인**(`> 1` → `> 0`): 유닛 1 failed · 래칫 `❌ 차단`. 복원 확인.

## 🕳️ 같은 함정을 두 번 밟았다 — 커밋 메시지의 `[SKIP_SIZE]`

`d89c67c` 의 메시지에 *"`[SKIP_SIZE]` 로 우회하지 않고 분리한다"* 라고 **안 쓰겠다는 선언**을
적었는데, 부분 문자열 매칭이라 그게 우회로 잡혀 **그 뒤 로컬 pre-commit 이 전부 skip** 을 찍었다.

놀라운 건 `check-file-size.mjs` 주석이 **이 사고를 이미 적어 두고 있었다는 것**이다(2026-07-29,
문구까지 거의 같다). 그때 고친 건 "게이트/CI 모드에선 안 읽는다" 쪽이고, **매칭 자체는 그대로**
뒀다 — CLAUDE.md 가 경고하는 *"문서 기재로 끝내지 말 것"* 의 표본이다.

⇒ **백틱 인용 구간을 지우고 판정**하도록 고쳤다(`msg.replace(/\`[^\`]*\`/g, '')`). 두 사고 모두
백틱 안이었고 실제 우회는 맨 토큰으로 쓴다. 양방향 실증:

| 메시지 | 판정 |
|---|---|
| `test bare token [SKIP_SIZE] here` | skip (우회 유효 — 유지) |
| 백틱으로 인용만 한 메시지 | **skip 안 함** |

⚠️ 못 막는 것: 백틱 없이 산문에 쓴 언급은 여전히 우회로 잡힌다.
🩹 **CI 는 영향 없었다** — PR 판정은 `--changed-only -s` 라 메시지를 아예 안 읽는다(그 레그는
07-29 에 이미 고쳐져 있었다). 이번에 샌 건 **로컬 pre-commit 뿐**이다.

---

## ✅ 라이브 판정 — #1015 머지(`242c5a0`) 후 3회차 실측 (KST 16:20~16:30)

| 항목 | 이전 | 지금 | 판정 |
|---|---:|---:|---|
| `yt_units.total` (성과 상한) | 2,000 | **7,000** | ①-b **성공** |
| **콜/행** | **19** | **1.29** (18 units / 14행) | ①-a **성공** |
| `led` | 필드 없음 | `naver`→`front`→`naver` | ③ **성공** |
| `elapsed_ms` | — | 4,694 / 6,932 / 5,221 | ④ **미판정**(7초 미도달) |
| YT 측정량 | 111행/일 | **회차당 14행 × 교대** = ~2,016/일 | **18배** |

### 🩸 하마터면 오진할 뻔한 것 — 쿼터일 롤오버

배포 직전 07:15 회차가 이미 `yt:14` 를 냈다. **내 수정 때문이 아니다** — YT 쿼터일은
태평양 자정(=UTC 07:00)이라 그 순간 `ads_yt_perf_units` 가 `2026-08-02:2003` →
`2026-08-03:48` 로 리셋됐고, **옛 코드가 옛 상한(2,000)으로** 되살아난 것뿐이다.
⇒ **배포 판별자는 `yt_units` 가 아니라 `led`·`yt_rows`**(이번에 신설한 필드)다.
`yt_units` 는 원래 있던 필드라 신구를 못 가른다. 다음에 같은 판정을 할 때 주의.

### 병목이 바뀌었다 — 이제 쿼터가 아니라 레인 예산

2,016행/일 × 1.29 = **~2,600 units/일** 로 상한 7,000 의 37% 만 쓴다.
즉 **YT 의 제약은 이제 쿼터가 아니라 회차당 서브리퀘스트 예산(45)** 이다.
지금 더 밀 이유는 없다 — 아래 소진 예상대로면 2~3일 안에 큐가 빈다.

### 소진 예상 (실측 rate 기준, 2026-08-03)

```
네이버 블로그  미측정 20,429 · 측정 3,391/일 · 유입 1,557/일 → net −1,834 → ~11일 (8/14)
유튜브         미측정  4,253 · 측정 2,016/일 · 유입   279/일 → net −1,737 → ~2.4일 (8/6)
네이버 카페     3,146 · 측정 경로 **없음**
티스토리          468 · 측정 경로 **없음** (그런데 유입은 110/일 — 못 쓸 행이 계속 쌓인다)
```

⚠️ YT 큐가 먼저 비면 YT-선두 회차가 놀게 된다. 그때 블로거가 그 회차를 가져가는지
(=`pickNaverFirst` 가 빈 큐를 어떻게 다루는지) **확인이 필요하다** — 안 가져가면 하루 절반을
버리게 된다. 8/6 전후에 `led`·`naver.selected` 로 판정할 것.

### 남은 결정 (대표 판단 — 시간이 해결 못 함)

1. **카페·티스토리 3,614행** — 측정 경로 신설 / 수집 중단 / 이메일 없는 축으로 명시. 셋 중 하나.
   티스토리는 RSS 가 있어 블로그 레인 재사용이 가장 싸다. 카페는 회원수 실패 이력이 있어 비싸다.
2. **이메일 보유율 14.7%** — 백로그를 다 비워도 45,000 중 ~10,500 이다. 이건 백로그가 아니라
   **수집원의 성질**이라 처리량으로 안 바뀐다. 더 늘리려면 수집 축 자체를 바꾸는 판단이 필요.

---

## 🩹 정정 — 앞 절의 "YT 큐가 비면 하루 절반을 버린다"는 **틀렸다**

코드를 읽고 확인했다. `runNaver()` 와 `runFront()` 는 **매 회차 둘 다 실행**되고 `naverFirst` 는
**순서만** 바꾼다. 게다가 `naverRoomFromRemaining(budget.left, naverMax)` 가 그 시점의 **실제 잔여**로
블로거 몫을 다시 계산한다 ⇒ YT 큐가 비면 front 가 즉시 끝나고 **남은 예산 전부를 블로거가 가져간다.**
손실 0이다. 작업 항목으로 남길 필요 없다.

> 왜 틀렸나: 실측 스냅샷에서 `led:"naver"` 회차의 `yt:0` 을 보고 *"선두가 아니면 못 돈다"* 로 읽었다.
> 실제 이유는 **블로거가 먼저 예산을 44/45 써 버려서**지, 구조가 막아서가 아니다. 같은 관찰에서
> 두 해석이 나오면 코드를 열어야 한다 — 이번엔 안 열고 인계에 리스크로 적었다.

## 🧾 무료 한도 전수 점검 (대표 질문 *"네이버·카카오 등 무료 한도 안에서 쓰고 있는 게 맞나"*)

| API | 무료 한도 | 실측 | 계측 |
|---|---:|---:|---|
| YouTube Data | 10,000 units/일 | 266 (성과 66 + 검색 200) | ✅ `ads_yt_perf_units`·`ads_yt_search_used` |
| Kakao Local | 100,000 회/일 | 410 | ✅ `ads_kakao_sweep_stats.day_lookups` |
| Naver 검색 오픈API | 25,000 회/일 | **계측 없었음** → 신설 | ✅ `ads_naver_api_used` (이번에 추가) |
| 네이버 블로그 직접조회 | (한도 아님) | ~6,800 요청/일 | 간접(`naver.failed`) |

**초과의 대가는 청구서가 아니라 차단이다** — 세 API 모두 한도 초과 = 403/429 이고 과금이 없다.
그래서 지켜야 할 이유는 비용이 아니라 **그날 수집·측정이 멎는 것**이다. 이 구분을 흐리면
"한도 안"이라는 안심이 엉뚱한 곳(비용)으로 간다.

### 고친 것 ① — 유튜브에 구조적 초과 구간이 있었다 (내가 오늘 넣은 코드)

`resolveYtPerfCap` 이 검색 몫 3,000 을 예약하면서 **성과 바닥 2,000 을 무조건 보장**했다.
검색 배정이 90회(9,000)라 `9,000 + 2,000 = 11,000 > 10,000` 이 **설정상 도달 가능**했다.
⇒ 바닥도 `min(2,000, 총쿼터 − 검색실사용)` 으로 깎는다. 남은 게 0이면 성과도 0이다.
옛 불변식 *"기본값 밑으로 안 내려간다"* 를 **총합 ≤ 쿼터**가 대체한다(그쪽이 상위 규칙이다).

### 고친 것 ② — 네이버만 계측이 없었다

`naver-api-usage.ts` 신설. 세 개 fetch 래퍼(`fetch-with-err`·`company-collect`·`contact-enrich`)가
호스트를 보고 세고, 수집 레인이 회차 끝에 **기존 settings batch 에 얹어** 저장한다
(**서브리퀘스트 추가 0** — 콜마다 D1 을 쓰면 네이버 콜 1회가 2회가 되어 레인 예산을 반토막 낸다).
B2B 레인은 batch 가 없어 `flushNaverCalls`(읽기1+쓰기1, 누적 0이면 왕복 0).
어드민 상태줄에 `naver_api: {used,total,day}` 노출.

⚠️ **이 값은 실사용의 하한이다** — 어드민 온디맨드 도구(`keyword-tools`·`rank-tracker`·
`competitor-tracker`)는 사람이 눌러야 돌고 볼륨이 낮아 계측 밖이다. 그리고 **실제 한도는 앱 설정에
따라 다를 수 있다** — 25,000 은 네이버 공표 기본값이고, 초과 판정의 진짜 근거는 429 응답이다.

### 🕳️ 자기검증에서 잡은 것 — 내 가드가 두 번 헛돌았다

1. `takeNaverCalls()` 호출 횟수를 원본에서 셌더니 **내 설명 주석**이 두 번째로 잡혀 정상 코드가 빨간불.
2. 래퍼 배선 검사가 `noteNaverCall(url)` **문자열만** 봐서, 주입이 그 줄을 **주석 처리**했는데 **초록불**.
   계측이 죽었는데 가드는 통과 — CLAUDE.md 의 *"주석에만 남아도 통과"* 그 함정이다.

⇒ 둘 다 **주석을 지운 사본**에서 판정하도록 고쳤고, 7건(래퍼 3 + take 중복 + batch 우회 + B2B flush +
YT 바닥) 전부 주입해 빨간불을 확인했다. `check-guard-mutations` 매니페스트에 4건 등록.

> 교훈: **"주입해 봤다"가 아니라 "주입이 실제로 적용됐고 빨간불이 떴다"** 까지 봐야 한다.
> 첫 회차에서 ①만 빨간불이었고 나머지는 초록이었는데, 그 초록이 "안전"이 아니라 "눈멂"이었다.

---

## ✅ 무료 한도 계측 라이브 판정 (#1018 머지 `7f13364` → KST 19:00 회차)

```
ads_naver_api_used = "2026-08-03:4"          ← 배선 성공(키 신설)
spent 56 / budget_total 56                    ← 직전 회차와 완전 동일
naver_api: {used:4, total:25000, day:"2026-08-03"}   ← 상태줄 노출
그 회차 수집: naver 200/38 · yt 117/24 · tistory 15
```

**설계 전제가 지켜졌다.** 계측은 보통 왕복이 늘어 레인 예산을 갉는데, 기존 `SETTING_KEYS` 읽기와
`writeSettings` batch 에 얹어 `spent` 가 **한 칸도 안 늘었다**(56 → 56).

### 세 축 실측 (KST 19:00)

| API | 무료 한도 | 실측 | 비율 |
|---|---:|---:|---:|
| YouTube Data | 10,000 units/일 | 1,122 (성과 322 + 검색 8회×100) | 11% |
| Kakao Local | 100,000 회/일 | 410 | 0.4% |
| Naver 검색 오픈API | 25,000 회/일 | 회차당 4 → 하루 ~96 | 0.4% |

⚠️ **네이버 96/일은 인플루언서 수집 레인만이다.** B2B(`ads_company_stats`)는 마지막 실행이
18:00 KST 로 **배포(18:04) 직전**이라 아직 안 잡혔다 — 다음 회차부터 같은 통에 합산된다.
⚠️ 어드민 온디맨드 도구는 계측 밖 ⇒ 이 값은 **실사용의 하한**이다.

### 🕳️ 판정 과정에서 걸러 낸 오진 2건

1. **19:00 정각 조회에서 키가 없었다** — "배선 실패"로 읽힐 뻔했다. 회차가 ~32초 걸려 **아직 안 끝난
   것**이었다(19:01:48 에 나타남). 정각 조회는 회차 완주 전이다.
2. **18:00 회차에 키가 없던 것도 실패가 아니었다** — CF API 로 `ur-ads` 배포 시각이 **18:04**(회차 4분 뒤)임을
   확인해 "아직 안 돈 것"과 "배선이 죽은 것"을 갈랐다. **키 부재는 두 원인 다 만든다 — 배포 시각을 봐야 갈린다.**

### 🔑 CF 토큰 — 스크래치 사본은 만료, D1 SSOT 는 정상

스크래치패드의 `cf.env` 가 `Authentication error` 였고, `platform_settings.cf_api_token` 의 것은
`verify: active` 였다(CLAUDE.md 절차대로 어드민 API 로 재취득). **토큰이 죽었다고 판정하기 전에 D1 에서
다시 꺼내 볼 것** — 07-29 세션이 "토큰 죽음"으로 오판했던 것과 같은 자리다.

---

## 📗 티스토리 측정 경로 신설 (대표 *"다 해줘"* — 남은 3항목 중 1)

```
platform   총     미측정   이메일
tistory    495    495      0        ← 측정 경로가 **아예 없었다**. 유입 ~216/일
```

수집(`discoverTistoryBloggers`)은 카카오 Daum 검색 **스니펫**에서만 컨택을 뽑는데, 스니펫은 남의 글
본문이라 `requireContext: true` 를 거의 못 넘는다 — 그래서 0/495 다. 네이버는 같은 문제를
`enrichNaverActivity`(RSS + 홈)로 풀었고 티스토리만 짝이 없었다. 두면 8/19 엔 ~3,900행이 못 쓰는 채로 쌓인다.

**신규 `influencer-tistory-performance.ts`** — 판정 로직(추출·분류·채점·스탬프 규칙)은 공용 헬퍼로
진짜 공유하고 루프 골격만 따로 갖는다. **네이버 파일은 한 줄도 안 건드렸다**(가드로 고정: `NAVER` 소스에
`tistory` 문자열 0). 티스토리가 더 단순해서 가능: 핸들 자가복구 불필요(서브도메인) · 이웃수 없음.

**몫은 `TISTORY_ROOM = 2`** — 이 값이 곧 블로거에게서 뺏는 양이다(회차당 최대 4 서브리퀘스트 ≈ 9%).
백로그 비(495 : 20,264 = 2.4%)로는 1 미만이 비례지만 유입 216/일을 못 따라가면 백로그가 되레 는다.
2 면 ~576행/일 → 며칠 안에 큐가 비고 **그 뒤엔 스스로 안 쓴다**(측정된 행은 `perf_checked_at ASC` 에서 뒤로).

⚠️ **이 환경에서 실물 응답을 한 번도 못 봤다** — 프록시가 `tistory.com` 을 CONNECT 403 으로 막는다.
⇒ **판정은 라이브 diag 로만**: `tistory.measured` 가 계속 0 → RSS 경로가 틀림 ·
`tistory.contacts`/`emails` 가 계속 0 → 홈에 연락처가 없음 ⇒ **추측으로 파서를 더 손대지 말고 경로를 접을 것.**

### 🕳️ 자기검증에서 또 헛돌았다 (오늘 세 번째)

순서 가드를 `c.indexOf('enrichTistoryActivity')` 로 썼는데 그게 맨 위 **import 문**을 먼저 찾아,
호출 순서를 실제로 뒤집는 주입에도 **초록**이 떴다(import 는 언제나 첫 번째다).
⇒ 호출부(`enrichTistoryActivity(DB, budget`)로 앵커를 옮겨 빨간불 확인. 매니페스트에 2건 등록.

> 오늘 같은 클래스를 세 번 밟았다: ① 주석이 카운트에 잡힘 ② 주석 처리해도 문자열이 남아 통과
> ③ import 가 호출부보다 먼저 매치. **문자열 매칭 가드는 "어디에 있는 문자열인가"까지 봐야 한다.**

## 🚫 카페 — 측정 경로를 만들지 않는 것이 맞다 (실측 근거)

```
naver_cafe  3,146행 · 이메일 0 · 인스타 13 · 링크 68 · 유입 ~51/일
표본: 부산경남 맘스홀릭 육아카페 · 세종시닷컴 · 다이렉트웨딩카페 · 베나자(베트남 여행)
```

전부 **커뮤니티**지 개인이 아니다. 제휴 제안은 카페 **매니저**에게 가야 하는데 네이버는 그 연락처를
공개하지 않는다(2026-07-29 '카페 회원수 실패 3/3' 도 같은 벽이다 — 로그인 없이는 안 보인다).
⇒ 티스토리와 달리 **경로를 만들어도 이메일 수율이 0에 수렴**한다. 만들지 말 것.
남은 선택지는 **수집 중단**이냐 **별도 축으로 명시**냐이고, 그건 DB 전략이라 대표 판단이다.

## 📊 B2B 레인 CPU 사망 — 처방이 "알람 이관"이 아닌 이유

12시간 실패 로그: `enrich-prospects`·`sheets-sync`·`collect-neis`·`collect-store-kakao`·
`collect-storeinfo`·`enrich-company`·`match-registry` 가 각 1~2회 `Worker exceeded CPU time limit`.
**`ads:collect` 는 목록에 없다** — #1000 알람 이관 이후 0건(목적 달성).

⚠️ **그렇다고 7개를 다 알람으로 옮기면 안 된다.** `lane-alarm-runners.ts` 헤더가 이미 경고한다:
알람은 자기 인보케이션을 쓰므로 **무료 한도를 레인 수만큼 곱하고**, 부트스트랩이 레인당
`DO fetch 1 + 하트비트 1` 이라 정각 부모의 서브리퀘스트가 3레인 6 → 10레인 20(예산 50의 40%)이 된다.
지금 부모가 죽는 이유가 바로 그 예산인데, 처방이 같은 예산을 더 먹는 형태다.

**진짜 구조**: 34개 레인이 `per_tick 3` 을 나눠 쓴다(시간당 3 슬롯 = 하루 72, 레인당 ~2회).
실측 하트비트가 그 결과다 — `sweep-kakao-phone` 마지막 실행 **7/29**(5일 전), `sweep-mx`·`sweep-nts`·
`scan-notices` 는 8/1. 사망이 아니라 **차례가 안 오는 것**이고, 알람 이관은 그 굶주림을 못 고친다.
⇒ 이건 "레인이 34개나 필요한가"라는 **범위 결정**이지 버그 수리가 아니다. 대표 판단 사항.

---

## ✅ 티스토리 라이브 판정 1차 — 경로는 맞았다 (#1027 `8d467cf`, KST 21:00)

```
tistory: {tried:2, measured:2, contacts:2, failed:0, selected:2, rss_intro:2}
DB: 507행 · 미측정 503(전 505) · 인스타 1 · 링크 7 · recent_posts_30d>0 4행 · 이메일 0
```

프록시 때문에 여기서 못 봤던 것이 전부 맞았다 — **RSS 주소·홈 파싱 100% 성공, 실패 0.**
설계대로 회차당 2행. 24행/시 = 576/일, 유입 216/일 → net −360/일 ⇒ **~1.4일이면 큐가 빈다.**

### ⚠️ 아직 판정 못 한 것 — 이메일 수율

4행에서 이메일 0. **표본이 너무 작아 결론이 안 된다**(네이버 수율 12%면 4행의 기대값은 0.5).
⇒ **~28행(1시간) 시점이 진짜 판정선**이다:
  · 3~4건 나오면 네이버와 동급 — 경로 유지
  · **계속 0이면** 티스토리 홈엔 이메일이 없다는 뜻 ⇒ 인스타·링크만 얻는 축으로 **재평가**
    (제휴 제안은 이메일이 있어야 보낼 수 있으므로 이 구분이 중요하다)

⚠️ **"작동한다"와 "쓸모 있다"를 섞지 말 것.** 지금 확인된 건 앞의 것뿐이다.

### ✅ 2차 판정 (KST 21:56, 표본 26행) — **둘 다 확인됐다. 경로 유지.**

```
측정 26/26(활동성 전부 기록) · 실패 0 · 외부링크 29
이메일 3 / 26 = 11.5%   ← 네이버 블로그 12.1% 와 사실상 동급
최근 회차: {tried:2, measured:2, contacts:2, failed:0, rss_intro:2, emails:1}
```

**1시간 전 "4행에서 0"은 역시 표본 부족이었다** — 26행으로 늘리자 11.5%가 나왔다.
걱정했던 "인스타·링크만 나오는 축"이 아니라 **제안을 보낼 수 있는 이메일이 나오는 축**이다.

```
잔여 481행 × 11.5% ≈ 55명 · 유입 216/일 × 11.5% ≈ 25명/일 지속
소진: 576/일 처리 − 216/일 유입 → net −360/일 → ~1.3일
```

📌 **다음 세션이 확인할 것**: 큐가 빈 뒤(8/5 전후) `tistory.selected` 가 0~2 로 떨어지는지.
   안 떨어지면 `perf_checked_at` 스탬프가 안 찍히고 있다는 뜻이다(=YT 에서 났던 churn 클래스).

---

## 📉 수율 감쇠 — 대표 관찰 *"유튜브는 이메일 비율이 떨어진 것 같네"* 가 맞았다 (KST 00:10)

### 유튜브: 새로 재는 것의 수율이 45% → 35%

| 코호트 | 행수 | 이메일% |
|---|---:|---:|
| 이전에 측정 | 3,186 | **45.2%** |
| 오늘 측정(#1015 이후) | 512 | **35.4%** |
| 미측정 잔여 | 3,907 | 23.8% |

전체(33.5%)는 안정돼 보이지만 **한계 수율이 떨어지고 있다.** 원인은 큐 정렬
(`subscriber_count DESC`)로 **큰 채널부터 소진**했기 때문이고, 남은 건 꼬리다:

| 구독자 | 총 | 미측정 | 이메일% |
|---|---:|---:|---:|
| 10만+ | 1,466 | 518 | 60.4% |
| 1만~10만 | 1,842 | 113 | 44.2% |
| 1천~1만 | 2,181 | 1,326 | 29.1% |
| 1천 미만 | 2,116 | 1,950 | 10.2% |

남은 3,907 중 **84%가 1만 미만**이다. 고장이 아니라 **수확체감**이다.

### 🩸 그 자리에서 내가 또 틀렸다 — 두 번

**① "1천 미만은 재는 값어치가 의심스럽다"** 고 대표에게 말했다가 **같은 답변 안에서 철회**했다.
근거로 삼은 `1~999 측정분 2.0% vs 미측정 11.5%` 역전은, 그 152행이 **전부 2026-07 측정분**
(평균 126 구독자)이라서다 — **오늘 레인은 그 구간을 아직 한 번도 안 재봤다.** 그 사이 컨택 추출이
여러 번 보강됐으므로 7월 수치로 오늘을 판단할 수 없다. ⚠️ 측정은 `COALESCE` 로 **더하기만** 하므로
"측정된 쪽이 더 낮다"는 언제나 **선택 편향**이지 측정의 효과가 아니다 — 그걸 먼저 의심했어야 했다.

**② 블로그를 과소평가했다(더 큰 오류).**

| 코호트 | 행수 | 이메일% | 평균 이웃수 |
|---|---:|---:|---:|
| 미측정 | 20,284 | **1.1%** | 0 |
| 최근 2일 측정 | 5,118 | **25.0%** | 1,912 |
| 그 이전 측정 | 10,129 | 28.8% | 3,056 |

**측정이 1.1% → 25% (+24pp)** — 유튜브(+11pp)의 두 배가 넘고 감쇠도 완만하다.

### 정정된 기대값

| 축 | 낮에 보고한 값 | 실측 기반 |
|---|---:|---:|
| 네이버 블로그 백로그 | 2,430명 | **4,000~4,800명** ↑ |
| 유튜브 백로그 | 1,390명 | **~430명** ↓ |

낮의 오류는 **재고 비율(stock)을 한계 수율(marginal)로 쓴 것**이다. 블로그 전체 12.1% 에는
이메일 1.1% 인 미측정 20,284행이 섞여 측정 효과가 희석돼 있었고, 유튜브 45% 는 이미 큰 채널을
걸러낸 뒤의 값이었다. **⇒ 백로그의 가치를 추정할 땐 "같은 구간 안에서 측정 전/후"를 볼 것.**

### 그래도 지금 할 일은 안 바뀐다

유튜브 백로그는 ~2일이면 비고, 그 뒤 블로그가 예산 전부를 가져간다(두 레인은 매 회차 둘 다 돌고
잔여를 서로 넘긴다 — 위 정정 절 참조). **재배분이 스스로 해결되므로 코드 변경 이유가 없다.**
바뀌는 건 기대값뿐: 8/19 소진 시점 발송 가능 리드 **11,000~11,800**(전 10,500 추정),
그 증가분의 **90%가 블로그에서 나온다.**

📌 **다음 세션이 볼 것**: 오늘 레인이 1천 미만 구간에 도달한 뒤(YT 큐 앞이 비면) 그 구간의
   측정 전/후 수율. 500행쯤 쌓이면 "그 구간을 큐에서 후순위로 뺄지"를 **그때 처음으로** 판단할 수 있다.

---

## 🚨 커버리지 붕괴 경보 — 집중 축 프리픽스가 일반 풀 커서를 얼려 놨다 (2026-08-04 KST 02:00)

대표가 붙여 준 경보: *"활성 399개 중 323개가 이틀째 미실행(수확은 나오지만 커버리지가 무너진 상태)"*.

### 실측

```
활성 399  ·  never 117  ·  1~2일 22  ·  2일+ 206      (= 323 정체)
회차: planned 16 → processed 5     spent 56 / budget_total 56 (예산 소진)
24h 실제 실행 키워드 54개            (하루 슬롯은 5×24 = 120)
diag: yt 128/21 · naver 347/167 · tistory 16/2   ← 수확은 정상
```

**수확이 멀쩡해 보인 이유**가 여기 있다 — 도는 54개가 수율 좋은 키워드라 `found/saved` 는 정상이다.
**무너진 건 폭이지 양이 아니다.** 그래서 이 종류는 총량 지표로는 절대 안 보인다.

### 원인 — 프리픽스 + `prefixDone` 의 결합

`picks = [...focusPicks, …]` 로 집중 축이 **무조건 앞머리**였다. 예산이 5개에서 끊기니 집중 4개가
앞자리를 먹고 일반 풀엔 1개. 그리고 `prefixDone` 은 **처리된 앞부분만** 세므로 뒤 풀은 잘릴 뿐 아니라
**커서가 안 움직여 다음 회차에 같은 키워드를 또 내놓는다.** 일반 풀(~300개) 한 바퀴에 12일 이상.

🗺️ **앞머리의 근거 주석이 이미 낡아 있었다**: *"YT 슬롯(희소)에 확실히 들어가게"* — 그런데 지금
`ytPicks` 는 `pickYtKeywords` 가 **전체 키워드에서 따로** 뽑고 `picks` 는 오히려 `ytIds` 를 **제외**한다.
⇒ YT 엔 아무 도움도 안 되면서 일반 풀만 굶기고 있었다. 이 레포가 부르는 **"낡은 지도"** 다.

### 수정 — 세 풀 라운드로빈 (몫은 불변)

`planKeywordSplit` 의 배분은 **그대로 두고**, 잘릴 때 **누가 먼저 잘리느냐**만 공평하게 했다.
집중 풀은 18개뿐이라 회차당 ~2개로도 하루 두 바퀴 이상 돈다(과잉 해소).
주입 검증: 앞머리 방식으로 되돌리면 빨간불 ✅ (매니페스트 152건).

### 🔎 아직 안 고친 더 큰 레버 — `processed 5 / planned 16`

예산 56 을 5개 키워드가 다 쓴다 = **키워드당 ~11 서브리퀘스트**. 이걸 줄이면 라운드로빈과 무관하게
커버리지가 배로 는다. 어디에 쓰이는지는 **아직 안 쟀다**(추측 금지 — 소스별 fetch 수와 save 배치 수를
세어야 한다). 다음 세션의 후보 1순위.
   · 대안 레버: `collect` 알람의 `runsPerHour` 1 → 2~3. ⚠️ 단, 이건 **네이버 부하**를 늘리므로
     대표 확인 사항이라고 `lane-alarm-runners.ts` 에 적어 뒀다. 실사용은 쿼터의 0.4% 라 여유는 있다.

### ▶️ 판정 (배포 후 24h — 아침 보고에 포함)

```bash
bash scratchpad/d1.sh "SELECT CASE WHEN last_run_at IS NULL THEN '0.never'
  WHEN last_run_at >= datetime('now','-1 day') THEN '1.24h내' ELSE '2.그이상' END g,
  COUNT(*) n FROM ad_discovery_keywords WHERE active=1 GROUP BY g"
```
**never 117 이 줄고 24h내가 54 → 100+ 로 늘면 성공.** 안 늘면 병목은 순서가 아니라 예산이다(위 레버).

⚠️ **라운드로빈만으로는 경보가 안 꺼질 수 있다** — 정직하게 계산하면 일반 풀(~300개)이 회차당
~1.5개 × 24 = 36/일 → **한 바퀴 8일**이다(전 12일). 경보 임계가 "2일째 미실행"이므로 여전히 걸린다.
⇒ **진짜 해결은 처리량**이고, 그건 아래 계측이 나온 뒤에 판단한다.

### 🧾 그래서 계측을 같이 넣었다 — `spend_by`

`ads_autocollect_stats.spend_by = {yt, naver, cafe, tistory, save}` 신설(추가 왕복 0 — 이미 있는
`budget.left` 를 소스 호출 전후로 읽기만 한다). 회차 `spent 56 / processed 5` = 키워드당 ~11인데
**어디에 쓰이는지 아무도 몰랐다.**

가장 유력한 용의자는 발굴 시점 **`enrichMax`(YT 8 · 네이버 5 = 키워드당 최대 13)** 이고, 그건 지금
**별도 보강 레인이 하는 일과 겹친다**(수집=폭, 보강=깊이). 다만 수집 시점 보강은 *"수집과 동시에
이메일을 얻는"* 경로이기도 해서 — 실제로 미측정 YT 행의 23.8% 가 이미 이메일을 갖고 있다 —
**추측으로 줄이면 수집 품질이 조용히 나빠진다.** ⇒ 줄이기 전에 재는 것이 먼저다.

**다음 세션 판독법**: `spend_by.yt + spend_by.naver` 가 `spent` 의 절반을 넘으면 enrichMax 가 범인이다.
`save` 가 크면 저장 배치가 범인이고 그건 품질 손실 없이 줄일 수 있다(더 좋은 소식).


### 🧱 같은 PR 에서 함께 처리한 두 가지

**① 배포 블로커 — `brace-expansion` GHSA-rgw5-rvv9-x895 (내 변경과 무관, main 에서도 막힘)**
새로 뜬 high advisory 가 `check-npm-audit` 를 빨간불로 만들었다. 취약 라인이 **v1·v5 둘 다**라
(`<=1.1.17 || 3.0.0-5.0.8`) 단일 override 로는 v1 소비자(minimatch@3)가 깨진다 ⇒ 버전별로 고정:
`"brace-expansion@1": "1.1.18"` · `"brace-expansion@5": "5.0.9"`. 게이트 재실행 통과 확인.
⚠️ **허용목록으로 넘기지 않았다** — `.audit-allowlist.json` 은 `accepted_by: 대표 승인` 을 요구하는데
세션이 그걸 대신 쓸 수는 없다. dev 전용이라 도달 불가로 보이더라도 **패치가 가능하면 패치가 먼저**다.

**② 600줄 래칫 — 계측 추가로 618줄**
`[SKIP_SIZE]` 우회 대신 병합 로직을 `influencer-keyword-rotation.ts` 로 추출(592줄).
`planKeywordSplit`·`interleavePicks` 와 같은 모듈 = 같은 관심사다.
🎁 부수 이득: 테스트가 **사본이 아니라 실제 함수**를 검사하게 됐다(사본 테스트는 구현이 갈라져도 초록).
주입 검증도 두 층으로 늘었다 — 호출부 회귀 · 함수 자체 회귀 **둘 다 빨간불** 확인.

---

## 📉 수집 예산 회수 + 폭 동결 (2026-08-04 KST 09:30 — 대표 *"수집과 보강 다 잘 되게 하면 안돼?"* → *"①만"*)

### 계측이 범인을 특정했다 (어젯밤 넣은 `spend_by` 의 첫 수확)

```
spend_by: yt 24 · naver 28 · cafe 0 · tistory 4 · save 0     (spent 56)
→ yt + naver = 52/56 = 93%.  save 는 0 (배치라 예산을 안 먹는다)
```
판독 기준이 *"절반 초과면 enrichMax 가 범인"* 이었는데 **93%** 였다. 확정.

### 네이버만 산출이 나쁜 이유 — 버그가 아니라 산수

```
미측정 행(= 순수 수집 결과)의 이메일:  네이버 1.3%   vs   유튜브 22.4%
키워드당 발굴 ~69명 → enrichMax 5명만 보강 = 7%
7% × 홈 수율 25% ≈ 1.8%   ← 실측 1.3% 와 일치
```
수집 시점 네이버 보강은 보강 레인과 **똑같은 코드**(`fetchNaverBlogHome`→`pickBusinessEmail`)를 쓴다.
차이는 **몇 명을 하느냐**뿐이고, 보강 레인은 같은 사람들을 **100% 커버해 25%** 로 만든다
⇒ **어차피 할 일의 7%를 미리 하면서 예산의 54%를 쓰는 중복.**
🎯 유튜브 22.4% 는 `enrichMax` 가 아니라 **`channels.list` 응답(공짜)** 에서 나온다 — 그래서 안 건드렸다.

### 🩸 그런데 "폭을 넓히자"는 내 제안은 내가 스스로 뒤집었다

대표가 *"그렇게 하면 어떻게 된다는거지?"* 라고 물어 확인했더니:
```
블로그  유입 3,895/일  vs  측정 4,184/일  →  여유 +289 (백로그 19,963 → 69일)
폭 1.8배면 유입 ~7,000/일  →  백로그가 매일 +2,800 으로 **증가 반전**
```
새 행은 이메일 1.3% 이고 그걸 25% 로 만드는 게 측정인데 **측정이 병목**이다.
⇒ 폭을 넓히면 **행 수만 늘고 발송 가능 리드는 거의 안 는다.** 커버리지 경보는 "폭이 좁다"고 말하지만
**지금 넓히면 못 쓰는 행만 쌓인다.** ⚠️ 어제 내가 "16일이면 블로그 소진"이라 한 것도 순간 표본이었다 —
**24시간 창으로 재면 69일**이다. 소진 예상은 24h 창으로 잴 것.

### 적용 — ①만

| | 값 | 근거 |
|---|---:|---|
| `NAVER_COLLECT_ENRICH_MAX` | 5 → **1** | 중복(무손실). 0이 아니라 1인 건 경로 생존을 diag 로 계속 보기 위해 |
| `COLLECT_KEYWORDS_PER_ROUND` | **6** (신설 캡) | 회수한 예산이 **자동으로** 5→9개가 되는 것을 막는다 |
| 유튜브 `enrichMax` | 8 (불변) | 22.4%는 공짜 응답에서 나옴 · 스니펫 기여는 **아직 안 쟀다** |

🔓 **푸는 법**: `ADS_COLLECT_KEYWORD_CAP` env — 재배포 없이 즉시 폭으로 전환된다.
**측정 처리량이 올라간 뒤에** 풀 것. ⚠️ 측정을 올리는 것 자체가 네이버 직접 조회 부하(~8,000/일)를
늘리는 일이라 차단 위험 판단이 먼저다.

### 🕳️ 내가 저지른 것 두 가지

1. **새 env 노브를 SSOT 목록(`env-drift.ts`)에 등록 안 함** — 레포에 이미 그 가드가 있어 잡혔다.
   등록 안 하면 런타임이 그 키를 **"미사용"으로 오신고**한다.
2. **빈 grep 출력을 "통과"로 읽음** — 재실행이 **아직 안 끝난 상태**였는데 `grep "Failed Tests"` 가
   비었다고 초록으로 판단했다. 이 레포가 반복해 만난 *"실패가 아니라 부재"* 를 내가 똑같이 했다.
   ⇒ **로그를 파일로 남기고 요약줄(`Test Files … passed`)을 직접 읽을 것.** 필터링된 빈 출력은 증거가 아니다.

### 🔁 반복 신호 — 새 npm advisory 가 하루에 두 번 배포를 막았다

`brace-expansion`(08-03) · `undici`(08-04) 둘 다 **내 변경과 무관**하고 둘 다 **dev 전용**이다
(`npm ls undici --omit=dev` 비어 있음 — jsdom 테스트 · wrangler/miniflare 빌드). 매번 override 로
개별 대응했고 이번엔 `undici: ^7.29.0`(취약 7.0.0–7.28.0).

⚠️ **이게 계속되면 머지마다 무관한 블로커에 시간을 쓴다.** 근본 처방 후보 두 가지 — **대표 판단 사항**:
1. **`wrangler` 4.84 → 4.118** — audit 이 권하는 fix 이고 undici·miniflare·sharp 3건이 한 번에 해소된다.
   빌드 툴체인 변경이라 별도 검증 필요.
2. **게이트를 프로덕션 트리로 한정**(`--omit=dev`) — 반복이 사라지지만 **게이트를 약화**시키는 방향이다.
⚠️ 허용목록(`.audit-allowlist.json`)은 `accepted_by: 대표 승인` 을 요구하므로 **세션이 대신 못 쓴다** —
   그래서 두 번 다 패치로 갔다. 이 제약은 의도된 것이니 우회하지 말 것.

---

## 🧪 배포 전 기준선 — #1041 판정용 (KST 12:00 회차, 배포 **전**)

```
picks     planned 16 / processed 5
spend_by  yt 23 · naver 29 · cafe 0 · tistory 4 · save 0   (합 56)
spent/budget_total  56 / 56  (전량 소진)
naver_api 187 / 25,000       (오픈API 한도 여유 — 병목 아님)
```
**판정 기준(배포 후 첫 정각 회차)**: `spend_by.naver` **29 → ~10** · `picks.processed` **≤ 6 유지**(폭 동결).
⚠️ 배포 반영 여부는 시각이 아니라 **값**으로 판정할 것 — 정각마다 도는 레인이라 "새 회차 = 새 코드"가 아니다.

## 🕳️ 내가 저지른 것 (추가) — typecheck 를 한 번도 실제로 안 봤다

`npx tsc --noEmit --skipLibCheck | head -5; echo "tsc=$?"` 로 확인하고 **통과로 보고**했다.
`$?` 는 **파이프의 마지막 명령(`head`) 상태**라 tsc 가 무엇을 뱉든 항상 0 이다.
그 결과 **TS2559 가 CI 까지 갔다**(`keywordsPerRoundCap(env: { ADS_COLLECT_KEYWORD_CAP?: string })`
— 워커 `Env` 에 그 키가 없어 "공통 속성이 없다"). 수리: 파라미터를 `unknown` 으로 받고 내부 캐스팅
(`alarmEnabled(env: unknown)` 과 동형).

⇒ **검증 명령에 파이프를 쓰지 말 것.** 파일로 받고 exit 를 직접 읽는다:
```bash
npx tsc --noEmit --skipLibCheck > /tmp/tsc.log 2>&1; echo "exit=$?"; wc -l < /tmp/tsc.log
```
이건 같은 날 위에 적은 *"빈 grep 출력을 통과로 읽음"* 과 **같은 클래스**다 — 둘 다
**성공을 확인한 게 아니라 실패의 부재를 봤다.**

## 🧾 CI 실패 로그를 읽을 때 — 블로킹 스텝을 먼저 가려라

#1041 CI 로그에 `product_stay_rooms … "status": "drift"` 가 함께 찍혀 있어 두 개의 원인을 의심했는데,
`.github/workflows/verify.yml` 의 **Schema verification (drift detection)** 스텝은
`continue-on-error: true`(정보 용도)다 — **블로커가 아니다.** 실제 블로커는 TS2559 하나였다.
⇒ 로그에 빨간 게 여럿 보이면 **워크플로에서 그 스텝의 `continue-on-error` 를 먼저 확인**할 것.

## 🕳️ 검증 공백 — head 가 문서 커밋이면 Verify 가 **한 번도 안 돈다** (2026-08-04 실측)

`verify.yml` 은 두 다리로 돈다: `push`(브랜치, **`paths-ignore: ['docs/**','**/*.md']`**) + `pull_request`
(paths-ignore 없음 — 2026-08-03 실사고 수습으로 일부러 뺐다). 설계 의도는 *"문서 커밋이 head 여도
pull_request 다리가 전수 검증한다"* 인데, **그 다리가 현재 안 돌고 있다.**

```
verify.yml · event=pull_request 최근 run : 2026-08-03T17:54:34Z (a8186f2) 이후 0건
그 뒤 내 푸시 75180f1 / 1c42f04 / e916e99 : 전부 event=push 로만 실행
문서 전용 커밋 c6f3029 (head)            : push=paths-ignore 로 skip, pull_request=미발화
  → GET /pulls/1041 check-runs = Cloudflare Pages 뿐. **Verify 체크 0건.**
```
⇒ **"PR 에 빨간 체크가 없다"가 "검증됐다"가 아니다.** 이 레포가 2026-08-03 에 겪은 사고와 같은 상태가
   경로만 바꿔 재현됐다(그때는 paths-ignore + concurrency, 이번엔 pull_request 다리 자체의 침묵).

🔎 **원인 미확정 — 다음 세션의 첫 액션**: `pull_request` 다리가 왜 침묵하는지. draft PR·중복 run 억제·
   Actions 설정 중 무엇인지 못 가렸다. 판정법: 코드 커밋을 하나 밀고
   `GET /actions/workflows/verify.yml/runs?event=pull_request` 에 새 run 이 잡히는지 본다.
⚠️ 이 환경에선 `workflow_dispatch` 가 **403(Resource not accessible by integration)** 이라 우회 못 했다.

🛟 **그동안의 실무 규칙**: **PR 의 마지막 커밋을 문서로 두지 말 것.** 문서 갱신을 코드 커밋보다 **먼저**
   밀거나, 문서를 민 뒤 코드 커밋을 한 번 더 밀어 head 를 코드로 만든다.
   (이번 건은 `git diff e916e99 c6f3029 --stat` 로 **차이가 handoff 1파일뿐**임을 확인하고,
    문서-민감 가드 3종(`check-current-work-sync`·`check-blog-seed-currency`·인계목차 재생성)을
    로컬로 돌려 통과시킨 뒤 진행했다. 그래도 **이건 우회지 검증이 아니다** — 위 원인 규명이 남는다.)

## ✅ #1041 머지 — `0f9ab7c` (2026-08-04 KST 12:48)

Verify 97스텝 통과 후 머지. ur-ads 재배포는 `deploy-ads.yml` 이 자동 처리
(변경 파일이 `src/features/marketing/**`·`src/worker-ads/**` 경로 필터에 걸린다).

### 🏃 머지가 세 번 막혔다 — 그 세 가지가 각각 다른 교훈이다

1. **draft PR** — `405 Pull Request is still a draft`. PR 이 draft 면 API 머지가 막힌다.
   ⚠️ **그리고 이게 위 "Verify 미실행" 의 유력한 원인 후보다** — `pull_request` 다리가 침묵한 구간과
   draft 였던 구간이 겹친다. 다음 세션이 판정할 것: **non-draft 상태에서 코드 커밋을 밀고**
   `event=pull_request` run 이 생기는지. (draft 해제 직후 밀었더니 실제로 Verify 가 정상 등록됐다 —
   정황은 맞지만 변수가 하나가 아니라 단정하지 않는다.)
2. **main 이 앞서가는 경주** — 내 사이클(병합→CI 15분→머지)이 도는 동안 main 에 #1040·#1043 이
   연달아 들어왔다. 세션이 여럿 도는 레포에선 **CI 를 기다리는 동안 base 가 움직인다**는 걸 전제할 것.
3. **양쪽이 같은 자리에 서로 다른 것을 추가** (`influencer-keyword-rotation.ts`) —
   우리 쪽 수집 예산 상수 vs main 의 `judgeRotation`. **둘 다 보존**이 정답이다.

### 🕳️ 그 보존에서 조용히 깨진 것 — 닫는 중괄호

git 은 두 함수가 공유하는 `}` 를 **충돌 영역 밖**에 남긴다. 양쪽을 이어 붙이면 앞 함수의 `}` 가
사라지는데, **본문은 멀쩡해 보인다**:
```
export function keywordsPerRoundCap(env: unknown): number {
  return ...                          ← '}' 가 없다
/* ── 🩺 순환 건강 판정 ── */          ← main 쪽 블록이 바로 이어짐
```
`tsc` 가 `TS1005: '}' expected` 로 잡았다. ⇒ **충돌 해소 후 타입체크는 선택이 아니다.**
(`grep '<<<<<<<'` 은 이걸 못 본다 — 마커는 이미 0이다.)

### 🤝 방향 일치 확인

main 의 #1043 이 남긴 실측 주석도 *"유입 1,613/일 vs 측정 3,600/일 — 측정이 이기는 중이니
처리량을 더 밀지 말 것"* 이다. 이 PR 의 **폭 동결과 같은 결론**이라 두 변경이 서로를 무르지 않는다.

## 🎯 #1041 라이브 판정 — 13:00 KST 회차 (배포 후 첫 회차)

```
                     배포 전 12:00      배포 후 13:00
spend_by.naver            29                18          ← 예측 ~10 은 빗나감(아래)
  키워드당 naver          5.8               3.0          ← ✅ 정확히 반토막 = 실제 효과
picks.processed            5                 6          ← ✅ 캡(6)에서 멈춤
spend_by.yt               23                33          ← 회수분이 여기로 흡수
spent / budget_total    56 / 56          56 / 56        ← 예산은 여전히 전량 소비
diag.naver          found 347/saved 167  found 463/saved 158   ← 경로 생존(캡 1 로도 정상)
저장 합계                 190               188          ← 사실상 동일
```

### 🩸 내가 틀린 예측 — "naver 29 → ~10"

**처리 키워드가 5 → 6 으로 는다는 걸 예측에 안 넣었다.** 회수한 예산은 사라지지 않고
**키워드 한 개를 더 도는 데 쓰인다** — 그게 폭 캡을 6으로 둔 이유인데, 정작 숫자는 5개 기준으로 말했다.
⇒ **총량으로 판정하지 말 것. 단위당(키워드당) 으로 봐야 한다** — 5.8 → 3.0 이 진짜 결과다.

### ❓ 아직 못 가린 것 — `processed 6` 이 캡 때문인가 예산 때문인가

`spent 56 == budget_total 56`(전량 소비)인데 `limit_hit=false` 다. 캡이 묶었다는 정황이 맞지만
**한 회차로는 두 원인을 분리 못 한다.** 판정법: 다음 회차 2~3개에서
`processed` 가 계속 정확히 6 이면 캡, 5 이하로 흔들리면 예산이 먼저 닿는 것이다.
후자라면 `COLLECT_KEYWORDS_PER_ROUND` 는 아직 **구속력이 없는 장식**이고, 그 사실을 알아야
나중에 `ADS_COLLECT_KEYWORD_CAP` 을 올렸을 때 아무 일도 안 일어나는 걸 오진하지 않는다.

### 🔭 다음 세션의 첫 액션

```bash
# 어드민 토큰($TOK)·UA 는 CLAUDE.md 절차. 정각 +2분에 읽는다.
curl -sS "https://live.ur-team.com/api/admin/ads/influencer-pool/stats" \
  -H "Authorization: Bearer $TOK" -H "User-Agent: $UA" \
  | python3 -c "import sys,json;r=json.load(sys.stdin)['run'];print(r['last_run'],r['picks'],r['spend_by'],r['spent'],r['budget_total'])"
```
· `picks.processed` 가 **6 고정**인가 → 폭 동결이 실제로 구속 중
· `spend_by.naver / processed` 가 **3.0 부근 유지**인가 → 중복 제거가 지속되는가
· 그리고 진짜 지표는 **발송 가능 리드**다: 이 변경은 행 수를 늘리는 게 아니라
  **예산을 덜 낭비하는** 것이므로, 하루 단위로 `with_email` 증가분을 봐야 한다(회차 단위로는 안 보인다).

### 🔬 원인 미상 — 후보 두 개가 **모두 반증됐다** (실측만 남긴다)

앞 절에서 나는 `draft` 를, 그 다음엔 `synchronize`(draft 에서만 안 돎)를 원인으로 적었다.
**둘 다 틀렸다.** 30분 사이에 각각 반증이 나왔다. 추측을 이어 붙이는 대신 실측만 남긴다.

verify.yml · `event=pull_request` 실행 여부:
```
시각(UTC)  커밋      PR     상태      내용        pull_request run
17:54(8/3) a8186f2  #1041  draft    코드         ✅ 실행 (opened)
02:21      75180f1  #1041  draft    코드         ❌ 없음
02:38      1c42f04  #1041  draft    코드         ❌ 없음
03:00      e916e99  #1041  draft    코드         ❌ 없음
03:03      c6f3029  #1041  draft    문서         ❌ 없음   ← head 가 미검증이 된 지점
03:06      a47cb64  #1041  draft    문서         ❌ 없음
03:07      371e85f  #1041  draft    문서         ❌ 없음
03:44      (draft 해제)
03:48      669e515  #1041  ready    코드+문서     ✅ 실행
04:06      3dd3899  #1044  draft    문서         ✅ 실행 (opened)   ← "draft 가 원인" 반증
04:09      a0f4b04  #1044  draft    문서         ✅ 실행 (synchronize) ← "synchronize" 가설도 반증
```
**#1041 의 02:21~03:07 구간만 통째로 비어 있다.** draft·이벤트종류·파일종류 어느 것으로도 설명이 안 된다
(#1044 는 같은 조건에서 전부 실행됐다). 그 구간에만 있었던 다른 요인이 있다는 뜻인데, 여기서는 못 가렸다.

🔭 **다음 세션이 가릴 방법**: 그 구간의 **workflow run 부재를 GitHub 쪽에서 확인**한다 —
   `GET /repos/{o}/{r}/actions/runs?event=pull_request&created=2026-08-04T02:00..03:30`
   그래도 0 이면 이벤트 자체가 배달 안 된 것이고, 그건 레포/조직 설정(Actions 사용량 제한·
   중복 run 억제) 쪽이라 **대표만 볼 수 있는 화면**이 필요하다.

🛟 **그때까지 유효한 실무 규칙 (원인과 무관하게 성립)**:
   **PR 의 마지막 커밋을 문서로 두지 말 것.** 문서를 코드보다 먼저 밀거나, 문서 뒤에 코드 커밋을
   하나 더 밀어 head 를 코드로 만든다. 그리고 **머지 직전 `GET /pulls/{n}` 의 check-runs 에
   Verify 가 실제로 있는지 눈으로 확인**한다 — "빨간 체크가 없다"가 "검증됐다"가 아니다.

> 🪞 **이 절이 남기는 교훈은 원인이 아니라 태도다.** 나는 30분 사이에 두 번, 정황이 맞는다는 이유로
> 후보를 원인처럼 적었다. 인계 문서는 다음 세션의 지도라서, 틀린 원인은 없는 것보다 나쁘다.
> **모르면 "못 가렸다"고 적고, 대신 판정법과 그때까지 유효한 규칙을 남긴다.**

## ✅ 폭 동결 분리 판정 — **캡이 구속 중이다** (14:00 KST 회차가 결정적)

한 회차로는 `processed 6` 이 캡 때문인지 예산 때문인지 못 갈랐다(`spent == budget_total` 이었다).
**두 번째 점이 갈라 줬다.**

```
              12:00(구코드)   13:00(신)     14:00(신)
processed          5            6            6      ← 캡(6)에 정확히 멈춤
spent/budget    56 / 56      56 / 56      34 / 56   ← ★ 예산 22 남기고 멈췄다
naver 총량         29           18           19
  키워드당        5.80         3.00         3.17    ← 중복 제거 지속
저장 합계          238          188          426
diag.naver   347/167      463/158      551/394
```

**★ 이 한 칸이 판정이다**: 14:00 회차는 **예산을 22 남긴 채 정확히 6개에서 멈췄다.**
예산이 병목이면 남은 22 로 7·8번째 키워드를 돌았을 것이다. ⇒ **`COLLECT_KEYWORDS_PER_ROUND` 는
장식이 아니라 실제 구속력을 갖는다.** 따라서 `ADS_COLLECT_KEYWORD_CAP` 을 올리면 **즉시 폭으로 전환된다**
(측정 처리량이 올라간 뒤에 할 것 — 지금 올리면 백로그만 증가 반전).

⚠️ **회차 간 변동이 크다는 것도 같이 기억할 것**: yt 33 → 9, 저장 188 → 426. 키워드 구성에 따라
(유튜브 큐가 비면 그 몫이 블로그로 간다) 소스별 배분이 크게 흔들린다. **총량 한 점으로 판정하지 말고
키워드당 단위와 여러 회차로 볼 것** — 이 세션이 첫 예측에서 정확히 그 실수를 했다.

### 📌 이 변경의 최종 성적

| | 전 | 후 |
|---|---:|---:|
| 키워드당 naver 서브리퀘스트 | 5.8 | **3.0~3.2** |
| 회차당 키워드 | 5 | **6** (캡, 구속 확인) |
| 회차 예산 소진 | 56/56 (항상 전량) | **34~56** (여유 발생) |
| 네이버 경로 생존 | — | ✅ `enrichMax=1` 로도 found 463~551 |

⇒ **행 손실 없이 예산을 회수했고, 회수분이 폭으로 새지 않게 묶는 것도 실제로 작동한다.**

## 🔬 측정 전 신호 ↔ 이메일 유무 상관 (2026-08-04, 대표 지시) — **상관 있음, 실익은 작음**

### 먼저 걸러낸 함정: `description` 은 측정이 덮어쓴다

```
소개글 길이 분포   미측정 100~299자 20,024/20,105  ·  측정완료 300~799자 14,342/17,566
```
측정(`enrichNaverActivity`)이 RSS 본문으로 `description` 을 **교체**한다. 따라서 **측정완료 모집단의
소개글로 "측정 전 신호"를 판정하면 누출(leakage)** 이다. 안 덮이는 신호(`source_keyword`·`name`·`url`)만 썼다.

### 결과 — 키워드는 실제 신호다

```
기저율 26.7% (측정 17,586 → 이메일 4,704)
최상  블로그수익화 68.1% · 독서 추천 46.7% · 캘리그라피 45.1%
최하  방배동 맛집 0.0%(표본 46) · 금천 맛집 5.9% · 캠핑 브이로그 10.6%
```
n≈175 에서 표준편차 3.4pp → 양 끝은 **6σ 밖**. 우연이 아니다.

### 그런데 재정렬 실익은 작다

```
미측정 20,085행 · 기대 이메일 4,951
  상위 10% 측정 → 756  (무작위 495)  1.53x
  상위 20%      → 1,298 (990)        1.31x
  상위 50%      → 2,910 (2,475)      1.18x
```
이유 둘: ① **백로그의 49%(9,912행)가 측정표본 30건 미만 키워드**라 점수를 못 매긴다
② 백로그를 결국 다 측정하면 **총량은 같다** — 순서는 *앞당길* 뿐이다.

### 🩸 내가 눈으로 본 패턴은 노이즈였다

꼬리만 보고 *"지역+업종 키워드가 체계적으로 저조하다"* 고 읽었다(방배동 0%·금천 5.9%·도봉 카페 5.2%).
**전수로 재니 반증됐다**:
```
지역 포함 25.7% (57개)  vs  지역 없음 27.2% (124개)     ← 사실상 동일
"맛집" 포함 25.5%  ·  "카페" 27.1%  ·  "네일" 35.1%
```
⇒ **일반화 규칙이 없다.** 신호는 키워드 개별적이고, 새 키워드는 표본 30건이 쌓이기 전엔 못 고른다.
(꼬리 몇 개로 규칙을 세우면 그게 바로 다음 세션의 틀린 지도가 된다 — 오늘 두 번 겪었다.)

### 📌 그래서 남는 실질 레버 — 저수율 키워드 **수집 중단**

```
저수율(<15%) 16개 : 측정 1,878 → 이메일   192 (10.2%) · 미측정 대기 1,035
중간(15~35%) 104개: 측정 11,995 → 이메일 2,737 (22.8%)
고수율(>=35%) 61개: 측정  3,599 → 이메일 1,744 (48.5%) · 미측정 대기  906
```
저수율 16개가 **측정의 10.7%를 먹고 산출은 4.1%**다. 그 몫이 고수율로 가면 같은 측정량에 **+719 이메일**.
⚠️ **다만 이건 관찰이지 인과가 아니다** — 저수율 키워드를 끄면 그 회차가 정말 고수율로 가는지는
실험(끄고 회차별 산출 비교)이 필요하다. 그리고 저수율 키워드도 **커버리지 목적**이 따로 있다면 그건 별개 판단이다.

🔭 **다음 액션 후보**(대표 판단): 저수율 16개를 순환에서 은퇴(또는 몫 축소). 소액·저위험·가역.
   측정 순서 재정렬은 이득이 1.2~1.5x 뿐이라 **지금은 보류**가 맞다.

## 🤖 연락처 수율 자동 조율 신설 (2026-08-04, 대표 *"자동으로 조율되게끔. 영구적으로."*)

위 상관 측정의 후속 구현. 요지: **기존 자동 조율 둘은 수집 단계만 봤다.**
`barren_streak`(못 찾음) · `yieldPenalty`(찾았는데 안 남음). 빠진 축이
**"잘 찾고 잘 남았는데 재 보니 연락처가 없음"** 이고, 이 DB 의 목적이 행이 아니라
*제안 보낼 수 있는 리드*라 정작 그게 제일 중요한 축이었다.

| 구성 | 값 | 왜 |
|---|---|---|
| `CONTACT_EVIDENCE_MIN` | 40 | 미만은 **절대 안 벌한다** — 없으면 신규 축 차단기가 된다 |
| `CONTACT_OK_RATE` | 0.15 | 기저 26.7% 대비 확실히 낮은 선만 |
| `CONTACT_PROBE_EVERY` | 5 | 억제돼도 5회차마다 통과 → **판정이 스스로 뒤집힐 수 있다** |

배선이 **둘**인 이유: 순환 풀(네이버/일반)은 커서 로테이션이고 YT 만 점수 기반이다.
한쪽만 걸면 저수율 키워드가 반대쪽 경로로 계속 뽑힌다(실제로 그 상태였다 —
방배동 맛집은 점수 감점이 닿지 않는 순환 쪽에서 뽑히고 있었다).

⚠️ **정비 레인에 슬롯을 새로 만들지 않았다.** 그 배정표는 *12가 24의 약수*라는 근거로 짜여
각 단계가 하루 정확히 2회 돈다. 13번째를 끼우면 그 계약이 깨진다 → 수집 레인의 탐침 회차에 얹었다.

### 🕳️ 주입 검증에서 내 가드 하나가 헛돌았다

`expect(DDL).toContain('ADD COLUMN measured_total')` — 컬럼명을 `measured_total_x` 로 바꾸는 주입에
**초록**이 떴다(부분문자열 매치). 문장 전체 정규식으로 교정 후 빨강 확인.
⇒ **`toContain` 으로 식별자를 지키지 말 것.** 접미사가 붙으면 그대로 통과한다.

### 🗺️ 낡은 지도 하나를 같이 고쳤다

`ads-keyword-focus-split` 이 `const focusPool = kws.filter(inFocus)` 를 찾고 있었는데
600줄 래칫으로 그 코드가 `buildRotationPools` 로 이사했다. **순수 이동인데 빨간불** —
지우지 않고 새 위치를 따라가게 고쳤다(지키는 불변식은 그대로).

## 🔧 wrangler 4.84.1 → 4.118.0 (+ workers-types 4 → 5)

대표 *"3번은 너 말대로 해줘"*. **취약점 해소가 타입 메이저와 묶여 있었다** —
취약 범위가 `4.16.0 - 4.113.0` 인데 4.114+ 는 전부 `@cloudflare/workers-types@^5` 를 요구한다.
그래서 **파급을 먼저 쟀다**(그냥 올리지 않았다):
```
frontend tsc   0 에러
worker tsc     304줄  ==  업그레이드 전 베이스라인 304줄   ← 추가 0
```
결과: `wrangler · miniflare · sharp · undici · ws` 전부 CLEAR (high 10 → 6).
**허용목록 16 → 11** — 수용해 둔 위험 5건을 *실제로* 제거했다(그 항목들의 `review` 노트가
정확히 *"wrangler 업그레이드 시 재확인 후 제거"* 였다).

⚠️ `undici` override 는 **유지**한다 — 지워 보니 중첩 의존의 취약 버전이 다시 잡혔다(실측).
   `npm ls undici` 가 7.29.0 을 보여도 audit 은 다른 자리를 본다.

## 🔭 다음 세션의 첫 액션 (이 변경의 라이브 판정)

```bash
# 정각 +2분. 탐침 회차(total_runs % 5 == 0)에만 kw_yield 가 실린다.
curl -sS "https://live.ur-team.com/api/admin/ads/influencer-pool/stats" \
  -H "Authorization: Bearer $TOK" -H "User-Agent: $UA" \
  | python3 -c "import sys,json;r=json.load(sys.stdin)['run'];print(r['last_run'], r.get('kw_yield'), r['picks'])"
```
· `kw_yield.updated` 가 60 안팎이면 갱신이 돈다 · `error` 가 있으면 사유가 거기 있다
· 며칠 뒤 `last_keywords` 에서 방배동 맛집·금천 맛집이 **탐침 회차에만** 보이면 억제가 작동 중
· 진짜 지표는 `with_email` 증가율 — 회차 단위로는 안 보인다(하루 단위로 볼 것)

⚠️ **못 막는 것**: 억제가 *옳은지*. 저수율의 원인이 우리 추출기 결함이면 억제는 오히려 손해다.
   그래서 탐침 회차를 뒀지만, 근본 확인은 **저수율 키워드 표본을 사람이 직접 열어 보는 것**뿐이다.

## 📗 티스토리 접음 + 📬 아웃리치 결과 유입 (2026-08-04, 대표 *"티스토리 접고 이메일 상태 경로도"*)

### 티스토리 — 하루 만에 내 판단을 뒤집었다

**2026-08-03 에 이 세션이 만든 경로**를 다음 날 접었다. 그때 근거는 *"495행 미측정인데 경로가 없다"* 였고
초기 소표본 수율을 11.5% 로 봤다. **표본 397건에서 3.0%** 였다(네이버 26.7% · 유튜브 40.6%, 최근 3일 325행 → 0건).
⚠️ **경로가 고장난 게 아니다** — 393/397 이 글 수를, 392 가 링크를 얻었다. 티스토리 블로거가
**연락처를 안 거는 것**이고 코드로 못 고친다. ⇒ `TISTORY_ROOM = 0`, `ADS_TISTORY_ROOM` 으로 복원 가능.
**코드는 안 지웠다** — 지우면 되살리는 데 다시 하루가 든다.

🪞 교훈: **소표본 수율로 새 경로를 정당화하지 말 것.** 어제 그 숫자로 하루를 썼다.

### 아웃리치 결과 유입 — 루프의 열린 끝

`email_status`·`opened_at`·`replied_at` 는 **읽는 곳이 이미 많았다**(발송 큐 제외 · 리마인더 · 통계).
그런데 **쓰는 경로가 하나도 없었다** — 이메일 보유 7,615행 전부 NULL. 대표가 외부 도구로 보내기 때문이다.

`POST /api/admin/ads/outreach/status` — `{items:[{email,status,at?}]}` 또는 `{csv:"email,status[,at]"}`.
상태: `sent|opened|replied|bounced|complained|opt_out`.

설계에서 **에러가 안 나서 안 보이는** 함정 넷을 막았다(각각 주입 검증):
| 규칙 | 안 지키면 |
|---|---|
| 타임스탬프 `COALESCE` | 재업로드가 "방금 열었다"로 덮어 반응 시점 분석이 망가진다 |
| `sent` 가 `contacted_at` 을 안 덮음 | 덮으면 리마인더 D+N 창이 매번 밀려 **후속 발송이 구조적으로 0** |
| `opted_out` 은 `opt_out` 에서만 | 법적 의사표시라 자동 해제 안 함 / 다른 상태가 세우면 멀쩡한 리드 영구 제외 |
| `unmatched` 를 응답에 담음 | 안 담으면 반쯤 먹힌 업로드가 "성공"으로 보인다 |

⚠️ **이 경로가 못 보는 것**: 대표가 쓰는 메일 도구의 실제 컬럼 순서·헤더. 우리는 본 적이 없다 →
**첫 업로드에서 `invalid` 가 크면 파서를 맞춰야 한다**(그래서 버리지 않고 센다).

### 🔭 "머신러닝처럼 스스로 개선" — 지금 상태와 남은 것

```
수집 → 측정 → 이메일 有無 → [ 발송 → 반응 ] → 다시 수집에 반영
       └──── 여기까진 닫혔다 ────┘   └── 오늘 유입구만 뚫었다 ──┘
```
· **닫힌 부분**: `barren_streak`(못 찾음) · `yieldPenalty`(안 남음) · `contactPenalty`(연락처 없음) —
  셋 다 관측 → 몫 조정 → 재관측의 밴딧 형태이고, 탐침 회차가 탐색(exploration)을 보장한다.
· **아직 열린 부분**: 목표가 이메일이 아니라 **제휴 성사**인데 그 신호가 DB 에 없었다. 오늘 유입구를 만들었으니
  **데이터가 쌓이면** `keyword-contact-yield` 의 판정 기준을 *연락처 유무 → 회신율* 로 바꿀 수 있다.
· ⚠️ **지금 바꾸면 안 된다** — 회신 데이터가 0 이라 모든 키워드가 0% 로 보여 **전부 억제**된다.
  최소 표본(키워드당 수십 건 발송)이 쌓인 뒤에 임계를 정할 것. 그전까지는 연락처 유무가 최선의 대리 지표다.
