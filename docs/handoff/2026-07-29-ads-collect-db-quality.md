## 유어애즈 수집 DB 품질 — 거부 의사 존중 · 지역 그리드 전국 · 실패 사유 관측 (PR #853)

> **맥락**: 대표가 *"발송은 한동안 안 한다 — 지금은 DB 수집에만 집중"* 으로 방향을 정했다.
> 그래서 이 PR 은 **보내는 쪽 기능을 늘리지 않고**, 나중에 보낼 때 쓸 DB 를 다듬는 데만 쓴다.

### ❗ 이번에 틀렸던 판단 (먼저 읽어라)
1. **노이즈 필터를 다 만들었으니 "안 보낼 사람"은 끝났다고 생각했다.** 아니었다. 대표가 유튜브
   `똘비 ddolbi` 화면을 보내왔다 — 소개글에 **"공동구매 제안은 정중히 사양합니다"**. 우리는 그걸
   *읽지 못한 채* 저장하고, 점수를 매기고, 발송 큐에 올리고 있었다. 노이즈는 *낭비*지만 이건
   **거부 의사 무시**다(급이 다르다). 게다가 하필 어제 공동구매 축을 시드에 넣어서(#835)
   **바로 그 키워드로 이런 분들이 대량 유입되는 중**이었다.
   ⇒ 교훈: "제외 대상"을 우리가 *추정하는 것*(브랜드·노이즈)만 보고 있었고, **상대가 직접 써 둔 것**은
   한 번도 안 읽었다. 소개글은 이미 DB 에 있었는데 아무도 파싱하지 않았다.
2. **`사양` 을 그냥 잡으려다 멈췄다.** "제품 사양"(spec)·"고사양 게임"이 걸린다. 오탐은 좋은 리드를
   조용히 잃는 방향이라 노이즈 필터보다 비싸다 ⇒ **주제어 + 거부어가 25자 안에 함께** 있을 때만 판정.

### ✅ 완료 (PR #853)
| 변경 | 파일 | 효과 |
|---|---|---|
| 🚫 **거부 명시 감지** `declinesOutreach()` | `influencer-quality.ts` | 저장 시점 + 야간 품질 패스에서 `opted_out=1` 태깅 |
| 발송 큐·엑셀·시트·목록에서 제외 | `outreach-queue.ts` · `influencer-pool-export.ts` · `sheets-sync.ts` · `admin-ads-influencers.routes.ts` | **손으로 고르는 모든 경로**에서 동시에 빠진다 |
| 점수 −60 (브랜드 −35 보다 강하게) | `influencer-quality.ts` | 상위 목록에서 대표 눈을 뺏지 않는다 |
| 🌏 지역 그리드 전국 확대 | `influencer-seed-keywords.ts` | 서울 25구 → +광역시 6·경기 10·관광 6 (시드 232 → 304) |
| 🏷️ cron 실패 사유 | `worker-ads/index.ts` | `ok:false` 하트비트에 `err=limit\|timeout\|<Name>` |

**태그는 sticky 다** — 재계산으로 풀지 않는다. 이유 둘: ① `description` 은 저장 시 500자로 잘려서,
뒤쪽에 있던 "제안 사절"이 잘리면 다음 패스가 태그를 **조용히 해제**한다(판정이 저장 길이에 좌우됨)
② 받은 거부 의사를 우리가 먼저 무를 이유가 없다. 해제는 어드민에서 사람이 `?optedOutOnly=1` 로 검수 후.

**⚠️ `summarizeResult` 는 24자 초과 문자열을 버린다.** 그래서 원문(`Too many subrequests…` 47자)이 아니라
짧은 코드로 줄여 넘긴다 — 그대로 넘기면 통째로 사라진다(`ads-cron-beat-errcode.test.ts` 가 이 계약을 잠금).

### ▶️ 다음 세션 첫 액션
1. **배포 후 어드민 stats 의 `opted_out` 카운트** — 0 이면 아직 품질 패스가 한 바퀴를 안 돈 것
   (38k 행 / 회차 8k → 5회차). 며칠 뒤에도 0 이면 감지식이 헛도는 것이니 `?optedOutOnly=1` 로 확인.
2. **오탐 검수**: `/admin/influencer-pool?optedOutOnly=1` 에서 몇 명을 열어 소개글을 눈으로 본다.
   수락 문장이 잡혀 있으면 `OPTOUT_TOPIC_RE`/`OPTOUT_DECLINE_RE` 를 좁힌다(유닛에 케이스 추가 필수).
3. **지역 그리드 효과**: 신규 저장률(4% 까지 떨어져 있었다)이 올라가는지. 안 오르면 커서가 아직
   서울 구간을 도는 중일 수 있다 — 한 바퀴가 며칠이다.
4. #841(AIMD) 효과 확인이 아직 남아 있다: `learned_cap` 이 55↔44 진동을 멈췄는지.

### 🔵 대표 판단이 필요한 것
- **거부 감지 범위**: 지금은 *제안 성격*(협찬·광고·제휴·공동구매·체험단)만 본다. "댓글 문의 안 받아요"
  같은 **채널 선호**는 일부러 안 잡았다(그건 거부가 아니라 경로 선호라 DM 은 열려 있다).
  더 보수적으로 갈지(연락 경로 언급도 전부 제외) 대표 판단.

### 검증
tsc 0 · 전체 **3,034 pass (240 파일)** · audit-gate **68 GREEN**
(파일 크기 래칫이 한 번 RED — `AdminInfluencerPoolPage` 602줄. rebaseline 으로 우회하지 않고
이름 칸을 `influencer-pool/LeadNameCell.tsx` 로 추출해 해소. 배지는 신호가 늘 때마다 한 줄씩 붙는
자리라 페이지 본문에 두면 계속 래칫을 밀어 올린다.)
