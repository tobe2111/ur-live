## 유어애즈 정비 슬롯 재배분 — 균등 순환이 "일이 남은 곳"을 못 보고 있었다

> **선행**: #853(거부 존중·고갈 회계) · #867(분류 규칙 공백) · #870('카페' 가드 사각지대) 전부 머지·배포됨.
> 이 문서는 그 다음, **정비 용량 배분**이다.

### 📏 무엇을 쟀나
어드민 `GET /api/admin/ads/influencer-pool/stats` 의 `maintenance` 스탬프(2026-07-29 10:39 UTC).

| 단계 | 마지막 회차 결과 | 판정 |
|---|---|---|
| `reextract` | `scanned 36,880 · filled 0 · done true` | **전수를 훑고 0건.** 저장 시점에 이미 추출하므로 남은 일이 구조적으로 없다 |
| `merge` | `merged 5 · groups 3` | 값은 있으나 소량·비시급 |
| `reclassify` | `scanned 38,382 · changed 219 · done true` | 전수 한 바퀴 13회차 |
| `quality` | `scanned 4,500 · scored 4,500 · done false` | 진행 중 |
| `handle` | `scanned 2,000 · fixed 2,481 · reopened 150 · done false` | **수율 최고이며 아직 안 끝났다** |

정비는 `PHASES[hourUTC % 5]` 로 **한 시간에 한 단계씩 균등 순환**한다(라이브 하트비트 5개와 정확히 일치:
06=reextract·07=reclassify·08=quality·09=handle·10=merge). 즉 각 단계는 5시간에 한 번이고,
전수 스윕은 reclassify 13회차 × 5h = **65시간**, quality 9회차 × 5h = **45시간**이었다.

⇒ **끝난 단계와 안 끝난 단계가 같은 몫을 받고 있었다.** `reextract` 는 아무것도 안 채우면서
정비 용량의 20%를 매번 가져갔고, 그 사이 `handle` 은 큐 앞머리의 측정 불가 행을 다 못 걷어냈다.
`handle` 이 밀린 만큼 **풀의 74%인 네이버 블로거 보강이 막힌다** — 가장 큰 레버의 관문이다.

### ✅ 바꾼 것
`PHASES[h % 5]` → **가중 10슬롯 배정표**(`MAINT_SCHEDULE`, influencer-maintenance 가 SSOT):

```
merge · reextract · reclassify · quality · handle · reclassify · handle · quality · reclassify · handle
```

| 단계 | 10시간당 (기존 → 변경) |
|---|---|
| `reclassify` | 2 → **3** (전수 65h → **43h**) |
| `handle` | 2 → **3** |
| `quality` | 2 → 2 |
| `reextract` | 2 → **1** |
| `merge` | 2 → **1** |

⚠️ **줄이는 쪽을 0으로 만들지 않았다.** 지금 `filled: 0` 은 "고장"이 아니라 "다 했다"라, 미추출 행이
새로 생기면 다시 값이 나와야 한다 — 10시간에 한 번이면 자기치유가 유지된다.
`phaseGapMinutes(PHASES.length)` 는 그대로라 stale 기준도 자동으로 따라온다(10슬롯 → 최대 간격 14h).

### 🛡️ 가드 — 주석 약속을 빨간불로
worker-ads 는 정적 import 를 피하려 배정표를 **리터럴로 복제**한다. 그동안 이 관계를 지킨 건
"추가 시 두 곳을 함께 고칠 것"이라는 **주석 한 줄**이었다(배정표에서 빠진 단계는 에러도 경보도 없이
영원히 안 돈다 — `cron-stale-watch` 는 기록이 아예 없는 이름을 판정 대상으로 못 잡는다).

`ads-lane-cadence.test.ts` 에 4검사 신설: ① 두 리터럴 순서까지 동일 ② `MAINT_PHASES` 전 단계가
배정표에 최소 1번 ③ 정의 밖 단계 혼입 금지 ④ 배분 의도(일이 남은 단계 > 끝난 단계, 그리고 아무도 0 아님).
**되돌려-검증 완료** — 한쪽 리터럴만 바꾸면 ①, 양쪽에서 단계를 빼면 ②④ 가 빨개진다.

⚠️ 이 가드가 **못 보는 것**: 배분의 *타당성*은 코드 밖 사실이다. 위 수치가 뒤집히면(예: `handle` 이
`done: true` 로 끝나면) 그 슬롯은 다시 남는 일 쪽으로 옮겨야 하고, 기계는 그걸 알려주지 않는다.

> 🧹 중복 정리: 같은 불변식을 `ads-influencer-handle-heal.test.ts` 도 검사하고 있었다(발견 당시엔
> `MAINT_PHASES` 와 비교). 두 곳에 두면 한쪽만 고쳐 놓고 지켜지는 줄 알게 되므로, 그쪽은 본래 관심사
> ("`handle` 이 실제로 배정돼 있는가")로 좁히고 전체 일치는 cadence 한 곳에서만 본다.

### ❗ 이번에 틀렸던 판단 (다음 세션이 같은 길로 안 가게)
**10:00 하트비트를 보고 "self-chain 이 안 돈다"고 결론 낼 뻔했다.** 근거는 그럴듯했다 —
`ads:enrich-influencer-driver` 의 `ms=18,615`(부모가 18.6초를 기다림 = 즉시응답 아님) 와
`enrich_lane.last_run = 10:00:18`(틱 + 16초 = 딱 1라운드). 둘 다 "체인 없음"을 가리켰다.
**그런데 #863(self-chain) 배포는 10:01:37 이었다.** 10:00 틱은 옛 코드였다.
⇒ **배포 시각과 틱 시각을 대조하기 전에는 라이브 수치로 코드를 판정하지 말 것.**
(이 레포의 "한 틱으로 판정하지 말 것" 룰이 이 경우엔 시각 대조 형태로 나타난다.)

### ▶️ 다음 세션 첫 액션 — 11:00 이후 회차로 판정할 것
`GET /api/admin/ads/influencer-pool/stats` 하나로 네 가지가 동시에 판정된다:

1. **self-chain 실효** — `enrich_lane.last_run` 이 틱 + 16초면 **1라운드(실패)**, 틱 + 수 분이면 체인 성공.
   성공 시 시간당 13명 → 156명, 미측정 26,694명 소진이 **85.6일 → 7.1일**.
2. **reextract 실패 사유** — 06:00 회차가 `ok=false`(ms 2,103)인데 `result: null` 이었다.
   #853 의 `cronErrorCode` 배포 이후 첫 reextract 는 **11:00**(새 배정표에선 `h%10==1`).
   `result` 에 `err=limit` 이 찍히면 예산 고갈, `err=timeout` 이면 다른 병이다.
3. **`opted_out`** — 아직 0. 저장 경로는 새 수집분부터, 소급은 `quality` 단계가 찍는다.
4. **`learned_cap`** — 44 에서 **+2씩 완만 상승**이면 #841 의 가산 회복 성공, 44↔55 왕복이면 실패.

### 🔎 아직 안 건드린 것 (근거만)
- `enrich_lane` 이 `spent 44 / budget_total 45` 로 **예산 포화**다. 라운드당 13명은 예산 천장이지
  코드 문제가 아니다 ⇒ 처방은 라운드 수(체인)뿐이고, 그건 #863 이 이미 했다. 위 1번으로 판정만 하면 된다.
- 하트비트 이름 충돌: 부모 `kick('/__ads/enrich-influencer-driver')` 이 유도하는 이름과
  `driverBeat` 의 이름이 **같다**. 즉시응답 이후엔 부모가 먼저(빠르게), 드라이버가 나중에 써서
  드라이버 값이 이기므로 **정상 동작에선 진실이 남는다.** 다만 드라이버가 부모보다 빨리 실패하면
  부모의 `ok=true`(디스패치 성공)가 마지막 값이 되어 **거짓 초록**이 될 수 있다.
  11:00 회차에 `result` 에 `planned=…  chained=…` 이 보이면 이 경로는 정상이다. 안 보이면 그때 고칠 것.

### 검증
tsc 0 · 전체 **3,181 pass (253 파일)** · audit-gate **72 GREEN** · 되돌려-검증(가드 4종) 완료
