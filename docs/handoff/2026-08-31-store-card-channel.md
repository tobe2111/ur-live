# 2026-08-31 — 어드민 매장 카드에 채널 스위치 + 돈 갈림표

## 대표 지시
> "되게 복잡해졌어" · "3번 결과는 운영자인 나만 보여야 해"

## 🩸 먼저 — 화면이 아예 없었다

`#1234` 에서 채널 API(`GET/PATCH /api/admin/sellers/:id/channel`)를 만들고 **부르는 화면을 안 만들었다.**
그래서 대표가 매장을 `direct` 로 바꿀 방법이 어디에도 없었고, **에러가 아니라 부재라 아무도 몰랐다.**

더 나쁜 건 PR #1254 본문이 *"어드민 `/admin/store-channel` 에서 채널을 지정해야"* 라고 적었다는 것 —
**그 페이지는 존재하지 않는다.** 그 PR 이 머지되면 "직접 입점만 2%" 가 켜지는데 direct 로 바꿀 길이 없어
아무도 2% 를 못 받는 상태가 됐을 것이다.

## 새로 만들지 않았다 — 이미 있는 카드에 붙였다

`/admin/merchant-commissions`(`AdminMerchantCommissionsPage`)가 **이미 매장 카드**였다.
매장 id 로 조회 → 영입자·기간·수수료율을 한 화면에서 본다. 빠진 건 채널 하나뿐이었다.
⇒ 새 페이지를 만들 뻔했다. (오늘 오전에 이미 있는 기능을 중복 구현한 것과 같은 함정 — 먼저 찾아봤다.)

## 무엇을 했나

- `src/pages/admin-merchant-commissions/StoreChannelCard.tsx` **신규**
  (페이지가 271줄이라 래칫에 걸려 컴포넌트로 추출 — 레포 룰)
  - 채널 라디오 2개(직접 10% / 대행 5%) → 기존 PATCH 재사용
  - **돈 갈림표** — 매장 수령 / 유어딜 수수료 / −PG 준비금 / −영입자 몫 / **유어딜 실수령**
  - 실수령이 0 이하면 빨강
- `admin-store-channel.routes.ts` GET 에 `rates` 추가(`loadSplitRates`) — 화면이 `platform_settings` 를
  직접 캐지 않게. **어드민 API 에만 있다.**

### 정직하게 표시하는 것 (숫자를 지어내지 않는다)
- **채널 미지정** → 계산 안 함 + "영입 2% 는 나가지 않습니다"
- **게이트 OFF** → "지금은 요율에 반영되지 않습니다"
- **영입자 없음 / 대행 경유** → 2% 줄에 `—` + 사유 표기

## 🔒 어드민 전용

표에 PG 준비금과 유어딜 실수령이 들어간다. 셀러·소개자 화면에 재사용 금지.
가드가 `seller-stores.routes` · `marketing.routes` 에 `pg_reserve_pct` 가 없는지 검사한다.

## 다음 세션 첫 액션
1. **QA 1단계**: 대표가 카카오 로그인 → 홍대돈까스(14) 운영권 → 이용권 1개 등록(금액 작게, Toss 라이브 키)
2. `/admin/merchant-commissions` 에서 매장 14 조회 → 채널을 `direct` 로 → 결제 1건 → 원장 fee **10%**
3. `brokered` 로 되돌려 결제 1건 → **5%** + 영입 2% **미지급**

## 남은 것
- 에이전시 영입 1% 폐지(#1260)의 **죽은 표면 12곳** 정리는 별도 PR(`2026-08-31-agency-intro-retired.md`)
- 매장 카드에 영입자 **지정** UI 는 아직 없다(조회만) — 지금은 DB 로만 세팅 가능
