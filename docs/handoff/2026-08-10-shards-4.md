# 측정 샤드 2 → 4 — 하루 관측이 조건을 채웠다

**2026-08-10 20:03 KST** · 대표 기승인 *"하루 지켜보고 차단 0이면 4배로"*

## 승격 근거 (2샤드로 만 하루)

```
① 차단   blocked 0 (ok 12,723)        ← 대표가 건 유일한 조건
② 생존   샤드 둘 다 (00시 되살아난 뒤 20시까지 0 없음)
③ 처리량 시간당 180(1샤드) → 440       ← 14~19시 정상 구간
④ 백로그 28,759 → 26,057               ← 감소 유지
```

⚠️ **24h 합계 7,764 은 낮게 잡힌 값이다** — 그 창의 앞 4시간(08-09 20~23시)이 샤드0 사망 구간이다.
정상 구간(14~19시) 기준 시간당 440 → 24h 환산 **~10,500**.

## ⚠️ 설명 못 한 이상 — 4샤드에서도 볼 것

```
KST        샤드0      샤드1
00~13시   16~85     233~264     ← 샤드0 만 낮다
14~19시  148~166    267~291     ← 갑자기 정상
```
샤드0 만 bio+YT 를 겸하므로 **행당 비용이 다른 것**이 유력하지만 **확인 안 했다.**
YT 일 쿼터 소진 시점과 겹칠 수도 있는데 그것도 안 쟀다 — **단정하지 않는다.**
4샤드에서 샤드1~3(전부 naverOnly)이 균일한지, 샤드0 만 같은 모양인지 보면 갈린다.

## 리스크와 롤백

네이버 부하가 또 배가 된다(ok 12,723 → 2만대 후반 추정). **그 구간 데이터가 없다.**
- 🔴 `blocked` 가 0 이 아니면 **즉시 `ENRICH_SHARDS` 를 1 로**
- 파일: `src/worker-ads/lane-alarm-runners.ts` — 값 하나. 롤백도 값 하나
- 2 로 내리는 중간 롤백도 안전하다(생성식이라 `slice.k` 가 자동 정합)

## 검증

유닛 **5,369 pass**(409 파일) · tsc 0 · build 0 · **audit-gate ALL GREEN 88**

샤드 가드는 **생성식**이라 값만 바꿔도 레인 수와 `slice.k` 가 자동 정합된다(테스트가 그 불변식을 고정).
다른 세션이 그 사이 킬스위치 가드(`ADS_INFLUENCER_ENRICH_DISABLED`)를 러너 안에 추가해 뒀고 함께 통과.

## 다음 세션의 첫 액션 — 하루 뒤

```sql
SELECT key,value FROM platform_settings WHERE key='ads_naver_crawl_block';   -- ⚠️ 첫 관문
SELECT strftime('%d %H', datetime(perf_checked_at,'+9 hours')) h, COUNT(*) 측정,
       SUM(CASE WHEN id%4=0 THEN 1 ELSE 0 END) s0, SUM(CASE WHEN id%4=1 THEN 1 ELSE 0 END) s1,
       SUM(CASE WHEN id%4=2 THEN 1 ELSE 0 END) s2, SUM(CASE WHEN id%4=3 THEN 1 ELSE 0 END) s3
FROM ad_influencer_leads WHERE perf_checked_at > datetime('now','-24 hours') GROUP BY h ORDER BY h;
SELECT key,value FROM platform_settings WHERE key LIKE 'cron_hb:ads:lane-alarm-boot:enrich%';
```
1. **`blocked` = 0 인가** — 아니면 즉시 1 로 되돌리고 대표 보고
2. **샤드 4개가 다 도는가** — 새 인스턴스(`enrich-influencer-3`·`-4`)는 첫 정각에 `started=true` 로 뜬다
3. 측정이 ~20,000/일 에 닿는가 · 백로그가 26,057 에서 더 빨리 줄어드는가
4. 위 "설명 못 한 이상"이 재현되는가(샤드0 만 낮은지 / 시간대에 따라 갈리는지)

## 남은 미확정

- 왜 알람이 안 깨어났는지(되살리기는 증상 처방이다). `revived:true` 빈도가 규모 지표
- 하루 수집 23,061 → 14,104(−39%) 원인. 이틀 연속 낮으면 진폭이 아니라 변화다
- `enrich-influencer-driver`/`-fanout` 은 여전히 죽어 있다(cron 게이트 뒤). 샤딩이 대체, 미정리
