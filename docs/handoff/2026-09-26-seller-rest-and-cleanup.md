# 마이에 남은 셋 + 안 쓰는 메뉴 정리 (2026-09-26)

> 대표: *"나머지도 다 해줘. 그리고 뺄 것들 빼자"*
> 앞선 지시: *"이용권 등록, 숙소까지 해줘 쿠폰은 앞으로 필요없을 것 같은데? 확인해줘"*
> PR: #1547 (브랜치 `claude/influencer-proposal-03yqmh`)

## 0. ✅ 머지·배포·판정 완료 (2026-09-26)

**머지 `39a465f`**(squash, PR #1547) · Verify ✅ · Pages 배포 ✅ · cron worker 배포 ✅.

### E4 판정 통과 — 라이브 번들·API 실측

| 축 | 판정 | 근거(라이브) |
|---|---|---|
| 🧹 숙소 메뉴 | **E4** | `app-seller-components` 에 `fallback:"숙소"` **0** · `seller.nav.stays` **0** · `BedDouble` **0** |
| 🧹 체험 캠페인 · 후기 인증 | **E4** | 라벨 **0** · `/seller/experience-campaigns` **0** |
| 🧹 쿠폰 · 프로모 코드 | **E4** | `/seller/coupons` **0** · `/seller/promo-codes` **0** |
| 🔒 직링크 탭 매핑 살아 있음 | **E4** | `/seller/stays`·`/seller/review-verifications` 가 **`also` 배열 안에만** 존재 |
| ✅ 리뷰는 안 내림 | **E4** | `/seller/orders` 묶음에 그대로 |
| 📖 운영백서 재시드 v37 | **E4** | `GET /api/guides/admin` → 꺼진 기능 **14** · `SELLER_DORMANT_HIDDEN` 포함 |
| 🧩 새 시트 셋 배포 | **E3** | `UserProfilePage` 청크에 문구·API 계약 전부 1 — **화면 동작은 미확인** |
| 🔴 알림톡 발송 경로 부재 | **E4** | 마이 청크에 `alimtalk/send`·`credits/charge`·`credits/confirm`·`templates` **전부 0** |

### 🩸 판정 중 내 측정이 한 번 헛돌았다 — 다음 세션이 같은 함정에 빠지지 않게

처음에 **`UserProfilePage` 청크에서** 메뉴 경로를 세고 "전부 0 → 내려감 ✅" 이라고 읽었다.
**틀렸다.** 0 인 이유는 메뉴가 빠져서가 아니라 **`seller-tab-groups` 모듈이 그 청크에 아예 없어서**였다.
드러난 계기는 대조군이다 — 내가 **안 건드린** `/seller/stores`·`/seller/influencer-deals`·`대행 승인` 도
같이 0 이었다. 그리고 "사용처리"·"파트너 찾기" 가 1 로 나온 건 메뉴가 아니라 **내가 이번에 쓴 버튼 문구**였다.

⇒ 올바른 청크는 **`app-seller-components-*.js`**(마이의 전체 도구 시트가 동적 import 한다).
⇒ **번들 문자열로 판정하기 전에 "내가 안 건드린 것이 거기 있는가"(대조군)를 먼저 확인할 것.**
   대조군 없이 0 을 성공으로 읽으면, 이 레포가 반복해 당한 *헛도는 가드* 를 판정에서 그대로 반복한다.

### 못 한 판정 (정직하게)

**셀러 계정 로그인이 필요한 화면 조작은 못 했다** — 이 세션엔 어드민 자격만 있다. 그래서
제안 수락 버튼이 실제로 눌리는지, 잔액 카드가 실제로 그려지는지는 **검증 안 됐다**(E3).
그 축은 `STAGING_CHECKLIST.md` **S-MYSELL-47~59** 로 남는다.

---

## 1. 다음 세션의 첫 액션

**머지·배포·라이브 판정은 끝났다(§0).** 남은 것은 **셀러 로그인이 필요한 화면 조작**이다 —
staging 에서 `STAGING_CHECKLIST.md` **S-MYSELL-47~59**. 그중 셋이 이번 변경의 핵심이다:

- **S-MYSELL-53** 잔액 조회를 실패시켰을 때 "0건" 으로 안 뜨는가(모르는 것과 0은 다르다)
- **S-MYSELL-57** 내린 메뉴 직링크(`/seller/review-verifications`)에서 하단 탭이 켜지는가
- **S-MYSELL-49** 수락 못 하는 제안에 **이유 문장**이 뜨는가(빈 카드 금지)

## 2. 이번에 한 것

### 🧩 마이 안으로 들어온 셋 (묶음 일곱이 됐다)

| 새 시트 | 하는 일 | 안 하는 일 |
|---|---|---|
| `PartnersSheet` | 받은 협업 제안 보기 · **수락/거절** | 내가 제안하기(상대 검색이라 전체화면) · 요율 변경 |
| `MessagesSheet` | 알림톡 **잔액 · 최근 발송** | 🔴 **발송**(등급 C) · 충전(결제) · 템플릿 편집 |
| `SettlementsSheet` | 지난 정산 목록 + 더 보기 | 취소·수정(어드민 지급 센터의 일) |

- 정산 명세는 **줄을 새로 만들지 않고** 출금 시트 바닥의 `[지난 정산 보기]` 로 연다
  (주문 → 환불과 같은 배치). 닫으면 출금으로 돌아온다.
- 파트너 수락 조건은 **서버 WHERE 와 1:1**(`status='proposed'` · `proposed_by='influencer'` ·
  `requires_content_proof=0`). 화면이 느슨하면 눌러도 404 고 사장님은 이유를 모른다 →
  순수 함수 `respondability()` 로 빼서 **테스트가 실제로 실행해** 잰다.

### 🧹 내린 것 — 근거는 전부 라이브 실측(2026-09-26 D1)

| 메뉴 | 실측 | 처리 |
|---|---|---|
| 숙소 | 셀러 소유 상품 **0** · 예약 **0** (`stay_voucher` 77건 전부 `seller_id IS NULL`) | `SELLER_DORMANT_HIDDEN` |
| 체험 캠페인 | 캠페인 **0** · 응모 **0** | 〃 |
| 후기 인증 | `kakao_review_submissions` **0** | 〃 |
| (기존) 쿠폰·프로모 코드 | 셀러 생성 **0** | `SELLER_COUPONS_HIDDEN` |

**지운 게 아니라 접은 것이다** — 라우트·페이지·API·`StaysSheet` 전부 살아 있고 플래그 한 줄이면
돌아온다. 테스트가 그 복구 가능성까지 고정한다(`<StaysSheet` 렌더 존재 검사).

### ❌ 내리지 **않은** 것 — 같은 0 이라도 뜻이 다르다

**리뷰(`/seller/reviews`)**. `product_reviews` 11.9만 행 중 셀러 소유 상품 리뷰가 0인 건 사실이지만,
원인이 *"아무도 안 써서"* 가 아니라 **셀러 소유 활성 상품이 아직 1개**라서다. 상품이 늘면 리뷰는
저절로 붙는다. 이 구분이 다음 세션에 지워지지 않게 **양방향 테스트 + 주입**으로 박아 뒀다.

## 3. 이번에 틀렸던 판단 · 배운 것

### 🩸 가드 둘이 내 변경을 잡았고, 둘 다 풀지 않고 재조준했다

1. **`/seller/review-verifications` 의 하단 탭이 꺼졌다.** 그 경로가 `tabGroupSiblings()` 로
   `also` 에 들어가고 있었는데, 탭 그룹에서 내리니 매핑에서도 사라졌다. **직링크는 여전히 열리므로**
   그때 탭이 안 켜지면 사용자가 위치를 잃는다 → 숙소가 이미 쓰던 방식대로 `also` 에 **명시**했다.
   ⇒ **접는 것과 매핑에서 지우는 것은 다른 일이다.**

2. **`folded >= 15` 가 빨간불.** 그 수는 *접힌 형제 수*라서 메뉴를 **내리면 같이 줄어든다** —
   사이드바는 더 짧아졌는데 검사는 실패했다. 지키려던 것은 "36줄로 돌아가지 않는다" 이지
   형제 수가 아니다 → **사이드바 줄 수 자체**(묶음 ≤ 8)와 **접힘이 일어나는가**(≥ 8)로 분리.
   ⇒ **대리 지표(proxy)는 전제가 바뀌면 조용히 틀린 것을 잰다.**

### 🩸 `--only` 는 정규식이 아니라 부분일치다

`--only "a|b"` 가 **0건을 돌고도** 안내만 띄운다. 13건을 확인하려고 이름을 뽑아 **한 건씩 루프**로
돌렸다(`scratchpad/names.txt`). 파일명으로도 못 거른다.

### 🩸 생성물 동기는 로컬 게이트가 잡았다 (CI 90분을 아꼈다)

`feature-flags.ts` 를 건드리면 **운영백서 + 기능 현황판**이 함께 재생성돼야 하고, 운영백서가
바뀌면 **`GUIDE_SEED_VERSION` 도 올려야** 한다(안 올리면 라이브 가이드가 조용히 안 바뀐다).
36 → **37**. 이 셋은 항상 같이 움직인다 — 다음에 플래그를 추가하면 바로 이 셋을 챙길 것.

## 4. 남은 결정 / 대기

- **머지 지시** — PR #1547 은 draft 이고 대표 지시를 기다린다.
- **E4 라이브 판정** — 머지 뒤 S-MYSELL-47~59. 특히 **S-MYSELL-53**(잔액 조회 실패가 "0건"으로
  안 뜨는지)과 **S-MYSELL-57**(내린 메뉴 직링크에서 탭이 켜지는지)이 이번 변경의 핵심이다.
- **정산 명세는 아직 E2** — `settlements` 가 라이브 0행이라 **빈 화면만 확인됐다.**
  실제 행이 있는 화면은 staging 에서 출금을 한 번 신청해야 볼 수 있다(S-MYSELL-55).
- 🔴 **CI 비용 구조(미해결)** — 이 브랜치는 문서 한 줄만 고쳐도 전수 주입 45~77분을 태운다.
  후보 처방 셋은 `CLAUDE.md` 방어선 표의 "주입 앵커가 낡음" 행에 적어 뒀다. **CI 정책 변경이라
  세션이 임의로 바꾸지 않았다.**

## 5. 실측 원본 (2026-09-26 라이브 D1 · 읽기 전용)

```
셀러            approved 9 · suspended 2      셀러 소유 활성 상품   1
주문 89 · 이용권 발급 2                        리뷰 11.9만(셀러 상품 것 0)
운영자 위임 9(전부 유효) · 팔로워 3 · 협업 제안 1(active)
숙소 상품 77(셀러 소유 0) · 숙소 예약 0 · 객실 154(전부 플랫폼)
체험 캠페인 0 · 응모 0 · 후기 인증 0
쿠폰 1(seller_id NULL = 플랫폼) · 프로모 코드 0
정산(settlements) 0 · 알림톡 계정/템플릿/발송/충전 0/0/0/0
```
