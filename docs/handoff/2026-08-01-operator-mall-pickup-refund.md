# 인계 — 운영자 몰 ④-b 미수령 환불 · 도달불가 라우트 래칫 (2026-08-01)

> 이 세션의 범위: 운영자 SaaS 공구(도매 인프라 재활용) 세션 ④-b + ⑤ 일부 + 가드 1종.
> 함께 본 축: `operator-mall-release-checklist.md` 의 잔여 항목 전수.

---

## 1. 다음 세션의 첫 액션

### ④-c 부분환불 금액 입구 — **머니 경로, 단독 세션**

**판정 명령**:
```bash
grep -n "refund_amount" src/features/returns/api/returns.routes.ts
```
**무엇을 보면 무엇이 판정되나**: `refund_amount` 가 나오는 줄 중 **`UPDATE returns SET ... refund_amount`** 이
**하나도 없으면** 아직 미구현이다(2026-08-01 현재 그렇다).

**현재 상태(실측)**:
- `returns.refund_amount` 는 **반품 신청 시점에 주문 총액으로 한 번 박히고**(`returns.routes.ts:154`)
- **그 값을 바꾸는 엔드포인트가 하나도 없다** — `approve` 는 `status` 만 UPDATE
- 환불 실행기(`PUT /:id/refund`)는 그 값을 충실히 쓴다 ⇒ **실질적으로 전액 환불만 가능**

⚠️ **체크리스트의 이전 서술을 믿지 말 것.** *"API 는 `refundAmount` 를 받는데 화면은 전액만 호출
(`order.routes.ts:848`)"* 이라고 적혀 있었는데 **그 파일은 존재하지 않고** 진단도 틀렸다.
화면 문제가 아니라 **경로 자체가 없다.** #936 에서 교정했다.

**설계 시 지킬 것**:
- 게이트 OFF 로 착수(④-b 와 동일 패턴)
- 금액 상한은 서버에서 **주문 실결제액**으로 클램프(클라 값 신뢰 금지)
- 승인·거절(상태)과 환불(돈)을 **한 화면에 섞지 않는다** — `SellerReturnsPage` 가 그렇게 돼 있다
- 유어딜 5% 불변

---

## 2. 완료분

| 항목 | PR | 커밋 |
|---|---|---|
| ④-b 미수령 환불(보관구분별) — **게이트 OFF** | #932 | `045ee18` |
| O9 운영자 문의 경로 + ⑤ 반품 큐 화면 + 인계 | #925 | `b54c166` |
| 도달 불가 라우트 래칫(가드 신설) | #934 | `6c95a7b` |
| 체크리스트 실측 sync | #936 | `0b74367` |

**④-b 가 실제로 하는 일**: `auto-settlement.handleExpiredVoucherRefunds`(cron `0 18`, 실행 확인됨)의
환불액을 `unclaimedRefundAmount()`(순수, `src/shared/pickup-refund.ts`)로 가른다.
깎인 만큼은 원장에 `unclaimed_forfeit` 로 남는다(적립-역전 대칭).

**안전판 3겹** — 하나라도 풀리면 라이브 환불액이 조용히 바뀐다:
1. 게이트 `pickup_unclaimed_policy_enabled` 기본 OFF
2. 비율 기본값 100 (켜도 값을 안 넣으면 동작 불변)
3. `storage` 가 `null` 이면 전액

---

## 3. 🔴 이번에 틀렸던 판단 — **이게 제일 값지다**

### ⓐ 없는 이름으로 검색해 가짜 갭을 보고할 뻔했다
`/seller/quick-gb` 에 링크가 0건이라고 판단했는데, **실제 경로는 `/seller/products/quick`** 이었고
진입 버튼도 가드 테스트도 이미 있었다. *경로를 확인하지 않고 이름을 추측해서 grep* 했다.
⇒ 이후 판정은 전부 **라우트 파일에서 실제 경로를 파싱**하는 탐지기로 다시 돌렸다.

### ⓑ 셸 모니터가 죽어 있었는데 30분간 몰랐다
`curl https://api.github.com/...` 이 이 컨테이너에서 **403** 이다(`GH_TOKEN` 이 `proxy-injected`).
모니터가 아무것도 못 내는 채로 폴링만 했다. **GitHub 상태는 MCP 툴로만 볼 수 있다** —
셸 폴링 방식은 이 환경에서 구조적으로 안 된다.

### ⓒ 가드를 느슨하게 만든 게 정답이었다
엄격한 도달성 판정(`to=`/`navigate(`/`href=` 만)은 오탐이 **62건**이었다.
`notifyUser(..., '/my-returns')` 같은 **prefix 없는 인자**를 못 보기 때문이다.
느슨하게(문자열 등장) 바꾸니 21건. **오탐 많은 가드는 결국 아무도 안 켠다** —
이 레포의 `check-input-text-color` 가 정확히 그래서 두 달간 미등록으로 방치됐다.

### ⓓ 첫 측정이 "고아 0건"을 냈는데 그게 함정이었다
가이드 시드의 **경로 텍스트를 링크로 셌다.** 문서에 경로가 적힌 것은 링크가 아니다.
이 레포가 여러 번 만난 *"주석에만 남아도 통과"* 와 같은 모양이다.
⇒ 되돌려-검증 3종에 **"문서 본문에만 적힌 경로 → red"** 를 넣었다.

### ⓔ 내가 만든 가드의 버그를 자기검증이 잡았다
`walk()` 안에서 `continue` 대신 **`return`** 을 써서 디렉터리 순회가 통째로 끊겼다.
*"측정 0건은 통과가 아니라 실패"* 규칙이 첫 실행에서 빨간불을 냈다. 그 규칙이 없었으면
경로 0건 → 고아 361건이라는 이상한 값을 조용히 냈을 것이다.

### ⓕ 테스트가 실제 머니 버그 2건을 잡았다 (사양 작성 시점엔 안 보였다)
1. 🔴 `platform_settings` 값을 **빈 문자열로 지우면 환불이 0원**이 됐다.
   `Number('')` 이 0 이라 **"미설정"과 "0%"가 구분되지 않았다.**
2. `Infinity` 결제액이 `Number(x) || 0` 을 통과했다.

### ⓖ 사양에서 판단 하나를 바꿨다
기준일(픽업일)을 **모르는데 유예가 설정돼 있으면 유예 안으로 본다.**
*"유예가 지났다"* 를 확인 못 한 채로 소비자 돈을 깎지 않는다.

---

## 4. 남은 결정 / 👤 대표 대기

| 순위 | 항목 | 풀리는 것 |
|---|---|---|
| 1 | **X2 예치금 4숫자** | 철거 PR — X8(문의)이 #925 로 해소돼 **남은 유일한 선행조건** |
| 2 | **④-b 정책값 D1·D2** (법무 **X4c** 선행) | 미수령 환불 게이트 활성. 안 넣으면 현행 전액 환불 그대로 |
| 3 | **`operator_support_contact`** 값 1개 | 운영자 문의 카드가 실제로 보임(지금은 미설정이라 **미렌더**) |
| 4 | **X7 알림 env 2개**(`DISCORD_WEBHOOK_URL`·`SENTRY_DSN`) | 런타임 에러 도달 경로(현재 0) |
| 5 | **P9 · P10 프로덕션 소액 실결제** | 공구 특가(#844) · 미수령 환불(#932) 활성 |
| 6 | cron 바인딩(`TOSS_SECRET_KEY`·`CACHE_KV` 등) | `payouts-generate`(`0 0 * * 1`) — **하트비트 여전히 0건** |

### ⚠️ `payout-center` 가 비어 있는 것을 "밀린 돈 0" 으로 읽지 말 것
그 화면이 읽는 `settlements` 는 **`payouts-generate` 가 만드는 테이블**이다.
cron 이 안 도니까 비는 것이지 받을 돈이 없는 게 아니다 — **순환**이다.
실제 미지급액은 이 환경에서 **못 낸다**(원장 조회 엔드포인트 404 + CF 토큰 사망 X6).
**추정치를 만들지 않았다.**

### 🧹 백로그 — 도달 불가 라우트 21건 (동결됨, 자동 수리 대상 아님)
`scripts/orphan-routes-baseline.json`. 링크를 달지 지울지는 판단이 필요하다.

- 🔴 **`/account/settings`** — 사용자가 자기 **테마·언어**를 바꾸는 화면인데 앱에서 가는 링크가 없다
  (경로가 나오는 곳은 전부 **주석**)
- 셀러 죽은 페이지 5건: `/seller/2fa` · `/seller/marketing` · `/seller/realtime` ·
  `/seller/store-dashboard` · `/seller/voucher-orders`
- 나머지는 **앱 밖 진입**(OAuth 콜백 · PG 리턴 `/success`·`/fail`)이라 **동결이 정답**이다
