# 🪑 소유자 오판 수리 + 이용권 영입자 도장 + 자물쇠 사각지대 (2026-09-09)

대표 *"남은 것들 다 해줘"* — `2026-09-07-store-handover-money.md` 의 "의도적으로 안 한 것" 처리.
착수 전에 라이브를 재 봤고, **재 보니 더 급한 게 나왔다.**

## 🔴 ① 직접 등록한 사장님이 자기 매장의 소유자가 아니었다 (잠복 결함 — 돈을 못 받는다)

`/store/new` 는 설계상 `sellers.linked_user_id` 를 **비워 두고**(UNIQUE 1인1행)
`seller_operators` 로 접근을 준다 — **직접(direct)=`role:'owner'` · 중개(brokered)=`role:'operator'`**
(`seller-stores.routes.ts:12-14, 479`).

그런데 소유 판정이 **토큰을 건너며 무너졌다**:
```
canOperateStore    → { role:'owner', source:'grant' }        ← 역할은 맞게 나온다
토큰 발급          → if (source==='grant') operator_user_id  ← 역할을 안 싣는다
resolveStoreActor  → isOwner = operatorUserId === null       ← 그래서 false
```
⇒ 직접 등록한 **진짜 사장님이 운영자로 오판**되고 소유자 전용 게이트가 전부 닫힌다:
① **정산 계좌 변경 403 → 그 매장은 돈을 받을 방법이 없다** ② 자기 사업자정보 마스킹
③ 탈퇴 불가 ④ 자기 매장 정산 내역이 합류 이후로 잘림(#1404 가 넣은 범위 게이트).

**수리**: 토큰에 `store_role` 을 함께 싣고, `resolveStoreActor` 가 **역할로** 소유를 가른다.
`operator_user_id` 는 그대로 둔다 — 그건 소유 판정이 아니라 **시트 분리**용이다(빼면 운영자
진입 시 사장님이 튕긴다). 옛 토큰(30일)은 `store_role` 이 없으므로 **종전 규칙으로 폴백**.

⚠️ 라이브에 직접 등록 매장이 아직 0이라(유일한 매장 14 는 brokered) 오늘 터지진 않았다 —
**다음에 "직접(업주)"로 등록하는 첫 사장님이 바로 밟는다.** 그게 주 경로다(수수료 10%).

## 🕳️ ② 손바뀜 자물쇠가 **지금 만드는 모든 매장에서 무력**이었다

어제 건 자물쇠(#1404)는 `sellers.linked_user_id` 만 봤다. 그런데 위 설계대로 그 칸은 항상 비어 있다.
라이브 실측: 매장 1곳, `linked_user_id` **전부 NULL**. ⇒ 잠긴 것처럼 보이는데 안 잠겨 있었다.

**수리**: `resolveCurrentOwner()` 신설 — `linked_user_id` **와** `seller_operators.role='owner'` 를
함께 본다. 셋을 구분한다: 주인 있음 / 없음(`null`) / **모름(`undefined`)** — 섞이면 모름이 통과가 된다.

## 🤝 ③ 이용권이 **판 시점의 영입자**를 기억한다 (인계의 "안 한 것" ①)

사용 레일이 사용 시점에 매장의 *현재* 영입자를 읽어, **과거에 팔린 이용권의 커미션까지 오늘의
영입자에게** 갔다(소급). 대표 원칙(*"귀속되는 시점부터 계산"*)의 마지막 미적용 자리.

- `vouchers.introduced_by_influencer_id` + **`intro_stamped_at`**(migration 0289 + repair-schema)
- `voucher-intro-stamp.ts` SSOT — 발급 3경로(/join · 카드결제 · 체험단)가 판 시점에 판정해 찍는다
- 사용 레일은 도장을 **먼저** 읽고, 수령자·중복·본인구매 판정 전부 `payeeId`(도장 우선)로 통일

🔑 **도장이 두 칸인 이유**: `introducerId` 만으로는 "영입자 없음"과 "옛 이용권(미판정)"이 구분되지
않는다. 구분 못 하면 옛 이용권이 전부 "없음"으로 접혀 **실제 보상이 사라진다.**
⚠️ **백필하지 않는다** — 오늘의 영입자를 과거에 적어 넣는 건 없던 사실을 만드는 일이다.

## 검증

tsc 0 · vitest **658파일 8,166건 pass** · 주입 지도 989 · guard-registry 128 · sql/file-size GREEN.
행동 시험(실제 SQLite + 실제 함수) 신규 2파일 · 기존 손바뀜 행동 시험 12 → 17건.

🩸 **이번에도 헛도는 가드를 셋 잡았다**(전부 주입 검증이 잡았다 — 눈으로는 못 봤다):
1. 소유 판정 시험이 **토큰 발급부를 재구현**한 사본이라, 진짜 발급 줄을 지워도 초록이었다.
2. 도장 배선 단언이 `resolveVoucherIntroStamp` 를 이름으로만 찾아 **import 줄에 걸렸다** —
   호출을 통째로 지워도 통과. ⇒ **호출 자리(인자 포함)** 로 앵커를 좁혔다.
3. 내가 `ledger.ts` 의 만료 판정 줄을 바꾸면서 기존 주입 지도 1건이 낡았다(`--map-only` 가 잡음).

그리고 **기존 가드가 내 변경을 잡았다**: `store-operator-scope.test.ts` 가 옛 규칙 문구
(`isOwner: operatorUserId === null`)를 고정하고 있었다. 규칙만 갱신하고 그 시험이 진짜 지키는 것
(**토큰이 들고 있는 사실만 본다 · `linked_user_id` 폴백 금지**)은 그대로 뒀다.

## 🧭 ④ 위치 없이 '가까운 순'이면 정렬이 통째로 사라졌다 (대표 신고 — 세션 중 추가)

대표: *"딜 50개 아래에 있는 쪽 이용권들은 거리순으로 정렬이 안되어있는데?"*

**서버는 멀쩡했다**(라이브 실측 `?near=37.2005,127.0980` → 0.1 · 0.2 · 0.3 · 0.5 · 0.8 · 1.0km).
클라가 `near` 를 안 보내고 있었다:
```
sort = sortBy==='distance' ? '' : sortBy      → 거리순이면 서버 sort 를 비운다
near = sortBy==='distance' ? userLoc : null   → 위치가 없으면 near 도 없다
클라 정렬: if (sortBy==='distance' && userLoc) → 위치가 없으면 건너뛴다
```
⇒ **셋이 겹쳐 아무 정렬도 안 된 목록이 "거리순" 라벨을 달고 나온다.** 에러도 빈 화면도 없다.

🕳️ 그리고 헤더의 동네 이름(`ur_near_dong_v1`)은 **localStorage 에 따로 남아** 위치가 없어도
계속 보인다 — 사용자는 위치가 잡힌 줄 안다. 이 구멍이 눈에 안 띈 이유다.

**수리** — `restaurant-map/effective-sort.ts` SSOT:
- **위치 없이 거리순은 존재할 수 없다** → 할인율 순으로 내려간다(정렬 부재 상태 금지)
- **서버 요청과 클라 재정렬이 같은 함수를 쓴다**(2026-09-03 에 둘이 갈려 "인기순"이 인기순이
  아니었던 그 클래스 재발 방지)
- 거리순을 고르면 **위치를 요청한다**(`chooseSort` → `requestNearMe`) — 막다른 상태를 애초에 안 만든다

⚠️ **거리순 1번이 항상 최단거리는 아니다** — "데모 이용권 항상 후순위"(2026-07-04 대표 지시)가
1차 정렬 키라 비데모가 먼저 온다(실측: 전주 151km 가 동탄 0.1km 앞). **의도된 규칙**이고
데모가 사라지면 함께 사라진다. 지금 바꾸지 않았다.

## ➡️ 다음 세션의 첫 액션 — staging 실측 4개

1. **직접(업주)로 매장 등록** → 정산 계좌 저장이 **되는가**(종전엔 403). 사업자정보가 안 가려지는가.
2. **중개로 등록** → 계좌 변경이 여전히 **403** 인가(게이트가 헐거워지면 안 된다).
3. 잔액 있는 매장에서 `seller_operators.role='owner'` 를 다른 사람에게 주려 할 때 자물쇠가 막는가.
4. 이용권 구매 → `vouchers.intro_stamped_at` 이 찍히는가. 영입자를 바꾼 뒤 사용 → **옛 영입자**에게 가는가.
5. **위치 거부 상태로 '가까운 순' 선택** → 위치 요청이 뜨는가, 거부하면 목록이 할인율 순으로
   **정렬돼 있는가**(종전엔 아무 정렬도 없었다).

## 🟡 남은 결정 — 승계 3단계 (대표 판단 필요)

설계(`store-operator-model.md` §5·§6)가 **착수 조건**을 명시해 뒀고, 오늘 재 보니 **조건 ②가 충족**됐다:
```
① 다중 매장 수요                 0건   (미충족)
② 대리 등록 매장                 1건   ← 충족: seller 14 홍대돈까스
   store_channel='brokered' · role='operator' · linked_user_id NULL · bank_account NULL
③ 위임 관계                      1건
```
**즉 홍대돈까스의 진짜 사장님은 오늘 자기 매장을 가져올 길이 없고, 계좌가 없어 아무도 돈을 못 받는다.**

다만 §6 은 동시에 경고한다 — *"수요가 0인 걸 정교하게 지으면 또 빈 코드가 남는다"*.
조건이 **딱 1건**이고 그 사장님이 아직 요청한 적이 없다. 그래서 지금 판단을 여쭙는다:

- **(a) 지금 짓는다** — 사업자번호로 매장 찾기 → 어드민 승인 → `role='owner'` 승격 + 중개자 강등.
  검증 파이프라인은 이미 있다(`sellers.business_registration_*`). 자물쇠도 이제 그 문을 본다.
- **(b) 사장님이 실제로 요청할 때 짓는다** — 그때까지 어드민이 수동으로 승격(1건이라 가능).

**추천: (b)**. 이번에 자물쇠가 그 문을 보게 만들었으므로 **수동 승격도 이제 안전하다**(잔액이
남아 있으면 막히고, 마감 창구로 이전 주인 몫을 떼고 넘긴다). 1건을 위해 신원 확인 UI 를 새로
짓는 것보다, 두 번째 사례가 생길 때 짓는 편이 §6 의 경고와 맞다.

## ✅ 커밋·CI 를 막던 것 — npm audit (해결됨. 대표 판단 불필요)

### 🩸 내가 처음에 틀리게 보고했다 — 두 가지가 틀렸다

첫 판에 *"high/critical 9건, vite·postcss·concurrently·wrangler·miniflare·shell-quote 등"* 이라고
적고 **대표 판단 사항(allowlist 등재 vs 업그레이드)** 으로 올렸다. **둘 다 틀렸다.**

1. **개수·패키지가 틀렸다.** 게이트 로직을 그대로 돌려 보니 실제 차단은 **10건 / 패키지 셋뿐**이다.
   내가 나열한 대부분(vite·postcss·shell-quote…)은 **이미 allowlist 에 등재된 것**인데 섞어 말했다.

   | 패키지 | 건수 | 취약 범위 | 경로 |
   |---|---|---|---|
   | `@xmldom/xmldom` | 8 | `<=0.8.14` | `@capacitor/cli` → `plist` |
   | `js-yaml` | 1 | `4.0.0 - 4.3.1` | `eslint-plugin-import` → `eslint` |
   | `sharp` | 1 | `<0.35.4` | `wrangler` → `miniflare` |

2. **대표 판단 사항이 아니었다.** 이 레포엔 **세 번째 길**이 이미 있다 — `package.json` 의
   `overrides`(12개 기존 항목: `tar`·`js-yaml@4`·`shell-quote`·`undici`…). 그리고 같은 사건의
   선례가 커밋 로그에 그대로 있다:
   `b93fe9c6c 2026-08-22 "fix(deps): tar 7.5.13 → 7.5.22 — 새 취약점이 모든 PR 의 감사 게이트를 막고 있었다"`
   ⇒ **allowlist(위험 수용)도 아니고 메이저 업그레이드도 아닌, 패치 상향**이 이 레포의 정답이다.

### 수정 — overrides 세 줄

```
"js-yaml@4":        "^4.3.1" → "^4.3.2"   # 기존 override 가 취약 범위 안에 있었다
"@xmldom/xmldom":   "^0.9.12"  (신규)
"sharp":            "^0.35.4"  (신규)
```

**런타임 위험 0**: 셋 다 `dependencies` 에 없고 `src/` 어디서도 안 쓴다(배포 번들 미포함).
`@capacitor/cli` 는 직접 devDependency 조차 아니고 `app:sync`/`app:android`/`app:ios`(모바일 패키징)
에서만 쓰여 **CI·웹 빌드 경로 밖**이다 — 0.8→0.9 를 넘긴 근거.

⚠️ **`npm audit` 은 npm 서버의 취약점 DB 를 실시간 조회한다** — 어제 초록이던 PR 이 오늘 빨간불이
되는 것은 정상이고, 이 레포에서 최소 세 번째다(07-15 · 07-25 allowlist 등재 · 08-22 tar 상향).
**내 브랜치 문제가 아니라 그 시점의 모든 PR 이 같이 막힌다** — 다음에 같은 일이 나면 대표에게
결정을 올리기 전에 **`npm audit --json` 의 `fixAvailable` 부터 볼 것.**

검증: 게이트 재실행 초록 · tsc 0 · `npm run build` 0 · vitest **659파일 8,173건 pass**.

## 🧹 남긴 것 — 주입을 인라인에 넣었다(규약 이탈)

CLAUDE.md 는 새 주입을 `scripts/mutations/<도메인>.mjs` 에 두라고 한다(동시 세션 간 머지 충돌
때문). 이번 11건은 `check-guard-mutations.mjs` **인라인 배열**에 넣었다 — 동작은 같지만 규약 이탈이고,
지금 열려 있는 다른 브랜치(#1413, 주입 7건)와 **같은 줄을 다툴 수 있다.** 다음 세션이 옮길 것.

---

# 🪑 후속 — 같은 병이 **돈 나가는 쪽에도** 있었다 (2026-09-09, #1414 머지 후)

대표 *"계속 해"*. #1414 로 소유 판정을 고친 뒤, **가설 하나를 전수로 검증했다**:

> `sellers.linked_user_id` 를 신호로 쓰는 코드는 전부 같은 병을 앓는다 —
> 그 칸은 `/store/new` 설계상 **항상 비어 있으니까.**

맞았다. 네 곳 더 있었고 **전부 돈에 닿는다.**

| 자리 | 직접 등록 사장님에게 일어나던 일 |
|---|---|
| `curator.routes:995` 출금 자격 | **403 "실제 돈 출금은 사업자 셀러만 가능합니다"** — 자기 매장 매출인데 |
| `curator.routes:1142` 출금 UI 분기 | "일반 회원 · 딜로만 적립" 으로 표시 |
| `curator.routes:1212` 사업자 인증 | 등록증까지 냈는데 콘솔이 계속 '사업자 등록' 을 요구 |
| `affiliate-credit:161` 자가구매·주인=추천인 | **가드 둘이 조용히 통과** — 판매수익 + 추천수수료 이중지급 |

마지막 것이 특히 나쁘다. `SELF_SELLER` 가드는 **2026-07-07 대표 결정**으로 넣은 것인데
(*"주인이 자기 유어샵에서 자기 상품을 팔면 이중지급"*), 오늘 등록되는 모든 매장에서 **죽어 있었다.**
에러가 안 나고 돈이 더 나가는 방향이라 아무도 신고하지 않는다.

## 수리 — 규칙을 한 군데로

`seller-operators.ts` 에 둘을 만들고 네 자리가 **같은 함수**를 쓴다:
- `findOwnedApprovedSeller(DB, userId)` — 유저 → 자기가 **주인**인 승인 매장
- `resolveStoreOwnerUserId(DB, sellerId)` — 매장 → 주인 (손바뀜 자물쇠 `resolveCurrentOwner` 가 위임)

⚠️ **`role='owner'` 만이다.** 중개(operator)는 볼 수 있는 매장을 넓힐 뿐 정산 귀속을 안 바꾼다
(그 파일 머리말). operator 를 통과시키면 중개자가 남의 가게 돈을 출금한다.

🛟 **넓히다 되던 걸 깨지 않는다**: `seller_operators` 조회가 실패하면 **옛 `linked_user_id` 판정으로
폴백**한다. 개선하려다 오늘 출금되던 사람이 막히면 그건 개선이 아니라 사고다.

## 🩸 이번에도 헛도는 가드를 잡았다 (오늘 다섯 번째 — 전부 주입이 잡았다)

"회수된 owner 는 주인이 아니다" 를 **유저→매장 방향에서만** 확인했다. 주입이 반대 방향
(`resolveStoreOwnerUserId`)의 `revoked_at IS NULL` 을 지웠는데 **시험이 통과**했다.
⇒ 같은 규칙을 쓰는 함수가 둘이면 **두 방향 다** 시험해야 한다.

그리고 SSOT 로 옮기면서 **기존 주입 2건의 지도가 낡았다**(`--map-only` 가 잡았다) — 지우지 않고
새 자리로 재조준했다. 지키는 불변식은 그대로다.

## 검증

tsc 0 · vitest **660파일 8,186건 pass** · 주입 지도 996 · 🪑 7건 · 🕳️ 9건 **전부 빨간불 확인** ·
guard-registry 128 · sql-column/bind/table · file-size · balance-write GREEN.

## ➡️ 다음 세션

- 이 PR 의 staging 실측: **직접 등록 사장님으로 출금 신청** → 403 이 아닌지. 중개자로는 여전히 막히는지.
- `seller-churn-detect.ts:41`·`seller-public-payload.ts:100`·`worker/index.ts:2605` 도 같은 신호를 쓴다.
  돈이 아니라 **알림·표시**라 이번 범위에서 뺐다 — 직접 등록 매장이 늘면 그때 함께 본다.
