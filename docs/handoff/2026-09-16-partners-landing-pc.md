# 입점 랜딩(/partners) 전면 재작성 — PC 신설 + 덱 정합 (2026-09-16)

> 대표: *"지금 매장 사장님이 가입하는 첫 페이지가 이거인데 전혀 메리트가 없어. 한번 확인해봐.
> 대충 만든 느낌이고. 특히 PC 버전은 따로 없어. 프레임에 갇혀있고. 우리 유어딜 서비스에 대해서
> 정확히 인지시키면서 가입하라고 유도해야하는데"*

**[E3] 배포까지. 라이브 판정(E4)은 배포 후 `urdeal.kr/partners` 를 PC 로 열어 눈으로 볼 것.**

## 1. 다음 세션의 첫 액션

```bash
# 배포 후 PC 판정 (이 한 가지만 보면 된다)
#   ① 좌우에 소비자 거터 레일(홈·교환권·이용권·유어샵 바로가기 + 앱 QR)이 "없는가"
#   ② 본문이 430px 이 아니라 화면 폭을 쓰는가
curl -s https://urdeal.kr/partners | grep -c 'ur-content-wide'   # 0 이면 배포가 안 된 것

# 로컬에서 다시 보려면
npm run build:client && node out/shot.mjs /partners 1440 pc   # out/shot.mjs 는 gitignore 밖 임시 하네스
```

## 2. 실측한 문제 (대표 지적이 전부 맞았다)

| # | 문제 | 원인 |
|---|---|---|
| ① | **PC 가 없었다** | `/partners` 가 `MobileAppLayout` 의 `HIDE_SIDEBAR_PREFIXES` 에 없어 430px 소비자 액자에 갇혔다. 그리고 액자가 만든 빈 거터를 `ConsumerFrameRails` 가 채운다 → **입점 검토 화면의 좌우가 전부 소비자 앱 광고**였다(대표 스크린샷이 그것) |
| ② | 신뢰 숫자가 사장님 것이 아니었다 | `19조 글로벌 동일 모델 시장 규모` 는 투자자 언어다. 사장님은 TAM 을 안 본다 |
| ③ | 덱의 심장이 빠져 있었다 | 대표 승인 소개서가 *"이 문서의 심장"* 이라 주석한 **체험단·배달앱 비교표**가 랜딩엔 없었다 |
| ④ | 계산기가 빼기만 했다 | 정가 → 할인 → 수수료 → 입금. 사장님이 *얻는 것*은 화면에 한 줄도 없었다 |
| ⑤ | 시작하는 길이 하나뿐 | "셀러 가입하기" 하나. 폰으로 가게를 등록해 본 적 없는 사장님에게는 문이 없었다 |

⚠️ `/partners` 는 **곁가지가 아니라 정문**이다 — `utils/seller-entry.ts` 가 PC 상단 네비·계정 메뉴·
푸터·마이의 "판매하세요" 를 전부 여기로 보낸다.

## 3. 무엇을 했나

- **액자 해제**: `MobileAppLayout` `HIDE_SIDEBAR_PREFIXES` 에 `/partners` 1줄.
  `App.tsx` 는 이미 fullScreen 처리 중이라 프레임만 풀면 됐다(블로그·`/design` 선례와 동일).
  ⚠️ 접두사를 `/partner` 로 줄이면 **입점 문의 폼 `/partnership` 을 삼킨다**(테스트가 고정).
- **내용을 대표 승인 덱으로 교체**: `urdeal-store-owner-deck.build.mjs`(09-15 v7, 15장) +
  기획 `three-decks-plan-2026-09.md` §0(사실)·§2(구성). 9개 섹션, `src/pages/partners/` 7파일.
- **숫자 SSOT 신설**: `src/shared/partners-facts.ts`. 덱 `deck-common.mjs` 의 `FACTS` 를
  테스트가 **실제로 파싱해 대조**한다 → 카톡 PDF 와 사이트가 갈리는 길을 막았다(§0-5 가 실측한 사고).
- **색은 기존 부품으로**: 다크 색면은 `.ur-panel-ink`(=`--home-field`, `--brand-text` 국소 치환),
  본문은 `text-ink`. 손으로 박은 hex 총량이 **2262 → 2225** 로 줄었다.

## 3-1. 대표 추가 지시(같은 날) — 장점 3 · 차별점 3 · 도구, 공구는 제외

대표가 넣을 내용을 직접 주셨고, **코드로 전부 대조한 뒤** 넣었다. 두 개는 라이브와 달라 문장을 고쳤고,
넷은 꺼져 있어 뺐다(대표 *"공구 내용은 빼줘"* 로 확정).

| 대표가 주신 문장 | 코드 실측 | 처리 |
|---|---|---|
| 장점 3 (선불 0원 · 노출→QR 추적 · 선결제 노쇼 없음) | 전부 사실 | 그대로 (`PartnerBenefits.tsx`) |
| vs 체험단 / vs 배달앱 / vs 예약솔루션 | 사실 | 비교표 3행으로. **'전단, 현수막' 행을 '예약, 포스 솔루션' 으로 교체** — 전단은 이미 접은 선택지고, 지금 사장님 폰에 깔린 경쟁자는 예약·POS 다 |
| "수수료 5% 고정" | 🔴 **직접 입점 10% / 중개 5%**(`fee-resolver.ts`) | 숫자를 고치고 **논지는 유지** — 수수료 말고 따로 나가는 돈이 없다 + 소개비는 매장이 정하고 내역이 남는다 |
| 인플루언서별 성과 | 🟡 부분 — `influencer_attributions`(migration 0247)는 **결제 건**만 담는다. 클릭·유입 수는 어디에도 없다 | "몇 명 유입" → **"몇 건이 팔렸고 소개비가 얼마"** + QR 사용 여부로 |
| 정산 자동화 | 계산은 자동, **승인·송금은 사람** | "자동 계산" 까지만. 송금 전 사람 확인을 같은 문단에 |
| "사업자번호 진위확인만 통과하면 자동 승인, 당일 판매" | 🔴 **거짓** — `seller-registration.routes.ts:239` *"자동승인 말고 수동 승인. 모든 사업자 가입은 어드민 수동 승인"*. NTS 결과는 승인 화면 참고 신호로만 저장. 게다가 #1468(같은 날 머지)이 앞문에도 등록증 사본을 필수로 만들었다 | **자동인 것은 진위확인뿐**이라고 적었다. 승인은 사람이 한다고 명시 |
| 단골 전용 할인코드 | 사실 (`SellerPromoCodesPage`, 게이트 없음) | 그대로 |
| 링크 전용가 · 기간한정 공구 · 인플루언서 딜 제안 · 소개비% 입력 | 🟡 **코드는 완성, 표면은 꺼짐** (`GB_ENGINE_ENABLED=false`, `SELLER_PROMO_FIELD_ENABLED=false`) | **제외**(대표 확정). 되돌아오는 길은 주입 `🏪 꺼진 공구 엔진을 랜딩이 약속한다` 가 막는다 |

> 🔑 **공구 엔진은 버린 게 아니라 잠긴 것이다.** `shared/gb-session.ts` 가 이미
> `mode(off/scheduled/live/ended)` · `deadline` · `price` · `target` · `linkOnly` · `promoPct` ·
> `proposerId`(인플루언서 제안→매장 승인)를 전부 갖고 있다. 대표가 말한 *"매장 상품들 공동구매화"* 가
> 그 파일이다. 켜려면 머니 경로라 **결재 + staging 실결제**가 짝이고, 켜지는 날 이 랜딩에 절이 하나 붙는다.

## 3-2. 시안 4안 (대표 *"시안을 제대로 받아야겠는데, 지금 디자인이라곤 뭐가 없네"*)

맞는 지적이었다. 안 A(= 지금 배포 대기 중인 판)는 **사진이 한 장도 없다.** 글꼴 크기와 여백만으로 서 있어서
잘 정리된 문서이지 디자인이 아니다.

⚠️ **소재가 없던 게 아니다** — 대표 승인 덱이 쓰는 **라이브 앱 캡처 18장**이
`docs/business/proposals/shots/` 에 이미 있었고(덱은 폰 프레임에 넣어 쓴다) 랜딩만 한 장도 안 썼다.

**보는 곳**: `/design/variants?set=partners-landing` (내부 전용 · 링크 0 · robots 차단 · noindex).
폭 토글(폰 430 / 태블릿 768 / 전체)과 `?side=0&v=b` 로 한 안만 크게 볼 수 있다.

| 안 | 무엇이 다른가 |
|---|---|
| **A** 지금 (글만) | 기준선. 비교 대상이 있어야 고를 수 있다 |
| **B** 실제 화면 | 폰 프레임 + 라이브 캡처. "이게 손님이 보는 화면 / 사장님 화면 입니다" |
| **C** 사진 먼저 | 음식 사진이 히어로를 차지하고 글이 그 위에 얹힌다. 커머스 톤 |
| **D** 돈 그림 | 사진 대신 도해. 16,500원이 어디로 가는지 막대 하나로 |

**이번에 밟은 것 둘** (다음 세션이 반복하지 않게):
1. **430px 스크린샷을 4.4배 확대해 히어로로 썼다가 뭉갰다.** 확대할 해상도가 없다.
   사진 히어로는 **원본 사진**이라야 한다 ⇒ 라이브 상품 2888 의 실제 사진(1080², 222KB)을
   `public/partners/deal-photo.jpg` 로 받아 썼다. ⚠️ **안 C 채택 시 매장 사진 사용 동의 확인**.
2. 폰 프레임이 체계 밖 모서리 2개(`rounded-[26px]`/`[20px]`)와 하드코딩 베젤색을 만들어
   `shape-lock`·`consumer-hex` 가 둘 다 빨간불을 냈다 → `rounded-2xl`/`rounded-xl` + `bg-gray-800`.

**🔴 같이 발견한 것 (이번 범위 밖, 수리 대상)**: `seller-settlements` 캡처 속 화면이
*"정산은 매주 **자동으로** 처리됩니다 … 별도의 정산 신청은 필요 없습니다"* 라고 말한다.
기획 §0-2 는 **승인·송금은 사람이 누른다**고 못박고 "자동 송금" 표기를 금지한다.
⇒ **셀러 대시보드의 자기 문구가 틀렸다.** 랜딩 문구는 맞게 썼지만, 그 캡처를 랜딩에 쓰면
화면과 글이 서로 다른 말을 한다. §0-5 의 "라이브 문구 수리" 목록에 속하는 별건.

## 3-3. 🩸 `public/` 에 넣은 이미지가 라이브에서 404 였다 (머지 후 실측으로 잡음)

시안을 머지하고 `curl https://urdeal.kr/partners/deal-photo.jpg` → **404(9B)**.
빌드는 멀쩡했다(`dist/client/partners/*.jpg` 존재). **라우팅 문제다.**

`public/_routes.json` 은 `"include": ["/*"]` 로 **전부 워커로 보내고**, `exclude` 의
**명시 목록만** 정적으로 뺀다. `/partners/*.jpg` 는 그 목록에 없어 워커로 갔고,
워커는 `/assets/*` 만 직접 서빙하므로(`index.ts`) 404 가 났다.

⚠️ **`/partners/*` 를 exclude 에 새로 넣지 말 것.** 그 파일 주석이 경고하는 #598 클래스다 —
urdeal.kr 에서 exclude 경로는 파일이 없으면 **정적 404** 가 나므로, 랜딩 경로(`/partners`)
자체를 삼킬 위험이 있다. 이미 검증된 **`/static/*`** 아래로 옮기는 것이 답이다.

```
public/partners/*.jpg  →  public/static/partners/*.jpg   (커밋 85d685a)
```

🔑 **다음 세션에게**: 이 레포에서 `public/` 에 새 정적 파일을 두면 **기본이 404 다.**
`_routes.json` 의 exclude 목록에 있거나 `/static/`·`/locales/` 아래여야 서빙된다.
빌드 산출물에 파일이 보인다고 서빙되는 게 아니다 — **배포 후 curl 로 확인할 것.**

## 3-2. 2차 — 안 B 채택 + PC 재설계 (대표 *"모바일은 오케이, 근데 PC 버전은 전혀 PC 버전 같지 않은데?"*)

대표 원문: *"모바일은 오케이, 근데 PC 버전은 전혀 PC 버전 같지 않은데? **안 B로 하는데** 우리
유어딜 서비스의 차별점, 장점 그리고 기존 체험단 서비스와 비교하는 것 그런게 필요해."*

**지적이 맞았다. 1440px 로 실제 렌더해서 재 봤다:**

| | 1차 | 2차 |
|---|---|---|
| `main img` | **0장** | **8장** (라이브 앱 캡처) |
| h1 @1440 | 48px | 62px |
| h2 @1440 | 전부 38px | 46~48px |
| 섹션 여백 | 전부 `lg:py-24` | `lg:py-24 / 32 / 36` |
| 레이아웃 계열 | 사실상 한 종류(가운데 정렬 블록) | 8종 |

즉 1차는 **"넓어진 모바일"** 이었다. 폭만 1600으로 열어 놓고 리듬·타이포·구성은 모바일 그대로였다.

**한 것**
- **안 B 채택** — `src/pages/partners/PartnerPhone.tsx`(폰 프레임 + `SHOT()` 경로 SSOT) 신설.
  캡처는 대표 승인 덱이 쓰는 것과 **같은 파일**(`docs/business/proposals/shots/`)을
  `public/static/partners/` 로 복사: 히어로 `home`·`detail`(eager) / 사장님 화면 `seller-scan`·
  `seller-settlements`·`seller-influencers`·`seller-operators` / 환불 `use` / 등록 `store-new`.
- **차별점 3을 표 위에 문장으로** (`PartnerCompare`) — vs 체험단 / vs 배달앱 / vs 예약솔루션.
  표는 훑는 사람에게 결론을 주지 않는다. 표는 그 아래에서 근거 역할만 한다.
- 표의 `유어딜` 행이 `bg-warm`(흰 배경 위)이라 **거의 안 보였다** → `bg-brand-tint`(선택됨 토큰).
- 섹션마다 레이아웃 계열을 갈랐다: 분할 / 고정제목+스택 / 문장3+넓은 표 / 계산기 분할 /
  가로 타임라인 / 폰 갤러리 4연 / 절차 3열 / 아코디언 / 중앙 CTA.
- 정직 고지(국세청 진위확인은 자동, **승인은 사람**)를 `PartnerTools` → `PartnerPaths` 로 이사.
  가입 절차를 말하는 자리가 맞는 집이다. 테스트·주입도 함께 옮겼다.

**가드**: 테스트 45 → **51건**(R6 신설 — 사진 0장 회귀 · 캡처 파일 실존 · `/static/` 경로 ·
폰 잘림 · PC 타이포 하한 · 여백 리듬 · 차별점 3 상대). 주입 **11 → 15건**.

## 4. 이번에 틀렸던 판단 (다음 세션이 같은 함정을 밟지 않게)

1. **`/초기 서비스/` 로 정직 고지를 지켰다고 착각했다.** 같은 파일 FAQ 답변에도 그 말이 있어
   고지 블록을 통째로 지워도 초록이었다. **주입 러너가 잡았다** → 문장 전체를 앵커로 교체.
   ⇒ *느슨한 낱말로 블록의 존재를 재지 말 것.*
2. **테스트 파일에 폐기 명칭 리터럴을 적었다가** `urshop-naming.test.ts` 의 전수 스캔에
   내 테스트가 위반으로 잡혔다 → 조각으로 조립(`new RegExp('링크' + '샵')`).
3. **`shape-lock` 이 stash 후에도 75건이라 "선재 결함" 으로 볼 뻔했다.** `git stash` 는
   **untracked 파일을 안 넣는다** — 새로 만든 `src/pages/partners/*.tsx` 가 그대로 남아 있었다.
   ⇒ 신규 파일이 있는 브랜치에서 `git stash` 로 before 를 재려면 `-u` 를 붙일 것.
3-2. **(3차) 스킬을 읽고도 여덟 가지를 위반했다.** anti-slop 스킬은 세션 시작에 자동으로
   잡히고 CLAUDE.md 가 다섯 가지를 요약해 두는데, 2차 판은 `01/02/03` · 3열 균등 · 행마다 밑줄 ·
   우상단 떠 있는 문단 · 제목 밑 해명 한 줄을 전부 넣었다. ⇒ **읽는 것과 대조하는 것은 다른 일이다.**
   §14 프리플라이트를 코드 내보내기 전에 한 줄씩 볼 것.
4. **(2차) "PC 도 했다" 고 보고했는데 액자만 벗긴 것이었다.** 액자 제거(`HIDE_SIDEBAR_PREFIXES`)는
   *막힌 것을 푼 것*이지 *PC 를 만든 것*이 아니다. 1440 으로 렌더해 보기 전까지 그 차이를 몰랐다.
   ⇒ **레이아웃 변경은 반드시 그 폭에서 렌더해 보고 말할 것.** 하네스는 `dist/client` 를
   `127.0.0.1` 로 서빙 + Playwright(`executablePath: /opt/pw-browsers/chromium-1194/chrome-linux/chrome`,
   외부 요청은 `route.abort()`)로 이 컨테이너에서 **잘 돈다**.
5. **(2차) 헤드리스 shell 은 `loading="lazy"` 이미지를 끝내 안 부른다**(실측: 스크롤해도
   `currentSrc` 가 빈 문자열). 그리고 `fullPage: true` 스크린샷은 **이미 로드된 이미지도 흰색으로**
   찍는다. ⇒ 사진을 눈으로 확인하려면 ① `img.loading='eager'` 로 바꿔 강제 로드 ②
   **fullPage 말고 스크롤 + 뷰포트 샷**. 이걸 모르면 "이미지가 안 뜬다" 고 오진한다(실제로 했다).
6-2. **(3차) 주입 러너가 두 번 잡았다.** ① *"제목 하나를 되돌린다"* 주입이 초록이었다 —
   비율 규칙(≤ 1/3)은 **페이지 전체 회귀**를 막는 것이라 한 줄로는 안 무너진다. 한 줄로 재현
   가능한 자리(h1)를 따로 고정했다. ② 그 h1 검사를 `headings[0]` 으로 썼는데 수집 순서가
   파일 순서라 **맨 끝 CTA 가 첫 번째**였다(실제 소스에 가짜 빨간불). 태그로 찾도록 교체.
   ⇒ **여러 파일에서 모으는 배열의 `[0]` 은 "첫 번째" 가 아니다.**
6. **(2차) 주입 러너가 또 헛도는 단언을 잡았다.** 표의 `예약, 포스 솔루션` 행을 통째로 뭉갰는데
   초록이었다 — 같은 문구를 위쪽 '차별점 3' 블록에도 새로 썼기 때문이다. ⇒ 행 단위로 앵커를 좁혔다.
   *같은 말을 페이지 두 곳에 쓰면, 그 말을 찾던 가드가 그 순간 죽는다.*

## 5. 검증

tsc 0 · `npm run build` 0 · vitest **719파일 9,077건 pass** · pre-push 게이트 **95개 가드 통과** ·
주입 매니페스트 **`--only='🏪 '` 전부 빨간불 확인**(2차에 4건 추가 + 낡은 앵커 3건 수리).
PC 1440 · 모바일 430 실제 렌더로 눈 확인(2차 포함).

## 5-2. E4 라이브 판정 (2026-09-16, #1476 머지 후 실측)

머지 `f577127` → Pages 배포 성공 → 라이브에서 직접 재 봤다:

| 무엇 | 결과 |
|---|---|
| `urdeal.kr/static/partners/*.jpg` **9장** | 전부 **200** (23~227KB) |
| `urdeal.kr/partners` | 200 · title `매장 입점 안내 - 손님이 온 다음에만 비용 - 유어딜` · canonical 정상 |
| 배포된 청크 `assets/PartnersPage-BV0rqrHk.js` | `/static/partners/` 빌더 존재 · 차별점 3(체험단·배달앱·예약) · '사장님 화면은 이렇습니다' · '국세청에 자동으로 조회' **전부 포함** |

⚠️ **로컬 빌드의 청크 해시로 라이브를 찌르면 404 다** — CI 빌드는 해시가 다르다.
라이브 청크를 찾으려면 **HTML 의 엔트리 청크를 받아 `"assets/PartnersPage-…js"` 문자열을 grep** 할 것
(이 세션이 그 404 를 한 번 "배포 실패" 로 오독할 뻔했다).

⚠️ **눈으로 본 판정은 아직 없다** — 이 컨테이너는 라이브 오리진을 브라우저로 못 연다(프록시).
PC 레이아웃이 실제로 예쁜지는 대표 화면 확인이 유일한 E5 다.

## 5-3. 3차 — AI 말투·디자인 걷어내기 (대표 *"AI 가 만든 디자인, 말투가 아니면 좋겠는데"*)

### 진단 방법이 이번의 핵심이다

**제목만 뽑아 한 줄로 늘어놓으면 보인다.** 렌더 하네스에
`[...document.querySelectorAll('main h1, main h2')].map(e => e.textContent)` 한 줄을 더했더니
원인이 즉시 나왔다 — **아홉 개가 전부 완결문**이고 **전부 "-습니다/-입니다"** 로 끝났다.
개별 문장은 다 멀쩡한데 **나란히 놓으면 기계가 쓴 티**가 난다. 문장 하나씩 읽어서는 안 보인다.

| before | after |
|---|---|
| 체험단 말고, 계산하는 손님을 부르는 방법입니다 | 체험단 말고, 계산하는 손님 |
| 유어딜이 사장님께 드리는 것은 셋입니다 | 먼저 나가는 돈, 없습니다 |
| 광고비 쓰고, 효과 봤는지는 아셨어요? | 그 광고비, 효과 보셨어요? |
| 여기는 손님이 돈을 내고 옵니다 | 그래서 얼마 남나 |
| 손님은 이렇게 옵니다 | 손님이 오는 길 |
| 사고가 나면 어떻게 되나요 | 안 오면요? 안 쓰면요? |
| 사장님 화면은 이렇습니다 | 사장님 폰에 뜨는 화면 |
| 시작하는 길은 세 가지입니다 | 시작하는 길, 세 가지 |
| 안 팔리면 0원입니다. 잃을 것이 없습니다. | 안 팔리면 0원입니다 |

완결문 9 → 1. 명사구 5 · 질문 2 · 조각 2.

### 디자인에서 걷어낸 것 — 전부 스킬이 이름을 붙여 둔 것들이었다

`.claude/skills/taste-skill/SKILL.md` §9 와 §14 프리플라이트를 한 줄씩 대조했다.
**규칙을 몰라서가 아니라 대조를 안 해서** 들어간 것들이다:

- 파란 `01 / 02 / 03`(장점·흐름) = "section-number eyebrow"
- 3열 균등 세 번(차별점 3 · 도구 노트 4 · 세 가지 길 3) = "three identical cards"
- 표의 행마다 밑줄 = "border on every row"
- 제목 우상단 작은 설명 문단(도구) = "floating top-right sub-text"
- 제목 밑 해명 한 줄(장점) = "micro-meta-sentence under the heading"
- 둥근 아이콘 상자 3개(계산기) = "cards omitted in favor of spacing"
- 히어로 글 덩어리 6개 = "max 4 text elements in hero"
- 다크 밴드 3개 = "Page Theme Lock" (줄무늬)

### 다음 세션용

🔑 **랜딩·대외 표면을 만들면 `#14 프리플라이트`를 한 줄씩 체크할 것.** 스킬을 읽는 것과
대조하는 것은 다른 일이다. 이번 2차 판은 스킬을 읽은 상태에서 여덟 가지를 위반했다.

🔑 **제목을 뽑아 나란히 읽어 볼 것.** 어미가 한 종류면 그게 AI 말투다.
가드로 박아 뒀다(`R7` — h1 완결문 금지 · 전체 어미 비율 ≤ 1/3 · 모양 2종 이상).

## 5-4. E4 라이브 판정 — 3차(#1479 머지 후 실측, 2026-09-16)

머지 `c1c989e` → Pages 배포 성공 → `urdeal.kr/partners` 실측(엔트리 청크에서
`assets/PartnersPage-BbMVFP6C.js` 를 찾아 그 본문을 검사 — **로컬 빌드 해시로 두드리면 404 다**):

```
체험단 말고,            OK      0{i + 1}      없음
그래서 얼마 남나        OK      업계 최저      없음
안 오면요? 안 쓰면요?   OK
사장님 폰에 뜨는 화면    OK      캡처 8장 전부 200
시작하는 길, 세 가지     OK      (/static/partners/*.jpg)
먼저 나가는 돈, 없습니다  OK
그 광고비, 효과 보셨어요? OK
```

⇒ 새 제목 7개 전부 배포본에 있고, 지운 것(단계 번호·금지 문구)은 없다. **E4.**

## 5-5. 4차 — 다크에서 글자가 안 보였다 (대표 *"글자들 개선해. 색깔이 뭐야 이게"*)

대표가 다크 모드 `/partners` 캡처를 보냈다. 장점 섹션 **제목 셋이 배경에 잠겨** 있었다.

### 원인 — 오타가 아니라 토큰 드리프트

```
bg-warm  = var(--bg)   → 다크 #11141C   ← 테마를 따라간다
text-ink = '#16181C'   → 다크에서도 그대로  ← 안 따라간다   ⇒ 대비 1.05 : 1
```

2026-09-15 '색 정리' 가 `surface`·`line`·`warm`·`rule` 을 테마 변수로 돌리면서 **`ink` 만 빼놓았다.**
배경은 어두워지는데 글자는 안 밝아지니 그 조합을 쓰는 화면은 통째로 안 읽힌다.

### 왜 가드 둘이 다 초록이었나 (각자 정당한 이유로 비켜 갔다)

| 가드 | 왜 못 봤나 |
|---|---|
| `check-theme-consistency` | **라이트 토큰**(`text-gray-900` 류)의 `dark:` 누락을 본다. `text-ink` 는 변수를 가리키면 `dark:` 가 필요 없는 설계라 목록에 없다 |
| `check-dark-contrast` | 실제 렌더로 재지만 **경로 목록이 곧 범위**인데 `/partners` 가 없었다. 게다가 `text-ink` 를 쓰는 화면이 사실상 이 랜딩뿐이라 나머지 49개를 아무리 돌아도 안 잡힌다 |

⇒ `text-ink` **총 40회 / 10파일** 중 8개가 이 랜딩이다. 즉 **내가 만든 화면이 그 토큰의 유일한 사용처**였고,
"토큰이니까 알아서 뒤집히겠지" 를 확인 안 한 것이 이번 오판이다.

### 수정

1. **tailwind `ink` → CSS 변수** (`var(--ink)`/`var(--ink-soft)`/`var(--ink-faint)`).
   `--ink*` 는 index.css 가 이미 양쪽 값을 갖고 있고, 라이트 고정 스코프
   (`.light-island`/`.force-light-theme`/대시보드 3종)가 같은 파일에서 되박는다 ⇒ 대시보드 무영향.
   **라이트는 byte-동일**(`--ink` 라이트 값이 옛 고정 hex 와 같은 `#16181C`). 바뀌는 것은 다크뿐.
2. **캡션 회색 한 단계 올림** — `text-gray-400 dark:text-gray-500` → `text-gray-500 dark:text-gray-400` (10곳).
   실측상 그 짝은 **라이트 3.21~3.65 · 다크 3.10~3.48** 로 **두 테마 모두** AA(4.5) 미달이었다.
   다크만의 문제로 오해하기 쉬웠는데 처음부터 안 읽히는 회색이었다.
3. `check-dark-contrast` 경로에 `/partners` 모바일·PC 추가.

### 검증

```
check-dark-contrast   50경로 · 텍스트 1,805개 · 신고 0건
자체 측정(1440)       라이트 AA 미달 0건 · 다크 최저 4.49(= gray 짝 반올림)
tsc 0 · build 0 · vitest 724파일 9,159건 · pre-push 가드 95개
주입 3건 되돌려-검증 전부 빨간불
```

### 다음 세션용

🔑 **토큰을 쓸 때 "이게 테마를 따라가는가"를 확인할 것.** 이 레포의 tailwind 색은 두 종류가 섞여 있다 —
`var(--…)` 를 가리키는 것과 **고정 hex** 인 것. 이름만 보고는 구분이 안 되고, 라이트에서는 둘이 똑같이 보인다.

🔑 **새 소비자 화면을 만들면 `check-dark-contrast` 경로 목록에 한 줄 추가할 것.** 그 파일 주석이
이미 그렇게 적어 뒀는데(2026-09-07 `/store/new` 사고) 이번에도 안 지켰다. 목록이 곧 범위다.

## 5-6. 5차 — 버튼이 모바일에서만 찌그러져 있었다 (대표 *"버튼 크기도 세로길이가 짧고, 버튼이 이게 뭐야"*)

### 실측 — PC 는 멀쩡하고 모바일만 깨진다

CTA 버튼 넷이 `h-[52px]` 를 달고도 **모바일에서 23~25px** 로 렌더됐다(PC 58~60px 정상).

```
<div className="flex flex-col sm:flex-row …">     ← 모바일은 세로 스택 = 주축이 '높이'
  <a className="flex-1 h-[52px] …">               ← flex-basis: 0% 가 height 를 이긴다
```

컨테이너 높이가 `auto` 라 나눠 줄 여유 공간이 0 → 버튼이 글자 높이까지 줄어든다.
`sm:flex-row` 로 바뀌는 순간 주축이 가로가 되어 `height` 가 되살아나므로 **PC 에서는 아무 문제가 없다.**

⇒ 개발도 리뷰도 PC 로 한다. **눈으로 찾는 부류가 아니라 기계가 찾아야 하는 부류**였다.

### 수정

| | |
|---|---|
| 높이 | `flex-1` → `sm:flex-1` (세로 스택에선 `align-items: stretch` 가 이미 가로를 채우므로 `flex-1` 이 할 일이 없다) |
| 보조 버튼 | 잉크 패널 위 실선 테두리만(`border-white/25`)이라 버튼으로 안 읽혔다 → `bg-white/[0.10]` 면 + 글자 `text-white/90` → `text-white` |

**전수 스캔에서 같은 결함이 도매몰에도 있었다** — `wholesale-catalog/BulkOrderPanel.tsx` 버튼 4개
(`h-11`·`h-10`). 🧱 서비스 분리 룰 4의 세 줄 보고: **(a) 레일** 도매몰 + 소비자(유어딜) 둘 다(같은 CSS 결함)
· **(b) 머니 경로** 없음(className 한 토큰) · **(c) 롤백** `sm:flex-1` → `flex-1` 환원.

### 오진할 뻔한 것

바닥 고정 바가 사업자 정보를 22px 가린 것처럼 보여 여백을 늘리려 했는데, **스크롤이 끝까지 안 간 상태**로 잰 값이었다.
제대로 바닥까지 내려 다시 재니 **87px 여유**였다. 고칠 게 없었다 — 없는 결함을 고치지 않았다.

### 검증

```
버튼 높이  모바일 23·25px → 52·52px  ·  PC 58·60px 그대로
tsc 0 · build 0 · vitest 729파일 9,247건 · pre-push 가드 96개
주입 2건 되돌려-검증 빨간불
```

### 다음 세션용

🔑 **`flex-col` 안에서 `flex-1` 과 고정 높이를 같이 쓰지 말 것.** 가드가 생겼다
(`check-flex-col-fixed-height`) — 이 레포의 "PC 에선 멀쩡해서 아무도 못 보는" 부류다.

## 6. 남은 것 (이번 범위 밖)

- **형제 랜딩도 같은 액자에 갇혀 있다** — `/about`·`/creators` 는 그대로다(대표가 지목한 건 `/partners`).
  같은 1줄로 풀 수 있지만 각 페이지에 PC 레이아웃이 없어 **풀면 오히려 퍼진다.** 내용 작업이 짝이다.
- `/business`(2026-05-15 랜딩)는 **고아**다 — `/influencer` 네비에서만 링크되고, 기획 §0-5 가
  수리 대상으로 지목했다(5%/4%/3% 등급제 시뮬레이터 등). `/partners` 와 대상이 겹치므로
  **통합할지 지울지 대표 판단**이 필요하다.
- `/partners` 는 `generate-route-chunk-map.mjs` 의 `ROUTES` 에 없어 하드로드 청크 병렬화를 못 받는다.
  등재하려면 잠금 파일(`worker/index.ts`)의 `chunkSurface` 분기가 짝이라 별건.
- 등록 소요 표기를 **10분**으로 통일했는데(기획 §7-3 확정), 다른 화면은 여전히 1분·3분·5분을 섞어 쓴다.

## 7. 대표 판단 대기

~~`/business` 를 `/partners` 로 합칠지, 지울지.~~ · ~~형제 랜딩도 같은 수준으로 PC 를 만들지.~~
→ **둘 다 2026-09-16 대표 답변으로 확정됐다**: *"지워줘. PC를 같은 수준으로 만들어주고."* (§8)

---

## 8. `/business` 삭제 + 형제 랜딩 PC 판 (대표 *"지워줘. PC를 같은 수준으로 만들어주고."*)

### 8-1. `/business` 삭제 — 페이지는 지우고 URL 은 살린다

`/partners` 와 **같은 사람에게 같은 말을 하는 두 번째 랜딩**이었다. 고아였고(네비 한 곳에서만
링크), 적힌 수수료가 낡아 있었다(옛 5%/4%/3% 등급제 — `fee-resolver` 엔 그런 규칙이 없다).

🔑 **URL 은 301 로 남긴다.** sitemap 에 올라가 있었고 밖에 뿌려진 링크가 있다. 페이지를 지웠다고
경로까지 404 로 만들면 그 신호를 버리는 것이다.

| 무엇 | 어디 |
|---|---|
| 서버 301 | `shared/seo/consumer-redirects.ts` `ALIAS_EXACT` 에 `'/business': '/partners'` |
| 앱 안 이동 | `App.tsx` 라우트를 `<Navigate to="/partners" replace />` 로 (SPA 내부 이동은 서버를 안 탄다) |
| 제출 중단 | `sitemap.routes.ts` 정적 목록에서 제거 |
| 메타 | `shared/seo/consumer-surfaces.ts` 항목 제거(설명이 *"수수료는 팔린 만큼만 5%"* 로 낡아 있었다) |
| 유입 링크 | `InfluencerLandingPage.tsx:43` → `/partners` |
| 파일 | `src/pages/BusinessLandingPage.tsx` 삭제(247줄) |

🩸 **삭제가 건드리는 자리는 파일 하나가 아니었다.** 함께 갱신해야 했던 것들:
`scripts/consumer-hex-baseline.json` · `docs/PAGE_AUDIT.md` ·
`check-guard-mutations.mjs` 의 주입 하나(그 라우트 줄을 **앵커로 쓰고 있었다** → 갱신 안 하면
"낡은 지도" 로 빨간불). ⚠️ `check-image-fallback` 은 `git ls-files` 로 목록을 만들어서
**삭제를 스테이징하기 전까지 ENOENT 로 죽는다** — 가드가 깨진 게 아니라 순서 문제다.

### 8-2. `/about`·`/creators` PC 판

`/partners` 가 안고 있던 문제 셋을 **똑같이** 갖고 있었다: ① 430px 액자(빈 거터를
`ConsumerFrameRails` 가 앱 설치 QR 로 채웠다) ② 본문 `max-w-xl`(576px) 고정 ③ 사진 0장.
`/creators` 는 거기 더해 **성과 화면 자리가 "(화면 준비 중)" 플레이스홀더 두 칸**이었다.

⇒ `/partners` 와 같은 규약으로 맞췄다: `HIDE_SIDEBAR_PREFIXES` 등재 · 잉크 헤더 ·
`ur-content-wide` · PC 타이포 단계 · 섹션마다 다른 레이아웃 계열(분할 / 고정제목+헤어라인 /
3열 레일 / 가로 타임라인 / 비대칭 / 색면) · 모바일 전용 고정 CTA(`lg:hidden`).

**캡처**는 대표 승인 덱이 쓰는 **같은 파일**을 쓴다(`public/static/partners/`, 새 부품
`pages/landing/PhoneShot.tsx`). `/creators` 의 플레이스홀더 두 칸은 실제 화면 셋으로 바꾸고
캡션을 덱 문구 그대로 달았다(`예시 데이터` / `라이브 화면`).
⚠️ **없앤 것**: *"실시간 적립 알림"* 칸. 그 장치는 아직 없다 — 없는 기능을 그림으로 약속하지 않는다.

### 8-3. 1440px 실측에서 두 번 고친 것 (눈으로만 잡히는 부류)

| 증상 | 원인 | 처방 |
|---|---|---|
| `/about` h1 이 `…매장에서 QR / 로` 로 끊겨 **낱말 하나가 홀로** | 60px 에서 어느 자리로 끊어도 마지막 낱말이 남는다 | 한 단 내림(`lg:46 / xl:52`) + 3줄 배치 |
| `/creators` 히어로 폰이 **섹션 밖으로 잘림** | `lg:w-full` 이 0.62fr 열에서 520px 가 됐다 | `lg:w-[19rem] xl:w-[21rem]` 로 상한 |
| 폰 캡션이 잉크 색면 위에서 **2.5:1** | `text-gray-500` 고정 | 색을 안 박고 `opacity-60` 으로(부모 색 상속) |

🧭 **교훈**: 시험에 `lg:text-[52px]` 를 **숫자로 박았다가** 바로 그 숫자를 내려야 했다.
크기를 못 박으면 문장이 바뀔 때마다 시험이 디자인을 가로막는다 → **단계가 있는가**(lg ≥ 44 ·
xl ≥ lg · lg > base)로 바꿨다.

### 8-4. 가드

- `src/tests/unit/landing-pc-2026-09-16.test.ts` 22건 — 액자 · 폭 · 타이포 단계 · 고정 바 ·
  캡처 실재 · 플레이스홀더 금지 · 다크 목록 · `/business` 삭제 5종.
- `scripts/mutations/landing-pc.mjs` 8건 **전부 되돌려-검증 빨간불 확인**.
- `check-dark-contrast` 경로 목록에 `/about`·`/creators` 모바일·PC 추가(옛 `/about` 한 줄은 흡수).
- ⚠️ 못 보는 것: **픽셀**. 여백 리듬·레이아웃 계열 반복은 렌더해서 눈으로 봐야 한다.

### 8-5. 검증

```
tsc 0 · build 0 · vitest 737파일 9,347건 pass · pre-push 가드 97개
dark-contrast 53경로 1,957텍스트 0건 · 주입 8건 빨간불
1440 렌더: about 액자 false / main img 2 · creators 액자 false / main img 4
```

### 8-6. ✅ E4 라이브 판정 통과 (배포 후 `urdeal.kr` 실측)

PR #1485 머지(`522c115`) → 배포(`377833c` 슬롯, 직후 머지된 #1483 과 같은 concurrency 그룹으로 갈아탐.
`git merge-base --is-ancestor 522c115 origin/main` 으로 포함 확인) 뒤 실제 라이브를 렌더해 쟀다.

```
/business        301 → https://urdeal.kr/partners        ✅ (페이지는 지우고 URL 은 살렸다)
/about /creators 200

               액자   거터레일   본문폭    h1 줄        고아낱말   main img   섹션밖 이미지
/about  1440    false    0      1440    3줄(504/551/375)  없음        2          0
/creators 1440  false    0      1440    3줄(260/371/409)  없음        4          0
/about   390    false    0       390    3줄(291/318/216)  없음        2          0
/creators 390   false    0       390    3줄(130/186/205)  없음        4          0

CTA 높이   모바일 52 / PC 58·60      (헤더 알약 36·40 · 3자 섹션 알약 44 · 모바일 고정바 48 = 설계값)
h1 크기    /about lg 46 → xl 52   ·  /creators lg 52 → xl 60
```

🩸 **첫 측정이 헛돌았다 — 기록해 둔다.** h1 의 줄 수를 `h1.getClientRects().length` 로 쟀는데,
**블록 요소의 그 값은 줄이 아니라 박스 하나**라 언제나 `1` 이 나온다(그래서 "마지막 줄 비율 1.0,
고아 없음" 이 통과처럼 보였다 — 사실은 아무것도 안 잰 것이다). **글자마다 `Range` 를 만들어
`top` 으로 묶어야** 진짜 줄 상자가 나온다. 위 표의 줄별 폭이 그렇게 다시 잰 값이다.
같은 함정: 거터 판정도 처음엔 본문에서 `/QR/` 를 찾았는데 `/about` 카피에 *"매장에서 QR 로"* 가
있어 늘 "레일 있음" 이 떴다 → **부품 선택자**(`.app-frame-host`·`.app-framed`)로 교체.
⇒ **판정은 "무엇을 재고 있는지" 부터 의심할 것.**

### 8-7. 남은 것

- Notion 개발 로그 1행(서비스: 유어딜 / 유형: UI / 머니 경로 false) — 이 세션에서 기록.
- ⚠️ **main 이 빨간불이었다(내 것 아님)**: #1483 이 114번째 가드
  (`check-groupbuy-headcount-claim`)를 `audit-gate.sh` 에 넣으면서 `AUDIT_INVARIANTS.md` 의
  `전체 (N개)` 줄을 113 그대로 뒀다 → `check-audit-registry-sync` 가 **모든 푸시를 막는다**
  (pre-push 게이트에서 실제로 막혔다). 한 글자 수정이라 같이 고쳤다.
