# 2026-09-07 — 대행사 향 서비스 소개·제안서 (.pptx, 16장 v2 "매장 모집 실행서")

**어느 레일**: 유어딜(소비자 서비스) 대외 문서. 코드 무접촉(`src/` 변경 0). 머니 경로 없음.

## v2 (대표 피드백 "내용이 모호하다. 대행사가 매장을 모집하게 하고 싶다")
v1 은 구조 설명이었고 "그래서 내일 어디 가서 뭘 하면 얼마가 남는가"가 없었다. v2 는 순서를 행동으로 바꿨다:
한 장 요약 → 이용권 예시(가격) → 돈 흐름 → **매장 1곳 단위 경제(가정 공개, 차트)** → **사장님 셈법** →
**데려올 매장 체크리스트 6** → **등록 10분 절차** → **설득 대본 + 거절 5 응답** → 인플루언서 5단계 →
주간 루틴 + 화면 6 → 사장님 안심 4 → 유어딜 지원 6 → **8주 파일럿 조건** → 손님 경험 → FAQ·CTA.
새로 확인한 사실: 매장 등록 = 사업자번호·대표자명·개업일 국세청 진위확인 + 카카오맵 장소 연결 + 채널 선택 + 확인 PIN
(`seller-stores.routes.ts`, 사장님 서명 불필요·대행사 계정으로 등록). 정산 = `used` 이용권만 주간(`auto-settlement.ts`).

## v3 (대표 피드백 "Pretendard, 더 세련되게, 실제 모바일 화면을 적재적소에")
- 글꼴 Pretendard(OTF 를 `~/.fonts` 에 두고 렌더). 여백 넓힘(M 0.75), 카드 줄이고 hairline·여백 위주, 제목 자간 −0.6.
- 라이브 모바일 캡처 4장(`scripts/capture-proposal-shots.mjs`, NODE_PATH 로 playwright-core·sharp 를 빌려 실행)을
  폰 프레임으로 1(홈)·3(상세+사용안내)·7(유어샵)·9(상세)·15(4장 전부)에 배치. 유어샵은 cropTop 없이 재캡처.
- 🩸 **틀렸던 것 2건**: ① pptxgenjs `addImage({rounding:true})` 는 둥근 모서리가 아니라 **원형 크롭**이다.
  ② `sizing:{type:'crop', w,h}` 의 w·h 는 **인치**이지 비율이 아니다(1×1 인치로 그려졌다). `cover` 로 교체.
- 5·6 장 구조 정정(대표 지적): 매장 몫 95% 안을 인플루언서 소개비 / 대행사 보수 / 매장 순수취 **세 갈래**로.
- 폰 프레임(대표 *"테두리가 엉망"*): 도형 위에 네모 캡처를 얹던 것을 `phone-frame.mjs`(sharp 합성 PNG: 둥근 화면·베젤·그림자)로 교체.
  시안 4종(`phone-frame-styles.png`)을 보내고 A(minimal)로 빌드. `PHONE_STYLE` 로 교체 가능.
- 셀러 대시보드 화면(대표 *"계정 없어도 들어가서 임의로 내용 만들 수 있지 않아?"* → 맞다): `capture-seller-shots.mjs` 가
  라이브 프론트에 `/api/seller/*` 만 예시 응답으로 끼워 넣어 `/seller/stores·influencers·operating·operators` 4장을 찍었다.
  로그인 0·프로덕션 쓰기 0(클라 가드 `isDashboardTokenUsable` 는 exp 만 본다). 8(매장 관리)·10(인플루언서 찾기 + DB 통계 열)·
  11(3장: 매장 관리/운영 요약/운영자 관리)·12(운영자 관리) 장에 배치, 캡션에 "예시 데이터로 렌더한 실제 화면".
  🩸 stores 페이지 에러 경계: 알림 벨·유입 바인딩 401 throw + `ReviewBonusCard` 가 `[]`.amount 를 읽음 → 엔드포인트별 명시 모킹으로 해결.
  pptx 는 프레임 PNG 를 1200px 로 줄여 7.5MB(줄이기 전 12MB).

## v4 (대표 *"이 PDF 최대한 참고해봐"* — 참고 PDF "유어딜 중개사 파트너 제안" 5장 A4)
참고 PDF 의 골격을 16:9 덱에 옮겼다(17장). 가져온 것: 표지 문장 "매장을 데려오시면 유어딜은 5%만 가져갑니다" + 5%/95%/0원 ·
돈 흐름도(손님→카드사 2.75%→유어딜 5%→매장 95%→귀사 보수) · "흔한 오해/실제" · 규모별 표(10/30/100/300곳, 귀사 3% 예시) ·
객단가 변수(숙박 155,824 = 식사의 4.8배) · 운영자 권한 가능/차단 표 + "제가 통장을 못 건드립니다" · 정직 고지(귀속 미추적 · 초기 상태 · 대표 인용) ·
시작하는 방법 3단계 · 기준일 각주. 단위 경제(5·6장)를 참고 PDF 기준(평균가 32,339원 · 하루 1건 · 보수 3% 예시)으로 통일해 문서 안에서 가정이 하나다.
- **참고 PDF 숫자를 라이브로 재검증**: 활성 338 = demo-deal-* 286 + seller_id 없는 숙박 51 + 실제 매장(홍대돈까스) 1. 식사 평균 32,339(242개)·숙박 155,824(51개) 일치.
- 🩸 **참고 PDF 의 오류 1건을 고쳐 적었다**: "채널은 나중에 어드민에서만 바꿀 수 있다" → 코드는 매장 소유자도 바꿀 수 있다(`seller-stores.routes.ts` `POST /stores/:id/channel`, 소유자 403 가드). 덱은 "매장 주인이나 유어딜만"으로.
- 🩸 **표 객체는 PDF 에서 자간이 벌어진다**: pptxgenjs `addTable` 셀은 UNO 문단 보정이 안 먹어 "10 곳 · 970 만원"으로 렌더 → 텍스트 상자로 교체. `export-pptx-to-pdf.py` 는 표 순회 API(`getRows().getCount()`)로 고쳤다.
- 🩸 **블록 치환 스크립트가 16·17장을 통째로 지웠다**: 뒤에서부터 치환하면서 n=15 의 "다음 마커(16)"가 이미 17로 이름이 바뀌어 있어 끝까지 잘라 냈다. 파일 끝을 확인해 복구. 마커 번호를 바꾸는 치환은 한 번에 조립할 것.

## 만든 것
- `docs/business/proposals/urdeal-agency-proposal.pptx` — 대행사(중개사) 향 제안서 14장, 16:9.
- `docs/business/proposals/urdeal-agency-proposal.build.mjs` — pptxgenjs 생성기(재생성 절차는 proposals/README.md).

## 내용의 근거 (전부 SSOT 또는 라이브 실측)
| 주장 | 출처 |
|---|---|
| 직접 입점 10% / 대행사 경유 5%, 유어딜은 대행사에게 지급하지 않음, 대행사 보수는 매장 몫 95% 안에서 매장과 직접 | `store-operator-model.md` §7 (2026-09-04 대표 확정), `fee-resolver.ts` 기본값 |
| 대행사 도구 = 셀러 대시보드 6화면(`/seller/stores·operators·influencers·influencer-deals·promo-spend·operating`) | 2026-09-07 main 라우트 실재 확인 |
| 운영자 마스킹·계좌 변경 403·회수 언제든 | `store-operator-model.md` §7.7 |
| 판매 중 이용권 338건 | `GET /api/group-buy/products?status=active` 2026-09-07 |
| 인플루언서 DB 198,704 / 연락 가능 45,725 / 블로그 170,307 / 유튜브 17,986 / 카페 9,803 | `GET /api/admin/ads/influencer-pool/stats` 2026-09-07 |
| 시장 규모 3종 | 사업계획서 B-1 공개 통계 |

## 일부러 뺀 것
- 셀러 등급별 수수료 인하(가이드의 3%/4%/5% 표) — `fee-resolver.ts` 에 등급 분기가 없어 라이브 정산과 일치한다는 확신이 없었다. 대외 문서에 넣지 않았다.
- 어필리에이트·초대·멀티티어 — 전부 종료된 축(2026-08-22/23). 언급 0.
- 에이전시 대시보드·1%·24개월 — 2026-09-04 삭제. 언급 0.

## 검증
- `validate.py` PASS · 14장 전부 LibreOffice 렌더로 눈 확인(제목 넘침 2건·겹침 3건 수정 후 재확인).
- 이 세션에서 알게 된 환경 사실: 컨테이너에 `libreoffice-impress` 가 없어 pptx→pdf 변환이 "source file could not be loaded" 로 실패한다. `apt-get update && apt-get install libreoffice-impress` 로 해결(첫 시도는 apt 인덱스가 낡아 404). `pdftoppm` 도 없어 `pymupdf` 로 래스터화했다.

## Notion 미기록
문서 산출물이라 개발 업데이트 로그 대상 아님(MCP 미사용).

## 다음 세션
- 대표가 요율·문구를 바꾸면 생성기를 고치고 다시 빌드(README 절차). 전달 전 3·10 장 숫자 재실측.
- 폰 프레임 스타일은 A(minimal) 로 빌드돼 있다. 대표가 B/C/D 를 고르면 `PHONE_STYLE=` 로 재빌드 + PDF 재출력.

## 2026-09-13 — 소개서 3종 기획 (사장님 · 인플루언서 · 대행사)

대표 지시 *"매장 사장님 대상 소개서. 먼저 서비스 내용을 모두 빠짐없이 확인 후 기획 먼저."* ⇒ 기획서
`docs/business/proposals/three-decks-plan-2026-09.md`. 제작은 대표가 §7 판단 8건에 답한 뒤.

확인 방법: SSOT 문서 4종 + 9월 결재 5건 + 인계 8건 + 머니 경로 코드 + 라이브 랜딩 8화면 + 라이브 API(09-13). 문서와 코드가 다르면 코드.

🩸 이번에 틀렸던 것(v4 덱에 이미 들어간 오기): **리뷰 보너스 "300딜" 은 없는 값**이다. 코드 기본은 1,000딜
(`review-bonus-funding.ts:53`), 유어딜 부담. v5 에서 고친다.

다음 세션 첫 액션: 대표 답 확인 → `deck-common.mjs` 분리 → `capture-seller-shots.mjs` 로 등록 마법사·쇼츠 화면 재캡처 → 사장님 덱.
소개서와 별개로 §0-5 의 라이브 문구 수리(약관 `/terms/influencer`, `/business`, `/introduce`, `/partners`)는 별 PR 로.

### 09-13 저녁: 사장님 덱 v1 제작 (12장, 16:9)

대표 답: 카드비 변동 가능 · 셋 다 16:9 PDF · 사장님 최우선 · "가장 권장하는 형태로". ⇒ `deck-common.mjs`(공통 모듈, `FACTS` 한 곳) +
`urdeal-store-owner-deck.build.mjs` → `urdeal-store-owner-deck.pptx/.pdf`. 12장 전부 LibreOffice 렌더로 눈 확인(겹침 6곳 수정 · 가운뎃점 줄당 1 정리).
캡처 3장 추가(정산 · 등록 마법사 1/3단계). 🩸 정산 화면 에러 경계 원인 = 예시 응답에 `total` 필드 부재(콘솔 무출력, 코드 읽어서 찾음).
🩸 등록 마법사 캡처는 소비자 세션 신호 + 큐레이터/위시리스트/세션헬스 3종 모킹이 없으면 `/login` 으로 튕긴다(README 에 기록).

다음: 인플루언서 덱(12장, 기존 HTML 대체) → 대행사 v5(공통 모듈로 이관 + 300딜→1,000딜 정정) → `/terms/influencer` 약관 수리 PR.

## 09-13 밤: 인플루언서 덱 v1 제작 (12장)

- `docs/business/proposals/urdeal-influencer-deck.{build.mjs,pptx,pdf}` — 사장님 덱과 같은 `deck-common.mjs`. 기존 9장 HTML 을 대체(HTML 파일은 아직 남겨 둠, 어드민 `/admin/proposals` 가 그걸 보여 준다).
- 캡처 4장 추가(`capture-seller-shots.mjs`): `influencer-offer` · `influencer-settlement`(둘 다 예시 데이터 mock) · `ushop` · `creators-apply`(라이브).
  - 🩸 `/influencer/settlement` 가 처음엔 에러 경계였다 — `PublicProfileSection` 이 `/api/influencer-profile/me` 응답을 객체로 기대하는데 폴백 mock 이 `[]` 를 줘서 `p.channels.map` 크래시. 프로필 mock 을 따로 넣어 해결.
  - `/videos` 는 유튜브가 이 환경에서 막혀(iframe·ytimg 전부 CONNECT 403) 재생기 자리가 비어 덱에서 뺐다. embed 를 가짜 HTML 로 대체해 봤지만 구매 바 오버레이가 안 떠서 못 쓴다.
- **기획서 §3 의 사실 오류 정정**: "최소 1만원·관리자 처리" 는 사장님 정산 값이었다. 인플루언서는 `influencer-payout.ts`: T+7 → 매월 1일 집계 → 현금 `influencer_payout_min`(기본 10만원)부터 원천징수 후 어드민 송금, 딜 수령은 하한 없음. 덱 10장이 이 값이다.
- 다음: 대행사 v5(`deck-common.mjs` 이행, 숫자 갱신, 300딜→1,000딜, 운영자·주인변경 행, `/agency-partner` CTA 제거) → `/terms/influencer` 약관 수정 PR(0.5%/2% 상한/10만원 자동지급 문구가 덱과 다르다).

## 09-13 밤: 대행사 덱 v5 (17장)

- `urdeal-agency-proposal.build.mjs` 를 `deck-common.mjs` 로 이행(자체 헬퍼 140줄 삭제, 슬라이드 본문 불변). 갱신: 활성 337 · 식사 평균 32,411 · DB 201,471 / 연락 가능 46,220 / 블로그 172,755 / 유튜브 18,170 / 카페 9,939 · 카드 수수료 "약 2.75%, 카드사 정책에 따라 바뀔 수 있음" · 규모표 재계산(32,411 기준) · 날짜 09-13.
- 운영자 권한 표(12장)·주인 변경 FAQ(17장)는 v4 에 이미 있었고, `/agency-partner` CTA 도 v4 에 없었다(기획서 §4 의 그 두 항목은 확인 결과 해당 없음). "리뷰 보너스 300딜" 도 v4 본문에 없다.
- 세 덱이 이제 한 모듈(`deck-common.mjs` FACTS)을 읽는다. 요율·연락처·실측 숫자를 바꾸면 세 파일을 다시 빌드하면 된다.

## 09-13 밤 2: 세 덱 보완 (대표 "모두 개선해줘")

- **대행사 v5.1**: 🔴 대리 등록 매장의 정산 잠금을 8장·12장·17장에 명시(사장님이 `/store/find` 로 소유자가 되기 전엔 정산 계좌를 넣을 사람이 없다 — store-operator-model §8.1 실측, 매장 14 가 그 상태). 대행사 보수는 매장이 직접 지급(4장·17장 FAQ). 12장 권한 표에 "소개 파트너에게 제안" 행 추가.
- **인플루언서 v1.1**: 광고 표시(경제적 대가 표시) FAQ 추가 · 소개자 프로필 "공개" 가 제안이 오는 실제 경로임을 7장·11장·12장에 · 조건부 딜 문장(5장) · 8.8% 는 5월 종소세에서 정산(10장). FAQ 4→6.
- **사장님 v1.1(13장)**: 12장 FAQ 신설(배달앱 병행·할인율 하한 없음·수량 상한·간이과세·세금계산서(준비 중)·승인은 사람) + `/seller/scan` QR 사용 처리 화면 캡처 · 6장에 `/store/find` 안내 · 5장에 "유효기간 안 정하면 무기한".
- 캡처 추가: `seller-scan`(mock `/api/group-buy/redemption-settings`). `seller-product-new` 도 찍었으나 쇼핑 풀 폼(placeholder "무선 이어폰", "라이브 스트림")이라 덱에 안 넣었다 — 이용권 전용 등록 폼이 생기면 교체.
- ⚠️ 덱이 약속한 것 중 코드에 없는 것: 세금계산서 역발행("준비 중"으로 표기 — `tax-invoice-gateway.ts` 는 있으나 연동 활성 여부 미확인) · 승인 소요 시간(약속 안 함).

## 09-14: 사장님 덱 v2 — 대표 8장 구성안으로 재구성

- 구조: 설득 5장(서비스 소개 → 기존 방식의 한계 비교표 → 차별점(실계산) → 노출 → 걱정 해소) + 이용 안내 3장(등록 → 운영·정산 → 시작). 13장 v1.1 은 git 히스토리(9bd2928)에만 남는다.
- 원칙: 성과 수치 없음. 2장 비교표(돈이 나가는 시점 / 오는 사람 / 효과 확인)가 심장. 4장 소개비는 %·풀 규모 숫자 없이 한 문단. 후기 보너스는 "포인트"로만.
- 대표 구성안 밖에서 남긴 것 둘: 6장 하단 `/store/find`(대리 등록 매장의 사장님 입구) 한 문장 · 5장 오른쪽 QR 사용 처리 화면.
- 캡처: 등록 마법사 바텀시트의 검은 딤(bg-black/40)을 캡처 때만 옅게(`addStyleTag`) — 뒤 화면은 원래 비어 있어 위쪽은 여전히 빈 면이다.
- 상인회장용 발췌: 1·2·3·5·8장.

## 09-14 (2): 사장님 덱 v3 — 16장 (대표 "내용 부실. 이용 방법을 모아서, 기대 매출·순수익을 정확히")

- 표지는 대표 지시대로 "우리동네 이용권, urdeal." (워드마크 크게). "팔린 만큼만 냅니다" 는 2장으로.
- 5장 **기대 매출과 순수익**: 숫자는 파일 상단 `CALC` 에서만 나온다(손으로 옮겨 적지 않는다). 실제 상품(정가 25,000 / 판매가 16,500) 기준 장당 입금 14,850 · 재료비 8,750(정가×35% 가정) · 남는 돈 6,100. 월 30/90/150장 순수익 183,000 / 549,000 / 915,000. 업종 평균가(식사 32,411 / 뷰티 45,253 / 숙박 155,824, 09-13 실측) × 30장 입금. **할인분 8,500 은 "덜 받는 값"으로 정직하게 적었다.** 추가 주문(음료·사이드)은 계산 밖.
- 8~15장 이용 안내: 8 전체 흐름 한 장(처음 한 번 둘·매일 하나·매주 하나·선택) → 9 등록 → 10 첫 이용권(다섯 칸 + 요령 4) → 11 사용 처리(QR·확인코드·사용 조건) → 12 정산 → 13 소개 파트너·후기 → 14 위임 권한 → 15 FAQ.
- 🩸 **워드마크 수리**: Poppins 가 이 환경에 없어 SVG 텍스트가 대체 폰트로 그려지고 점(●)이 고정 좌표라 글자에서 떨어져 있었다(세 덱 상단 로고 전부). `deck-common.mjs wordmark()` 를 "글자를 먼저 그려 폭을 실측하고 점을 붙이는" 방식으로 바꿨고 반환값이 `{data, ratio}` 가 됐다(chrome 도 ratio 로 그린다). 세 덱 다시 빌드.
- 상인회장용 발췌: 1·2·3·4·5·7·16장.

## 09-14 (3): 사장님 덱 v4 — 대표 최종 구성안 v2 (8장) · 16장은 상세판으로

- 본판 `urdeal-store-owner-deck.*` = 8장. 헤드라인 여덟 개 전부 대표 문안 그대로(공감·질문·평서·약속·무심·안심·배려·권유). 표지의 날짜 라벨 제거. "정직하게 말씀드립니다" 블록 제거(대표 "굳이 안 넣어도 돼").
- 3장에 기대 수익 미니표(하루 1/3/5장 → 계좌 입금·순수익, 재료비 35% 가정)만 남겼다. 전체 표(업종 평균가 포함)는 상세판 5장.
- 상세판 `urdeal-store-owner-deck-detail.*` = 16장(이용 안내 8장). 마지막 장도 v2 문안("일단 메뉴 하나만")으로 맞췄다.
- 6장·8장의 "저희가 대신 만들어 드립니다 / 사람이 답합니다" 는 대표 운영 약속이라 코드 근거 없음 — 카카오 채널 응대가 실제로 돼야 한다.

## 09-14 (4): 사장님 덱 v5 — 대표 최종 구성안 9장 ("이렇게.")

- 4장 노출·판매에 크리에이터 풀 숫자가 들어갔다(대표 문안): 유튜버 18,170 · 네이버 블로거 172,755 (2026년 9월 기준, 09-13 실측 `influencer-pool/stats`). `FACTS.influencerYoutube/NaverBlog/AsOf` 신설 — 숫자가 바뀌면 여기 한 곳.
- 5장 활용 시나리오 신설(5개). "매장 QR" 은 전용 포스터 기능이 없어 "매장 페이지 주소를 QR 로 만들어" 로 적었다. 사용 조건 프리셋(평일만·점심시간)은 `voucher-usage-conditions.ts` 실재.
- 3장의 기대 수익 미니표는 대표 문안에 없어 뺐다(상세판 5장에 그대로).
- 8장에 "어떤 이용권이 언제 많이 팔리는지도 보입니다" 추가(대시보드 daily_revenue·상품별 판매 수).

## 09-14 (5): 사장님 덱 4장에 인플루언서 풀 실제 화면 (대표 "연락처는 블러처리해서 참고로")

- `capture-admin-shots.mjs` 신설: 자동화 어드민으로 `/admin/influencer-pool` 데스크톱 캡처, 이메일·IG·TT 블러(184개 요소), 통계 카드 줄 + 목록 표 크롭. `shots/admin-influencer-pool-{table,stats}.jpg`.
- 4장: 왼쪽 자동 노출 4 + 파는 사람들 숫자 3, 오른쪽 표 캡처(헤더 + 7행) + 캡션 "실제 화면, 연락처는 가렸습니다".
- 채널명·핸들(@levis 등)은 공개 채널 식별자라 그대로 뒀다. 가려야 하면 `blur` 정규식에 `@` 핸들을 추가하면 된다.

## 09-14 (6): 세 덱 어조·화면 개선

- 사장님 표지: "체험단, 할 만큼 해보셨잖아요" → "체험단 말고, 계산하는 손님을 부르는 방법입니다." (대표 "예의 있는 어조가 아니다"). 본문 첫 문장도 "이제 안 하셔도 됩니다" 로.
- 대행사 v5.2: 10장 소개 파트너 화면을 mock 폰 → 인플루언서 풀 실제 데스크톱 캡처(연락처 블러) + 숫자 5개(FACTS). 13장 "사장님용 한 장 안내" → "사장님용 소개서 (PDF 9장)". 14장 "초기입니다(337 중 1)" 블록 제거, 귀속 미추적 + 코드 확정 두 카드로.
- 인플루언서 v1.2: 표지 날짜 라벨 제거, 11장 정직 고지 제거 → FAQ 6개를 크게.

## 09-14 (7): 대행사 덱 화법 수정 + 세 덱 전체 정합 점검

- 대행사 v5.3: 헤드라인 17장 전부를 사장님 덱과 같은 리듬(공감·질문·평서·약속·무심·안심·배려·권유)으로 다시 썼다. 명령조("데려오세요", "만드십시오") 는 권유·배려로, "가져갑니다" 는 "받습니다" 로. 4장 리드의 "대부분의 플랫폼 제안서가 흐리게 씁니다" 는 남 얘기라 뺐다. 12장 제목이 두 줄로 넘쳐 리드를 덮어 짧게 줄였다. 17장 연락처에 카카오톡 채널 추가(사장님·인플루언서 덱과 같은 세 줄).
- 전체 정합 점검(PDF 텍스트 추출 → grep): 낡은 값 `2026-09-07`·`338`·`32,339`·`15,980`·"링크샵"·"정직"·em-dash·공구권/식사권 **네 파일 모두 0**. 수수료(직접 10% / 중개 5%)·카드 수수료(약 2.75%, 유어딜 부담)·환불 100%·영입 2% 1년·인플루언서 정산(7일 → 매월 1일 → 10만원, 3.3/8.8)·연락처(이메일·카카오 채널·사업자번호)가 덱마다 같은 FACTS 값이다.
- 남긴 차이(의도): 사장님 덱은 대표 문안대로 "크리에이터 풀", 대행사·인플루언서 덱은 "인플루언서"(어드민 화면 이름과 같다). 사장님 덱 본판엔 대행사 얘기가 없다(상세판 14장에만 위임 권한 + "대행사 경유 매장 5%").
- 점검 못 한 것: 라이브 사이트 문구(`/terms/influencer` 등 — 별건 PR 예정), 유어쇼츠 화면(이 환경에서 YouTube 차단).

## 09-14 (8): 대표 참고 제안서(REVU 상품소개서 16장) 반영 + 로고 표지 + 화면 칩

- 참고 덱에서 가져온 것: ① 로고를 크게 박은 표지(페이지 번호에 안 셈) ② 화면 캡처 위 말풍선 칩 ③ "Q. 왜 …?" 뒤 이유 1·2·3 ④ 3열 비교 카드(A/B/우리 강조) ⑤ 채널별 상품 페이지(네이버·유튜브·인스타·유어쇼츠). 안 가져온 것: 시장 통계(오픈서베이·아이보스 — 원문을 못 확인해 인용 불가), 고객 인터뷰·브랜드 로고 월(실제 매장 1곳이라 만들 수 없다), 코스닥·회원 140만 류 회사 신뢰 블록(대응 사실이 없다).
- `deck-common.mjs`: `wordmark(fill, scale)` 해상도 인자 · `cover()` · `chip()` · `screen()`(둥근 데스크톱 캡처) · `qa3()` · `statTile()` · `customerSteps caps` 옵션 · 흰 체크/회색 대시 아이콘.
- 사장님 덱 v6 = 11장: 로고 표지 + 대표 9장 구성 그대로 + **손님 쪽 흐름 1장(화면 4장)** 을 차별점 뒤에 끼움(사장님 덱에만 손님 경험이 빠져 있었다). 4장 크리에이터 풀 캡처를 둥근 카드 + 칩 2개로, 등록·정산 화면에 칩.
- 대행사 덱 v6 = 21장: 로고 표지(다크) + **비교표(체험단·광고 vs 유어딜)** + **인플루언서 고르는 기준**(SellerInfluencersPage 실제 필터: 카테고리·팔로워 구간·정렬 5종·표 컬럼) + **채널별 무엇을 기대하나**(Q 1·2·3). 매장 관리·운영자 관리·인플루언서 풀 화면에 칩.
- 인플루언서 덱 v1.3 = 14장: 로고 표지(다크) + **채널별로 링크를 어디에 두나** + 유어샵 화면 칩. 상세판 17장: 로고 표지.
- ⚠️ 채널별 "무엇을 기대하나"는 사용법 안내이지 성과 약속이 아니다(성과 수치 0 유지). "유어쇼츠 = 이용권 상세 안 세로 영상 + 구매 버튼"은 `/videos`·`ProductShortsField` 실재.

## 09-15 (9): 라이브 문구를 덱과 맞춤 (대표 "끝까지 다 해줘")

- 라이브 `platform_settings` 실측(09-15, 어드민 읽기): `fee_channel_rates_enabled=true` · `platform_fee_pct_direct=10` · `platform_fee_pct_brokered=5` · `influencer_store_intro_pct=2` · `influencer_payout_min=100000` · `influencer_payout_day_of_month=1` · **`influencer_deal_bonus_pct=0`**(코드 시드는 20이지만 라이브는 0).
- 고친 페이지 5개(전부 소비자·대외 표면, 머니 경로 무접촉):
  - `/terms/influencer` 제3조: "기본 0.5% · 6개월 +1% · 합산 2% cap · 사용자 referral 0.5%" → 딜 소개비(매장이 정한 %, 상한 없음, 딜 없으면 0) · 영입 2% 1년 직접 입점만 · 7일 확정 · 친구 추천은 미운영. 제4조: 매월 1일 · 현금 10만원부터 · 딜 보너스 "현재 0%" · 딜 수령 하한 없음.
  - `/influencer` 랜딩: "최소 10,000원부터 출금" → 매월 1일 · 10만원부터 · 3.3/8.8. "수수료 5%" → 10%(직접 입점).
  - `/business` 랜딩: 수수료 계산기 `5% + GMV 1천만/1억 차등 4%/3%`(코드에 없는 수치) → 10% 고정, 차등 문구 제거.
  - `/partners` 랜딩: `PLATFORM_FEE 0.05` → 0.10, 신뢰 숫자 "5% 업계 최저" → "0원 미리 내는 돈", FAQ·계산기 라벨 10%.
  - `/about/print` 수익 표: "동네 공구 5%" → 이용권 직접 10% / 대행사 경유 5% / 상품 10%.
- 🩸 덱 사실 검증 때 코드 시드(20%)를 믿었으면 약관에 틀린 보너스를 적을 뻔했다. **요율·보너스는 코드 기본값이 아니라 라이브 `platform_settings` 로 판정할 것.**
- 검증: anti-slop 가드 GREEN(em-dash 0). tsc/vitest 는 `npm ci` 완료 후(이 세션은 npm 통과).

## 09-15 (10): 최종 상태 — PR #1381 CI GREEN (head 250d056)

- Verify 가 02bc2e4 에서 한 번 빨갛게 났다: 내가 `/about/print` 표에 행을 하나 더 넣어 `AboutPage.tsx` 가 778줄(baseline 777) 이 됐다. 두 행을 한 행으로 합쳐 250d056 에서 GREEN. 🧭 baseline 동결 파일은 **한 줄도** 못 늘어난다(pre-commit 은 안 막고 CI 만 막는다 — `--changed-only` 를 로컬에서 먼저 돌릴 것).
- **다음 세션 첫 액션**: 대표가 PR #1381 을 머지하면 ① `curl -s https://urdeal.kr/terms/influencer | grep -o '매월 1일'` · `curl -s https://urdeal.kr/partners | grep -o '수수료 10%'` 로 E4 판정 ② Notion 개발 로그에 한 줄(서비스: 유어딜 · 유형: 문구 정정 + 대외 소개서 · 머니 경로: 없음 · PR 링크).
- 남은 결정: 머지(대표). 유어쇼츠 캡처는 이 환경에서 불가(YouTube 차단) — 대표 PC 에서 `/videos` 캡처를 `shots/shorts.jpg` 로 주면 덱 생성기가 바로 싣는다(`capture-seller-shots.mjs` 의 `shorts` 슬롯).

## 09-15 (11): [E4] PR #1381 머지 · 배포 · 라이브 판정 (대표 "머지해줘")

- 머지: 스쿼시 `390727f`(main 관례와 동일). 머지 직전 main 이 두 번 움직여(#1434·#1436) `origin/main` 을 한 번 더 합쳤고(7ca8a3b, 충돌 0) 그 head 의 Verify GREEN 을 보고 머지했다. 배포: `Deploy to Cloudflare Pages` success, 라이브 `/api/version` = `index-CZGt7B_9.js`.
- E4 판정 방법: 이 환경은 브라우저가 프록시 CA 를 못 믿어 `page.goto` 가 `ERR_CERT_AUTHORITY_INVALID` 로 죽는다(TLS 검증을 끄지 않는다). 대신 **라이브 엔트리 청크에서 페이지 청크 이름을 찾아 그 청크 본문을 grep** 했다(curl 은 CA 번들로 통과). 결과:
  - `InfluencerTermsPage-Dp9lfZA9.js`: `매월 1일` · `10만원` 있음 · `0.5%` 없음
  - `InfluencerLandingPage-DIT1jUcb.js`: `매월 1일` · `10만원` 있음 · `최소 1만원` 없음
  - `PartnersPage-YhVfeDhg.js`: `수수료 10%` 있음 · `수수료 5%` 없음
  - `BusinessLandingPage-CVowf8X9.js`: 요율 리터럴 `.1` 1개, 구간별 🎉 문구 0
  - `AboutPage-BZ4qSkBt.js`: `대행사 경유 5%` 있음
  - 이전 판 (10) 에 적어 둔 `curl … | grep '매월 1일'` 은 **안 된다** — 그 페이지들은 프리렌더가 아니라 SPA 라 HTML 에 본문이 없다. 청크 grep 이 맞는 방법이다.
- Notion 개발 업데이트 로그 1행 기록(유어딜 · 문서/설계 · 머니 경로 없음 · PR #1381).
- ✅ 렌더된 DOM 도 확인했다(같은 날 후속): 헤드리스 Chrome 을 CDP 로 붙여 `document.body.innerText` 를 읽었다 — `/terms/influencer`(매월 1일·10만원·2%) · `/influencer`(매월 1일·10만원) · `/partners`(수수료 10%) · `/business`(10%) 전부 있음, 옛 문구 0. 🔑 이 환경에서 Chromium 이 `ERR_CERT_AUTHORITY_INVALID` 를 내는 이유는 정책이 아니라 **NSS 저장소(9/7)가 프록시 CA 번들(당일 재발급)보다 낡아서**다. TLS 검증을 끄지 않고, 번들 안의 Anthropic 프록시 CA 6개 SPKI 만 `--ignore-certificate-errors-spki-list` 로 신뢰시키면 된다(그 외 인증서는 그대로 검증). 스크립트는 세션 스크래치 `e4cdp.mjs` 였고 레포엔 안 넣었다.
- ⚠️ 못 한 것: 유어쇼츠 캡처는 여전히 대표 PC 몫.

## 09-15 (12): 두 번째 참고 덱(히로인스 36장) 반영 — 세 덱 전부 개선 (대표 "가져오면 좋은 것들을 모두 활용해서")

- 참고 덱은 규모 숫자(회원 50만·체류 20분·ROAS)로 설득하는 문서라 숫자는 하나도 안 가져왔고, **구조를 보여주는 장치 7개**를 `deck-common.mjs` 헬퍼로 넣어 세 덱이 같이 쓴다: `section`(브랜드 블루 구분 장 + 우상단 "PART n · …" 라벨) · `takeaway`(하단 결론 바) · `callouts`(화면 인출선) · `personas`(말풍선 페르소나 행) · `procedureColumns`(절차 N열) · `flywheel` · `table hiCol`(강조 열). 안 가져온 것: 지표 타일·ROAS·로고 월·3D 아이콘·경쟁 시장가 표.
- 사장님 11 → **15장 v7**: 이런 매장에 맞습니다(페르소나 4행, 답은 전부 등록 화면 설정값) · 목표별 이용권 설계 표(5줄, 인플루언서 소개비 열 강조) · 쌓이는 자산(매장 페이지·소개자 유어샵·손님 지갑) · 정산 화면 인출선 3개 · 시작하는 세 가지 길(직접 10% / 유어딜 대신 10% / 대행사 5%). 결론 바 5장.
- 인플루언서 14 → **20장 v2**: PART 구분 장 3 · 이런 채널에 맞습니다(블로거·유튜버·소규모 인스타·영입형) · 플라이휠(소개→결제→사용→확정→재제안, 가운데 "유어샵에 남는다") · 유어샵 화면 인출선 3개(담는 방법을 화면 자리에 직접) · 경로 A/B/정산 절차 3열.
- 대행사 21 → **30장 v7**: PART 구분 장 4 · 직접 vs 경유 비교표(8행, 경유 열 강조) · 플라이휠(등록→이용권→인플루언서→결제·방문→보고·청구) · 이런 대행사에 맞습니다(체험단 운영사·지역 광고대행·MCN·다매장) · 목표별 이용권 설계 표(상담용) · 운영자 관리 화면 인출선 · 사장님/인플루언서/대행사 절차 3열. "사장님용 소개서 PDF 9장" → 15장.
- 🩸 **LibreOffice 변환이 `transparency` 런의 숫자를 지운다**: pptx 에는 `PART 1` 이 있는데 PDF 에는 `PART ` 만 남았다(실측, `get_text`). 투명도 대신 옅은 색(`CFE0FD`)으로 바꿔 해결. 브랜드 블루 바탕에서는 워드마크 점이 사라지므로 `wordmark(fill, scale, dotFill)` 로 흰 점 변형을 추가했다.
- 🩸 `pres.shapes.TRIANGLE` 은 없다(`ISOSCELES_TRIANGLE`). 첫 빌드가 그걸로 죽었다.
- 인출선 좌표는 캡처 위에 10% 격자를 그려 실측했다(정산 30/58/85% · 유어샵 8/48/80% · 운영자 31/44/47%). 캡처를 다시 찍으면 좌표를 다시 재야 한다.
- 검증: 세 덱 PDF 텍스트에서 폐기 명칭·em-dash 0, 요율 문구 일치. 컨택트 시트로 전 장 눈 확인(겹침 0).
- 남은 것: 유어쇼츠 캡처(대표 PC). 상세판 17장은 이번 장치 미적용(공통 chrome 만 공유 — 섹션 라벨 없음, 동일 출력).

## 09-19 (13): 인플루언서 덱 v3 — 중개사 5% 흐름으로 재구성 (대표 20장 전수 지시)

[E2] 대표: *"인플루언서는 중개사의 계정으로 가입을 하기 때문에 10% 수수료가 아닌 5% 수수료 형태로 중개사 플로우로"* + 20장 페이지별 지시.
- 한 것: `urdeal-influencer-deck.build.mjs` v3(20장 유지). 영입 2%·1년·직접 입점 전부 제거 · 카드 2.75% 제거 · 돈의 흐름을 [손님 → 유어딜 5% → 사장님 95% → 나(소개비 %)] 로 · 수익 두 갈래의 둘째를 "내가 섭외한 매장(중개사 계정 등록, 5%)" 으로 · 계산 장 원천징수 행 삭제(+"차감 별도" 한 줄) · 정산 "7일 뒤 확정" → "이용권 사용 후 확정" · 유어샵/유어쇼츠 두 폰 + 양쪽 인출선 · SNS 로고(si) · `creators/apply`·`urdeal.kr/u/…` 문자열 0 · 연락처 https 하이퍼링크 · 제목 8곳 교체.
- 캡처 4장 신규: `ushop`(재캡처, 사진 3장 로드) · `shorts`(`/videos?v=jHPacJoCEt8`, 이용권 붙은 라이브) · `voucher-wallet` · `voucher-qr`(`/my-vouchers` 예시 데이터, "사용하기" 클릭). 하네스: 스크래치패드 `capture-inf.mjs`(레포 `capture-seller-shots.mjs` 복제 + 지갑 mock + 클릭 + 시트 style).
- 🩸 `pkill -f soffice.bin` 은 **자기 셸도 죽인다**(명령줄에 그 문자열이 있으므로) — `pkill -x soffice.bin` 으로.
- ⚠️ 코드와 문구가 다른 것 2건(대표 확인): ① 영입 2% 는 코드(`influencer_store_intro_pct=2`)·`actor-benefit-map.md`·대행사 덱 비교표 1행에 아직 살아 있다 ② 인플루언서 소개비 확정은 코드가 결제+7일(`helpers.ts available_at`)인데 덱은 "이용권 사용 후".
- ⚠️ 대표 지시대로 PART 2·PART 3·정산 장 제목이 **셋 다 "유어딜 정산 프로세스"** 다 — 구분 장 두 개가 같은 이름이라 되짚어 볼 것.
- 남은 것: 07 경로 A(13페이지) 재작업(대표 "마지막에") · 자격 선택 가입 UI 가 생기면 가입 화면 캡처.

## 09-19 (14): 대표 확정 플로우(대행사→매장→인플루언서 매칭) — 결재 기록 + SSOT 3종 + 인플루언서 v4 · 대행사 v8

[E2] 대표: *"이 플로우로 결정났어"* (11단계 — 매장 코드 자동 생성 · 두 요율 · 사장님이 코드 입력해 주인 · 인플루언서 코드 링크 한 탭 · 매장 단위 링크 · 대행사 몫 유어딜 직접 송금).
- 결재 기록: `docs/decisions/2026-09-19-broker-matching-flow.md`(approved · 대표 말 그대로 · 원안과 달라진 2곳 · 이미 있는 레일/없는 부품 표 · 불변식 9 · 구현 순서 ①~⑥ 등급).
- SSOT: `store-operator-model.md` §9(§8 대비 달라지는 자리 표) · `urdeal-platform-model.md` §2 중개사 행 + 09-16/09-19 블록 · `actor-benefit-map.md` 중개사 행 + §3 #5(09-16 에 뒤집힘 표기). **§7.2 "장부 밖" 문단은 소급 수정 안 함**(작성 당시 기록).
- 덱: 인플루언서 v4(07 경로 A 재작업 — 대표 "마지막에" 항목 · 05/08/10/11/11-2/12 문구 · `제안` 0) · 대행사 v8(01/02/04/04-2/05/06/06-3/07-2/08/09/10/11/12/14/15/16-2/17 — `보수` 0 · `청구` 는 "청구할 일이 없습니다" 부정문 2건만 · `매장과 직접` 0 · 영입 행 삭제 · §14 인용을 09-19 결정으로 교체). 컨택트 시트로 전 장 눈 확인(겹침 3건 수정: 인플 05 카드 5줄 · 07 카드 하단 · 08 조건 카드).
- 🩸 **이번에 틀렸던 판단**: 위 커밋(140e0fe6)에 "코드 미착수" 라고 6곳에 적었는데, **같은 날 다른 세션이 대표와 같은 플로우를 확정받아 PR #1499 로 구현·머지하고 있었다**(03:07 KST 머지). 내 브랜치는 3시간 전 main 기준이었다. ⇒ **결정을 기록하기 전에 `git fetch origin main` 으로 병렬 세션의 머지를 먼저 본다.** 후속 커밋에서 main 을 머지(충돌 2건은 main 채택)하고 "미착수" 6곳 + "등록 때 정한 소개비" 문구를 실제 모델(중개사 몫 % · 인플루언서 **상한** % · 협업 코드 기본 % · 딜별 조정)로 고쳤다. 결재 파일은 정본 `agency-flow-final.md` 를 가리키는 **대표 문장 원문 기록**으로 재구성.
- 남은 것: 인플루언서 덱 경로 A 카드 → 배포 후 `/i/join/:code`·마이페이지 캡처로 교체 · 대행사 덱 08/14 폰 캡처에 코드 열·인플루언서별 행 반영(재캡처). 대표 판단 대기: `broker_share_enabled` ON(S-BROKER 뒤) · 소개비 확정 시점(코드 결제+7일 ↔ 덱 "사용 후") · PART 2/3 동명 제목.

