# 키워드 순환 기아의 **진짜 원인** — 커서는 맨 앞 축 하나만 전진한다 (2026-08-12)

어제(#1134) 축 배분을 고쳐 회전율 비가 **7.3 : 1 : 3.2 → 2.5 : 1 : 1.8** 로 내려왔다(설계 1.5 : 1 : 0.5).
그런데 `starved` 경보는 안 꺼졌다. 파 보니 **별개의 원인**이 있었고, 그게 이 문서다.

## 라이브에서 확정한 원인

커서 전진은 `prefixDone`(처리된 **선행** 픽 수)인데 회차가 예산에서 잘린다(계획 16 → 처리 7).
⇒ **뒤쪽 축의 픽은 매번 잘려 `prefixDone = 0` → 그 축 커서가 영원히 제자리 → 다음 회차도 같은
키워드를 내놓고 또 잘린다.** 무한 반복이다.

커서 값이 그대로 증거다 — **앞자리가 바뀌자 움직이는 커서도 그대로 바뀌었다**:

```
#1134 이전 (집중이 맨 앞)   집중 17 전진   ·  우선  5 정지  ·  일반 52 정지
#1134 이후 (우선이 맨 앞)   우선 5→51 전진 ·  집중  1 정지  ·  일반 53 정지
```

그래서 우선 축 위치 50~149(102개)가 **15일간 한 번도 순번을 못 받았다.** 4분 간격 커서 관측으로
정각 회차마다 +3 씩 오르는 것을 확인했고, **11:00 회차에 48 → 51 로 통과**하면서 그 구간이 실제로
열렸다(위치 50·51·53~56 실행). 되감김이 아니라 통과다.

## 수리 두 가지 (짝이다)

| | 무엇 | 역할 |
|---|---|---|
| 🔄 `mergeKeywordPicks(…, lead)` | 맨 앞자리를 회차마다 축끼리 돌린다(`roundIndex % 3`) | **얼어붙지 않게** 하는 보험 |
| 📏 `planRoundWidth(...)` | 최근 처리 수 중앙값 + 여유 20% 로 계획 폭을 맞춘다 | **빨리 돌게** 하는 근본 처방 |

⚠️ **총 처리량은 안 줄어든다** — 예산이 상한이라 처리 수는 그대로고, 줄어드는 건 "뽑아 놓고 버리는 수"뿐이다.
그 대가로 **모든 축의 커서가 전진**한다. 발굴 가속이 아니므로 미측정 백로그도 안 키운다.

⚠️ 회전만으론 부족하다(3회차당 +1 은 일반 풀 76개 한 바퀴에 9일). 폭 맞춤이 있어야 실제로 빨라진다.
⚠️ `planKeywordSplit` 의 몫·불변식 ④(최소 1슬롯)는 **무접촉**이다. 순서와 폭만 바꿨다.

## 검증

- 신규 유닛 10(회전 3축 · 몫/순서 불변 · 기본값 회귀 · 빈 축 · 폭 맞춤 5) — **되돌려-검증 완료**
  (회전 제거 → `expected 21 to be 11` 빨간불 / 폭 맞춤 무력화 → 3건 빨간불, 복원 시 37 pass)
- `check-guard-mutations.mjs` 주입 2건 추가 → **271개 전부 빨간불 확인**
- tsc 0 · audit-gate

## 다음 세션의 첫 액션 — 배포 +24h 판정

```sql
SELECT key,value FROM platform_settings WHERE key LIKE 'ads_autocollect_cursor%';
```
**세 커서가 모두 움직였는지**가 판정이다(지금은 우선만 움직인다). 그리고:
```sql
SELECT COUNT(*) FROM ad_discovery_keywords
 WHERE active=1 AND last_run_at <= datetime('now','-14 day');   -- 08-12 = 107
```
이 값이 줄어들면 성공. 0 이 되면 `starved` 경보도 자동으로 멎는다
(경보 = 최고령 ÷ 한 바퀴 > 3, 08-12 실측 16.1 ÷ 4.7 = 3.44).

## 이번에 틀렸던 판단 (반복 방지 — 이게 제일 값지다)

원인을 찾는 동안 **가설 넷을 세워 넷 다 데이터로 반증했다**:
1. 저수율 자동 억제 → 정체군 연락처율 24.7% 로 오히려 평균(23.5%)보다 높음. 억제 대상은 107 중 7개뿐
2. 은퇴 축이라 제외 → `RETIRED_CATEGORIES` 는 **비어 있다**
3. 커서가 못 닿는 id 구간 → 같은 id 구간의 다른 키워드는 정상 실행 중
4. 커서 명단이 ~50개로 잘려 되감김 → **11:00 회차에 48→51 통과**. 되감기지 않았다

🧭 **집계만 보고 기전을 추측하면 틀린다.** 답은 커서 값을 **4분 간격으로 실제로 지켜봐서** 나왔다
(정각에만 +3 → 시간당 1회차 → 앞자리 축만 전진). 앞선 세션들이 같은 함정에 반복해 빠졌으니,
다음에도 **"그 레인이 남긴 값을 시간축으로 찍어 보는 것"** 을 먼저 할 것.

---

## 🔕 굳이 필요없는 알람 제거 (2026-08-13 — 대표 *"필요없는 알람은 없애줘. 에러도 있는지 확인"*)

**먼저 진짜 에러부터 셌다 — 0건이다.** 3일치 `cron_failures` 중 `stale:*` 아닌 것 0 ·
하트비트 `ok=false` 0 · 레인 스탬프 `error` 0. 24시간 실패 8건은 **전부 아래 오탐**이었다.

### ① 슬롯 cron 오탐 — 매일 8건

소비자 cron 은 대부분 **5분 캐리어**에 얹혀 `slotDue(...)` 로 자기 시각에만 돈다. 그런데 하트비트엔
캐리어 식이 기록돼, 경보가 기대치를 **40분**(5×2+30)으로 잡고 하루 1회 작업을 23시간 내내 `stale` 로
신고했다. 실측(08-13 04:00 KST) 8건 전부 **18:40 KST 일 1회** 작업이고 **정상 실행 중**이었다.

**수리**: `safeCron(name, task, gapMin?)` + `slotCron(expr)` — 슬롯 블록이 자기 주기를 cron 식으로
넘기면 `expectedMaxAgeMinutes`(SSOT)가 환산한다. 새 상수를 만들지 않았다(규칙이 두 벌이 되면 갈라진다).
5개 블록 27개 호출 전부 이관.

### ② 순환 편식(`starved`) — 회복 중에도 계속 울림

`oldestDays / cycleDays > 3` 인데 `oldestDays` 는 **최악값 하나**라, 밀린 키워드가 자기 차례를
기다리는 동안 계속 커진다. **수리가 먹혀 밀린 무리를 갚는 며칠 내내 경보가 울린다.** 실측:

```
7일+ 밀린 수  107 → 60 (−44%)   인데   worstCycles 3.46 (오히려 상승)
```

**수리**: `judgeRotation` 에 추세 판정 — 밀린 무리가 **줄고 있으면** `starved` 를 내리지 않는다
(`recovering: true`). 고장이면 늘고 회복이면 준다, 그 방향만 본다(임계가 아니라 추세).
⚠️ 직전 표본이 없으면 **억제하지 않는다** — 모르는 상태의 침묵은 경보의 존재 이유를 지운다.
⚠️ `stopped`(24시간 실행 0)는 추세와 무관하게 항상 울린다.

### 🕳️ 이 수리가 기존 가드를 하나 무력화했다 (harness 가 잡음)

`ads-rotation-health` 의 "나이 COALESCE 에 activated_at 이 낀다" 검사가 **개수(`>= 2`)로 세고**
있었는데, `behind7` 지표가 셋째 occurrence 가 되면서 **한 곳을 망가뜨려도 통과**하게 됐다.
지표 이름으로 앵커하도록 고쳤다. ⇒ **개수로 세는 가드는 지표가 늘면 조용히 죽는다.**

### 검증
tsc 0 · 되돌려-검증 4방향(회복 억제 제거 / 무조건 억제 / 슬롯 생 safeCron / 주기 신고 무력화) 전부 빨강 확인
· `check-guard-mutations` **289개** 전부 빨강 확인
