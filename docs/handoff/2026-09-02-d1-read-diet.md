# 2026-09-02 — D1 읽기 다이어트 (유료 전환 전 "가장 이상적인 최적화")

> 대표 지시: *"유료 전환 전에 유어딜, 유어애즈 모두 가장 이상적인 최적화가 필요할 것 같은데? 특히 유어딜.
> 문제가 생기면 이제 안돼."* 그리고 그 전 문답의 결론 — 한도(일 500만 행)는 **DB 가 아니라 계정 단위**라
> 유어애즈 분리로는 못 막고, 유어딜 혼자 얼마 읽는지도 **아무도 잰 적이 없다**(8/27 수리 후 미판정).

## 0. 다음 세션의 첫 액션

> ✅ **2026-09-02 17:20 KST — 다섯 PR 전부 main 에 올라갔다** (대표 *"일단 너가 배포까지 해"*). GitHub API 가
> 시간당 한도에 걸려 PR 버튼을 못 써서 **git 으로 직접** 올렸다 — 각 브랜치에 main 을 병합·로컬 검증(관련 vitest ·
> tsc 0 · file-size · map-only) 후 `--no-ff` 로 main 에 얹었고, 매번 **main 트리 == CI 초록 head 트리** 를 확인했다.
> main: `780a75b`(#1299) → `0805935`(#1306) → `e3bb467`(#1300) → `2f0aaa5`(#1303) → `8ed9b2d`(#1304).
> 배포 워크플로: ur-ads · cron 워커는 각 커밋마다 success, Pages 는 concurrency 로 중간 커밋이 취소되고 **8ed9b2d 가 최종**(17:20 확인 예정).
>
> 🩸 **이번에 틀렸던 것**: 병합 검증 스크립트에 `node -e "import('./scripts/check-guard-mutations.mjs')"` 를 넣었다 —
> **import 가 곧 bare 실행**이라 655건 주입이 통째로 돌았고, 10분 타임아웃이 부모 셸을 죽여 자식이 **주입 상태로**
> 남았다(`silence-digest.ts` 에 `Math.round(gap/60)` 이 박힌 채 발견 — `git diff` 로 잡아 `checkout` 으로 복원).
> CLAUDE.md 가 두 번 경고한 바로 그 사고다. 매니페스트는 `--map-only`/`--only` 로만 부른다. 그리고 `git add -A` 금지.
> 또 하나: 충돌 해소 스크립트의 `assert '<<<<<<<' not in s` 가 **CLAUDE.md 안의 설명 문구**(`grep '^<<<<<<< '`)에
> 걸려 아무것도 안 쓰고 죽었는데, 그 다음 줄 `git add` 는 그대로 돌아 **마커가 박힌 파일이 staged** 됐다. 판정은
> 줄 앞 앵커(`^<<<<<<< `)로, 그리고 해소 실패 시엔 add 로 가지 말 것.

1. **09:00 KST 리셋 직후** 유어딜이 살아 있는 창에서(하루 중 유일한 실측 창):
   ```bash
   # 작업별 읽기량 — 이번 세션이 만든 계량기(cron_hb 의 rr/rw/q/qu)
   curl -sS "https://live.ur-team.com/api/admin/cron-heartbeats" -H "Authorization: Bearer $TOK" -H "User-Agent: $UA" \
     | python3 -c "import sys,json;d=json.load(sys.stdin)['data']['items'];[print(f\"{i['rows_read']:>10}  q={i['queries']} qu={i['unmetered']}  {i['name']}\") for i in sorted(d,key=lambda x:-(x.get('rows_read') or 0))[:40]]"
   ```
   `qu` 가 큰 작업은 `first()` 로 읽는 것이라 `rows_read` 가 **하한**이다(계량기 헤더 참조).
2. 같은 창에서 CF 분석(DB 별 총량)으로 8/27 대비 판정: `d1AnalyticsAdaptiveGroups → rowsRead` (토큰은
   `platform_settings.cf_api_token`, CLAUDE.md "☁️ Cloudflare API 접근").
3. 유어딜 총량이 500만 아래면 무료 유지 가능 판정 → §3 의 남은 항목 계속. 넘으면 유료 전환이 선행.
4. **읽기 예산 차단기(PR `claude/ads-read-budget`, #1299 위에 스택)** 배포 뒤: `cron_hb:ads:read-budget` 에
   `used=… budget=1500000 over=false` 가 매 정각 찍히는지. `unknown=true` 가 보이면 DO 원장 조회가 실패한 것이고
   **그 상태는 fail-closed 라 유어애즈가 통째로 서 있다** — 원인은 `ADS_LANE` 바인딩/DO 응답. 값을 바꾸려면
   ur-ads env `ADS_DAILY_READ_BUDGET`(0=끔). 유료 전환 뒤엔 이 값이 장애 방지가 아니라 **비용 상한**이 된다.

### 0-c. 로딩 — 홈 청크 다이어트 · 유어샵 0-RTT (2026-09-02 19:00~20:00 KST, 대표 "홈 청크 다이어트도 지금 해주고, 유어샵도 이상적인 판정")
- **실측(프록시 릴레이 Playwright, iPhone 13)**: `/vouchers` 0.6s · 상세 0.8s · `/u/jiwon1228` 0.9~1.6s · `/` 0.9~3.7s(편차). 서버는 4~120ms, SSR 시드 전부 edge/kv-hit.
  ⚠️ 이 세션이 대표에게 "CACHE_KV 바인딩이 안 붙어 있다"고 말한 것은 **틀렸다** — `/vouchers` 가 `VOUCHERS:kv-hit` 를 찍는다.
- **유어샵(PR #1309)**: 워터폴에서 셀러 시드 뒤에도 `/api/products?seller_id&limit=100` 이 JS 실행 후(≈750ms) 나가 콘텐츠 완성을 정함 → curator 응답에 `linked_seller_products` 동봉 + `SellerPublicPage` 동기 소비/fetch 생략. 부수: xl 전용 레일 둘이 폰에서도 마운트돼 QR `codes` 82KB 를 받던 것 → `useMediaQuery` 게이트.
- **홈(브랜치 claude/home-chunk-diet)**: 번들러 그래프 실측 → `app-home` 규칙. 27→20청크, 956→774KB. 방법·함정은 CLAUDE.md 로딩 잠금 audit log(2026-09-02 vite.config) 에.
  🩸 **틀렸던 것**: `deal/` 폴더째 넣기(DetailFloatingHeader 가 app-components 를 도로 끌고 옴) — 규칙은 "닿는 파일" 단위로. Vite 는 전이 import 를 `chunk.imports` 에 끌어올리므로(hoisted preload) "누가 끌고 오나"는 **모듈 간선**으로 찾아야 한다.
- **다음**: `app-utils` 102KB 중 72KB 홈 미도달(별건) · 배포 후 `probe-loading.mjs / /u/jiwon1228` 재실측 · 실사용자 web-vitals 는 Sentry 할당량 소진 상태.

### 0-b. "유어딜이 유어애즈 영향을 아예 안 받으려면" (대표 질문 2026-09-02 16:00 KST)
같은 계정인 한 **완전 격리는 불가능**하다 — 무료 한도(일 500만 행)도, 유료 청구서도 계정 단위다. 층은 셋:
| 층 | 무엇 | 막는 것 | 못 막는 것 |
|---|---|---|---|
| ① 자기 예산 차단기(이 PR) | 유어애즈 레인이 하루 150만 행 넘기면 스스로 정지 | 레인 폭주(9/2 유형 중 레인 안의 것) | 레인 밖 읽기(서비스몰 API·부팅 마이그레이션 — #1302 가 따로 막음) |
| ② 정지 스위치(#1299) + 다이어트(#1300·#1303·#1304) | 사람이 끄는 길 하나 · 평상시 읽기 자체를 줄임 | 사고 후 확산 · 평시 한도 근접 | 사고 전 자동 정지(그건 ①) |
| ③ 계정 분리 | 유어애즈를 다른 Cloudflare 계정으로 | 한도·청구서 공유 자체 | **비용**: 유어애즈 서비스몰이 메인 DB 를 직접 읽는다(`marketing.routes` 46곳 · `searchad` 27 · `media-gateway` 22 · `ads-pay` 16 …). 전부 HTTP 브리지로 바꿔야 해 2~3일 + 유어애즈도 유료(DO) 필요 |
⇒ 지금은 ①+② 로 가고, ③은 유어애즈가 유어딜과 무관하게 커질 때. 실측 근거: `grep -rn "env\.DB" src/features/marketing/api src/worker-ads`.

## 1. 계량기 (이 세션 · PR 별도)

- `src/worker/utils/d1-read-meter.ts` — D1 래퍼. `all/run/batch` 의 `meta.rows_read` 합산, `first/raw/exec` 는
  `qu`(unmetered) 로 횟수만. **`first()` 를 `all()` 로 바꿔 세지 않는다**(무제한 SELECT + first 가 37만 행을 실어 와
  워커를 죽일 수 있다).
- `d1-read-meter-als.ts` — cron 워커 작업별 귀속(AsyncLocalStorage). `scheduled.ts` 의 `safeCron` 이 계량기 안에서
  작업을 돌리고 `recordCronBeat(…, meter)` 로 하트비트에 `rr/rw/q/qu` 를 싣는다. 어드민 `/cron-heartbeats` 응답에
  `rows_read/rows_written/queries/unmetered`.
- 🩸 **`node:async_hooks` 를 정적으로 import 했더니 ur-wholesale Pages 프리뷰 빌드가 죽었다**(Pages 배포 단계 번들러는 프로젝트에
  `nodejs_compat` 이 없으면 `node:` 정적 import 를 못 푼다 — 워커 번들에 `node:` import 가 이 파일이 처음이었다). 런타임 문자열
  `import(spec)` 로 바꾸고 없으면 계량을 끈다(기능 불변). `esbuild.worker.config.js` 는 main 에서도 top-level await 로 이미 안 도는 스테일 파일.
- ⚠️ 이미 있던 도구: `d1-profiler.ts`(2026-07-22, `D1_PROFILE_ENABLED='true'` 로 Pages 요청경로 SQL 패턴별 집계,
  isolate 메모리·미저장, 어드민 `GET /api/admin/d1-profile`). **요청 경로는 이걸 켜서 재면 된다**(대표가 env 설정).
  계량기는 cron/레인 **작업별·영구 기록**이라 상보적이다.

## 2. 정적 감사 — 유어딜 (Explore 2건, 2026-09-02)

### 2-1. cron 워커 (`*/5` = 288회/일이 거의 모든 것의 배수)
| # | 작업 · 위치 | 왜 | 추정/일 | 처방 |
|---|---|---|---|---|
| 1 | `cache-prewarm` `scheduled.ts:210` → `cron/cache-prewarm.ts:44-101,259-311,231-250` | 5분마다 ~34개 API 자체호출. 카테고리 필터 변형은 materialized 를 우회(`group-buy-public.routes.ts:169-176`)해 `products⋈sellers` 전수+정렬. `/api/home/bundle` 6쿼리(products 3·live_streams 3) | 1천만~5천만 | 카테고리 변형을 **materialized 에서 서빙**(cron 이 이미 20변형 저장), 동적 셀러/상품/큐레이터 워밍 5→30분, `normalizeSupplyProductData` UPDATE 2개(`:172-174`) 일 1회로 |
| 2 | `group-buy-feed-cache` `cron/group-buy-feed-cache.ts:75-83` | 288 × 20쿼리. `(status = ? OR ? = 'all')` OR 가 인덱스 무력화 + `ORDER BY CASE … LIKE 'demo-%'` 정렬 | 500만~1,500만 | **더티 플래그**(상품 쓰기 → `invalidateGroupBuyProductsCache` 가 플래그 set, cron 은 플래그 없으면 skip · 안전망 60분 1회) + OR 분리 |
| 3 | `scheduled-cleanup` `cron/scheduled-cleanup.ts` (`:342-357,368-384,687-742,872,390,156,204,220,314,331,234,848,953,441,511`) | 5분마다 ~33개 스캔 — `created_at/expires_at < now` 에 인덱스 없음, `OR` 2곳(orders PENDING/AWAITING · products category OR deal_only) | 500만~1,500만 | GC 성격은 **일 1회 레인**으로, 시간 민감(주문 만료·time_deals)만 5분 유지 + 인덱스 |
| 4 | 쿠폰 만료 `scheduled-cleanup.ts:630` | `SELECT DISTINCT id FROM users LIMIT 100` — DISTINCT 가 전수 | 288×users | DISTINCT 제거 |
| 5 | 재고 부족 `:555-561` | `COALESCE(stock,stock_quantity)` 로 인덱스 불가 | 288×products | 시간당 1회 |
| 6 | 위탁 정산 `:1022` → `lib/consignment-settlement.ts:154-171` | `order_items.consignment_id` 인덱스 없음 + NOT EXISTS 상관 | 288×order_items | 일 1회 + 부분 인덱스 |
| 7 | `cache-prewarm.ts:172-174` supply normalize | UPDATE 2개가 products 전수 | 576×products | 일 1회 |
| 8 | `group-buy-deadline-push.ts:31-39` | `datetime(col)` 래핑 → 인덱스 불가 + 매 틱 ALTER 2개 | 576×products | 원컬럼 비교 + `(group_buy_status, group_buy_deadline)` 인덱스 |
| 9 | `appointment-noshow-alert.ts:43-56` | 문자열 결합+datetime() | 288×bookings | 시간당 1회 |
| 10 | `prospects-commission-activate.ts:21-40` | 루프당 200 쿼리 + `seller_prospects.status` 인덱스 없음 | 288×(2×prospects+200) | 일 1회 + 단일 UPDATE…FROM |
| 11 | `d1-backup-chunked` **이중 배선** `scheduled.ts:224-229` + `:418-420` | 같은 작업이 두 트리거 | ≤220만 | `:224-229` 블록 제거 |
| 12 | `auto-seed-reviews` 시간당 (`review-gen-tuning.ts:67`, `demo-review-generator.ts:478-523`) | product_reviews 전수+정렬 매시 | 25×reviews | 통계 24h 캐시, 일 1회 |
| 13 | `demo-image-rehost.ts:63-73` | 로그용 COUNT(*) + LIKE 3개 | 24×4×products | COUNT 제거 + 수렴 플래그 |
| 14 | `healDemoNamesInPlace` (`scheduled.ts:253`) | LIMIT 없는 LIKE 전수 매시 | 24×products | LIMIT 200 + 수렴 플래그 |
| 15 | 하트비트 리더 `cron-heartbeat.ts:312,319` | `LIKE 'cron_hb:%'` 가 BINARY PK 에서 전수(case_sensitive_like OFF) — 144회/일(uptime 프로브) | 15~50만 | `key >= 'cron_hb:' AND key < 'cron_hb;'` |
| 16 | `retry-alimtalk.ts:28-33` | COUNT(*) 전수(alimtalk_failures 인덱스 0) | 288×rows | `SELECT 1 LIMIT 1` + 인덱스 |
| 17 | 드레이너 2종 `bulk-email-drain.ts:66`, `outreach-email.ts:169-176` | 빈 큐에 COUNT/ORDER BY 288회 | 작음 | `SELECT 1 LIMIT 1` 존재 프로브 |

인덱스 묶음: `user_notifications/dashboard_notifications/agency_notifications/chat_messages/csp_violations(created_at)`,
`refresh_tokens(expires_at)`, `rate_limit_attempts(window_start)`, `idempotency_keys(expires_at)`,
`products(group_buy_status,is_active,created_at DESC)`, `products(group_buy_status,group_buy_deadline)`,
`seller_prospects(status)`, `order_items(consignment_id)`, `alimtalk_failures(resolved,next_retry_at)`.

### 2-2. 요청 경로 (Pages)
| # | 위치 | 왜 | 처방 |
|---|---|---|---|
| 1 | 봇 OG 블록 `worker/index.ts:2354,2398-2479` | 크롤러 1회 = 미캐시 메타 쿼리 **+** SSR 자체호출(이중). `sellers WHERE slug=? OR username=?` 전수 | SSR head rewriter 가 이미 같은 메타를 만든다 → 블록 제거([UNLOCK_LOADING]) |
| 2 | `/api/admin/metrics` `admin-metrics.routes.ts:35` | `AdminHealthPage.tsx:66` 이 **10초** 폴링 × COUNT 6개(webhook_events·user_sessions 인덱스 없음) | 60초 + cacheGet 10s + 인덱스 |
| 3 | `/api/products` 목록 `ProductRepository.ts:99,121` | 상관 `NOT EXISTS sellers` 행당 실행 + `(deal_only, price)` 인덱스 없음 | LEFT JOIN + 복합 인덱스 |
| 4 | `/sitemap.xml` `sitemap.routes.ts:27` | 캐시 미들웨어 0 + `computeRegionStats` 무제한 | `publicCache(3600)` |
| 5 | `/api/group-buy/products` `group-buy-public.routes.ts:229,340-397` | demo CASE 정렬 전수 + fcfs/onnuri enrich 가 `cacheGet` **밖** | enrich 를 클로저 안으로 |
| 6 | `/api/curator/:handle` `edge-cache.ts:105` | `Cookie.includes('session')` 부분일치 → 로그인 방문자 전원 우회 | 소유자만 우회 |
| 7 | `/api/home/bundle` `public-utility.routes.ts:276,283` | `view_count` 인덱스 없음 → 전수 정렬, 5분 예열 | 부분 인덱스 |
| 8 | `/api/flash-deals` `:153` | **요청마다 `PRAGMA table_info`** + 캐시 0 | WeakMap 메모 + publicCache(300) |
| 9 | `/api/sections` `section-rules.ts:101` | 섹션당 전수+정렬 | ROW_NUMBER 1쿼리 |
| 10 | `/api/home/categories` `:418,433,447` | products 전수 집계 3개 × 288 | materialized |
| 11 | `/api/vouchers/categories` `:346` | deal_only 2,260행 JOIN GROUP | 인덱스 + KV 1h |
| 12 | 검색 자동완성 `products.routes.ts:236,271` | `LIKE '%q%'` 키 입력마다 전수 + rateLimit D1 쓰기/읽기 | FTS prefix 또는 3자 이상 |
| 13 | DETAIL `publicCache(30)` `index.ts:1502` + 404 폴백 3연쇄 `group-buy-public.routes.ts:544,556` | 30초 TTL + 404 마다 3쿼리 | TTL 300 + no-such-column 만 폴백 |
| 14 | `/api/mall/:slug` | 캐시 미들웨어 0 (MALL 슬롯 catch-all) | publicCache(300) |
| 16 | `/api/admin/dashboard/stats` `admin-stats.routes.ts:141-147` | `DATE(created_at,'+9 hours')=?`·`UPPER(status)` 비-sargable → orders 전수 ×3 | 반개구간 |

검증됨(문제 아님): 전역 rateLimiter 는 in-memory · botProtection UA 만 · 익명 요청의 auth 는 D1 0행.

## 3. 정적 감사 — 유어애즈 (09-01 #1284·#1287 이후 남은 것)
| # | 위치 | 왜 | 추정/일 | 처방 |
|---|---|---|---|---|
| 1 | `influencer-keyword-yield.ts:88-96` | `GROUP BY source_keyword` 전수, reclassify 슬롯 92회/일 무조건 호출(`influencer-maintenance.ts:452`) | 1,410만 | **일 1회** 또는 `changed>0` 일 때만 |
| 2 | `influencer-maintenance.ts:69,79,100,119` mergeDuplicatePool 집계 4개 | GROUP BY 전수 ×4, `:100` LIMIT 없음 | 1,410만 | `:100` 부분 인덱스+LIMIT, `:69` `LOWER(email)` 선택 |
| 3 | `:126-128` 이름쌍 조회 | 표현식 인덱스 없음, 그룹당 전수 | 350만 | `(account_id, LOWER(TRIM(name)), LOWER(COALESCE(category,'')))` |
| 4 | `sheets-sync.ts:246-249` | OFFSET 페이지네이션 꼬리 29만 | 410만 | keyset(`id > ?`) + COUNT 커서 캐시 |
| 5 | `influencer-maintenance.ts:577-597` cleanSelfLinkNoise | LIKE 후필터 루프 + 커서 리셋 | 350만 | 부분 인덱스 + `rows<PAGE` break |
| 6 | `store-info-collect.ts:139` | `source` 인덱스 없음 + id DESC 정렬 | ≤390만 | ORDER BY 제거 |
| 7 | `influencer-cafe-members.ts:132` | `COALESCE(subscriber_count,0)=0` 0건 모양 | 0.2~1,060만 | 부분 인덱스 |
| 8 | `consented-reminder.ts:49`, `inbound-onboarding.ts:74` | source/consented_at/contacted_at 인덱스 없음, 0건에 전수 | 180만 | 부분 인덱스 |
| 9 | `prospect-enrich.ts:134,161,202` | CASE 정렬 + COUNT 게이지 | 120만 | REMAINING_TTL 재사용 |
| 10 | `commerce-notify-collect.ts:356` | 자가치유 UPDATE 5.5만 | 70만 | (보류) |
| 11 | `inflow-watchdog.ts:152` | `ad_influencer_leads(collected_at)` 인덱스 없음(company 는 08-31 추가) | 50만 | 인덱스 1줄 |

🔴 **라이브 결함(스캔 아님)**: `inflow-watchdog.ts:300-303` 이 한 문장에 `ad_influencer_leads` + `ad_company_leads` 를
섞는다 → 라우터(`leads-db.ts:135`)가 company DB 로 보내 테이블 없음 → `.catch(()=>null)` → **`sendable_*` 축이 조용히
빠진다**(대표의 유일한 성공 지표가 미감시). 그리고 `:212` 일자 스탬프 쓰기가 실패하면 `maybeAlertInflow` 가 276회/일
→ 1.45억/일.

## 4. 계획 (PR 분리 — 서비스 분리 룰)
- PR-A `claude/d1-read-meter`: 계량기(유어딜 cron + 유어애즈 레인) + 유어애즈 일시정지 스위치.
- PR-B 유어딜 cron 다이어트(§2-1 #1~#3 + 인덱스 + 소품).
- PR-C 유어딜 요청경로(§2-2 — `worker/index.ts` 건드리는 건 `[UNLOCK_LOADING]` 로그).
- PR-D 유어애즈(§3 #1~#5 + 라우터 결함).
