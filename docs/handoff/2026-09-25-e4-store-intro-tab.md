# 2026-09-25 — #1543 라이브 판정(E4)과 거기서 나온 결함 하나

## 이 세션이 한 일

대표 문서 «유어딜_추가제안» 여섯 항목을 #1543 으로 머지(`840c01cd8`)하고 **라이브에서 직접 쟀다.**

| 항목 | 라이브 실측(2026-09-25, iPhone UA / 390px) |
|---|---|
| ① `/pass/2645` 가게 소개란 | ✅ "두툼한 삼겹살 500g…" 이 **상품 구성** 아래, **이용 안내보다 앞**(대표 지적 해소) |
| ② `/blog` 가독성 | ✅ 이모지 **0**, 글 카드 8장 렌더 |
| ③ `/introduce` 지어낸 수치 | ✅ `240만`·`4.8 평점` 0건, 새 밴드 `100% / 0원 / 3초` 렌더 |
| ④ `/stays` 큐레이션 | ✅ 3줄(10만원 미만 13 · 강원 27 · 도심 13) |
| ⑤ `/soon` | ✅ 200 · 카드 8장 · API `total 8` · 전건 `fcfs` 배지 · 거짓 알림 문구 0 |
| ⑥ 비슷한 스테이 | ✅ `/stays/2887` 하단에 같은 지역 숙소 렌더 |

## 🔴 판정에서 나온 결함 (이 커밋이 고친 것)

**'가게 소개' 탭은 무조건 뜨는데 섹션은 내용이 없으면 사라진다.**
설명·가게소개·사장님 말이 전부 빈 상품에서는 **눌러도 아무 데도 안 가는 탭**이 남는다.
에러가 없고 대부분 상품엔 설명이 있어서 화면으로도 잘 안 보인다 — 이 레포가 반복해 당한 *조용한 부재*.

- 실측(라이브 D1): 활성 이용권 **2,620건 중 23건**이 설명이 비었거나 제목과 같다.
- 탭은 PC 전용(`hidden lg:flex`)이라 영향은 PC 상세에 한정.
- 수리: 판정을 `hasStoreIntro()` **한 함수**로 모으고 탭·섹션이 같은 것을 본다(이웃 탭들과 같은 모양).
- 가드: `store-intro-tab-parity-2026-09-25.test.ts` 5건 + 주입 3건 되돌려-검증 빨간불 확인.

## 🩸 이번에 내가 틀린 것 (다음 세션이 같은 자리를 안 밟도록)

1. **페이지를 잘못 짚고 "확인됐다"고 할 뻔했다.** ③ 을 `/about` 에서 쟀는데, 내가 고친 건
   `/introduce`(`IntroducePage`)이고 `/about` 은 `AboutServicePage` 다. 지운 문자열도 0,
   **새 문자열도 0** 이라 완전히 헛돈 측정이었다. ⇒ "없어야 할 것 0" 만 보지 말고
   **"있어야 할 것이 있는가"를 같은 측정에 넣을 것.** 그게 없으면 안 뜬 페이지가 합격으로 보인다.
2. **덜 렌더된 화면을 재고 0을 보고할 뻔했다.** 상세·랜딩의 아래폴드는 `DeferUntilVisible` 이라
   스크롤하지 않으면 `innerText` 에 안 들어온다(본문 492자에서 멈춘다). ⇒ 라이브 판정 하네스는
   **스크롤 + 본문 길이 하한**을 반드시 넣을 것.
3. **주입 ①이 헛돌았다.** "옛 한 줄 모양이 아니다" 로만 봤더니 else 가지에 같은 탭을 넣어
   항상 뜨게 만드는 변형을 통과시켰다. ⇒ **모양을 부정하지 말고 있어야 할 구조를 긍정**하도록 교체
   (탭 1회 + `? [탭] : []` 의 빈 else).

## ④ '별점' 줄이 안 뜨는 건 버그가 아니다

`후기 1건 이상 & 평점 4.5 이상` 인 숙소가 **0곳**(실측 49곳 전부 후기 0)이라, 3곳 미만이면
줄을 안 그리는 설계가 작동한 것. 후기가 쌓이면 자동으로 나타난다. **다시 파지 말 것.**

## 다음 세션의 첫 액션

- 이 커밋이 배포되면 PC 로 `urdeal.kr/pass/<설명 없는 상품>` 을 열어 '가게 소개' 탭이 **안 뜨는지** 확인.
  대상 찾기: `SELECT id FROM products WHERE is_active=1 AND group_buy_status='active'
  AND (TRIM(COALESCE(description,''))='' OR TRIM(COALESCE(description,''))=TRIM(name)) LIMIT 5`

## 대표 판단 대기

- `/introduce` 의 **App Store · Google Play 배지**가 실제로 존재하는 앱을 가리키는지
  (이 컨테이너에서 두 도메인 모두 차단이라 확인 불가). 없는 앱이면 ③ 과 같은 클래스다.
- ⑥ '비슷한 스테이' 를 지금처럼 맨 아래(후기 다음)에 둘지, 문서 그림처럼 **숙소 소개 바로 아래**로 올릴지.
