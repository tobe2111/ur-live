# 🔥 탭을 누를 때마다 화면이 덮이던 것 + 이번 세션 전체 판정 (2026-09-15, 5차)

대표: *"머지해주고, 판정까지 해줘 모든 작업들 한 것들. 특히 로딩은 더 문제가 있을 것 같은데"*
→ **대표가 맞았다.** 더 있었고, 이번엔 세션을 넣고 재서 찾았다.

## 다음 세션의 첫 액션

```bash
# 배포 뒤: 탭을 두 번 눌러 로더가 두 번째에도 뜨는지 본다(판정은 로더 프레임 수)
node /tmp/.../revisit.mjs   # 또는 같은 방식으로: /vouchers 진입 → 마이 → 교환권 ×3
```
**판정**: 교환권 2·3회차 로더 프레임 **0**. 마이 1회차(콜드)는 로더가 떠야 정상이다.

## 찾은 것 — 콜드용 처방이 웜 재방문에 걸려 있었다

하단바 '교환권' 을 누를 때마다 **1·2·3회차 전부** 풀스크린 로더가 떴다(마이 탭은 2회차부터 안 뜸).
각자 옳은 결정 둘이 겹친 자리다:

| 결정 | 근거 | 적용 범위 |
|---|---|---|
| 풀스크린 로더 | 정적 청크 로더와 이어져 '한 번'으로 보인다(2026-07-01) | **콜드에서만** 맞다 |
| PUSH 는 복원 안 함 | 탭을 새로 누른 사람은 맨 위를 기대한다(2026-09-13) | 스크롤 얘기였다 |

재방문엔 청크 로더가 **아예 없으므로** 첫째 근거가 성립하지 않는데도 화면을 덮었다.
⇒ `vouchers/warm-seed.ts` 가 PUSH 재진입에 **1페이지분만** 시드한다. 데이터는 되돌리고 스크롤은 안 되돌린다.

## 이번 세션 전체 판정

| PR | 무엇 | 판정 |
|---|---|---|
| #1451 | 로딩이 "없어요"라고 먼저 말함 | ✅ **E4** — 라이브 `/map` 프레임 147장에 `"0곳"` 0회 |
| #1453 | 통신 오류를 "없음"으로 위장 | ✅ 실 401 에서 **"이용권을 불러오지 못했어요 / 다시 시도"** 확인 |
| #1454 | 안 쓸 푸시 키 조회 | ✅ 권한 default 에서 요청 **0건** |
| #1455 | 검사기 0건 매칭 구멍 | 🔧 CI 빨간불 → 수리 후 재검(아래) |
| 이 PR | 탭 재진입 로더 | ✅ 1·2·3회차 로더 프레임 **0** (이전 2·2·2) |
| 장바구니 | 게이트 ① ON · ② OFF | ⏸️ **S-CART 실결제 대기 — 대표 몫** |

## 이번에 틀렸던 판단 — 이게 제일 값지다

1. 🩸 **러너를 `import` 했다.** CLAUDE.md 가 *"never `import` the runner"* 라고 금지한 바로 그것을
   진단하려다 했고, import 는 러너를 **통째로 실행**시켜 내 다른 실행과 **동시에** 파일을 주입했다.
   그래서 `--map-only` 가 실행마다 **다른 파일**을 "낡은 지도" 로 보고했다(전부 허상이었다 —
   죽이고 다시 재니 **1,326건 전부 성함**). 더 나쁜 것: 멈춘 뒤 트리에 **Toss 잠금 파일**
   (`TossWidgetPayPage.tsx`)의 주입이 **남아 있었다**(위젯에서 `light-island` 제거 = 다크에서 결제
   입력 글자가 안 보이게 되는 실제 결함). `git add -A` 를 안 쓰고 경로를 명시해 staging 하는 규칙과
   커밋 전 `git status` 확인이 이걸 막았다. ⇒ **진단이 급해도 러너는 CLI 로만 부른다.**
2. 🩸 **"푸터가 화면을 덮는다" 고 보고할 뻔했다.** 텍스트 프레임 캡처는 **스켈레톤(회색 상자)을 못 본다** —
   `/my-orders` 가 "로더 1프레임" 으로 보인 건 스켈레톤이 텍스트가 아니어서였다. **스크린샷을 찍자
   멀쩡했다.** ⇒ 로딩 판정에 텍스트만 쓰지 말 것.
3. 🩸 **PR #1453 의 "중복 요청 제거" 는 과장이었다.** 실측: **3건 → 2건**이지 →1 이 아니다.
   내가 없앤 건 190ms 간격의 그 중복이 맞지만, 남은 2건은 원인이 다르다(늦게 마운트되는 두 번째
   컴포넌트 + `refetchOnMount: 'always'`). 이전판을 실제로 빌드해 재 보고서야 알았다.
4. 🩸 **종료 코드를 파이프 뒤에서 읽었다**(`node … | tail; echo $?` = `tail` 의 코드). 늘 0 이었다.

## 남은 것

- **위시 요청 2건**: `refetchOnMount: 'always'` + 늦은 두 번째 마운트. 줄이려면 그 옵션을 건드려야 하고
  그건 2026-05-27 사고(발급 후 빈 화면)의 방어라 **함부로 만지면 안 된다.** 별건으로 판단할 것.
- S-CART 실결제 15항목 대기(게이트 ① ON · ② OFF) — C 등급 결재.
