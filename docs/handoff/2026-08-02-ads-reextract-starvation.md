## 대표 지시 두 건이 "머지됐는데 실행된 적이 없었다" — 재추출 단계의 굶주림

> 대표 지시: *"카페 회원수는 반영이 안되고 있음(카운팅이 안됨)"* · *"정보 최대 수집"* ·
> *"응 계속해서 완료까지 해줘"*

### ❗ 이번에 배운 것 (먼저 읽어라)

**코드가 머지됐다 ≠ 그 코드가 돈 적 있다.** 07-29·08-01 에 지역 백필과 카페 회원수를
`reextract` 정비 단계에 배선하고 "완료"로 표시했는데, 라이브에서 **한 번도 실행된 적이 없었다.**

판정법(새 배포 없이): 어드민 `/api/admin/tools/settings` 의 `ads_maintenance_last` 를 본다.
`region`·`cafemembers` **키 자체가 없으면** 그 코드는 도달한 적이 없는 것이다.
(하트비트는 `err=Error` 만 말할 뿐 *어디까지 갔는지*는 말하지 않는다.)

### 🔎 원인 — 커서 저장 한 줄

```ts
.bind(CURSOR_KEY, String(done ? 0 : cursor))   // 다 훑으면 0 으로 되돌림
```
전수를 끝내면 커서가 0 이 되어 **매 회차가 36,880행을 처음부터 다시** 훑었다
(라이브 `scanned: 36,880 · filled: 0` — 저장 시점에 이미 추출하므로 재수확이 구조적으로 0).
그 CPU 로 인보케이션이 끝났고, **같은 단계에서 뒤에 서 있던** 지역·카페는 시작조차 못 했다.

`poolScanShouldStop` 은 이 함수 안에서만 멈출 뿐, **이미 쓴 CPU 를 되돌려주지 않는다** —
"상한이 있으니 괜찮다"는 잘못된 안심이었다.

### ✅ 수리 셋 (하나만으론 부족했다)

| # | 수리 | 왜 이것만으론 부족한가 |
|---|---|---|
| ① | 다 훑으면 커서를 **그 자리에 주차**(전수 재스캔은 `REEXTRACT_RULES_VERSION` bump 로만) | 순서가 그대로면 여전히 앞에서 예산을 다 쓴다 |
| ② | 지역·카페를 재추출 **앞**으로 | 지역 스윕이 `budget.left` 바닥까지 도는 구조라 **뒤가 또 굶는다** |
| ③ | `sweepRegions(…, reserve)` 로 카페 몫 예약 | — (예약은 **하한이지 상한이 아니다**: 지역이 끝나면 남은 예산이 통째로 카페로) |

②를 하고 나서야 ③의 필요를 발견했다. **"순서를 바꿨다"로 끝냈으면 병이 자리만 옮겼을 것이다.**

💱 지역을 앞에 둔 근거는 취향이 아니라 환산이다 — **ops 당 산출 83배**
(지역 500행/6 ops vs 카페 1행/1 op).

### 📅 배정표 재배분 — 주석이 예고한 그대로

`MAINT_SCHEDULE` 주석은 *"handle 이 done:true 로 끝나면 그 슬롯을 남는 일 쪽으로 옮겨야 한다"*
고 적어 뒀다. 라이브에서 그 조건이 성립했다: `handle → { scanned: 34, fixed: 0, unfixable: 34, done: true }`.
⇒ `handle` 3 → 1 · `reextract` 1 → 3.

🕘 **자리가 결과를 바꾼다**: 인덱스 7 은 hour 19(야간 재보정 양보)에 걸려 **하루 1회**다.
1슬롯 단계를 거기 두면 간격이 조용히 24h 로 벌어져 경보 창(12h)을 깬다 —
`maxScheduleGapHours(MAINT_SCHEDULE)` 를 양보 없이 재면 12 가 나와 **통과해 버린다.**
첫 판이 실제로 `handle` 을 인덱스 7 에 둘 뻔했고, 그래서 별도 검사를 신설했다.

### 🧪 유닛이 *정당한 변경*을 막은 사례 — 사실을 테스트가 소유하면 안 된다

이전 배정표 유닛은 `busy = ['reclassify','handle']` / `idle = ['reextract','merge']` 로
**07-29 그날의 라이브 사실을 박아** 뒀다. 그 사실이 뒤집히자 유닛이 빨간불을 냈다.
⇒ 사실은 코드 옆(`MAINT_SLOT_INTENT.why`)에 두고, 테스트는 **선언표와 배정표의 일치**만 본다.
배정표를 고치면 근거 한 줄을 같이 고치게 되어 **둘이 같이 늙는다.**

### 🔀 `check-rules-version-bump` 에 `watch` 신설

`REEXTRACT_RULES_VERSION` 은 상수와 규칙이 **다른 파일**이다(규칙이
`influencer-discovery`·`-email-rules`·`-parse` 세 곳에 흩어져 어느 하나에 둘 수 없다).
`watch` 로 그 간극을 잇고 양방향 실측했다(watched 변경 → 발화 / 상수 파일만 변경 → 침묵).
⚠️ **상수를 신설하는 PR 자체는 검사되지 않는다**(merge-base 에 상수가 없어 건너뜀) — 다음 브랜치부터다.

### ▶️ 다음 세션 첫 액션 — 판정

1. **`ads_maintenance_last` 에 `region`·`cafemembers` 키가 생겼는가.**
   `reextract` 슬롯은 UTC hour **1·7·10·13·22**(=KST 10·16·19·22·07). 배포 후 첫 슬롯에 확인.
   생겼으면 이 수리가 성공. 안 생겼으면 원인이 **커서가 아니라 다른 것**이므로 재진단할 것
   (그때는 `ads_dispatch_last.deferred` 에 `maintenance` 가 있는지부터 — 미디스패치와 사망은 다르다).
2. **`stats.region_pending` 이 줄고 있는가** — 36,269 에서 회차당 ~1,500 감소 기대(5회차/일 → 약 5일).
3. **`stats.naver_cafe` 대비 회원수 채움** — 3,142행. 지역이 끝나기 전엔 회차당 ~20, 끝난 뒤 ~40.

### 📊 동시 진행 — 블로거 측정 백로그 수동 드레인

`total_measured` **1,264 → 8,110+**, `naver.failed` 내내 **0**.
미측정 블로거 **28,083 → 23,001**. 회차당 22명 · 5~7초. 관측 속도 ~93/분.

⚠️ 수동 드레인 중 `enrich-run` 이 **502 를 절반쯤 돌려준다**. 토큰 문제가 아니다 —
직전 호출이 남긴 `waitUntil` 체인이 아직 도는 동안 다음 호출이 겹쳐서다. 작업은 그 체인이 계속하므로
**처리량 손실이 아니다**(측정 누적은 계속 오른다). 재로그인으로 고치려 들지 말 것.

### 🩸 같은 세션에서 낸 회귀 하나 — 수동 버튼이 0건을 처리하게 됐다

#930 에서 버튼을 드라이버(팬아웃)로 돌렸더니 버튼이 **더 많이가 아니라 아무것도** 안 하게 됐다.

```
킥 → {"success":true,"fanout":4}   (즉시 반환)
30초 후 nb_unmeasured 변화 = 0      ← 직전 단발 경로는 한 번에 22 감소
```

원인은 **이 레포가 이미 적어 둔 규칙**이다(`dispatchRoundChain` docblock, 07-29):
*"서비스 바인딩 피호출자는 호출자보다 오래 살 수 없다."*
cron 은 부모 scheduled 가 다른 레인을 kick 하느라 살아 있지만, **어드민 버튼은 응답을 받는 순간
메인 워커 요청이 끝나** ur-ads 의 `waitUntil` 자식이 통째로 취소된다.

> 🔑 **교훈**: 같은 코드가 **호출자에 따라 다르게 죽는다.** cron 에서 depth 3 을 확인했다고
> 버튼도 된다고 볼 수 없었다 — 나는 그렇게 봤고, 그래서 하루치 수동 드레인을 날릴 뻔했다.

⇒ 수동 경로는 `sync=1` 로 자식 K개를 **`await`** 한다(버튼 1회 = K라운드). 자식에게도 `sync` 를
물려 어차피 취소될 릴레이를 안 낳게 했다. cron 경로는 동작 불변.

### ❓ 미해결 관측 두 개 (추측으로 손대지 말 것)

1. **`?single=1`(단발 경로)이 배포(17:49 UTC) 이후 502 를 낸다** — 2.2초에 일정하게, 본문은
   Cloudflare 의 plain `error code: 502`(우리 핸들러의 JSON 502 가 아니다). 같은 워커의 `stats` 는
   200/0.9s 로 멀쩡하고 **cron 경로는 살아 있다**(18:01 틱 `ms=8390`). 원인 미상.
   ⇒ 판정 전에 원인을 단정하지 말 것. 후보: ur-ads CPU 한도(#927 계열) · 배포 직후 isolate 상태.
2. **18:01 UTC 틱이 `depth=0` 으로 끝났다** — 01:01 틱은 `depth=3` 이었다. 조각이 릴레이를 못 잇는다.
   틱마다 흔들리는지, 배포 후 고정인지 **최소 두 틱을 더 보고** 판단할 것.

### 🟠 안 한 것 (근거만)

- **`enrich-company` 드라이버는 릴레이 그대로.** 같은 처방(슬라이스 팬아웃)이 먹히지만 그 큐의
  SELECT 가 선점인지 확인 안 했다 — 확인 없이 병렬로 돌리면 중복 크롤이 된다.
- **팬아웃 K 상향(4 → 6/8) 보류.** `naver.failed` 는 0 이라 여지는 있으나, 차단은 조용히 오고
  `measured` 만 보면 안 보인다. 올린 뒤 `failed` 를 먼저 볼 것.

### 검증
tsc 0 · 전체 **3,933 pass** · audit-gate **77 GREEN** · 되돌려-검증(신규 가드 3종 + rules-version `watch`)

---

## ⏰ DO 알람 레인 시범 (대표 승인 "한 레인 시범 후 확장")

### 왜 — 오늘 하루가 증명한 천장

```
KST 16:00  디스패치 8 → 완주 2 · 사망 4 (ms 3,880~4,152)   ← 값이 같다 = 같은 순간에 끌려감
KST 17:00  디스패치 6 → 완주 3 · 사망 3 (ms 3,649~3,701)   ← per_tick 을 줄여도 사망률 50% 그대로
```

**피호출자의 CPU 는 호출자 몫에서 나간다.** 그래서 부모 CPU = 동시 레인 수 × 각자의 시간이고,
per_tick 을 8→6 으로 줄여도 완주가 2→3 으로만 늘었다.

🔑 **레인을 싸게 만드는 것으론 못 고친다**가 이 관측의 핵심이다. 사망 목록의 `sheets-sync` 는
**같은 날 커서로 잘라 12,000행만 보게 해 둔 레인인데도 죽었다** — 자기 일에 지친 게 아니라
**부모가 죽으며 끌려간** 것이다. ⇒ 레인 슬라이싱(㉠)으로는 천장이 안 사라진다.

DO 알람은 **호출부가 없다.** 런타임이 예약 시각에 독립 인보케이션으로 깨우므로 자기 CPU 예산(30초)과
자기 서브리퀘스트 예산을 받고, 알람 안에서 다음 알람을 걸어 스스로 이어진다(정각을 안 기다린다).

```
cron 경로 : 시간당 30초 CPU 를 전 레인이 나눠 씀  → 이 레인 몫 ≈ 1회/시간
알람 경로 : 회차마다 자기 30초                    → 이 레인 12회/시간(기본 5분)
```

### 무료 한도를 모르는 채로 켜므로, 코드가 스스로를 제한한다

무료 플랜 DO 알람의 정확한 한도를 이 환경에서 확인할 수 없었다(대시보드·요금 페이지 프록시 차단).
**추측으로 "안전하다"고 말하지 않고** 상한을 코드에 박았다 — 시간당 실행 상한 12회 · 간격 하한 60초
(env 오타로 0 이 들어와도 폭주 없음) · 실패 시 지수 백오프(상한 8배, 죽은 체인도 재시도 폭풍도 아님).

### 다음 세션의 첫 액션 — 판정은 스탬프로만 된다

```bash
# 알람이 실제로 이어지는가 + 무료 한도에 닿는가
curl -sS "https://live.ur-team.com/api/admin/tools/settings" -H "Authorization: Bearer $TOK" -H "User-Agent: $UA" \
  | python3 -c "import sys,json;print(json.load(sys.stdin)['data'].get('ads_lane_alarm_last'))"
# 부트스트랩이 섰는가(매 정각) — 하트비트 이름 ads:lane-alarm-boot
curl -sS "https://live.ur-team.com/api/admin/cron-heartbeats" -H "Authorization: Bearer $TOK" -H "User-Agent: $UA"
```

읽는 법 — **`runs_this_hour` 가 매 회차 오르고 `next_at` 이 5분 뒤**면 체인이 살아 있다.
`fail_streak` 가 계속 오르면 무료 한도(또는 다른 벽)에 닿은 것이고, 그때 `error` 원문이 근거다.
⏱ 알람 배포 후 **30분**이면 6회차가 쌓여 판정된다(정각을 기다릴 필요 없다).

- **살아 있으면** → 나머지 14개 레인으로 확장(같은 DO 패턴, 레인당 인스턴스 1개).
- **한도에 닿으면** → `ADS_LANE_ALARM_ENABLED='false'` 로 되돌린다(코드 변경 0 — 알람이 다음 회차를
  안 걸고 체인이 멎으며 cron 게이트가 풀려 기존 경로로 복귀).

### ⚠️ 이번에 틀렸던 판단 (같은 오진 반복 방지)

1. **DO 를 엔트리에서 re-export 하자 무관한 테스트가 죽었다.** `cloudflare:workers` 는 번들러가 못 푸는
   런타임 내장 모듈이라, 그걸 import 하는 파일을 **거쳐서 도달하는** 테스트까지 통째로 로드 실패한다
   (`ads-lane-cadence` 가 엔트리 상수 하나를 읽다가 같이 죽었다). 실패가 DO 와 무관한 곳에서 터져
   원인이 안 보인다. ⇒ `vitest.config.ts` 에 스텁 alias 를 깔았다(`src/tests/stubs/cloudflare-workers.ts`).
   덤으로 이 레포의 기존 DO 3개도 이제 테스트에 걸 수 있다.
2. **안전장치를 DO 안에 두면 테스트를 못 붙인다.** 무료 한도가 미확인인 알람을 폭주 방지 상한 검증 없이
   켜는 건 받아들일 수 없어, 정책을 순수함수(`lane-alarm-policy.ts`)로 갈랐다. DO 본체는 실행만 한다.
   ⚠️ **그래서 DO 가 정책을 자기 안에서 다시 정의하면 상한 테스트가 장식이 된다** — 그 갈라짐을 막는
   검사를 같이 넣었다(이 레포의 '낡은 지도' 클래스).
3. **부트스트랩을 생 `waitUntil` 로 두려다 가드에 걸렸고, 가드가 옳았다.** 부트스트랩이 조용히 실패하면
   알람이 안 서고 cron 킥은 게이트로 꺼져 있어 **레인이 통째로 사라진다** — 예외를 다는 대신
   하트비트(`ads:lane-alarm-boot`)를 성공·실패 양쪽에 배선했다.

### ⚠️ 이 테스트들이 못 보는 것

**실제 무료 한도**와 **DO 런타임 계약**(스토리지·알람 예약이 진짜 도는지). 스텁은 "해석되게" 할 뿐
동작을 흉내내지 않는다. 붙였다고 DO 본체가 검증됐다고 여기지 말 것 — 판정은 위 스탬프뿐이다.

### 검증
tsc 0 · 전체 **4,191 pass** · audit-gate **81 GREEN** · 되돌려-검증 5건(상한 제거 · cron 게이트 제거 ·
DO 자체 정의 · 게이트 이중판단 · 부트 하트비트 제거 — 전부 빨강 확인 후 복원)

---

## ✅ DO 알람 판정 (KST 19:01) — 부모가 살았다

```
KST 18:01 (알람 배포 전, per_tick 6)  레인 6개 중 사망 3  💀 collect-company/enrich-company ms=4467 동일
KST 19:01 (알람 배포 후, per_tick 6)  레인 10개 중 사망 0  ✅
④ DO 알람  runs=1/12  ms=6048  fail_streak=0  next=19:06   부트스트랩 ok=true started=true
   레인결과 naver tried 21 · measured 21 · contacts 4 · emails 4 · failed 0
① 시트 미러 ok=True rows=12,000  커서={off:12000,total:43547}   ← 41시간 만의 첫 성공
```

🔑 **`ms=6048` 이 증거다.** 부모 밑에선 4.4초에 죽던 일이 알람에선 6초를 쓰고도 완주했다 —
피호출자가 아니라 **독립 인보케이션**이라 자기 CPU 예산을 받는다. 레인 하나를 부모에서 들어낸 것만으로
나머지 10개가 전부 살았다.

⇒ **`FREE_LANES_PER_TICK` 은 6 유지**(4 로 내리지 않는다). 대표 확정 비율 3/3/1/1 보존.
⚠️ 이전 인계에 적힌 *"여전히 죽으면 4로 내려라"* 는 **알람이 없던 시점의 지시**다. 지금 내리면
비율이 1/1/1/1 로 눌리는 데다 같은 회차에 두 가지를 바꿔 **알람의 효과를 측정할 수 없게 된다.**

## 🔤 카페 회원수 — 원인 확정: EUC-KR (18/18 실패)

#957 의 표본이 답을 줬다:
```
status 200 · len 200,000 · peek '::�ǻ�::�Ǽ���������ϴ»���� / No.1 ���Ͼ�Ʈ...'   (3건 전부 동일)
```
차단(ⓐ)도 프레임셋(ⓑ)도 아니다. **카페 홈이 EUC-KR 인데 `res.text()` 가 UTF-8 로 디코딩**해
한글이 전부 U+FFFD 가 됐다 — `멤버|회원` 정규식은 매치될 수가 없었다.

수리: `arrayBuffer()` 로 받고 ① UTF-8(게이트 JSON·UTF-8 페이지) → ② `TextDecoder('euc-kr')`(런타임이
지원하면) → ③ **EUC-KR 바이트 직접 탐색**(라벨 바이트를 찾고 뒤의 ASCII 숫자를 읽음 — 디코더 지원
여부에 안 걸린다). 어느 경로가 먹었는지는 `via`(`home:euckr` / `home:bytes`)로 라이브에서 보인다.

## 🩸 같은 세션에서 낸 회귀 — #957 이 지역·재추출을 굶겼다

```
KST 19:00 reextract 슬롯:  cafemembers tried 18 · failed 18
                           region {filled: 0, chunks: 0}     ← 0
                           reextract {scanned: 0, filled: 0} ← 0   커서 13398 불변
```
카페를 맨 앞으로 옮겨 굶주림(3/20)은 풀었는데, **18번이 전부 실패하면서 예산을 다 태워** 뒤 단계를
0 으로 굶겼다. 순서를 고치면서 *"안 되면 접는다"* 를 안 넣은 것이 원인이다.
⇒ `CAFE_ABORT_AFTER_FAILS = 4` — 한 건도 못 채운 채 4연속 실패면 회차를 접고 예산을 뒤에 넘긴다
(카페는 채운 행이 제외되므로 다음 회차가 이어 잡는다 — 잃는 것이 없다).

> 🔑 **교훈**: 굶주림을 순서로 고칠 때는 **앞 작업의 실패 상한**을 같이 넣어야 한다. 안 그러면
> "앞으로 옮긴 것"이 그대로 새 굶주림 원인이 된다 — 고친 자리에서 같은 병이 재발했다.

## 다음 세션의 첫 액션

```bash
bash /tmp/.../scratchpad/verdict.sh    # ⓪ 사망 수 → ④ 알람 → ①②③ 순서로 읽는다
```
1. **알람 확장 판단** — `runs_this_hour` 가 12/12 까지 오르고 `fail_streak` 0 이 유지되면 무료 한도에
   안 닿는다는 뜻 → 나머지 14개 레인으로 확장. `fail_streak` 가 오르면 `error` 원문이 근거다.
2. **카페** — `via` 가 `:euckr` 인지 `:bytes` 인지, `filled > 0` 인지. 여전히 0 이면 새 표본은 ASCII 창이라
   마크업이 보인다(숫자를 태그가 쪼갰는지 판정 가능).
3. **지역** — `region_pending` 이 32,761 에서 줄기 시작하는지(조기 중단이 예산을 넘겼는지의 증거).

### 검증
tsc 0 · 전체 **4,200 pass** · audit-gate **81 GREEN** · 되돌려-검증 2건(조기 중단 제거 · 바이트 파서 무력화)
