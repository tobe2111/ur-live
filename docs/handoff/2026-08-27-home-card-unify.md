# 2026-08-27 — 홈 카드 룩 통일 · 폰에서 죽어 있던 더보기 · 사진 스와이프

대표 지시 4건: ① 인기 이용권 더보기가 클릭이 안 됨 ② 첫 번째 형태의 카드로 통일
③ 이미지 썸네일 좌우 스와이프 ④ 글자색 검정.

## 완료분

| | commit / PR |
|---|---|
| 카드 사진 과대 해상도(모바일·태블릿 2~3배) | `8f806a2` |
| 카드 룩 통일 · 모바일 더보기 배선 · 스와이프 | `316ca71` |
| 가드 주입 지점 앵커 + 스와이프 클릭취소 가드 | `e82e000` |
| **머지** | `6ba98cd` (PR #1221) |
| 더보기 쿼리 유실 근본수리 | 아래 §참조 |
## 🐛 배포 후 실측에서 드러난 것 — "더보기"의 진짜 원인 (2026-08-27, PR #1221 머지 뒤)

`6ba98cd` 배포 직후 라이브를 재 보니 **"지금 인기 이용권" 더보기 버튼이 아예 안 보였다.**
`/api/sections` 실측:

```
 2  지금 인기 이용권   more_href='/?sort=popular'   ← 버튼 없음
 4  주말에 떠나는 숙소  more_href='/stays'          ← 정상
```

**원인**: `safeInternalPath()` 는 경로 검증기이면서 **쿼리 제거기**다(2026-05-01, `?error=…` 누적 차단).
`ref`/`aff`/`invite` 만 화이트리스트로 살리고 나머지는 버린다. 그래서 `/?sort=popular` → `/`.

⚠️ **이번에 틀렸던 판단 (제일 값진 부분)**
2026-08-19 에 같은 신고("더보기 눌러도 반응 없음")를 받고 *"정규화는 경로에만 하고 쿼리를 다시 붙인다"*
고 고쳤는데, **`safeInternalPath` 의 결과에서** 쿼리를 찾았다 — 이미 버려진 뒤라 붙일 게 없었다.
**그 수정은 한 번도 동작한 적이 없고, 소스만 고쳐진 것처럼 보였다.** 그 위에 08-27 세션이
"죽은 링크(`/`)는 숨긴다"를 얹자 납작해진 `/` 가 그 규칙에 걸려 **버튼이 통째로 사라졌다.**

⇒ 교훈: **문자열이 소스에 있는지로는 이 클래스를 못 잡는다.** 세 번 다 "고친 코드가 거기 있다"로는
통과했을 것이다. 그래서 순수 함수(`src/components/home/section-more-href.ts`)로 빼고
**실제 입력 → 출력**을 테스트로 고정했다(`src/tests/unit/section-more-href.test.ts` 7건).
08-19 버그를 그대로 재현하면 3건이 빨간불이 된다(확인함).

**수정**: 순서를 뒤집는다 — ① 먼저 `?`/`#` 로 쪼개고 ② 경로만 `safeInternalPath` + `resolveConsumerAlias`
③ 쿼리는 따로 소독(`SAFE_QUERY_RE`)해 다시 붙인다. 외부 URL·`/login`·`/auth/*` 차단은 그대로.
가드 2건 등록(493개, 되돌려-검증 빨간불 확인).

## 📏 배포 후 라이브 실측 (`index-B9EEHSa4` → `index-dUC4W69A`)

| | 이전 | 이후 | |
|---|---|---|---|
| 모바일 390 dpr3 | 11장 / **814KB**, 요청폭 1200 | 11장 / **336KB**, 요청폭 400·600 | **−59%** |
| 태블릿 810 dpr2 | — | 27장 / 610KB, 요청폭 400 | |
| PC 1440 dpr2 | — | 19장 / 1161KB, 요청폭 800 | |
| 최대 1장 (모바일) | 259KB | **77KB** | −70% |

카드 룩 통일 확인(섹션↔피드 동일): 배경 `rgba(0,0,0,0)` · 상품명 `rgb(26,44,66)` ·
머천트명 `rgb(95,107,122)` · 비율 `4/3`.

홈 HTML 은 이미 빠르다 — TTFB 0.69~0.71s(warm), `x-ssr-status: MAIN:kv-hit`, 서버측 15ms.

## 다음 세션의 첫 액션

1. **이 브랜치의 더보기 수정을 머지·배포**한 뒤 라이브에서 판정:
   `curl -sS https://urdeal.kr/api/sections | python3 -c "import sys,json;[print(s['title'],repr(s.get('more_href'))) for s in json.load(sys.stdin)['data']]"`
   → 폰에서 "지금 인기 이용권 더보기"가 **보이고**, 누르면 정렬이 인기순으로 바뀌며 그리드로 스크롤되는지.
2. **스와이프는 실기기에서만 판정 가능** — 이 환경에서 못 잰다. 카드 사진을 좌우로 문질러
   ① 넘어가는지 ② 손 떼며 상세로 튀지 않는지 ③ 사진 위에서 세로 스크롤이 되는지.

## 남은 것 (대표 판단 필요)

- **JS 번들 마운트 3.3초**(느린 회선 + 4× CPU 스로틀 기준). 이번 작업 범위 밖이고,
  배포해도 "첫 화면이 뜨기까지의 렉"은 그대로다. 번들 자체를 줄이는 별도 작업.

---

## 📦 번들 다이어트 — 소비자 임계 경로에서 역할 전용 청크 제거 (2026-08-27, 대표 "계속 작업")

**배경**: 사진은 −59% 됐는데 "첫 화면이 뜨기까지의 렉"은 그대로였다. 라이브 실측:
```
홈 첫 화면 JS  압축 384KB / 해제 1,396KB   (FCP 이전)
그 안에: app-seller-components 65KB · app-wholesale-hooks 9KB · admin/wholesale/marketing 컴포넌트
```
어느 것도 소비자 화면에서 렌더되지 않는다.

**원인은 큰 코드가 아니라 "작은 공용 모듈이 역할 전용 봉투 안에 있었던 것"** — 두 건:

| 모듈 | 실제 성격 | 어디 있었나 | 대가 |
|---|---|---|---|
| `shared/seller-roles.ts` (116줄) | 역할 판별 **SSOT** — CLAUDE.md 가 모든 UI 에 강제 | 규칙 없어 Rollup 이 셀러 컴포넌트와 한 청크 | 공용 `RoleGate.tsx` 하나가 import → 홈에 **65KB** |
| `lib/seller-tracking.ts` (70줄) | **이름만 셀러** — URL 추천코드 캡처, 소비자 4개 페이지가 씀 | 2026-05-28 주석의 전제("셀러만 사용")가 틀린 채 셀러 청크 | `/browse` 에 **65KB** |

**수정(규칙 추가만, 제거 0)**: 둘을 `app-shared` 로 · `components/admin/`→`app-admin-components` ·
`components/wholesale/`+도매 로고/테마→`app-wholesale-components`(신규) · Marketing 2개→`app-marketing`(신규).

**실측 결과**
```
홈 폐쇄   26청크 972KB → 22청크 840KB raw   (−132KB)
app-components  211KB → 166KB
app-seller-components / app-wholesale-hooks / app-misc  → 홈에서 완전 소멸
/browse 의 65KB    → 제거
```

### 🛡️ 가드가 이 자리를 안 보고 있었다 (90번째 불변식 신설)

`check-critical-chunks` 는 **`index.html` 만** 본다. 그런데 2026-07-12 부터 워커가 표면별로
modulepreload 를 **추가 주입**한다(`route-chunk-map`). **홈의 실제 첫 페인트 = index.html ＋ 그 맵**인데
후자는 **어느 가드도 안 봤다** — 그 사각지대가 이 누수가 살던 자리다.

신설 `scripts/check-surface-role-leak.mjs`: 소비자 7표면 preload 에 역할 전용 청크 이름이 뜨면 실패.
검사 대상 0건·맵 파싱 실패·빌드했는데 맵이 빈 것도 전부 실패로 본다(측정 실패는 통과가 아니다).
**되돌려-검증 2건**(seller-roles·seller-tracking 각각 원복 → 빨간불) 확인.

### ⚠️ 이번에 틀렸던 판단

`app-components` 를 더 쪼개려고 정적 import 추적기를 짜서 "소비자가 안 쓰는 모듈 35개" 목록을 뽑았는데,
거기에 `ui/button`·`ui/card`·`ConsumerFrameRails` 처럼 **실제로 쓰이는 것**이 들어 있었다.
추적기가 `export … from` 재수출과 **앱 셸(MobileAppLayout) 경유**를 못 따라갔기 때문이다.
그 목록으로 청크를 쪼갰으면 홈이 쓰는 컴포넌트를 lazy 쪽으로 보내 오히려 느려졌을 것이다.
⇒ **청크 판정은 번들러의 실제 모듈→청크 그래프(소스맵·매니페스트)로만 한다.** 손으로 짠 추적기는 근사치다.

## 다음 세션의 첫 액션

1. 배포 후 라이브에서 재확인:
   `curl -s https://urdeal.kr/ -H 'User-Agent: Mozilla/5.0 (iPhone…)' | grep -o 'app-seller-components' | wc -l` → **0** 이어야 한다.
2. `app-components` 는 아직 **166KB / 58모듈**이다. 더 쪼갤 여지가 있지만
   **소스맵으로 모듈 목록을 뽑고 매니페스트로 청크 간선을 확인한 뒤**에만 손댈 것(위 오판 참조).

---

## 🖼️ 히어로 사진 + 첫 화면 preload (2026-08-27, 대표 "히어로 사진이 엄청 늦게 뜬다 / 메인 로딩도")

### ① 히어로는 "늦게 뜨는" 게 아니라 **없었다**

라이브 실측:
```
/api/banners?type=hero   →  0건            ← 어드민 히어로 배너 미등록
SSR 시드 50건의 slug     →  50/50 데모     ← 라이브 카탈로그가 100% 데모
```
사진 소스가 두 개(어드민 배너 / 시드 첫 실상품)인데 **둘 다 비어** 히어로가 빈 색면이었다.
(그리고 **모바일엔 히어로 자체가 없다** — `HomeHeroDefault` 는 PC 홈 전용. 대표가 본 건 섹션 카드.)

원인: 2026-08-04 워터마크 사고(타사 보도사진)의 처방이 **"데모 전면 금지"** 였는데,
카탈로그가 100% 데모가 되자 그 규칙이 히어로를 **영구 빈 색면**으로 만들었다.

**수정 — 금지의 축을 옮긴다**: "데모냐" → **"사진 출처가 우리냐"**.
사고의 원인은 데모라는 사실이 아니라 **남의 사진**이었다.
⇒ 실상품 우선은 그대로, 실상품이 없으면 **우리 R2(`/api/media/` · `media.ur-team.com`) 데모만**
마지막 수단으로. 외부 호스트 데모는 어느 단계에서도 안 온다(실측: 데모 50장 중 48장이 우리 R2).

### ② 첫 화면 카드 사진 preload

```
실측(모바일)  TTFB 919ms → FCP 1356ms → JS 마운트 1341ms → 첫 사진 표시 2221ms
```
사진 URL 은 **이미 HTML 안**(SECTIONS 시드)에 있는데 `<img>` 를 React 가 만들어서
마운트 뒤에야 다운로드가 시작됐다. 상세 히어로가 2026-07-02 에 고친 것과 **정확히 같은 병목**.

⇒ 워커가 MAIN 슬롯에서 첫 섹션 4장을 `<link rel="preload" as="image" fetchpriority="high">` 로 당긴다.

⚠️ **URL 이 byte-일치해야 한다** — 한 글자만 달라도 preload 를 버리고 같은 사진을 두 번 받는다
(에러 없음·화면 멀쩡·트래픽 두 배). 폭이 뷰포트로 갈리므로(2·3열 200 ↔ 4열 400) 특히 위험하다.
⇒ 폭·중단점·eager 개수를 **`src/shared/home-card-image.ts` SSOT** 로 모으고 `media=` 로 갈랐다.
세 부모(`HomeSections`·`GroupBuyFeed`·`WishlistPage`)가 각자 리터럴을 적던 것도 그 상수로 통일.

### 가드

- 히어로 주입 항목을 **출처 검사**로 교체(`isOwnMedia` 제거 → 빨강 확인)
- 신규 `home-card-preload.test.ts` 4건 + 주입 1건(폭을 240/480 으로 바꾸면 빨강 — 확인)
- 기존 두 테스트(`home-image-priority`·`home-card-image-width`)를 SSOT 기준으로 갱신
  — 리터럴로 되돌리면 3건 빨강(확인)

## 다음 세션의 첫 액션

배포 후 라이브에서:
```
curl -s https://urdeal.kr/ -H 'User-Agent: Mozilla/5.0 (iPhone…)' | grep -c 'rel="preload" as="image"'
```
→ **8** 이어야 한다(사진 4장 × 뷰포트 2). 그리고 첫 사진 표시 시각이 2.2초보다 당겨졌는지 실측.
히어로는 PC 폭에서 사진이 실제로 뜨는지 눈으로 확인(우리 R2 데모가 올라온다).

⚠️ **대표가 `/admin/banners` 에 히어로 배너를 올리면 그게 항상 이긴다** — 데모 폴백은 어디까지나
"아무것도 없을 때"의 대비책이다.
