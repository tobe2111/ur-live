# 2026-09-21 — 주문관리가 이용권을 택배로 그리던 것 (+ 결제 복구 E4 통과)

## 🎉 먼저: 결제 복구가 라이브에서 판정됐다 (E4)

2026-09-19 PR #1498(깨진 외래키 수리)이 머지·배포된 뒤, **대표가 직접 이용권을 결제했다.**

```
orders    88 → 89   id 89 · GB-3-1789611467065 · PAID · 1,000원 · 2026-09-21 10:36:10 KST
vouchers   1 → 2    UR-LUBA-RCP5 · unused · order_id 89
deal_used 900 + 카드 100        ← 부분결제도 정상 동작
payments/tax_invoices → REFERENCES orders(order_number)   ← 수리 모듈이 재빌드한 흔적(테이블명 따옴표)
```

3개월 만에(마지막 주문 2026-06-26) 주문이 다시 만들어졌다. **E4 통과.**

> 🩸 **판정 근거를 한 번 잘못 짚을 뻔했다.** `pragma_foreign_key_check('orders')` 가 0행인 걸 증거로
> 쓰려 했는데, 깨진 스키마를 로컬에 재현해 보니 **그 검사는 깨진 상태에서도 통과한다.**
> 실제로 갈리는 건 `INSERT … RETURNING` 뿐이다. 판정 근거는 pragma 가 아니라 **DDL 원문**이다.

---

## 🧾 이번 작업 — 대표 신고

> *"이거 지금 이용권 결제인데 배송이고, 지금 셀러대시보드고 어드민대시보드고 주문관리 페이지가 알맞지 않아."*
> *"제품 배송이 아니라 이용권 구매잖아."*

대표가 `/seller/orders` 에서 주문 89를 연 화면:
배송 정보에 **빈 칸 셋**(받는 사람·연락처·주소) · 주문 상품 **0원** · 결제상태 raw `pending` ·
상태 변경 버튼 **"준비중"(으)로 변경**.

### 원인이 넷이었다

| | 무엇 | 실측 |
|---|---|---|
| ① | `delivery_kind` 는 **죽은 컬럼** | 값은 `'shipping'` 이지만 **코드 어디서도 읽지도 쓰지도 않는다**(스키마 기본값뿐). 화면이 배송으로 보이는 건 이 컬럼 때문이 아니다 |
| ② | 두 주문관리가 **모든 주문을 택배로 가정** | 운송장·택배사·배송지·CSV 주소 칸, 상태 전이 `PAID → PREPARING → SHIPPING → DELIVERED` |
| ③ | 어드민 '종류' 칸 **판정이 틀렸다** | `isVoucherCategory(category)` 하나로 갈라 `meal_voucher` 를 **"교환권"** 이라 찍었다. 그건 이용권이다(`deal_only=0`, 카드) |
| ④ | 상품 금액이 **항상 0원** (별개 버그) | 서버 enrich 가 `unit_price` 만 보내는데 화면은 `item.price` 를 읽는다 → `undefined*n → NaN → "0"`. **이용권만이 아니라 모든 주문**. 2026-08-02 enrich 추출 때 컬럼이 갈렸다 |

### 고친 방식 — 판정을 새로 만들지 않았다

대표 선택: **"한 화면에서 분기"**(별도 탭 아님).

판정은 **이미 있는 `getNoShippingKind()`**(`product-flow.ts`)를 쓴다. 그 함수는 2026-09-01 에
*"배송비·배송지 판정의 단일 진실원천"* 으로 만들어졌고, 주문관리가 묻는 질문
(*"이 주문에 배송이 있나"*)이 정확히 같은 질문이다.

- **신규 `src/shared/order-kind.ts`** — `orderKindOfItems` / `orderKindOf` / `ORDER_KIND_META`
  - 🔒 **모르면 배송이다** — 라인이 비거나 배송 상품이 섞이면 `'shipping'`.
    이용권을 배송으로 그리면 화면이 이상할 뿐이지만, 배송 주문에서 운송장 칸을 지우면
    **셀러가 송장을 넣을 방법이 없어진다.**
  - ⚠️ `group_buy_status` 로 분류하지 않는다(migration 0146 이 전 상품을 DEFAULT `'active'` 로 만들었다).
- **서버** `order-list-enrich.ts` — `order_kind` 부여 + SELECT 에 `price` 추가(④ 수리). fail-soft.
- **어드민 API** — `first_item_deal_only` 추가(카테고리만으론 이용권↔교환권이 **구조적으로 안 갈린다**).
- **화면** — 셀러 상세: 배송 없는 주문이면 배송 정보 섹션 → 종류 배지 + 한 줄 안내 + `/seller/scan` 링크,
  운송장 폼·배송 상태 전이 숨김. 목록: 주문자 칸이 배송 없는 주문에선 계정 정보로 떨어진다(빈 칸 방지).
  어드민: `orderKind` 를 SSOT 로 교체 — 이용권/교환권/상품 3종.

## 🩸 이번에 틀렸던 것 (다음 세션이 같은 걸 반복하지 않게)

1. **템플릿 리터럴 안 SQL 주석에 백틱을 썼다** → 문자열이 끊겨 TS1005 셋. tsc 가 잡았다.
   `admin-orders.routes.ts` 의 SQL 은 백틱 템플릿이다 — 주석에도 백틱 금지(그 자리에 경고를 남겼다).
2. **내 테스트 하나가 헛돌았다** — 어드민 쿼리 검사를 별칭(`as first_item_deal_only`)으로 앵커해서,
   값을 `NULL as first_item_deal_only` 로 바꿔도 통과했다. **주입이 잡았다.**
   → `SELECT p.deal_only FROM …` 로 재앵커.

## 검증

- 유닛 신규 **17건** + 주입 **7건 되돌려-검증 전부 빨간불 확인**
- tsc 0 · 전체 vitest **746파일 9,494건 pass**
- 파일크기: `AdminOrdersPage` 679(동결선 정확히) · `SellerOrdersPage` 597(cap 600)

## ⏭️ 다음 세션의 첫 액션

1. **배포 후 화면 판정** — `/seller/orders` 에서 주문 89를 열어 ① 배송 정보 섹션이 **안 뜨고**
   ② 상품 금액이 **1,000원**이고 ③ "준비중으로 변경" 버튼이 **없고** ④ '사용처리' 링크가 있는지.
   `/admin/orders` 에서 종류 칸이 **"이용권·식사"**(교환권 아님)인지.
2. **남은 것 — 이번 범위 밖**:
   - `payment_status` 가 `'pending'` 으로 남아 있다(주문은 `PAID`). 화면에 raw 영문이 그대로 뜬다.
     **데이터 결함이고 머니 경로**라 별건(단독 세션). 라이브 실측: 89번 포함 최근 주문 전부 `pending`.
   - `delivery_kind` 죽은 컬럼 — 지울지 살릴지 미결. 지금은 아무도 안 읽으므로 무해.
   - 결제 로딩 후속(발급 후 회계 블록 `waitUntil` 이동) — 머니 경로, 단독 세션.
3. **대표 확인 대기**: 결제가 멎어 있던 기간(6/26~9/21) 동안 토스에 승인됐다가 자동 취소된 건 중
   **취소가 실패한 것**이 있는지. 있으면 청구만 되고 이용권은 안 나간 상태다.
4. **딜 조절 게이트**(`voucher_partial_deal_enabled`)는 여전히 OFF — 켜기 전 `STAGING_CHECKLIST.md` **S13**.

---

# (같은 날 후속) 라이브에 없는 컬럼을 읽어 **조용히 죽던 곳 셋**

#1513 을 머지·배포한 직후 대표가 두 건을 더 신고했고, 내가 배포 후 실측하다 한 건을 더 찾았다.
**셋 다 같은 클래스다**: 마이그레이션엔 있는데 **라이브 D1 엔 없는 컬럼**을 SELECT 해서
SQLite 가 파싱 단계에서 던지고 쿼리 전체가 죽는다. 그런데 셋 다 **에러가 안 보였다.**

| | 증상 | 없는 컬럼 |
|---|---|---|
| ① | 셀러 이용권 **편집·삭제 불가**(콘솔 `/api/seller/products/2915` 500) | `products.live_only_price` · `live_price_enabled` |
| ② | 어드민 주문 목록이 **폴백**으로 서빙(고객명·셀러명·상품명·종류 전부 빈 값, `success: true`) | `orders.shipping_zipcode` |
| ③ | `production-schema.ts`(SSOT)가 낡음 — 라이브에 실재하는 컬럼이 빠져 있었다 | `orders.shipping_address_detail`(SSOT 쪽 누락) |

①은 **읽기만 막힌 게 아니다**: 편집 페이지가 `LivePriceSection` 을 **렌더하지도 않으면서**
폼 기본값으로 그 필드를 항상 보내(`null`) `!== undefined` 가 늘 참이라 UPDATE 에도 섞여
들어갔다 → 저장도 매번 500. 삭제 핸들러 자체는 멀쩡했고, **편집 화면이 안 열려서 버튼에 못 닿은 것.**

②가 제일 고약했다. 폴백이 `success: true` 로 빈 값을 돌려줘서 **어드민 주문관리가 몇 달간
반쪽**이었는데 아무도 몰랐다. #1513 에서 내가 추가한 `first_item_deal_only` 가 라이브 응답에
**키 자체가 없길래** 파고들다 찾았다 — 내 변경이 아니라 그 전부터 있던 `first_item_name` 도 없었다.

## 처방

- ①: 오늘 다른 세션이 `PUT` 에서 같은 사고를 만나 남긴 처방(`seller-product-response.ts` —
  "있으면 읽고 없으면 **그 둘만** 뺀다")을 GET 에 포팅. UPDATE 는 그 컬럼을 더는 쓰지 않는다.
  ⚠️ **컬럼 부재 외의 에러는 그대로 올린다** — 삼키면 다음 결함이 조용해진다.
- ②: 없는 컬럼 참조 제거(목록·상세) + **폴백이 소리를 내게** — DEV 게이트를 벗긴 `console.error`
  + 응답에 `degraded` 표시. 화면이 "데이터가 원래 비어 있다" 와 구분할 수 있어야 한다.
- 영구 가드: `admin-orders` 의 `o.<컬럼>` 참조가 전부 `OrdersTable`(SSOT)에 있는지 대조.

## 🩸 이번에 틀렸던 것

1. **`pragma_foreign_key_check` 를 증거로 쓰려 했다** — 깨진 스키마에서도 통과한다(로컬 재현으로 확인).
   실제로 갈리는 건 `INSERT … RETURNING` 뿐이다.
2. **테스트를 별칭으로 앵커했다** — `as first_item_deal_only` 만 보면 값을 `NULL` 로 바꿔도 통과한다.
   주입이 잡았고 `SELECT p.deal_only FROM …` 로 재앵커했다.
3. **템플릿 리터럴 안 SQL 주석에 백틱을 썼다** — 문자열이 끊겨 TS1005 셋. tsc 가 잡았다.
4. **내가 쓴 주석이 내 가드를 오탐시켰다** — `stripComments` 는 `--` SQL 주석을 못 지운다.

## ⏭️ 다음 세션의 첫 액션

1. **배포 후 판정**: `/seller/products/2915` 200 · 편집 저장 성공 ·
   어드민 주문 응답에 `first_item_deal_only` 키 존재(= 폴백이 아니다).
2. **🔴 미해결 — 대표 신고 "로딩 중에 urdeal 로고도 안보이고"**: 스크린샷은 밝은 배경에
   **파란 점 하나**뿐(워드마크·스윕바 없음). 점이 **정중앙**이라 로고 요소 안의 텍스트가
   폭 0 이거나 배경과 같은 색이라는 뜻이다. 가장 유력한 가설은 **밝은 표면인데 다크 색
   워드마크(`#F8F7FC`)가 적용된 것**(= `forceLight` 미전달). 확정하려면 **어느 URL 인지**가
   필요하다 — 대표에게 물어 둔 상태. 확인 지점: `App.tsx` 의 `PageLoader` 가
   `/seller|/admin|/agency|/ads` 에만 `forceLight` 를 주므로, 그 밖의 밝은 표면이면 재현된다.
3. **`payment_status` 가 `'pending'` 으로 남는 건** 여전히 별건(머니 경로, 단독 세션).
   ⚠️ 단 ② 때문에 **어드민 화면의 `pending` 은 폴백 하드코딩일 수도** 있었다 —
   이제 폴백이 안 도니 배포 후 다시 확인할 것(DB 값도 `pending` 인 건 직접 조회로 확인했다).
