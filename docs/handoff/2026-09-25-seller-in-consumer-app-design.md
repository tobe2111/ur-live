# 2026-09-25 — 셀러 핵심 업무를 소비자 앱 안으로 (설계 + 시안)

대표: *"생각해보니 지금 셀러대시보드를 그냥 우리 유어딜 서비스 페이지의 프로필 페이지에서 어떻게 안될까?
대규모 작업이지?"* → 범위 질의에 **"핵심 업무들은 다 하도록. 설계 먼저 하고 시안까지도."**

**등급: E2(검증됨).** 설계 SSOT + 시안 + **단계 1·2 코드**(tsc 0 · build 0 · 주입 13 빨간불 확인). 배포 전.

---

## 다음 세션의 첫 액션

1. **`docs/design/seller-in-consumer-app-2026-09.md` §0 을 먼저 읽을 것.**
   거기에 *내가 틀렸던 추정*이 적혀 있다. 그걸 안 읽으면 이미 있는 것(인증 다리)을 다시 짓는다.
2. 착수 전 **대표 판단 2건**(같은 문서 §9)이 남아 있다 — 단계 5(정산, 머니 경로)까지 갈 것인가 ·
   사장님 승계를 같이 갈 것인가.
3. 착수하면 **단계 1**(마이에 판매 카드)부터. 신규 화면 0 · 읽기만 · 위험 0.
   판정: 좌석이 있는 계정으로 `/user/profile` 을 열었을 때 판매 카드가 뜨고 숫자가 맞는가.

---

## 이번에 틀렸던 판단 (제일 값진 것)

대표 질문에 처음 답하면서 **"진짜 벽은 인증 축이다 — 셀러 API 28개 모듈이 별도 JWT 를 요구하니
그걸 다 고쳐야 한다"** 고 말했다. **측정해 보니 틀렸다.** 다리는 이미 라이브에서 돌고 있다:

```
resolveActorUserId(c)                          ← 소비자 세션 쿠키를 직접 읽는다
  → canOperateStore(DB, userId, sellerId)      ← owner(linked_user_id) | operator(seller_operators)
    → POST /api/seller/stores/:sellerId/token  ← 그 좌석의 셀러 JWT
GET /api/seller/my-stores                      ← 소비자 세션만으로 좌석 목록
```

⚠️ **왜 틀렸나**: 파일 개수(`seller_token` 138파일 · `requireSeller()` 38곳 · `/api/seller` 28모듈)를
보고 규모를 말했는데, 그 숫자들은 **전부 한 함수로 수렴**한다(`requireUserType` · `getSellerIdFromToken`).
그리고 *이미 만들어진 우회로*(좌석 토큰)를 안 보고 답했다.
**교훈: "몇 곳에 있나" 가 아니라 "몇 곳에서 결정되나" 를 세라.**

---

## 실측값 (2026-09-25)

| | |
|---|---|
| `/seller/*` 라우트 / 줄 | 68 / 32,359 |
| **핵심 넷**(홈·주문·이용권·정산) | **22라우트 · 54파일 · 9,060줄 · API 63개**(`/api/seller/*` 50) = 전체의 28% |
| `requireSeller()` 호출 | 38곳 → 정의 1곳(`requireUserType`) |
| `getSellerIdFromToken()` 호출 | 82곳 → 정의 1곳(`lib/seller-shared.ts`) |
| 라이브 셀러(도매 제외) | 11곳 · 승인 9 · **`linked_user_id` 있는 곳 1** |
| 라이브 좌석 | `seller_operators` **9행 / 4명** · user 35 = **5좌석** · user 3 = 2좌석 |
| 핵심 넷의 `dark:` | **사실상 0**(orders 0 · group-buy 0 · analytics 0 · settlements 1) |

**이 둘이 설계를 결정했다**:
- 다중 매장은 **가설이 아니라 현실** → 좌석 전환을 미룰 수 없다.
- 실제 경로는 소유가 아니라 **운영**(`seller_operators`) → 화면은 `GET /my-stores` 를 쓴다.
  `switch-to-seller`(= `linked_user_id` 전용)로는 그 4명의 9좌석이 **안 보인다**.

---

## 결정

**이식이 아니라 재구성.** 경계 한 줄:
> **폰에서 한 손으로 하는 일 = 소비자 앱 / 앉아서 하는 일 = 셀러 대시보드.**

- 위치 = **마이**(`/user/profile` → `/sell/*`). 유어샵(`/u/{handle}`)은 **손님 진열대**라 안 쓴다 —
  거기에 주문·정산을 얹으면 §5.5 소유권 단일화(CI 가드) 가 흐려진다.
- `/seller/*` 는 **살려 둔다**. 301 하지 않는다(운영 도구 40여 개 + 이미 공유된 URL).
- 신규 화면은 **다크를 지원**한다. `light-island` 로 흰 섬을 만들지 않는다.
- 단계: 1 판매 카드 → 2 `/sell` 홈+좌석 → 3 주문 → 4 이용권 → **5 정산(🔴 머니 · 단독 세션 + staging)**

---

## 완료분

| | |
|---|---|
| 설계 SSOT | `docs/design/seller-in-consumer-app-2026-09.md` |
| 시안(테마 토글) | `docs/design/assets/seller-in-consumer-app-mock.html` |
| 렌더본 | `…-light.png` · `…-dark.png` |
| 시안 목록 등록 | `docs/design/README.md` |

## 시안을 그리다 잡은 결함

판매 홈 '이번 주' 를 `[매출|수수료|받을 돈]` 3열로 그렸더니 **6자리 금액 셋이 붙었다**
(`1,240,00062,0001,178,000`). 430px 에서 각 열이 ~130px 인데 23px 숫자로는 안 들어간다.
2026-08-31 홈 카드의 *"6자리 가격에서 할인 pill 이 다음 줄로 밀린다"* 와 **같은 클래스**.
⇒ 구현 규칙: **판매 모드에서 금액 3개를 한 행에 균등 배치하지 않는다.** 3열은 *개수*에만.

## 대표 확정 (같은 날)

> *"디자인이 마음에 안들긴 해. 정산까지도 다 해주고, 주인 연결도 같이 가줘"*

- **정산 포함 · 주인 연결 동반** — 둘 다 범위 안. 그런데 재 보니 **둘 다 이미 만들어져 있다**:
  출금은 `POST /api/seller/deal-withdraw`(⚠️ 2026-09-25 정정 — 원래 `account/withdraw` 라고 적었는데 **그건 탈퇴다**), 승계는 `store-codes`(`owner_claim`) ·
  `/store-claims` 4라우트 · `/store/find` · 어드민 `AdminStoreOwnerPage`. **기능이 아니라 자리가 없었다.**
  주인 연결은 **단계 2에 얹는다**(좌석 목록이 이미 `owner_claim_code` 를 내려준다 — 신규 API 0).
- **1차 디자인 반려** → 방향 3안 재작업. 갈리는 축은 *무엇이 주인공인가*:
  **A 숫자**(오늘 한 장) · **B 물건**(티켓 지갑 뒤집기) · **C 할 일**(큐).
  시안 `assets/seller-in-consumer-app-directions.html` · 렌더 `seller-directions-{light,dark}.png`.

### 1차 시안에서 내가 어긴 것

- **강조색 자리가 여덟이었다** — 규칙은 「강조색 하나, **자리 셋**」. 파란 게 많으면 아무것도 안 강조된다.
- 판매 홈에 **주인공이 없었다**(같은 무게 카드 4장 스택).
- 주문·이용권·정산이 **서로 구별이 안 됐다**(전부 흰 카드 + 리스트 행).
- **소비자 앱 같지 않았다** — 작은 대시보드로 보인다.

### 🩸 3안 시안이 또 하나 잡았다

`안 A/B/C` 태그를 `background: var(--ink); color: #fff` 로 그렸더니 다크에서 `--ink` 가 `#F5F3F1`
(거의 흰색)이라 **흰 면 위 흰 글자**가 됐다. `check-dark-contrast` 가 존재하는 바로 그 클래스.
⇒ **면 색이 토큰이면 글자도 토큰으로**(`--pagefg` 면에는 `--page` 글자). `#fff` 를 박으면 한쪽 테마가 깨진다.

## 🔴 대표 화면 실측 — 내 시안이 틀린 두 곳

대표가 실제 마이 스크린샷을 보내며 *"지금은 어떤 링크를 들어가는 구조잖어"* 라고 물었고,
그 화면이 둘을 바로잡았다.

1. **소비자 하단 탭은 5개다** — 홈 · 교환권 · **이용권(`/my-vouchers`)** · 유어샵 · 마이.
   내 1차·3안 시안의 마이 화면은 `이용권` 을 빠뜨리고 **4개**로 그렸다.
   ⚠️ `seller-in-consumer-app-mock.html`·`…-directions.html` 에 그 오류가 남아 있다(`seller-where-in-my.html` 만 교정).
   **그 그림을 치수 근거로 쓰지 말 것.**
2. **판매 진입점이 이미 있다** — `SellerSwitchInline`(123줄)이 프로필 이름 옆 칩으로 살아 있고
   셀러가 아니면 `내 가게 등록` 을 띄운다. ⇒ 단계 1은 *"새 카드를 만든다"* 가 아니라 **"그 칩이 자란다"**.
   새 자리를 만들면 진입점이 둘이 된다.

> 🧭 **교훈**: 화면을 코드로만 읽고 **한 번도 안 봤다.** `/user/profile` 은 `ProtectedRoute` 라 이 컨테이너에서
> 못 여는데, 그걸 "못 본다" 로 넘기고 소스에서 추론했다. **못 보면 대표에게 스크린샷을 먼저 청했어야** 했다.

## ⭐ 추천안 (대표 *"가장 이상적인 형태로 제안줘봐 시안이랑"*)

**“보는 건 마이에서 다, 하는 건 들어가서.”** 시안 `assets/seller-recommended.html`
· `seller-recommended-{dark,light}.jpg` · 설계 §13.

- **형태 1**, 단 마이 판매 카드가 **오늘 매출 + 확인할 주문 + 후기 인증 + 받을 돈**까지 전부 보여준다.
- 🔑 **탭바를 바꾸지 않는다** — 판매 화면에서도 하단은 홈·교환권·이용권·유어샵·마이 그대로고
  **마이가 켜져 있다.** 판매 안 이동은 **상단 알약**. ⚠️ **셀러 전용 하단 탭을 만들지 말 것**
  (1·3안 시안이 그렇게 그렸고, 그 순간 소비자 앱이 아니라 축소된 대시보드가 된다).
- **문법은 화면마다** — 마이·정산 = A(숫자) · 판매홈·주문 = C(할 일) · 이용권 = B(티켓).
  근거: 마이의 `내 딜 잔액 10,300딜` 카드가 **이미 A 문법**이라 그 이웃이 달라지면 어긋난다.
- **강조색 자리 셋** — 티켓 밴드 · 지금 눌러야 하는 버튼 · 처리 대기 숫자.
- 주인 연결은 **좌석 시트 안에서** 보인다(`사장님 미연결` + 줄 코드 — `my-stores` 가 이미 내려준다).

## ⭐⭐ 대표 확정 — 마이 안에서 다, 필수 작업만

> *"하는 것도 마이에서 하는걸로. 근데 필수적인 것들 선별해서"*

**§13(형태 1)은 §14 가 대체했다.** 시안 `assets/seller-in-my-final.html`
· `seller-in-my-final-{dark,light}.jpg` · 설계 §14.

- **페이지 이동 0.** 주문 확인·판매 on/off 는 **인라인**, QR·출금·가게 전환은 **시트**로 마이 위에.
- ⚠️ **`/sell/*` 별도 라우트를 만들지 않는다**(§13 계획 철회).
- **필수 4작업** — 네 기준(손님 앞 / 놓치면 손해 / 빈도 / **한 손**)으로 채점:
  QR 사용처리 · 주문 확인 · 판매 중지·재개 · 출금 신청.
  제외: 환불(사유·신중) · 이용권 등록(사진·옵션) · 분석/원장(표) · 쿠폰/파트너/알림톡.
  **"한 손" 이 실질적인 칼이다** — 폰으로 못 하는 건 넣어도 안 쓴다.
- 마이 **14 → 18블록**, 판매가 **맨 위**. 셀러가 아니면 **0블록**(지금 `내 가게 등록` 칩 그대로).
- 쓰는 API 전부 기존: `PUT /seller/orders/:id/status` · `POST /group-buy/vouchers/:code/use` ·
  `PUT /seller/products/:id` · `POST /seller/deal-withdraw`(출금 — ⚠️ `account/withdraw` 는 **탈퇴**다) · `GET /my-stores`.

### 단계

| | | 머니 |
|---|---|---|
| 1 | 마이 판매 섹션(오늘 카드 + 가게 전환) — `SellerSwitchInline` 칩이 자란다 | 없음 |
| 2 | 주문 확인 인라인 + 판매 on/off 토글 | 없음 |
| 3 | QR 사용처리 시트 (⚠️ 소각은 되돌릴 수 없다 — 시트가 먼저 경고) | 없음 |
| 4 | 출금 신청 시트 (가게 이름을 시트 안에 다시 적는다) | 🔴 단독 세션 + staging |

## 🔴 착수 전 실측 3건 (설계 §15) — 단계 1 이 반드시 들고 가는 것

대표 *"지금 형태가 가장 이상적인 형태야?"* → 재 보고 답하다 나왔다. 상세는 설계 §15.

1. **저장 모델은 이상적이지 않다. 그래도 지금 안 고친다.**
   `sellers` **100/100 컬럼**(D1 한도에 정확히 닿음) · 11행 중 **10이 매장** · 가짜 이메일 **9** ·
   `seller_meta` 21키 111행. 이 어긋남이 이미 두 번 터졌다(09-02 두 번째 매장 등록 100% 실패 ·
   09-20 등록증이 meta 에만 들어가 어드민이 못 봄). **쪼개지 않는 이유**: `seller_id` 가
   주문·원장·정산·커미션·payout·토큰 전부에 박혀 있다 — 등급 C 를 한꺼번에 여는 일이고 화면 작업과 못 섞는다.
2. **`switch-to-seller` 는 라이브 9좌석을 못 본다.** 그 길은 `linked_user_id`(UNIQUE 1인1행) 한 행만
   보는데, `POST /store/new` 는 설계상 그 칸을 **비우고** `seller_operators` 로만 권한을 준다.
   ⇒ **새 판매 화면의 좌석 출처는 `GET /my-stores` 하나.** `switch-to-seller` 는 호환 경로로만 남긴다.
3. **🔴 좌석 토큰 ↔ 인라인 작업 충돌 — 단계 1 에서 막는다.** 좌석 토큰은 JWT 안에 `seller_id` 가
   박혀 있어 **가게를 바꾸면 토큰이 통째로 바뀐다.** 종전엔 전환이 곧 페이지 교체라 틈이 없었는데,
   §14 가 작업을 마이 안 인라인으로 옮기는 순간 *"A 의 목록을 펼쳐 둔 채 B 로 전환 → A 행에서 확인"* 이
   생긴다. 규칙 셋: ① 전환하면 열린 시트를 닫고 재조회 ② 화면이 든 `seller_id` ≠ 토큰 `seller_id` 면
   **보내지 않고 재조회** ③ 판매 화면은 `seller_id` 를 URL·localStorage 에서 읽어 API 에 넘기지 않는다.

## 단계 1 — 지은 것 (같은 날, 코드)

신규 `src/lib/seller-seat.ts`(좌석 SSOT) · `user-profile/useMyStores.ts` · `SellerSection.tsx` ·
`StoreSwitchSheet.tsx`. 수정 `SellerSwitchInline`(좌석 있으면 사라진다) · `UserProfilePage` ·
`AccountPcPane`(좌석을 페이지가 한 번만 묻는다) · `seller-operators.routes`(요약에 `status` 추가).

**핵심은 하나다: 하드 리로드를 세대(generation)가 대신한다.** 셀러 대시보드는 전환 뒤
`location.assign` 으로 전역 캐시를 버리는데, 마이는 리로드를 못 한다(그러면 인라인이 아니다).
`bumpSeatGeneration()` 이 그 일을 하고 구독자가 스스로 다시 부른다.

🕳️ **가드가 곧바로 하나 잡았다(교훈)**: `seller-seat.ts` 는 `/src/lib/` catch-all 로
`app-utils`(첫 페인트 **안**)에 떨어지는데, 거기서 `@/utils/enter-store`(첫 페인트 **밖**
= `app-utils-deferred`)를 정적으로 import 하니 그 청크가 통째로 홈 첫 페인트로 끌려왔다
(`check-critical-chunks` 빨간불). import 하나를 동적으로 바꿔 해소 — **`vite.config.ts`(잠금)는 무접촉**.
⇒ 청크 경계는 *파일이 어디 있느냐*가 아니라 **무엇을 정적으로 부르느냐**가 정한다.

⚠️ **다음 세션이 지켜야 할 것 둘**:
1. **`이용권 등록 · 주문 · 정산 · 매출 분석 ›` 줄을 지우지 말 것.** 종전 '사업자 모드' 칩을 대신한다 —
   없으면 마이에서 셀러 대시보드로 가는 길이 통째로 사라진다(`RoleCtaGrid` 타일은 `seller_token` 이
   이미 있어야 떠서 대체가 안 된다).
2. **마이를 여는 것만으로 좌석 토큰을 발급하지 말 것.** `startDashboardSession` 이 단일 세션을 갱신해
   대표가 다른 기기에서 보던 대시보드를 끊는다.

**판정(E4)**: 좌석이 있는 계정으로 `/user/profile` → ① "내 가게" 카드가 뜨고 오늘 숫자가 대시보드와 같은가
② 가게 2곳이면 헤더 오른쪽을 눌러 전환 → 카드 숫자가 **그 자리에서** 바뀌는가 ③ 전환 후 옛 가게 숫자가
남지 않는가 ④ 셀러가 아닌 계정에선 섹션이 **안 뜨고** `내 가게 등록` 칩만 보이는가.

## 단계 2 — 주문 확인 · 판매 on/off (같은 날)

신규 `seller-section/{useSellerWork.ts,PendingOrders.tsx,SellingList.tsx}`. 상세는 설계 §17.

**좌석 토큰 모델을 유지하기로 했다.** 대안(소비자 세션 + `canOperateStore` 새 엔드포인트 묶음)은
충돌 자체를 없애지만, 주문 상태 핸들러에 얽힌 **무환불 CANCELLED 차단·상태 기계·알림톡**이
두 벌이 된다. ⇒ 문은 한 벌로 두고 화면이 좌석을 정확히 다룬다.

규칙 셋이 코드로: ① 좌석이 맞을 때만 부른다 ② 보내기 직전 `assertSeat`, 어긋나면 **안 보낸다**
③ 낙관적 갱신 금지(서버가 상태 기계로 거절할 수 있다).

**좌석은 사람이 누를 때만 발급한다** — 일감 블록은 앉았을 때만 그리고, 안 앉았으면
`주문 확인 N건 ›` 한 줄이다. 마이를 여는 것만으로 다른 기기의 대시보드를 끊으면 안 된다.

🩸 **이번에 주입이 내 시험 둘을 헛돈다고 잡았다**: `"부르지 않는다"` 를 `waitFor` 로 썼는데
**통과하는 단언은 첫 확인에서 즉시 끝나서** 요청이 나갈 기회를 주기 전에 초록이 됐다.
⇒ **"일어나지 않는다" 는 `waitFor` 로 증명할 수 없다.** 실제로 기다렸다가 센다.

**판정(E4)**: 좌석 있는 계정 `/user/profile` → ① `주문 확인 N건 ›` 을 눌러 목록이 펼쳐지는가
② `[확인]` 후 그 줄이 사라지고 오늘 카드의 '확인 대기' 가 줄어드는가 ③ 스위치로 끄면 소비자
화면에서 그 이용권이 사라지는가 ④ **가게 2곳**에서 A 목록을 펼친 채 B 로 전환 → A 행의 버튼이
"가게가 바뀌었어요" 안내를 내고 **아무것도 안 보내는가**.

## 단계 3 — 이용권 사용처리 (같은 날)

**카메라 화면을 다시 만들지 않았다.** `/store/scan` 이 이미 풀스크린 계산대이고 스캔 코어를
셀러 대시보드와 공유한다 — 시트로 옮기면 그 코어가 두 벌이 될 위험이 있다. 대신 **빠져 있던 둘**을 채웠다:
① `되돌릴 수 없습니다` 경고를 스캐너 위에 ② `지금은 {가게} 이용권만 처리됩니다`(표시 전용).
그리고 진입을 `enterSeat('/store/scan')` 로 바꿔 **먼저 그 가게 좌석에 앉히고** 보낸다 —
종전엔 마이에서 A 를 보고 들어와 **B 의 이용권을 소각**할 수 있었다.

검은 계산대 슬래브는 **폴백으로만** 남는다(`stores.length === 0`) — 좌석 요약 실패·정지 매장.
둘이 같이 뜨면 하나는 좌석을 맞추고 하나는 안 맞춘다.

## 🩸 2026-09-25 — 내가 이 문서에 심어 둔 지뢰 (정정, 제일 중요)

**"출금 = `POST /api/seller/account/withdraw`" 는 틀렸다. 그건 셀러 탈퇴(계정 삭제)다.**
이름이 `withdraw` 라서 "출금" 으로 읽었고 설계·PR·이 문서에 그대로 퍼뜨렸다.
그 오기를 믿으면 사장님의 [출금] 버튼이 가게를 지운다 — **에러도 안 난다**(그 API 는 성공한다).

| 엔드포인트 | 진짜 |
|---|---|
| `POST /api/seller/deal-withdraw` | ✅ 출금(환급) — PIN · 사업자검증 · 최소 10,000딜 · 잔액 CAS |
| `POST /api/seller/settlements/request` | 정산 요청 |
| `POST /api/seller/account/withdraw` | 🚫 **탈퇴** |

🧭 교훈: 엔드포인트 이름이 한국어 업무명과 겹칠 때 **이름만 보고 계약을 단정하지 말 것.**
핸들러 본문을 한 번만 열었으면 됐다.

## ⭐⭐⭐ 대표 재확정 — 등록·환불·분석·출금도 마이에서 (2026-09-25)

*"등록, 환불, 분석, 출금도 마이에서 돼야해."* → **§14 의 "필수만 선별" 은 폐기.** 상세는 설계 §19.
그릇은 무게가 정한다: 환불·분석·출금 = 시트 / **등록 = 기존 전체화면 폼 그대로**(복제 금지, §18 과 같은 판단).
🔴 출금은 PIN·사업자검증·계좌 재인증까지 있어 412 가 넷이다 — 각각을 사람 말로 번역해야 한다.

**지은 것**: 공용 시트 셸 + 환불·분석·🔴출금 시트 + 등록 진입. 도구는 **좌석을 맞춘 뒤에만** 열린다(`openTool`).

### 🏦 구현 중에 잡은 결함 하나 — 안 보내면 **송금 못 하는 지급 행**이 생긴다 (설계 §19-3)

처음엔 *"계좌는 화면이 안 보낸다 — 서버가 매장 계좌로 보낸다"* 로 지었다. **틀렸다.** 실측 둘:

1. `deal-withdraw` 는 `settlements` 행에 **요청 본문 값 그대로** 넣는다 — `sellers` 폴백 **없음**.
2. 어드민 지급 센터도 **그 행에서만** 읽는다 — 여기도 조인 없음.

⇒ 안 보내면 계좌가 빈 지급 행이 조용히 쌓인다(에러 0). 사장님은 "신청됐다"를 보고 어드민은 보낼 곳이 없다.
**그렇다고 셀러 대시보드처럼 `localStorage`(`seller_bank_name` …)에서 읽으면 더 나쁘다** — 그 키는
**좌석을 안 따라간다**. 가게를 옮긴 뒤 출금하면 **직전 가게 계좌로 송금 행이 생긴다**.
⇒ 좌석 인증된 `GET /api/seller/profile` 값을 **그대로 되돌려보낸다**. 없으면 **신청 자체를 막는다**.
화면엔 **뒤 4자리만**. 가드 4건(주입 전부 빨간불).

⚠️ **셀러 대시보드의 `localStorage` 경로는 그대로 남아 있다** — 그쪽에선 같은 사고가 여전히 가능하다.
머니 라우트/다른 화면이라 별건이고 **대표 판단 대기**(아래).

🧭 교훈: *"서버가 알아서 할 것"* 은 확인 전까지 가설이다. 이번엔 **두 곳**(쓰는 쪽·읽는 쪽)을 다 열어 봐야 했다.

## ⭐⭐⭐⭐ 대표 — "모두 마이에서 하도록" (2026-09-26, 설계 §20)

**65개를 시트로 복제하지 않는다.** "모두" 를 이렇게 읽었다 — *사장님이 "셀러 대시보드라는 게 따로
있다" 를 몰라도 되게. 마이가 출발점이고 도착점이다.*

| 지은 것 | 왜 |
|---|---|
| 🧰 **전체 도구 목록**(마이 안 시트, 검색) | 목록은 대시보드와 **같은 색인**(`commandItems`). 따로 적으면 도구가 늘 때 한쪽을 잊는다 |
| ↩️ **귀환 띠** — `SellerLayout` **한 곳** | 65개가 한꺼번에 덮인다. 페이지마다 붙이면 안 붙인 곳이 생기고 하필 거기가 일이 끝나는 화면이다 |
| 🪑 **좌석-종류 수리** | `localStorage.seller_type` 이 좌석 전환을 안 따라가 **A 의 메뉴를 B 에서** 봤다 |
| 📱 **시트 뒤로가기** | 안 쌓으면 뒤로가기가 마이를 통째로 닫는다 |

🩸 **좌석-종류는 마이만의 문제가 아니었다** — 대시보드 매장 전환도 하드 리로드를 하지만 그 값을
다시 안 쓴다. **원래 있던 결함**이고, 마이가 그 위에 도구 목록을 올리려다 드러났다.

🩸 **주입 러너가 또 내 앵커를 잡았다** — `seller-inline-seat` 의 *"좌석이 맞으면 재발급 안 한다"* 가
`enterSeat('/seller')` 문자열에 앵커돼 있었는데, 그 행이 시트로 바뀌며 사라져 **주입 전부터 빨갛게**
됐다. 지키려던 것은 멀쩡했다 → 불변식 자체(`currentSeatId() === … || await switchSeat(`)로 재조준.
**앵커는 우연한 문자열이 아니라 불변식에 걸어야 한다** — 이 파일에서 두 번째다.

### ❌ 아직 마이 밖 — 온보딩 셋 (§20-5, 다음 단계)
출금 선행조건 셋(사업자등록증·PIN·계좌)이 아직 대시보드 화면이다. 전체 도구로 닿고 띠로 돌아오지만
**돈이 나가는 흐름 한복판에서 화면이 바뀐다.** 그대로 못 옮기는 이유는 그 폼들이 **라이트 고정
대시보드 부품**(`DashboardCard`, `dark:` 금지)이고 마이는 다크를 지원하기 때문이다.
⇒ **PIN·계좌는 시트로**(짧고 출금 도중 요구됨), **사업자등록증은 진입으로**(업로드+OCR — 등록 폼과 같은 무게).

## 남은 결정 (대표)

1. **PR 머지** — Verify 초록, draft.
2. **🔴 staging 실결제** — `STAGING_CHECKLIST.md` **S-MYSELL**(환불 3·5, 출금 9·10). **통과 전 "출금 된다"고 말하지 않는다.**
3. **결재 15건 아카이브** — `AdminDecisionsPage` 가 `docs/decisions/*.md` 19개를 `eager: true` 로 인라인(140.4 KB).
   그중 15개가 처리 끝난 결재. 승인 시 번들 예산을 **7.50 → 7.47 로 되돌린다**(이번이 6번째 상향이었다).
4. **셀러 대시보드 `DealBalanceCard` 의 계좌 localStorage 경로** — 위 §19-3 과 같은 사고가 그쪽에 남아 있다.

---

## 2026-09-26 이어서 — PR #1546 머지 + **여섯 묶음 시안**

**머지됨**: PR #1546 → `55dccae`(squash). 인계 목차 자동 재생성.

### 🔴 다음 세션이 제일 먼저 알아야 할 정정 — "~55개" 는 틀린 규모다

내가 대표께 *"셀러 화면이 ~55개라 전부 마이에 넣을 수 없다"* 고 보고했다. **라우트 수는 맞지만
결론을 왜곡한다.** `seller-nav.ts` 를 실제로 세면 매장 사장님(`store_owner` +
`SELLER_STORE_ONLY_MODE`)이 **사이드바에서 보는 항목은 12개**다:

```
홈     /seller · /u/me
이용권 /seller/meal-voucher/new · /seller/group-buy · /seller/stays
매출   /seller/orders · /seller/settlements
성장   /seller/coupons · /seller/influencer-deals        (earnings·prospects 는 store_owner 숨김)
설정   /seller/business-info · /seller/stores · /seller/alimtalk   (notify-followers 는 live 모드)
```
나머지 53개는 **탭 형제 라우트 · 검색 전용 11개(`SELLER_SEARCH_ONLY`) · 로그인/콜백 통과 화면**이다.
⇒ *"대부분 마이에서"* 는 55개를 옮기는 일이 아니라 **12개를 옮기는 일**이다. 규모 판단이 바뀐다.

### 두 번째 실측 — 12개 중 **완전히 끝나는 것은 2개**뿐이다

어제 넣은 것들로 *손이 닿는 범위*는 넓어졌는데, **메뉴 단위로는 대부분 반쪽**이다:
`/seller/orders` 는 확인 대기만 · `/seller/settlements` 는 출금만 · `/seller/group-buy` 는
중지·재개만 · `/seller/business-info` 는 계좌만 · `/seller/stores` 는 전환만.
**완전 2 · 반쪽 5 · 나감 5.** "시작은 마이, 마무리는 대시보드" 가 지금의 정확한 상태다.

### 시안 (코드 0 · 대표 판단 대기)

`docs/design/my-seller-six-groups-2026-09-26.md` · https://claude.ai/artifact/Lq8d3pnYM9xWZALsa1YFDE

낱개 `ToolRow` 목록을 계속 늘리면 끝은 **마이 안 두 번째 사이드바**다(화면만 옮기고 대시보드를
없앤 게 아니다). ⇒ **오늘 · 주문 · 이용권 · 손님 · 정산 · 가게** 여섯 묶음 시트로 **화면 수를 고정**하고,
도구가 늘면 묶음 *안에서* 자라게 한다. 완전 9 · 나감 2 가 목표.

**나가는 셋은 복제하지 않는다** — 같은 폼이 두 벌이 되면 반드시 한쪽만 고쳐진다:
이용권 등록(사진 여러 장·옵션) · 숙소(달력은 가로 폭) · 사업자등록증(촬영+OCR, 연 1회).

**순서**: 주문 → 이용권 → 정산 → 가게 → 손님. 각 단계 독립, 중간 상태가 어색하지 않다.

### 남은 결정에 3건 추가 (대표)

5. **정산 묶음 시점** — 출금은 마이에 있으나 **라이브에서 돈이 나가 본 적이 없다**(E3).
   명세·지난 내역을 붙이기 전에 S-MYSELL-9·10 이 먼저다.
6. **쿠폰 발행 · 알림톡 발송을 마이에서 허용할지** — 되돌릴 수 없는 일 = 등급 C. 기본안은 "확인 한 번 더".
7. **가격 수정에 확인 단계를 둘지** — 손님이 보는 값이 한 손으로 바뀐다(실수도 한 손으로 난다).

---

## 2026-09-26 (2) — 묶음 셋 구현 (승인 없이 되는 범위)

대표 *"이제 다 했어? 가장 이상적으로 모두 마이에서 끝내고 있나?"* → **아니오**(그때 2/12)라고
답하고, 대표 판단이 필요 없는 **주문 · 이용권 · 가게** 세 묶음을 만들었다.

### 🔴 먼저 — 어제 머지한 코드의 결함을 찾았다

`BankSheet` 가 **계좌 변경의 412 `PIN_REQUIRED` 를 안 받는다.** 서버
(`seller-profile.routes` — `bankChanged` 면 PIN 강제, 위임 운영자는 403)를 확인하다 드러났다.
증상: **출금이 막혀 계좌를 넣으러 온 사장님이 거기서 또 막힌다** — *"계좌 변경은 PIN 인증이
필요합니다"* 문장만 보이고 대시보드로 나가지 않으면 풀 방법이 없다. 어제 §20-5 에서 없앴다고
적은 바로 그 막다른 길이 **한 칸 옆에** 남아 있었다.
⇒ 수리 + `pinReturn` 상태(요구한 쪽으로 돌아간다 — 출금이면 출금, 계좌면 계좌).
⚠️ **403(소유자 전용)은 PIN 으로 못 푸는 벽**이라 서버 문장을 그대로 보여 주고 멈춘다.

### 무엇을 만들었나 (서버 변경 0)

낱개 `ToolRow` 목록 → **묶음 다섯**(주문 · 이용권 · 정산 · 매출 분석 · 가게) + 전체 도구.
셋 다 이미 있는 엔드포인트만 쓴다 — 늘어난 건 화면뿐이다.

| 새 파일 | 하는 일 | 쓰는 API |
|---|---|---|
| `OrdersSheet` | 지난 주문 · 상태 칩 4 · 더 보기 · 주문 열기 · 확인 | `GET /api/seller/orders` |
| `VoucherSheet` + `VoucherEditSheet` | 목록 · 중지/재개 · 가격 · 정가 · 수량 · 이용 안내 | `GET/PUT /api/seller/products/:id` |
| `StoreSheet` | 이름 · 연락처 · 주소 · 소개 · 승인 상태 · 가게 전환 | `GET/PUT /api/seller/profile` |

설계 판단 셋:
- **주문 시트는 상태 전이를 다시 구현하지 않는다** — `useSellerWork.confirmOrder`(assertSeat 보유)를 쓴다.
  여기서 `api.put` 을 부르면 그 가드를 우회하고 규칙이 두 벌이 된다(테스트가 쓰기 자체를 금지한다).
- **가게 시트는 계좌 필드를 안 보낸다** — 서버의 `bankChanged` 는 계좌 필드가 **섞이기만 해도** 참이라,
  상호를 고치려던 사람이 PIN 을 요구받는다. 테스트가 그 부재를 고정한다.
- **가격은 바뀌었을 때만 한 번 더 묻는다** — 안 바꿨는데 매번 물으면 확인이 무의미해진다.
  결정 ③(대표) 전까지의 **보수적 기본값**이다.

♻️ `SellingList` **삭제** — 이용권 묶음이 목록·토글을 이어받았다. 같은 목록이 카드와 시트 두 곳에
있으면 한쪽만 새로고침되는 날이 온다. **할 일(확인 대기)만 카드에 남는다.**

### 🩸 이번에 내 앵커가 두 번 틀렸다 (코드가 아니라 시험이)

1. `not.toMatch(/refund|환불/i)` 로 "주문 시트에 환불 없음"을 재려 했는데 **상태 라벨
   `REFUNDED: '환불'`** 에 걸렸다 — 표시 문자열이지 실행이 아니다. ⇒ 판정을 **엔드포인트**로 옮겼다.
2. `toContain('work.products')` 로 "목록을 다시 안 부른다"를 재려 했는데 그 파일은 **구조분해**로
   받는다. ⇒ 앵커를 **출처**(`} = work`)에 걸었다.
   **둘 다 "무엇을 재고 있는가" 를 안 보고 문자열을 고른 탓이다.**

### 검증

tsc 0 · build 0 · pre-push 게이트 **가드 99개** · 셀러 시험 4파일 **92건** pass ·
주입 15건 **전부 되돌려-검증 빨간불 확인**(잔존 0). 번들 `UserProfilePage` 92 → 108 KB(전부 lazy),
critical-chunks **17 불변**, 예산 7.51 → 7.54(**8번째 상향** — 결재 아카이브 ① 여전히 대기).

### 남은 것

- **손님 묶음**(쿠폰·알림톡·소개 파트너) — 발행·발송이라 **등급 C**. 대표 승인 뒤.
- **정산 묶음의 명세·지난 내역** — 출금이 아직 E3(라이브에서 돈이 나가 본 적 없다). S-MYSELL-9·10 뒤.
- staging: **S-MYSELL-27~37** 신설(주문 묶음 3 · 이용권 4 · 가게 2 · 계좌 PIN 2).

### 📏 숫자 정정 (2026-09-26, 같은 세션)

PR 본문에 *"이 PR 로 완전 9"* 라고 썼다가 **틀려서 고쳤다.** 9 는 시안의 **목표치**다.
구현 뒤 실제는 **완전 5**(홈 · 유어샵 · 주문 · 이용권 관리 · 가게) · **반쪽 2**(정산 — 출금·계좌·PIN 만 /
사업자정보 — 서류는 나감) · **나감 5**. 9 까지 남은 넷은 **손님 묶음 3 + 정산 명세 1** 이고
둘 다 대표 판단 대기다. **목표치를 달성치로 적으면 다음 세션이 다 됐다고 믿는다.**

---

## 2026-09-26 (3) — 이용권 등록 · 숙소도 마이 안으로 / 쿠폰은 내림

대표 *"이용권 등록, 숙소까지 해줘 쿠폰은 앞으로 필요없을 것 같은데? 확인해줘"*

### 📏 쿠폰 확인 결과 — 대표 말이 맞다. **그런데 숙소도 같은 상태다**

라이브 D1 실측(2026-09-26):

| | 값 |
|---|---|
| 셀러 | 11곳 |
| **셀러가 만든 상품** | **3개 — 전부 `meal_voucher`** |
| 숙소 상품 | 77개인데 **셀러 소유 0개**(전부 `seller_id IS NULL` = 플랫폼·데모) |
| 셀러가 만든 쿠폰 | **0건**. `coupons` 유일 1행 `WELCOME2026`("카카오 채널 추가 감사 쿠폰", 2026-04-17)은 `seller_id IS NULL` = 어드민이 만든 플랫폼 쿠폰 |
| `promo_codes` · `district_coupons` | 각 0행 |
| `user_coupons` | 2행(위 플랫폼 쿠폰을 받은 것) |

그리고 할인 수단이 이미 셋 더 있다 — 이용권의 정가↔판매가 · 딜 포인트 · 소개비(promo).
쿠폰은 넷째라 겹치고, 겹치는 수단은 정산 계산만 복잡하게 만든다.

⚠️ **다음 세션이 알아야 할 것**: 쿠폰을 빼는 근거(사용 0)가 **숙소에도 똑같이 적용된다.**
그래도 숙소를 만든 것은 대표가 명시로 요청했기 때문이다 — "앞으로 팔 물건인가" 는 대표 판단이다.
안 쓰일 것으로 정해지면 쿠폰과 **같은 플래그 방식**으로 내리면 된다(라우트 보존).

### 🎟️ 등록 — 폼을 복제하지 않고 **같은 파일을 연다**

위저드는 3단계(매장·이용권·판매 설정)에 카카오맵·사진 업로드·임시저장(로컬+서버)까지 얹힌
495줄 + 지원 파일 9개다. 시트용으로 다시 만들면 **반드시 한쪽만 고쳐진다.** ⇒ 복제 대신:

- `SellerLayout` 에 **`bare`** 추가 — 사이드바·상단바·하단 탭·⌘K·**도매 리다이렉트**를 건너뛴다.
  🔴 조기 반환은 **모든 훅 뒤**(조건부 훅 금지)이고 **`wholesaleOnly` 보다 먼저**다 —
  시트 안에서 `/wholesale` 로 튕기면 마이가 통째로 사라진다(도매 겸업 사장님).
- `SellerMealVoucherNewPage` 에 `embedded`/`onClose`/`onCreated` — 완료·취소가 `navigate` 대신
  콜백, 로그인 리다이렉트도 시트에선 안 탄다(3단계까지 쓴 내용이 날아간다).
- `VoucherNewSheet` = `Sheet tall` + **`light-island` 클래스** + `lazy` 위저드.
  🏝️ 그 페이지들은 대시보드 규칙상 `dark:` 가 금지돼 있다 — 클래스 없이 넣으면 다크에서
  흰 폼 위 흰 글자가 난다(2026-09-03 지도 검색창과 같은 사고). **주석이 아니라 클래스여야 한다.**

⚠️ **폼·검증·제출 payload·임시저장은 한 글자도 안 바뀌었다** — 시험이 `handleSubmit` 안에
`embedded` 가 없음을 고정한다.

### 🏨 숙소 — 목록은 시트, 달력은 전체화면

`StaysSheet`(읽기 전용, `assertSeat` 없음 — 쓰기가 0이라). 이용권 묶음 안에서 연다.
달력·객실 편집은 전체화면 유지: 한 달 달력은 가로 폭을 요구하고, 시트 폭에서 그리면 날짜 칸이
손가락보다 작아진다.

### 🩸 이번에도 주입 러너가 내 헛도는 단언을 잡았다

*"쿠폰 라우트를 지우지 않는다"* 를 `toContain('/seller/coupons')` 로 썼는데
**`/seller/coupons-removed` 도 그 문자열을 포함**해서, 경로를 바꿔도 초록이었다.
⇒ `path="/seller/coupons"` 로 닫는 따옴표까지 앵커. **부분 문자열 앵커는 접미사에 뚫린다.**

그리고 기존 시험 둘의 **전제가 대표 지시로 뒤집혀** 재조준했다(지우지 않았다):
*"등록은 전체화면으로 나간다"* → *"이 시트가 자기 폼을 갖지 않는다"*. 지키려던 값은 같다.

### 검증

tsc 0 · build 0 · 가드 99 · 셀러 시험 5파일 **111건** · **주입 29건 전부 되돌려-검증 빨간불**.
번들: `UserProfilePage` 108 → **112 KB**(+4), 위저드 **45 KB 는 별도 청크**(누를 때만) —
총 7.53/7.54 로 **추가 상향 없음**. critical-chunks 17 불변.

staging: **S-MYSELL-38~46** 신설(등록 6 · 숙소 1 · 도매 겸업 1 · 쿠폰 1).
