# 2026-09-25 — 셀러 핵심 업무를 소비자 앱 안으로 (설계 + 시안)

대표: *"생각해보니 지금 셀러대시보드를 그냥 우리 유어딜 서비스 페이지의 프로필 페이지에서 어떻게 안될까?
대규모 작업이지?"* → 범위 질의에 **"핵심 업무들은 다 하도록. 설계 먼저 하고 시안까지도."**

**등급: E1(작성됨).** 코드 미착수. 설계 SSOT + 시안만.

---

## 다음 세션의 첫 액션

1. **`docs/design/seller-in-consumer-app-2026-09.md` §0 을 먼저 읽을 것.**
   거기에 *내가 틀렸던 추정*이 적혀 있다. 그걸 안 읽으면 이미 있는 것(인증 다리)을 다시 짓는다.
2. 착수 전 **대표 판단 2건**(같은 문서 §9)이 남아 있다 — 단계 5(정산, 머니 경로)까지 갈 것인가 ·
   사장님 승계를 같이 갈 것인가.
3. 착수하면 **단계 1**(마이에 판매 카드)부터. 신규 화면 0 · 읽기만 · 위험 0.
   판정: 좌석이 있는 계정으로 `/user/profile` 을 열었을 때 판매 카드가 뜨고 숫자가 맞는가.

---

## 이번에 틀렸던 판단 (제일 값진 것)

대표 질문에 처음 답하면서 **"진짜 벽은 인증 축이다 — 셀러 API 28개 모듈이 별도 JWT 를 요구하니
그걸 다 고쳐야 한다"** 고 말했다. **측정해 보니 틀렸다.** 다리는 이미 라이브에서 돌고 있다:

```
resolveActorUserId(c)                          ← 소비자 세션 쿠키를 직접 읽는다
  → canOperateStore(DB, userId, sellerId)      ← owner(linked_user_id) | operator(seller_operators)
    → POST /api/seller/stores/:sellerId/token  ← 그 좌석의 셀러 JWT
GET /api/seller/my-stores                      ← 소비자 세션만으로 좌석 목록
```

⚠️ **왜 틀렸나**: 파일 개수(`seller_token` 138파일 · `requireSeller()` 38곳 · `/api/seller` 28모듈)를
보고 규모를 말했는데, 그 숫자들은 **전부 한 함수로 수렴**한다(`requireUserType` · `getSellerIdFromToken`).
그리고 *이미 만들어진 우회로*(좌석 토큰)를 안 보고 답했다.
**교훈: "몇 곳에 있나" 가 아니라 "몇 곳에서 결정되나" 를 세라.**

---

## 실측값 (2026-09-25)

| | |
|---|---|
| `/seller/*` 라우트 / 줄 | 68 / 32,359 |
| **핵심 넷**(홈·주문·이용권·정산) | **22라우트 · 54파일 · 9,060줄 · API 63개**(`/api/seller/*` 50) = 전체의 28% |
| `requireSeller()` 호출 | 38곳 → 정의 1곳(`requireUserType`) |
| `getSellerIdFromToken()` 호출 | 82곳 → 정의 1곳(`lib/seller-shared.ts`) |
| 라이브 셀러(도매 제외) | 11곳 · 승인 9 · **`linked_user_id` 있는 곳 1** |
| 라이브 좌석 | `seller_operators` **9행 / 4명** · user 35 = **5좌석** · user 3 = 2좌석 |
| 핵심 넷의 `dark:` | **사실상 0**(orders 0 · group-buy 0 · analytics 0 · settlements 1) |

**이 둘이 설계를 결정했다**:
- 다중 매장은 **가설이 아니라 현실** → 좌석 전환을 미룰 수 없다.
- 실제 경로는 소유가 아니라 **운영**(`seller_operators`) → 화면은 `GET /my-stores` 를 쓴다.
  `switch-to-seller`(= `linked_user_id` 전용)로는 그 4명의 9좌석이 **안 보인다**.

---

## 결정

**이식이 아니라 재구성.** 경계 한 줄:
> **폰에서 한 손으로 하는 일 = 소비자 앱 / 앉아서 하는 일 = 셀러 대시보드.**

- 위치 = **마이**(`/user/profile` → `/sell/*`). 유어샵(`/u/{handle}`)은 **손님 진열대**라 안 쓴다 —
  거기에 주문·정산을 얹으면 §5.5 소유권 단일화(CI 가드) 가 흐려진다.
- `/seller/*` 는 **살려 둔다**. 301 하지 않는다(운영 도구 40여 개 + 이미 공유된 URL).
- 신규 화면은 **다크를 지원**한다. `light-island` 로 흰 섬을 만들지 않는다.
- 단계: 1 판매 카드 → 2 `/sell` 홈+좌석 → 3 주문 → 4 이용권 → **5 정산(🔴 머니 · 단독 세션 + staging)**

---

## 완료분

| | |
|---|---|
| 설계 SSOT | `docs/design/seller-in-consumer-app-2026-09.md` |
| 시안(테마 토글) | `docs/design/assets/seller-in-consumer-app-mock.html` |
| 렌더본 | `…-light.png` · `…-dark.png` |
| 시안 목록 등록 | `docs/design/README.md` |

## 시안을 그리다 잡은 결함

판매 홈 '이번 주' 를 `[매출|수수료|받을 돈]` 3열로 그렸더니 **6자리 금액 셋이 붙었다**
(`1,240,00062,0001,178,000`). 430px 에서 각 열이 ~130px 인데 23px 숫자로는 안 들어간다.
2026-08-31 홈 카드의 *"6자리 가격에서 할인 pill 이 다음 줄로 밀린다"* 와 **같은 클래스**.
⇒ 구현 규칙: **판매 모드에서 금액 3개를 한 행에 균등 배치하지 않는다.** 3열은 *개수*에만.

## 대표 확정 (같은 날)

> *"디자인이 마음에 안들긴 해. 정산까지도 다 해주고, 주인 연결도 같이 가줘"*

- **정산 포함 · 주인 연결 동반** — 둘 다 범위 안. 그런데 재 보니 **둘 다 이미 만들어져 있다**:
  출금은 `POST /api/seller/account/withdraw`(+`withdraw-check`), 승계는 `store-codes`(`owner_claim`) ·
  `/store-claims` 4라우트 · `/store/find` · 어드민 `AdminStoreOwnerPage`. **기능이 아니라 자리가 없었다.**
  주인 연결은 **단계 2에 얹는다**(좌석 목록이 이미 `owner_claim_code` 를 내려준다 — 신규 API 0).
- **1차 디자인 반려** → 방향 3안 재작업. 갈리는 축은 *무엇이 주인공인가*:
  **A 숫자**(오늘 한 장) · **B 물건**(티켓 지갑 뒤집기) · **C 할 일**(큐).
  시안 `assets/seller-in-consumer-app-directions.html` · 렌더 `seller-directions-{light,dark}.png`.

### 1차 시안에서 내가 어긴 것

- **강조색 자리가 여덟이었다** — 규칙은 「강조색 하나, **자리 셋**」. 파란 게 많으면 아무것도 안 강조된다.
- 판매 홈에 **주인공이 없었다**(같은 무게 카드 4장 스택).
- 주문·이용권·정산이 **서로 구별이 안 됐다**(전부 흰 카드 + 리스트 행).
- **소비자 앱 같지 않았다** — 작은 대시보드로 보인다.

### 🩸 3안 시안이 또 하나 잡았다

`안 A/B/C` 태그를 `background: var(--ink); color: #fff` 로 그렸더니 다크에서 `--ink` 가 `#F5F3F1`
(거의 흰색)이라 **흰 면 위 흰 글자**가 됐다. `check-dark-contrast` 가 존재하는 바로 그 클래스.
⇒ **면 색이 토큰이면 글자도 토큰으로**(`--pagefg` 면에는 `--page` 글자). `#fff` 를 박으면 한쪽 테마가 깨진다.

## 🔴 대표 화면 실측 — 내 시안이 틀린 두 곳

대표가 실제 마이 스크린샷을 보내며 *"지금은 어떤 링크를 들어가는 구조잖어"* 라고 물었고,
그 화면이 둘을 바로잡았다.

1. **소비자 하단 탭은 5개다** — 홈 · 교환권 · **이용권(`/my-vouchers`)** · 유어샵 · 마이.
   내 1차·3안 시안의 마이 화면은 `이용권` 을 빠뜨리고 **4개**로 그렸다.
   ⚠️ `seller-in-consumer-app-mock.html`·`…-directions.html` 에 그 오류가 남아 있다(`seller-where-in-my.html` 만 교정).
   **그 그림을 치수 근거로 쓰지 말 것.**
2. **판매 진입점이 이미 있다** — `SellerSwitchInline`(123줄)이 프로필 이름 옆 칩으로 살아 있고
   셀러가 아니면 `내 가게 등록` 을 띄운다. ⇒ 단계 1은 *"새 카드를 만든다"* 가 아니라 **"그 칩이 자란다"**.
   새 자리를 만들면 진입점이 둘이 된다.

> 🧭 **교훈**: 화면을 코드로만 읽고 **한 번도 안 봤다.** `/user/profile` 은 `ProtectedRoute` 라 이 컨테이너에서
> 못 여는데, 그걸 "못 본다" 로 넘기고 소스에서 추론했다. **못 보면 대표에게 스크린샷을 먼저 청했어야** 했다.

## ⭐ 추천안 (대표 *"가장 이상적인 형태로 제안줘봐 시안이랑"*)

**“보는 건 마이에서 다, 하는 건 들어가서.”** 시안 `assets/seller-recommended.html`
· `seller-recommended-{dark,light}.jpg` · 설계 §13.

- **형태 1**, 단 마이 판매 카드가 **오늘 매출 + 확인할 주문 + 후기 인증 + 받을 돈**까지 전부 보여준다.
- 🔑 **탭바를 바꾸지 않는다** — 판매 화면에서도 하단은 홈·교환권·이용권·유어샵·마이 그대로고
  **마이가 켜져 있다.** 판매 안 이동은 **상단 알약**. ⚠️ **셀러 전용 하단 탭을 만들지 말 것**
  (1·3안 시안이 그렇게 그렸고, 그 순간 소비자 앱이 아니라 축소된 대시보드가 된다).
- **문법은 화면마다** — 마이·정산 = A(숫자) · 판매홈·주문 = C(할 일) · 이용권 = B(티켓).
  근거: 마이의 `내 딜 잔액 10,300딜` 카드가 **이미 A 문법**이라 그 이웃이 달라지면 어긋난다.
- **강조색 자리 셋** — 티켓 밴드 · 지금 눌러야 하는 버튼 · 처리 대기 숫자.
- 주인 연결은 **좌석 시트 안에서** 보인다(`사장님 미연결` + 줄 코드 — `my-stores` 가 이미 내려준다).

## ⭐⭐ 대표 확정 — 마이 안에서 다, 필수 작업만

> *"하는 것도 마이에서 하는걸로. 근데 필수적인 것들 선별해서"*

**§13(형태 1)은 §14 가 대체했다.** 시안 `assets/seller-in-my-final.html`
· `seller-in-my-final-{dark,light}.jpg` · 설계 §14.

- **페이지 이동 0.** 주문 확인·판매 on/off 는 **인라인**, QR·출금·가게 전환은 **시트**로 마이 위에.
- ⚠️ **`/sell/*` 별도 라우트를 만들지 않는다**(§13 계획 철회).
- **필수 4작업** — 네 기준(손님 앞 / 놓치면 손해 / 빈도 / **한 손**)으로 채점:
  QR 사용처리 · 주문 확인 · 판매 중지·재개 · 출금 신청.
  제외: 환불(사유·신중) · 이용권 등록(사진·옵션) · 분석/원장(표) · 쿠폰/파트너/알림톡.
  **"한 손" 이 실질적인 칼이다** — 폰으로 못 하는 건 넣어도 안 쓴다.
- 마이 **14 → 18블록**, 판매가 **맨 위**. 셀러가 아니면 **0블록**(지금 `내 가게 등록` 칩 그대로).
- 쓰는 API 전부 기존: `PUT /seller/orders/:id/status` · `POST /group-buy/vouchers/:code/use` ·
  `PUT /seller/products/:id` · `POST /seller/account/withdraw` · `GET /my-stores`.

### 단계

| | | 머니 |
|---|---|---|
| 1 | 마이 판매 섹션(오늘 카드 + 가게 전환) — `SellerSwitchInline` 칩이 자란다 | 없음 |
| 2 | 주문 확인 인라인 + 판매 on/off 토글 | 없음 |
| 3 | QR 사용처리 시트 (⚠️ 소각은 되돌릴 수 없다 — 시트가 먼저 경고) | 없음 |
| 4 | 출금 신청 시트 (가게 이름을 시트 안에 다시 적는다) | 🔴 단독 세션 + staging |

## 남은 결정 (대표)

**시안 승인 여부.** 승인되면 단계 1부터 착수한다.
