## 🎯 2026-07-29 (9차-f) — **인플루언서 DB 4축의 병목은 하나다: 측정 처리량이 유입의 1/5**

> 📌 **대표 방향 확정 (2026-07-29) — 다음 세션들은 이것만 한다.**
> **이메일 아웃리치는 하지 않는다.** 유어애즈에서 앞으로 할 일은 네 가지뿐:
> **① DB 수집 · ② 카테고리화 · ③ 필터링 · ④ 정보 최대 수집.**
> ⚠️ 이전 인계들에 남아 있는 `RESEND_API_KEY`·발송 큐·PIPA 관련 "대표 액션"은 **폐기**한다
> (2026-07-28 콜드 발송 결정도 이 방향 변경으로 **더 이상 추진하지 않는다**).

---

### 실측 — 4축을 한 번에 재보면 (2026-07-29 09:24 UTC)

| 축 | 값 | 판정 |
|---|---|---|
| ① 수집 | 총 **38,761** · 하루 ~1,900 · 유튜브 6,477 / 네이버블로그 **29,040(75%)** / 카페 3,142 | 잘 돈다 |
| ② 카테고리화 | 분류 36,442(94%) — 그런데 **`cat_keyword` 30,555(84%)** · `cat_content` 5,834(16%) | ⚠️ 신호가 약하다 |
| ③ 필터링 | platform·category·region·emailOnly (`buildSendQueueWhere`) + `PoolFilters.tsx`(#857) | 최근 보강됨 |
| ④ 정보 | 연락처 3,609(**9%**) · 이메일 2,376 · 브랜드태그 560(1.4%) · **네이버 미측정 26,673(91%)** | ⚠️ 비어 있다 |

### 🔑 핵심 — ②와 ④는 **독립 문제가 아니다**

`enrichNaverActivity` 는 **이미 본문 기반 분류를 한다**: RSS 글 제목을 모아
`classifyCategory(name, descForClass)` → `category_source = 'content'`.
연락처(이메일/인스타)도 같은 fetch(홈 HTML)에서 뽑는다.

⇒ **블로그를 한 번 측정하면 ②(본문 카테고리)와 ④(연락처)가 같이 채워진다.**
`cat_keyword` 84% 와 연락처 9% 는 **분류기나 추출기가 약해서가 아니라, 91%가 아직 측정된 적이 없어서**다.
**네 축의 병목이 하나로 모인다: 네이버 블로그 측정 처리량.**

### 📉 그런데 그 처리량이 유입을 못 따라간다 (이 세션의 최대 발견)

이 세션이 3.6시간에 걸쳐 4회 측정한 값으로 계산:

| 구간 05:47 → 09:24 (3.62h) | 증분 | 시간당 |
|---|---|---|
| 유입(`naver_blog`) | +367 | **101/시간** |
| 측정(`enrich_lane.total_measured`) | +71 | **20/시간** |
| **미측정 백로그(`nb_unmeasured`)** | **+482** | **+133/시간** |

**측정이 유입의 1/5이다.** 백로그 26,673 은 줄기는커녕 **시간당 133씩 벌어진다.**
(유입 101 보다 백로그 증가 133 이 큰 것은 유지보수 레인이 손상 핸들을 고치며 되돌리는 몫 — `handle.reopened`.)

⚠️ **내가 중간에 54/시간이라고 말했는데 틀렸다** — 짧은 구간(1시간) 하나로 계산한 값이었다.
4점 회귀로 다시 재니 **20/시간**이다. 처리량은 반드시 **여러 시점**으로 재라.

### 🔧 왜 20/시간인가 — 라운드가 완주를 못 한다

09:00 라운드 자체는 건강하다(#857 의 `NAVER_CONCURRENCY=3` 효과):

| 지표 | 08:00 | 09:00 |
|---|---|---|
| `naver.tried` | 3 | **13** |
| `deadline_hit` | true | **false** |
| `spent` | 25/45 | **44/45**(예산 소진) |
| `elapsed` | 20.3s | **9.3s** |
| `failed` | 0 | **0** |

라운드당 13건 × 시간당 12라운드 = **156/시간**이 나와야 하는데 실제는 20/시간 →
**약 1.5라운드만 완주**한다는 뜻이다. ~~원인은 #847 이 고친 것과 같다: 부모의 `kick` 이 드라이버
응답을 기다려, 부모가 회수되면 남은 라운드가 통째로 날아간다.~~
🔴 **이 원인 추정은 아래 10:00 실측으로 기각됐다** — #847 배포 후에도 1라운드에서 멈춘다.

### 🔴 예측은 **빗나갔다** — 10:00 UTC 실측 (같은 세션 후속)

| 10:00 틱 | 값 |
|---|---|
| 라운드 1회 diag | `tried 13 · measured 13 · contacts 13 · emails 8 · failed 0 · spent 44/45 · elapsed 16.0s` |
| 드라이버 하트비트 | `ads:enrich-influencer-driver · ok:true · **ms 18,615**` |
| 09:24 → 10:09 (0.75h) | 유입 +34 · 미측정 백로그 **+21** ⇒ 측정 ≈ **13건** |

**12라운드를 계획했는데 드라이버가 18.6초 만에 끝났다.** 라운드 1회가 16초니 **한 라운드만 돌았다**는 뜻이다.
백로그 산술도 같은 답을 준다(0.75시간에 13건 = 그 한 라운드 분량). ⇒ **여전히 ~20/시간.**

⚠️ **내가 "#847 이면 156/시간" 이라고 적은 것은 틀렸다.** #847 은 *부모의 수명 결박*을 풀었고 그건
실제로 풀렸다(레인 8→15개 발화). 하지만 **라운드 체인이 1라운드에서 멈추는 별개의 원인**이 남아 있었고,
나는 그걸 확인하지 않은 채 "부모 결박만 풀면 12라운드가 다 돈다"고 가정했다.

**그리고 왜 멈췄는지가 어디에도 없었다** — `runRoundChain` 은 첫 실패의 원문 error 를 들고 돌아오는데
`driverBeat` 이 그걸 안 받고 `ok`/`ms` 만 남겼다. `ok:true` 라 화면상 정상이었다(done>0 이면 ok).
이 레포가 반복해 만난 형태: **실패가 아니라 조용한 부분 실행.**

**처방(이 세션에서 배선)**: 드라이버가 `{done, planned, partial, error}` 를 하트비트에 남긴다.
요약기(`summarizeResult`)가 24자 넘는 문자열을 **통째로 버리던 것**도 함께 고쳤다 —
길어지는 값은 대개 `error` 라, 하필 가장 필요한 값만 증발하는 규칙이었다(자르되 버리지 않는다).

### ✅ 다음 세션 첫 액션 — 이제 기록이 답을 말한다

1. **`/api/admin/cron-heartbeats` 의 `ads:enrich-influencer-driver` 행의 `result` 를 읽어라.**
   `done=1 planned=12 partial=true error=round2: …` 형태로 **멈춘 지점과 이유**가 찍힌다.
   - `Too many subrequests` 계열이면 → 서비스바인딩 체인이 부모 예산을 공유한다는 뜻이다.
     처방은 라운드를 **다음 정각으로 분산**하거나(틱당 1라운드 + 레인 수 늘리기),
     건당 서브리퀘스트를 2→1 로 줄이는 것(연락처가 이미 있는 리드는 홈 fetch 생략).
   - 시간/CPU 계열이면 → 라운드 수를 줄이고 틱을 쪼개는 방향.
   ⚠️ **어느 쪽인지 확인하기 전에 `ADS_INFLUENCER_ENRICH_ROUNDS` 를 올리지 마라** — 1라운드에서
   멈추는 원인이 그대로면 20으로 올려도 **여전히 1라운드**다(숫자만 커지고 아무것도 안 바뀐다).
   ~~예측: 20/시간 → ~156/시간~~ 🔴 위 실측으로 기각. `ADS_INFLUENCER_ENRICH_ROUNDS` 20 상향도
   **1번이 풀린 뒤**에나 의미가 있다. `NAVER_CONCURRENCY` 상향은 `naver.failed` 가 0을 유지할 때만.
2. **측정 진도와 `cat_content` 비율을 같이 기록해 인과를 확인할 것** — ②는 따로 고칠 게 없다는
   가설(측정이 따라잡으면 `cat_content` 가 자동으로 오른다)의 검증이다. 안 오르면 그 가설이 틀린 것이다.
3. **이번에 배선한 RSS 신호의 diag 카운터**(`rss_cat`/`rss_intro`/`rss_emails`/`cat_body`)를 확인.
   계속 0 이면 네이버 RSS 에 그 필드가 안 오는 것 — 추측으로 파서를 더 손대지 말고 그 경로를 접을 것.
   (작성 환경이 `rss.blog.naver.com` 프록시 차단이라 응답 실물을 못 봤다. 이 카운터가 유일한 판정 근거다.)

### ⚠️ 아직 답 없는 것 (다음 세션이 판단)

- **유입 101/시간이 전부 값어치 있는가?** 측정이 못 따라가는 상태에서 수집만 늘리면
  백로그만 커진다. 수집 키워드의 **저품질 축**을 줄여 유입을 낮추는 것도 레버다
  (지금은 아무도 이 균형을 안 보고 있다).
- **측정 순서가 FIFO 다** — `ORDER BY perf_checked_at ASC`(미측정 우선, 그 안에선 수집순).
  유입이 계속 앞서는 한 **무엇을 먼저 재느냐**가 중요해진다(점수 높은 순 등).
  ⚠️ 단, 그 정렬은 인덱스(`idx_ad_inf_leads_perf`)와 형태를 맞춘 것이라 바꾸면 **29k 풀스캔**이 된다
  (#857 이 그 문제를 이미 겪고 지금 형태로 고쳤다). 인덱스를 같이 설계하지 않고 손대지 말 것.

### 이 세션의 완료분

- PR **#835**(`77008bf`) 보강 레인 예산/격리 · **#846**(`3071827`) 인계 세션별 분리 ·
  **#847**(`d581660`) cron 관측·격리 6종.
- PR **#864** — 대표 4축 확정 반영 + ②④ 실제 개선:
  ① 이미 받은 RSS 에서 **자기분류·소개글·본문**을 더 추출(추가 fetch 0. 서브리퀘스트가 천장이라
     *이미 산 데이터를 더 쓰는 것*이 처리량을 안 건드리고 품질을 올리는 유일한 방향)
  ② 어드민에 **분류 신뢰도·측정 여부 필터**(84%/91% 를 화면에서 골라낼 수 없어 품질 작업의 대상
     집합 자체를 특정 못 하던 것)
  ③ 드라이버 **부분 실행 관측**(위 🔴 절)

### 🧨 이번에 내가 틀린 것 (다음 세션이 같은 오진을 반복하지 않게)

1. **"#847 이면 156/시간"** — 부모 결박이 풀리면 12라운드가 다 돈다고 **가정**했다. 실제로는
   1라운드에서 멈추는 별개 원인이 남아 있었다. *한 병목을 고쳤다고 다음 병목이 없는 게 아니다.*
2. **처방을 실측 없이 예고했다** — 예측을 적을 때 "무엇을 보면 판정되는가"는 적었지만
   "그 판정이 어긋나면 무엇을 의심할 것인가"는 안 적었다. 그래서 빗나갔을 때 다시 처음부터 팠다.
3. (07-29 이전 항목) **54/시간** — 1시간 창 하나로 계산. 4점 회귀로는 20/시간이었다.
   **처리량은 반드시 여러 시점으로.**

- 상세 경위·틀린 판단 6건: `docs/handoff/2026-07-29-ads-lane-cadence.md`

---

## 🧪 11:00 UTC 틱 — **판정 보류**(내가 성급히 결론 낼 뻔한 자리)

**관측(12분 후 재확인까지 동일)**:

| 항목 | 값 |
|---|---|
| `enrich_lane.last_run` | **10:00:18** (11:00 틱이 안 남김) |
| `total_measured` | 797 (변화 0) |
| 풀 카운터 전부(`total`·`nb_unmeasured`·`cat_content`·`with_contact`) | **+0** |
| 11:00 하트비트 | `ads:scheduled`·`collect`·`enrich-influencer-driver` 등 **전부 `ms=0`** |

즉 **11:00 틱은 아무 일도 안 했다**(수집·보강 둘 다). 10:00 틱은 13건 측정 + 52건 수집이었으니 후퇴다.

### ⚠️ 여기서 "self-chain 이 수집을 죽였다"고 쓸 뻔했다 — 틀릴 뻔했다

배포 이력을 보고 멈췄다:

```
deploy-ads(ur-ads) 마지막 실행:  10:59:26 시작 → 11:00:12 완료
cron 틱:                        11:00:02
```

**틱이 배포 창 안에서 발화했다.** 워커 교체 중이면 detach 된 `waitUntil` 작업이 통째로 죽는 것이
당연하다. 그러면 관측된 모든 것이 설명된다 — 코드 결함 없이도.

⇒ **두 가설이 아직 살아 있다**:
- **(a)** 즉시응답 + `waitUntil` 조합에서 서비스바인딩 하위 인보케이션의 detach 작업이 폐기된다.
- **(b)** 11:00 틱이 **배포와 겹쳐** 죽었다(일회성).

**(b) 가 타이밍상 충분한 설명이므로 (a) 를 단정하면 안 된다.** 오늘 이미 두 번 같은 방식으로 틀렸다
(#847 예측 · 40분 간격 표본). 세 번째를 만들지 않는다.

### ✅ 다음 세션 첫 액션 — **12:00 UTC 틱이 깨끗한 시험대**

배포가 겹치지 않은 틱에서 다시 보라. 판정표:

| 12:00 틱 관측 | 결론 | 다음 행동 |
|---|---|---|
| `last_run` 12:00 + `total_measured` **+150 근처** | self-chain 성공 | `ADS_INFLUENCER_ENRICH_ROUNDS` 20 검토 |
| `last_run` 12:00 + **+13 근처** | 1라운드만 — 체인이 안 이어짐 | 드라이버 `result` 의 `chained` 를 볼 것 |
| `last_run` 여전히 옛날 + 카운터 +0 | **(a) 확정 — 즉시응답이 작업을 죽인다** | 라운드를 응답 **전에** await 하도록 되돌려 최소 1라운드 복구 |

⚠️ 세 번째 줄이면 **라이브 데이터 수집이 0** 이라는 뜻이다 — 네 축 전부가 멈춘다. 최우선.
⚠️ 배포가 또 겹쳤는지 **먼저** 확인할 것(`deploy-ads` 실행시각 vs 틱 시각). 이게 이번 교훈이다.

**보조 신호**: `ads:collect` 의 `result=started=true · ms=0` 은 #869 의 "관측을 앞으로" 설계라
그 자체로는 실패 증거가 아니다. 착각하지 말 것.

> ✅ **결론 났다(다른 세션 #874)** — 위 두 가설 중 **(a) 가 맞았다.** '즉시 응답 + waitUntil' 이
> 서비스바인딩 하위 인보케이션의 detach 작업을 폐기시켜 **라운드가 0회**가 됐고, #874 가 수리했다.
> 배포 겹침(b)은 그 위에 겹친 우연이었다. 판정을 미룬 것 자체는 옳았다 — 다만 **다른 세션이 같은 시간에
> 같은 곳을 파고 있었다.** 착수 전 `origin/main` 확인이 왜 룰인지의 또 하나의 사례.

## 🔴 11:00 실측 — 내가 낸 회귀(#863)와 그 수리 (세션 후반)

### 다음 세션의 첫 액션 (이게 이 문서에서 제일 중요하다)

12:00 이후 틱에서 **어드민 하트비트의 `ads:enrich-influencer-driver` 의 `depth`** 를 봐라.
(`GET /api/admin/cron-heartbeats` — 접속 절차는 CLAUDE.md "어드민 진단 접근".)

| `depth` | 판정 | 처방 |
|---|---|---|
| **0** | self-chain 이 depth 1 로 못 넘어간다 | 라운드를 체인이 아니라 **cron 이 직접 N번 kick** — 루트가 수명을 쥔다 |
| **1 이상** | 체인 생존 | 남은 일은 처리량(라운드 수·라운드당 건수) 튜닝 |

같이 볼 것: `run.last_run` 이 그 시각으로 갱신되는가(수집 완주) · `run.picks.from_cursor`(0 이면
커서 순환이 구조적으로 안 도는 것) · `learned_cap` 이 유기 감지로 내려갔는가.

### 무슨 일이 있었나

`dispatchRoundChain` 을 "**즉시 응답** + 라운드는 `waitUntil`" 로 바꿨다(#863). 부모(`kick` 의
`await SELF.fetch`)를 빨리 풀어 주면 수명 문제가 사라진다고 봤다. **결과는 라운드 0회.**

11:00 틱 실측:
- `ads:enrich-influencer-driver` 11:00:02 · ok · result=null → **즉시 응답 경로가 실제로 돌았다**
- `ads:collect` 11:00:02 · `"started=true"` → 시작은 했다
- 9분 뒤 `enrich_lane.last_run`=10:00:18 · `run.last_run`=09:00:04 → **아무것도 안 끝났다**
- 직전(구 코드) 10:00 틱은 최소 1라운드를 돌렸다

**서비스 바인딩 피호출자는 호출자보다 오래 못 산다.** 응답을 앞당길수록 호출자의 `await` 이 먼저
풀리고 그 순간 피호출자의 `waitUntil` 작업이 취소된다. 즉시 응답은 (A)를 고친 게 아니라 **작업을 지웠다.**

### 수리 (PR #874)

1. **라운드를 응답 전에** 끝낸다. 다음 라운드 spawn 만 `waitUntil`(ACK 만 기다림). 부모가 잡히는
   시간은 전체 체인이 아니라 1라운드(~16초)라 main 의 cadence 불변식 의도도 지켜진다.
2. **하트비트를 라운드마다** 남기고 `depth` 동봉 — 위 표의 판정 근거. 코드 모양으로는 체인 생존을
   알 수 없어서 **관측점을 심었다**(추측 대신).
3. **말없이 죽은 회차도 상한을 내린다** — 자기교정의 하향이 `hitLimit`(잡히는 예외)에만 걸려 있어서,
   인보케이션째 사라지는 실패에서는 상한이 오히려 `+2` 올라가는 닫힌 고리였다. 정상 종료는 lease 를
   `'0'` 으로 반납하므로, **반납 안 된 lease = 말없이 죽었다는 유일한 흔적**이다
   (`acquireLeaseDetect` → `capAfterAbandonedRun`, ×0.8·바닥 25, 추가 쓰기 0).
4. **재조우 보강 스킵** — 저장이 빈 칸만 백필하므로 이미 연락처가 있는 리드의 보강 fetch 는 통째로
   버려진다. D1 조회 1회로 최대 8(YT)/5(네이버) fetch 를 없앤다. **그 조회도 예산에서 뺀다**(D1 도
   서브리퀘스트 — 아끼자고 만든 장치가 #784 의 비대칭을 다시 파면 안 된다).
5. 제휴 제안 문안 두 벌 통합(법정 표기 SSOT) — 실사용 경로(화면)에 `(광고)`·전송자 정보가 없었다.

### 🧨 이번에 내가 틀린 것 (앞 목록에 이어서)

4. **"즉시 응답이 부모 결박을 푼다"** — 푸는 게 아니라 **자식을 죽인다**. 서비스 바인딩의 수명 규칙을
   확인하지 않고 "부모를 빨리 풀어주면 좋다"는 직관으로 갔다. 그 직관은 *호출자가 계속 산다*는
   숨은 전제 위에 있었고, 그 전제가 틀렸다.
   → **교훈**: `waitUntil` 로 떼어낸 작업은 "누가 이 인보케이션을 살려 두는가"를 먼저 답해야 한다.
5. **불변식 테스트를 '호출 형태'로 썼다** — `mail_subject: outreachSubject(` 를 검사했더니, 바로 다음
   커밋에서 그 매핑을 파일크기 때문에 모듈로 옮기자 빨강이 났다. 지키려던 건 *응답에 문안이 실린다*는
   사실이지 조립 위치가 아니다. → **불변식은 사실로, 구현 위치가 아니라.**
6. **가드 출력을 `grep` 으로 걸러 요약만 봤다** — `audit-gate | grep "ALL GREEN"` 로 통과를 확인했는데
   실제로는 file-size 가 601줄을 잡고 있었고 CI 에서 막혔다(가드 자체는 정상 — 재현 확인함).
   → **게이트 출력은 걸러서 보지 마라.** 이 레포의 사고 다수가 "실패가 아니라 안 보임"이었다.

---

## 🟢 13:00 실측 — 체인 생존 확인, 그리고 **네 번째 꼬리 굶주림** (세션 후반 2)

### 다음 세션의 첫 액션

14:00 이후 틱에서 **`enrich_lane.naver.tried`** 하나만 보면 된다.

| 값 | 판정 | 다음 |
|---|---|---|
| **> 0** | 선두 교대가 먹혔다 | `nb_unmeasured` 가 **감소 전환**하는지 추적(현재 26,926, 아직 증가 중) |
| **0 (selected > 0)** | 여전히 굶는다 | 라운드 마감(`ADS_ENRICH_DEADLINE_MS`, 기본 20s)이 **한 건(최대 ~16s)보다 너무 짧은 것**을 의심. 상한이 아니라 **창 자체**를 늘려야 한다(대표 env). |

### 확정된 것

- **self-chain 은 산다** — `enrich_lane.depth: 2`. 준비해 뒀던 "cron 이 라운드를 직접 kick" 전환은 **불필요**하다.
  같은 틱 측정 +67(직전 +14). ⚠️ 단 `depth` 는 **레인 스냅샷**에서 봐라 — 하트비트는 부모가 덮어써 항상 null 이다.
- **커서가 돈다** — `picks.from_cursor: 1`(직전 0). 픽 번갈이 + YT 슬롯 멤버십 게이트가 먹었다.
- **카페는 대표가 껐다**(행동으로 확인) — `cafe.found 0` 인데 체인상 **더 뒤에 있는** tistory 가 `found 1`.
  예산 소진이면 tistory 가 먼저 굶는다. `picks.processed` 도 2→3.

### ❌ 아직 안 되는 것: 블로거 측정 0건

`naver { selected: 12, tried: 0 }` — **사전 마감(`frontStageDeadline`)을 넣은 뒤에도 그대로**.
`elapsed 20.8s · deadline_hit · spent 19/45`(예산은 절반 넘게 남음).

**왜 몫 보장이 안 통했나**: 중단은 **건 사이에서만** 일어난다. YT 한 건이 마감 직전에 시작해
타임아웃(~9s)을 물면 창을 넘긴다. **상한을 낮춰도 같다** — 한 건이 창보다 길 수 있다.
⇒ 처방을 바꿨다: **홀수 라운드는 블로거가 먼저**(마감 전체 사용). 자원을 나누는 대신 자리를 바꾼다.

### 🧨 오늘의 관통 패턴 — **줄을 세우면 꼬리가 굶는다**

| # | 어디 | 못 받은 자원 | 처방 |
|---|---|---|---|
| ① | 보강 레인 마지막 track | 예산 | 잔여 승계(`naverRoomFromRemaining`, 선행 세션) |
| ② | 같은 자리 | 시계 | 사전 마감(`frontStageDeadline`) |
| ③ | 키워드 픽 꼬리(커서) | 순번 | 번갈이 배치 + YT 슬롯 멤버십 게이트 |
| ④ | 같은 자리(②가 실패) | 시계(원자적이지 않음) | **선두 교대** |

**교훈**: 몫 보장은 "한 단위가 몫보다 작을 때만" 통한다. 단위가 몫을 넘을 수 있으면 **순서를 돌려야** 한다.

### 🧨 이번에 내가 틀린 것 (앞 목록에 이어서)

7. **"자기링크 26k행"** — 26,694 는 `nb_unmeasured`(미측정 블로거)이고 자기링크와 **무관**하다. 두 숫자를 섞었다.
   `?platform=naver_blog&hasContact=1` 의 **`total: 1,029`** 가 실제 상한이다.
   → **교훈**: 규모를 말하기 전에 `total` 을 찍어라. 목록 API 는 필터별 `total` 을 준다.
8. **게이트 출력을 `grep` 으로 걸렀다** — `audit-gate | grep "ALL GREEN"` 로 통과를 확인했는데 file-size 가
   601줄을 잡고 있었다. 그리고 그 다음이 더 나쁘다: 커밋 메시지에 `[SKIP_SIZE]` 를 **"미사용"이라고 적으면서**
   그 토큰을 그대로 써서 **로컬 가드가 그 뒤로 계속 꺼졌다**(`.git/COMMIT_EDITMSG` 는 *마지막* 메시지다).
   → 가드를 고쳤다(전수·PR 모드는 그 파일을 안 읽는다). **게이트 출력은 전문으로 볼 것.**
9. **계측을 하트비트에 실었다** — 같은 이름에 부모(`kick`)가 자식보다 **나중에** 쓰므로 항상 덮인다.
   판정 창은 이름을 다투지 않는 곳(레인 스냅샷)이어야 한다.
10. **11번째 슬롯을 그냥 덧붙였다** — 정비 배정표의 최대 간격이 10h→13h 로 벌어져 경보가 무뎌졌다(유닛이 잡음).
   24 의 약수(12)로 맞춰야 슬롯이 하루 정확히 2회 돈다.

### 이 세션 후반 완료분

- **#874** 회귀 수리(라운드 0회) · 재조우 스킵 · 제휴 문안 SSOT
- **#877** 카페 수확 분리 관측(`diag.cafe`)
- **#879** 사전 마감 · `depth` 관측 위치 이동 · 픽 번갈이 · DDL 분리 · 가드 우회 누수 차단
- **#885**(진행) 자기링크 판정 SSOT + 정리 패스(`selflink`) · 정비 배정 12슬롯 · **선두 교대**

---

## ✅ 12:00 UTC 틱 — **판정 완료** (깨끗한 시험대였다)

**0. 배포 겹침 없음** — 11:00 은 전 레인이 `ms=0` 이었는데 12:00 은 23~28초로 정상 완주.
(`ads:enrich-company` 는 `planned=8 depth=1 chained=true` 를 남겼다 — #874 의 self-chain 이 실제로 돈다.)

**1. 결과 — 판정표의 '1라운드 근처'** : `last_run 12:01:03` · `total_measured 797 → 811(+14)`.

**그런데 판정표가 예상 못 한 것이 나왔다. +14 는 전부 유튜브고 블로거 측정은 0 이다.**

```
마지막 라운드: yt 14 · naver { selected: 13, tried: 0 }
               spent 18/45 · elapsed 23.4s · deadline_hit true
```

블로거 단계가 **대상 13건을 골라 놓고 첫 fetch 도 못 했다.** 예산은 27이나 남았다 —
**막은 것은 예산이 아니라 시간**이다. YT 가 20초 창을 통째로 썼다.

### 🔁 이건 이미 한 번 난 사고다 (레인을 건너 반복)

`enrich-lane.ts`(파트너풀) 2026-07-28 주석:
> *"진짜 병목은 서브리퀘스트가 아니라 시간인데, 비율 게이트는 예산만 보고 시간을 안 봤다."*

인플루언서 레인은 예산 배분만 정교하게 다듬고(`planInfluencerEnrich`·`naverRoomFromRemaining`)
**벽시계는 아무도 안 나눴다.** 백로그 26,696건(네이버의 91%)이 전부 그 단계 뒤에 있다.

**수리**: 같은 시각 **다른 세션이 #880 으로 먼저 고쳤고, 그쪽이 더 낫다 — 내 것을 버리고 그걸 취했다.**
- 내 `ytPhaseDeadline` 은 **YT 만** 묶었다. 앞 레인은 [링크인바이오 → YT] 둘인데,
  링크인바이오가 느린 회차엔 여전히 블로거가 굶는다.
- #880 의 `frontStageDeadline` 은 **앞 레인 전체**에 사전 마감을 씌우고, 프레이밍도
  "YT 에 얼마를 줄까"가 아니라 **"블로거에게 최소 몇 %를 보장할까"**(floor) 다 — 굶는 쪽을
  기준으로 잡는 것이 맞다. env(`ADS_ENRICH_NAVER_FLOOR_PCT`, 기본 40%)로 조정도 된다.

⚠️ **오늘 네 번째 중복 개발이다**(#840·#857·`CLASSIFY_RULES_VERSION`·이번 벽시계).
같은 실측을 보면 같은 곳을 판다 — 착수 전 `origin/main` 확인 + `check-branch-overlap` 을
믿을 것. 이번엔 내가 만든 것을 버리는 쪽이 옳았다(더 나은 구현이 이미 있었다).

**3. RSS 카운터(`rss_cat`·`rss_intro`·`cat_body`)는 아직 판정 불가** — 블로거 단계가 0회 돌아서
찍힐 기회가 없었다. 위 수리가 배포된 뒤 첫 틱에서 확인할 것.

### 📌 다음 세션 첫 액션
1. `enrich_lane.naver.tried` 가 **0이 아닌지** — 이 수리의 유일한 판정 기준.
2. 그 다음에야 `rss_cat`/`rss_intro`/`cat_body` 를 본다(0 이면 네이버 RSS 에 필드가 없는 것 → 파서 손대지 말 것).
3. `total_measured` 시간당 증가를 **두 틱 이상** 재라(한 틱으로 계산하지 말 것 — 이 세션이 두 번 틀렸다).

### 🧬 발견 — `ensureDiscoveryKeywords` 등 키워드 헬퍼가 **두 벌**이다 (미해결)

`influencer-auto-collect.ts` 와 `influencer-keyword-store.ts` 가
`ensureDiscoveryKeywords` · `listDiscoveryKeywords` · `addDiscoveryKeyword` 를 **각자 정의**한다.
정규화 비교로 세 함수 **본문은 동일**했다.

⚠️ 그런데 `ensureDiscoveryKeywords` 는 각자 파일의 `KW_DDL`·`SEED`·`REGION_SEED`·`BANGBAE_SEED`
상수를 참조한다 — **그 시드 목록이 같은지는 확인하지 않았다.** 시드가 갈리면 어느 쪽이 먼저 실행되느냐에
따라 활성 키워드 집합이 달라진다(수집 축이 조용히 바뀐다).

**지금 안 고친 이유**: ① 시드 동일성 미확인 ② 같은 파일을 다른 세션이 활발히 고치는 중(오늘만 4 PR).
**다음 세션이 할 것**: 두 파일의 시드 상수를 실제로 비교 → 같으면 store 를 SSOT 로 두고 auto-collect 는
re-export 로 (약 55줄 감소 + 드리프트 위험 제거). 다르면 **어느 쪽이 라이브에 반영돼 있는지부터** 판정.

---

## 🚫 13:00 틱 — **또 배포와 겹쳤다** (판정 불가 · 세 번째)

```
ur-ads 배포:  12:59:55 → 13:00:37
cron 틱:      13:00:0x            ← 배포 창 안
```

관측(`yt 14 · naver { selected 12, tried 0 } · spent 19/45 · elapsed 20.8s · deadline_hit`)만 보면
"#880 의 바닥이 부족하다"로 읽힌다. **나는 실제로 그렇게 읽고 순서를 뒤집으려 했다** —
블로거를 맨 앞에 두어 신선한 창을 주는 변경까지 작성했다가, 배포 이력을 보고 되돌렸다.

⚠️ **#880 의 바닥은 12:31 부터 라이브다. 12:00 회차는 그 이전이고 13:00 회차는 배포와 겹쳤다.**
⇒ **아직 #880 을 깨끗하게 시험한 회차가 없다. 14:00 이 첫 시험대다.**

### 왜 이 기록이 중요한가

배포 겹침이 **세 번째**로 측정을 오염시켰다(11:00 · 13:00 · 그리고 09~11시 구간 전반).
이 레포는 유어애즈 PR 을 하루에 10회+ 머지하고, 각 머지가 ur-ads 를 재배포하며,
cron 은 매시 정각이다 — **정각 회차가 배포에 잘릴 확률이 구조적으로 높다.**

측정 전 `deploy-ads.yml` 실행시각 확인은 선택이 아니라 **필수 절차**다.

### 다음 세션 첫 액션 — 14:00 이후 회차

1. **먼저 배포 겹침 확인**(`deploy-ads.yml` 실행시각 vs 정각). 겹쳤으면 다음 정각으로 미룰 것.
2. `enrich_lane.naver.tried` 가 **0이 아닌가** — #880 의 유일한 판정 기준.
3. `tried` 가 여전히 0 이면 그때 **순서 뒤집기**를 검토하라(아래 근거·설계는 이미 정리돼 있다):
   - 사전 마감은 *"언제까지 새 일을 시작하나"* 만 정하고 *"언제 끝나나"* 는 못 정한다.
     앞 레인의 마지막 fetch 가 마감 직전에 출발하면 타임아웃(8s)까지 달려 바닥을 통째로 먹는다.
     ⇒ 바닥이 **명목상**일 수 있다.
   - 처방: 굶는 레인을 **맨 앞**에 둬 t=0 의 신선한 창을 준다. 백로그가 그 순서를 지시한다 —
     블로거 미측정 **26,926** vs 유튜브는 6,495 중 6,338(97%)이 이미 규모 보유 · 건당 1 fetch.
   - ⚠️ 그때 `ads-invocation-lifetime.test.ts` 의 `복원이 블로거 호출 앞에 온다` 불변식이
     **현 설계를 잠그고 있다.** 순서를 바꾸려면 그 불변식도 함께 갱신해야 하고,
     **그건 다른 세션의 설계를 뒤집는 것이므로 실측 근거(깨끗한 회차)를 반드시 먼저 확보할 것.**

---

## 🎯 14:00 — 첫 클린 회차의 답: 바닥이 아니라 **바닥을 읽는 쪽**이 없었다

`deploy-ads` 마지막 실행 13:44:50→13:45:36. 14:00 정각과 **14분 간격** = 오늘 처음으로 오염 없는 회차.

```
last_run 14:01:08 · depth 0 · elapsed 28,095ms   ← 창은 20,000ms
naver { selected: 12, tried: 0 } · spent 19/45 · deadline_hit true
```

**#880 의 바닥은 듣지 않았다.** 그리고 원인은 "바닥이 부족해서"가 아니었다 — 위 §다음 액션 3번이
예상한 *"마지막 fetch 가 마감 직전에 출발해 타임아웃까지 달린다"* 도 아니다. 더 단순하다:

> **바닥은 `budget.deadline` 인데, 앞 단계(`enrichYouTubePerformance`)가 그 값을 한 번도 읽지 않았다.**

세 루프가 전부 `budget.left` 만 봤다(채널 배치 · 채널별 playlistItems · 영상 통계 배치).
건당 fetch 타임아웃은 **10초**라 채널 두세 개면 12초 사전 마감을 그냥 넘긴다.
형제 단계인 링크인바이오(`enrichPoolFromLinkInBio`)는 처음부터 마감을 봤다 — **비대칭이었고,
하필 마감을 안 보는 쪽이 느린 단계였다.**

### 같은 버그의 두 번째 피해 — 체인 수명

| 회차 | elapsed | 도달 depth | 완료 라운드 |
|---|---|---|---|
| 13:00 | 20.8s | 2 | 3 |
| 14:00 | **28.1s** | **0** | **1** |

라운드가 창을 넘기면 그 라운드가 **체인 수명을 통째로 먹어** 뒤 라운드가 아예 안 생긴다.
계획은 `ADS_INFLUENCER_ENRICH_ROUNDS=12` 인데 실제 도달은 1~3 이다 —
**9~11 라운드는 한 번도 존재한 적이 없다.** 그동안 여러 세션이 "라운드를 12로 늘렸다"를
처리량 근거로 인용해 왔는데, 그 근거는 실재하지 않았다.

### 이번 시간에 블로거는 몇 명 늘었나 — **0명**

```
total_measured 878 → 892 (+14)   ← 전부 yt. naver.measured 는 0
nb_unmeasured  26,926 → 27,093   ← 오히려 +167 (수집이 측정보다 빠르다)
```
`total_measured` 는 `naver.measured + yt` 합이라 **유튜브만으로도 올라간다.** 이 값이 오르는 걸
"백로그가 줄고 있다"로 읽으면 안 된다 — 실제로 오늘 그렇게 읽힐 뻔했다.

### 수리 (이 세션)

`influencer-performance.ts` 에 `outOfTime(budget)` 를 세 지점에 배선:
진입 가드(시간 끝났으면 D1/DDL 도 안 건드림) · 채널별 루프 · 영상 통계 배치 루프.
시간으로 멈춘 건은 **예산으로 멈춘 건과 같은 취급**(`budgetSkipped`) — 스탬프를 찍으면
"측정했더니 0" 으로 각인돼 재선택에서 빠지는, 이 파일이 반복해 지켜온 불변식을 유지한다.

가드는 **행동 테스트**다(`ads-yt-deadline.test.ts`, 가짜 시계 + fetch 카운트).
소스 검사로 하면 "조건은 있는데 항상 거짓"을 못 잡는다(이 레포의 gzip 예산 0 사건과 같은 형태).
⚠️ **수리를 빼면 2개가 빨간불** 되는 것까지 확인했다.

### main 의 처방(#885 선두 교대)과의 관계 — 서로를 보완한다

같은 시간 다른 세션이 홀수 라운드에 **블로거를 먼저** 돌리는 교대를 넣었다(`naverFirst = depth % 2 === 1`).
그 판단("몫 보장이 실패했으니 자리를 바꾼다")은 옳았고, **이 세션의 발견이 그 실패의 이유를 설명한다** —
보장한 몫이 *마감*이었고 그걸 읽는 쪽이 없었다. 둘을 합치면 짝수 라운드도 살아난다.
⇒ 순서를 통째로 뒤집을 필요는 **없다**. `ads-invocation-lifetime.test.ts` 의 불변식은 건드리지 않았다.

### 다음 세션 첫 액션

1. 이 수리 배포 후 **배포와 안 겹친 정각**에서 `enrich_lane`:
   - `naver.tried > 0` 인가(짝수 라운드가 살아났는가)
   - `chain.rounds` vs `chain.rounds_planned` — 라운드가 1~3 에서 늘었는가(수명 회복)
   - `elapsed_ms` 가 창(20,000) 안으로 들어왔는가 ← **이게 선행 지표다**
2. `nb_unmeasured` 가 **줄기 시작하는가**. 유입(시간당 ~100)보다 측정이 빨라야 백로그가 흐른다.
   여전히 늘면 병목은 라운드가 아니라 **수집 속도 대비 측정 단가**다(건당 2 fetch × 8s).
3. 그 뒤에야 `ADS_ENRICH_DEADLINE_MS`·라운드 수 조정을 논할 것. **지금 창을 키우면 라운드가 줄어든다.**

### 이번에 틀렸던 판단

- 13:37 측정을 "틱 결과"로 읽을 뻔했다 — `last_run 13:01:36` 이고 아직 14:00 전이었다.
  **같은 정각 안에서 두 번 재는 것은 측정이 아니다**(오늘 두 번째로 밟은 함정).
- `depth 2`(13:00) 한 장으로 "이 회차가 0명"이라 읽었다. 스냅샷은 **마지막 라운드만** 남긴다.
  → `chain` 합계를 만들어 그 오독이 구조적으로 불가능하게 했다.

---

## 🛒 공동구매 축 — 대표 지시 "이 카테고리 인플루언서 DB 확보" (2026-07-29)

### 실측: 축은 이미 다 있었고, 줄이 막혀 있었다

| 확인한 것 | 상태 |
|---|---|
| 분류 규칙 `공동구매` | ✅ `RULES` 최상단(工具 동음이의어 방어 포함) |
| 우선 카테고리(+50) | ✅ `PRIORITY_CATEGORIES` 포함 |
| 검색 키워드 | ✅ **42개 시드됨**(2026-07-29 07:00, `active=1`) |
| 실제 확보 리드 | 205명(블로그 179 · 유튜브 26) |
| **키워드 실행 이력** | ❌ **42개 중 41개가 `last_run_at = NULL`** |

즉 205명은 **노려서 찾은 게 아니라 우연히 걸린 것**이다(203명이 `category_source='content'`
= 다른 키워드로 발굴된 뒤 본문을 읽어 재분류된 것. 키워드로 잡은 건 2명).

### 왜 41개가 안 돌았나 — 탐색 대기줄이 FIFO 였다

```
미실행 키워드 703 · 그중 자동확장 해시태그 557
공동구매(오늘 07:00 seed) → 미실행 큐 582~622위   (id 오름차순 정렬)
탐색 슬롯 라운드당 1개 · 오늘 실행된 키워드 30개
⇒ 첫 공동구매 키워드 차례까지 ≈ 75일
```

`PRIORITY_CATEGORIES`(+50)는 **이미 돌아본 키워드(`cooled`)의 점수에만** 쓰이고,
미실행 큐는 아무 기준 없이 `sort(a.id - b.id)` 였다. **새 전략 축은 정의상 가장 늦게
들어오므로 항상 맨 뒤**가 된다 — 축을 추가할 때마다 재발하는 구조였다.

**수리**: `exploreRank` — ① 사람이 고른 것(`seed`/`manual`) ② 우선 카테고리 ③ 들어온 순서.
탐색 슬롯 수(1)는 **일부러 안 건드렸다** — 늘리면 성과 검증된 키워드 몫을 깎는데,
이번 병목은 슬롯 수가 아니라 순서였다(75일 → 다음 라운드).

### 그런데 확보 전에 필터가 먼저였다 — '공동구매'는 두 가지 뜻이다

205명을 열어 보니 **아파트 입주 공동구매**(단지 단위 시공/자재 계약) 업체가 섞여 있었다:

```
'LX Z:IN 인테리어 광주남구점 …공식대리점' → LX창호 공동구매 행사 (7/30~8/5)
'팔도방충망님의 블로그'                  → 블랙스텐망 공동구매 시공 후기
'베니시모 휴앤홈 가구'                   → 입주예정자협의회 심사 거쳐 공동구매 제휴업체 선정
```

문자열은 같지만 **입주예정자협의회와 계약하는 시공업체**라, 우리가 찾는 것(자기 팔로워에게
직접 파는 사람)과 정반대 축이다. 성격상 **업체 DB(파트너풀)** 대상이다.

⚠️ **순서가 중요했다**: 키워드 대기줄을 방금 풀었으므로("공동구매"·"최저가 공구"·"특가 공구"…)
그 키워드가 돌면 이 부류가 **대량 유입**된다. 필터를 나중에 고치면 이미 섞인 뒤다.

**수리**: `isFitoutBulkBuy` — 셀러 신호(공구 오픈/링크/맘카페 공구/쇼핑라이브)가 있으면 **면제**
(겸업 보호: 리빙 인플루언서가 '리모델링 후기 + 리빙 공구 오픈'을 실제로 쓴다).
패턴 토큰은 전부 실측 문구에서만 뽑았다 — 추측으로 넓히면 겸업 셀러를 자른다.

라이브 205명 적용 결과: **27명 업체 판정 · 셀러 신호 보유자 중 잘린 사람 0 · 유튜브 26명 중 0.**

### 현재 공동구매 205명의 질 (냉정하게)

```
이메일 보유 11명(5%)  ·  인스타 5명  ·  활동성 측정 완료 14명
유튜브 26명 = 최대 66.5만 구독(가전주부 44.7만 · 진영 52.8만 · 우미스베이킹 66.5만)
블로그 179명 = 이메일 2명뿐, 이웃수 대부분 0(=미측정)
```
블로그 쪽이 얇은 이유는 분류가 아니라 **측정 병목**(이 문서 위쪽 전체 주제)이다.
205명 중 139명은 소개글이 짧아 업체/셀러 판정조차 안 된다 — 그것도 측정이 풀어야 한다.

### 다음 세션 첫 액션

1. 배포 후 `GET /api/admin/ads/influencer-pool/keywords` 에서 **공동구매 키워드의 `last_run_at`
   이 채워지기 시작하는가**. 안 채워지면 `exploreRank` 가 안 먹은 것이니 `source` 값이 실제로
   `'seed'` 인지부터 확인할 것(라이브 실측으로는 seed 였다).
2. 수율: 유일하게 돈 "공동구매" 키워드가 **found 145 → saved 24(16.6%)**. 나머지 41개가
   비슷하면 5일에 한 바퀴 돌며 1,000명 안팎 추가가 기대치다. **추정이므로 첫 며칠 실측 필수.**
3. `category=공동구매` 의 `total` 증가분에서 **업체가 다시 늘지 않는지** 확인
   (`isFitoutBulkBuy` 가 실제로 유입을 막는지 — 신규 유입에만 적용된다).

### 남은 결정 (대표 판단 — 코드로 우회하지 말 것)

- **탐색 슬롯을 1 → 2로 올릴 것인가.** 공동구매 확보가 2배 빨라지지만 성과 검증 키워드
  (예: '동네 맛집' found 2,072 → saved 1,179)의 슬롯이 줄어든다. 속도 ↔ 총량 맞교환.
- **이미 들어온 업체 27명을 소급 정리할 것인가.** 현재는 측정 시 재분류로 점진 교정되는데,
  블로그 측정이 느려 한동안 남는다. 즉시 정리하려면 백필(쓰기)이 필요 — 대표 지시 있을 때만.

### 이번에 틀렸던 판단

- 대표 질문 "공동구매 관련한 DB들은 어때?"를 **소비자 공동구매 서비스 테이블**로 읽고
  `vouchers`/`community_group_buys` 를 조사했다. 실제 요청은 **유어애즈 인플루언서 DB의
  공동구매 카테고리**였다. 유어애즈 세션 중이었으므로 그쪽이 기본값이었어야 했다.
- 어드민 API 응답 래퍼를 **세 번** 잘못 짚었다(`data` 리스트 / `data.rows` / `data.keywords`).
  0건으로 보고할 뻔한 게 두 번이다. ⇒ **새 엔드포인트는 `head -c 300` 으로 원본부터 볼 것.**
