# 🎟️ 이용권 등록 플로우 전수조사 (2026-09-02)

대표: *"이용권 등록 플로우 너가 전수조사 해봐. 지금 문제가 많네."*

## 🔴 다음 세션의 첫 액션

**배포 후 실제 등록 1회가 유일한 판정이다.** 순서:

1. 셀러 대시보드 → 이용권 등록 → 1단계에서 **매장 칩이 보이는지**(대표 계정은 매장 14 운영자 · 개인 좌석 5)
2. 칩을 눌러 좌석이 바뀌는지 → 2·3단계 진행 → **등록 성공**
3. 매장을 하나 더 등록 → **"매장 등록 중 오류" 가 안 나는지**(이게 이번 최대 건)
4. 확인 쿼리:
```sql
-- 매장이 늘었는가 (이전엔 영원히 1행이었다)
SELECT id, username, email, status FROM sellers WHERE seller_type='store_owner';
-- 마감이 KST 로 맞는가 (저장은 UTC — 9시간 차이가 나면 실패)
SELECT id, name, group_buy_deadline, datetime(group_buy_deadline,'+9 hours') AS kst
  FROM products WHERE seller_id IS NOT NULL AND group_buy_deadline IS NOT NULL ORDER BY id DESC LIMIT 3;
```

## 📌 이번 조사의 출발점 — 라이브 실측이 전부를 설명했다

D1 실측(2026-09-02):

| | 값 |
|---|---|
| 전체 셀러 | 11 |
| **매장(store_owner)** | **1** (id 14 홍대돈까스, 2026-08-26 등록, `email=''`) |
| 그 매장의 상품 | **0** |
| 활성 이용권 340개 중 `seller_id` 있는 것 | **0** |
| 마감(`group_buy_deadline`)이 들어간 셀러 이용권 | **0** |

⇒ 개별 버그가 아니라 **플로우가 끝까지 통한 적이 없다.** 아래 7건이 그 원인이다.

## ✅ 고친 것 (전부 `voucher-flow-audit-2026-09-02.test.ts` 23건 + 주입 10건으로 고정)

| # | 무엇 | 왜 아무도 몰랐나 |
|---|---|---|
| ① | **매장 등록이 두 번째부터 100% 실패** — `sellers(email)` 부분 UNIQUE 인덱스에 `''` 를 넣고 있었다. `''` 은 NULL 이 **아니라서** 인덱스에 들어간다 → 첫 매장이 슬롯을 차지 | 첫 매장은 성공했다. 실패는 catch 로 잡혀 "매장 등록 중 오류" 라는 **원인 없는 알럿**만 떴다 |
| ② | `GET /api/seller/gb/support-contact` **400** — `app.get('/:id')` 가 먼저 등록돼 id="support-contact" 로 잡혔다 | 경로 문자열이 달라 **라우트 중복 가드가 못 본다**. Hono 는 등록 순서로 매칭한다 |
| ③ | **매장 게이트가 좌석만 봤다** — 매장은 별도 `sellers` 행이고 사람은 `seller_operators` 로 접근한다. 개인 좌석에 앉아 있으면 "매장을 등록하세요"가 계속 떴고, **시킨 대로 해도 중복 매장만 생기는 막다른 길**이었다 | 대표 실사례 그대로(user 3 은 매장 14 운영자, 좌석은 5) |
| ③-b | 매장이 **하나뿐이면 선택 칩이 아예 안 보였다**(조건이 `length >= 2`) | 자기 매장이 있는데 고를 방법이 없었다 |
| ③-c | 그 게이트를 넓히면서 **승인 대기 매장까지 세면 더 나빠진다** — 게이트는 열리는데 좌석 토큰은 `active\|approved` 에만 나와서 **이용권이 개인 좌석으로 등록된다** | 막히는 것보다 나쁘다(잘못된 매장 이름으로 팔린다). 그래서 앉을 수 있는 것만 센다 |
| ④ | **화이트 고정 대시보드 안에서 `dark:` 유틸이 살아났다** → 흰 배경에 흰 글자(대표 신고: 매장 검색 결과) | `check-dashboard-theme.sh` 는 `src/pages/Seller*` 만 보고, `.force-light-theme` 는 **입력 글자만** 지킨다. 공용 컴포넌트(`KakaoMapPicker`)의 `dark:` 는 **그 자체로 올바른 코드**라 아무 가드도 안 걸렸다 |
| ⑤ | **판매 마감이 9시간 늦게 걸렸다** — `datetime-local` 은 타임존 없는 KST 벽시계인데 워커·cron 은 UTC 로 읽는다. 게다가 기본값만 `toISOString()` 이라 **한 칸에 두 규약**이 섞였다 | 마감이 들어간 셀러 이용권이 라이브에 0건이라 아직 아무도 안 겪었다(순수 선제 수정) |
| ⑥ | **위저드가 없어진 카테고리 3종을 계속 내밀었다** — health/pet/activity 는 2026-05-17 통합으로 레거시. 서버가 저장 직전 접어 넣는다(헬스→미용) | 셀러가 "헬스" 를 고르면 **조용히 "미용" 으로** 등록됐다. 에러가 없다. (서버의 그 정규화는 2026-08-22 에 "피드에 아예 안 뜨던 것"을 고친 것이라 **옳다** — 화면만 낡아 있었다) |

## ⚠️ 이번에 틀렸던 판단 (제일 값진 부분)

1. **③ 을 고치다가 더 나쁜 걸 만들 뻔했다.** `listOperableStores().length` 로 게이트를 열면 승인 대기
   매장까지 세어져, 게이트는 열리고 좌석은 안 바뀌어 **이용권이 개인 좌석으로 등록**된다. 게이트를 넓힐
   때는 *"열린 다음에 실제로 할 수 있는가"* 를 같이 봐야 한다(③-c 로 교정).
2. **④ 는 컴포넌트 버그가 아니었다.** 처음엔 `KakaoMapPicker` 의 `dark:text-white` 를 지우려 했는데,
   그건 소비자 화면에서도 쓰이는 **정상 코드**다. 고칠 자리는 컴포넌트가 아니라 **대시보드가 `dark:` 를
   끄지 않는다는 사실**이었다(tailwind variant 한 줄). 같은 증상이 다른 공용 컴포넌트로 계속 번졌을 것이다.
3. **가드가 계약 대신 문자열을 못박고 있었다.** `home-panel-light-island.test.ts` 가 darkMode variant
   **전체**를 정규식으로 고정해, 같은 자리에 형제를 *추가*하자 — 그 섬의 계약은 그대로인데 — 빨간불이
   났다. 계약만 검사하도록 고쳤다. **가드는 "지금 값"이 아니라 "지켜야 할 성질"을 검사해야 한다.**
4. `--only` 는 정규식이 아니라 **부분일치**다. `"a|b"` 로 부르면 0건을 돌고 실패한다(가드가 그걸 잡아 준다).

## 🕳️ 같은 커밋에 든 별건 — cf-image 되돌림

같은 날 내가 넣은 "미지 호스트도 cdn-cgi 로" 를 **되돌렸다.** 근거였던
*"`onerror=redirect` 가 실패 시 원본으로 302"* 가 **라이브 실측에서 반증**됐다:
`HTTP/2 403 · cf-resized: err=9408` (검증 호스트의 없는 경로로 시험해도 동일 — URL 이 아니라 폴백 자체가
안 돈다). 그리고 `digitaloceanspaces.com` 을 **실측 없이 승격**해 403 을 만들었다(이 파일의 원래 규율은
`cf-resized: internal=ok` 실측 후 승격이고, 내가 그걸 안 지켰다). `cloudfront.net` 은 실측했으므로 유지.

## 📋 남은 것

- **셀러 대시보드 느림** — ② 의 400 이 한 요인이었으나 그것만인지는 미확인. 배포 후 재측정 필요.
- 매장 등록은 `status='pending'` → **어드민 승인이 있어야 이용권을 만들 수 있다**(설계대로). 파일럿에서
  이 대기가 병목이면 대표 판단 필요(자동 승인 여부는 코드가 아니라 정책 결정).
- 이 조사가 **못 본 것**: 실제 브라우저 렌더·결제·알림톡. 정적 배선만 고정했다.
