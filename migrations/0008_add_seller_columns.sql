-- 이 마이그레이션의 모든 변경사항은 이미 이전 마이그레이션에서 적용됨
-- 0005: orders.seller_id
-- 0006: sellers 프로필 컬럼, live_streams.seller_id
-- 0005: users 카카오 컬럼, shipping_addresses 테이블
-- 0001: orders 배송지 정보

-- Placeholder migration - no changes needed
SELECT 1;
