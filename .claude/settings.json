{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/session-start.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'git push.*origin' && ! echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'origin main'; then bash $CLAUDE_PROJECT_DIR/scripts/auto-merge-main.sh; fi"
          }
        ]
      }
    ]
  }
}
