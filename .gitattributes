# 🔀 병합 전용 규칙 (2026-07-29 신설)
#
# 세션(브랜치)이 여러 개 동시에 도는 레포라, 아래 두 파일은 **내용이 충돌한 게 아닌데도**
# 거의 모든 병합에서 충돌한다 — 서로 다른 브랜치가 서로 다른 줄을 append 하기 때문이다.
# 2026-07-29 하루에만 손으로 10번 넘게 병합했다. 규칙이 결정론적이면 사람이 할 일이 아니다.
#
# ⚠️ 드라이버는 로컬 git config 에 등록돼 있어야 동작한다 — `bash scripts/install-git-hooks.sh`
#    가 자동 등록한다. 등록 안 된 환경에서는 그냥 평소대로 충돌이 나므로 안전하다(조용한 오작동 없음).

# 파일 크기 래칫 baseline: "키별 최대값"으로 병합. 근거는 스크립트 주석 참조.
scripts/file-size-baseline.json merge=filesize-baseline

# 세션 인계 로그: 양쪽 다 남긴다(append-only 성격). 지금까지 손으로 하던 keep-both 와 같다.
# 순서가 뒤섞일 수 있으나 인계 문서라 내용 보존이 우선이다.
docs/CURRENT_WORK.md merge=union
